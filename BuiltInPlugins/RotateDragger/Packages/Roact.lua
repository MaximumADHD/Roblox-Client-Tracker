--[[
	Package link auto-generated by manage_libraries and Rotriever
]]
if not game:GetFastFlag("LuaDraggersDeduplicatePackages") then
	local PackageIndex = script.Parent._Old

	local package = PackageIndex["Roact-e3b370a8-ec68e60e"].Packages["Roact"]

	if package.ClassName == "ModuleScript" then
		return require(package)
	end

	return package
end

local PackageIndex = script.Parent._Index
local Package = require(PackageIndex["Roact"]["Roact"])
return Package

