--[[
	A box select has been completed, perform any finalization needed on the
	current selection.

	This function will be useful if you are implementing any sort of soft
	selection, and need to promote the soft selection changes that have happened
	since the box select started into an actual hard selection change, you would
	put that promotion code here.
]]
return function(draggerContext) end
