local strict = require(script.Parent.Parent.strict)

return strict({
	Constants = require(script.Constants),
	Mouse = require(script.Mouse),
	RequireRoact = require(script.RequireRoact),
	Text = require(script.Text),
	Theme = require(script.Theme),
	ThemeChangeListener = require(script.ThemeChangeListener),
})
