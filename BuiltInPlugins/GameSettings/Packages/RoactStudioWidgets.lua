local strict = require(script.Parent.strict)

return strict({
	ButtonBar = require(script.ButtonBar),
	ButtonBarWithToolTip = require(script.ButtonBarWithToolTip),
	Hyperlink = require(script.Hyperlink),
	MultilineTextEntry = require(script.MultilineTextEntry),
	RadioButton = require(script.RadioButton),
	RadioButtonSet = require(script.RadioButtonSet),
	RangeSlider = require(script.RangeSlider),
	RoundTextBox = require(script.RoundTextBox),
	RoundTextButton = require(script.RoundTextButton),
	Separator = require(script.Separator),
	StyledScrollingFrame = require(script.StyledScrollingFrame),
	Text = require(script.Text),
	TextEntry = require(script.TextEntry),
	TitledFrame = require(script.TitledFrame),
	ToggleButton = require(script.ToggleButton),
})
