-- Geometry the preview renders a material on; Enums.MaterialPreviewGeometryType holds
-- the matching runtime values.
export type MaterialPreviewGeometryType = "Cube" | "CubeCornerOn" | "Plane" | "Sphere"

return {}
