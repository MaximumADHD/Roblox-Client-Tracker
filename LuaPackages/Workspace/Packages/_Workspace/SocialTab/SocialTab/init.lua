return {
	Components = require(script.Components),
	Context = require(script.SocialTabContext),
	installReducer = require(script.installReducer),
	Requests = require(script.Requests),
	AndroidBackButtonConnection = require(script.AndroidBackButtonConnection),
	EnumScreens = require(script.EnumScreens),
	Flags = require(script.Flags),
	utils = require(script.utils),
	["jest.config"] = script["jest.config"],
}
