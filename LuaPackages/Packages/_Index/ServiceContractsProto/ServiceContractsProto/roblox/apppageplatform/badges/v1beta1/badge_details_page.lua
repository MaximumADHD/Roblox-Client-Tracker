--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	BadgeDetailsPageRequest: _BadgeDetailsPageRequestMessage,
	BadgeDetailsPageResponse: _BadgeDetailsPageResponseMessage,
	BadgeDetailsPageResponse_TemplatesEntry: _BadgeDetailsPageResponse_TemplatesEntryMessage,
	BadgeDetailsPageEntry: _BadgeDetailsPageEntryMessage,
	BadgeDetailsContent: _BadgeDetailsContentMessage,
	BadgeDetailsContent_ShareLinkData: _BadgeDetailsContent_ShareLinkDataMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_page_entry = require(script.Parent.Parent.Parent.shared.v1beta1.page_entry)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)

type _BadgeDetailsPageRequestImpl = {
	__index: _BadgeDetailsPageRequestImpl,
	new: (fields: _BadgeDetailsPageRequestPartialFields?) -> BadgeDetailsPageRequest,
	encode: (self: BadgeDetailsPageRequest) -> buffer,
	decode: (input: buffer) -> BadgeDetailsPageRequest,
	jsonEncode: (self: BadgeDetailsPageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BadgeDetailsPageRequest,
	descriptor: proto.Descriptor,
}

type _BadgeDetailsPageRequestFields = {
	badge_id: number,
	user_id: number,
}

type _BadgeDetailsPageRequestPartialFields = {
	badge_id: number?,
	user_id: number?,
}

export type BadgeDetailsPageRequest = typeof(setmetatable(
	{} :: _BadgeDetailsPageRequestFields,
	{} :: _BadgeDetailsPageRequestImpl
))
type _BadgeDetailsPageRequestMessage = proto.Message<BadgeDetailsPageRequest, _BadgeDetailsPageRequestPartialFields>

type _BadgeDetailsPageResponseImpl = {
	__index: _BadgeDetailsPageResponseImpl,
	new: (fields: _BadgeDetailsPageResponsePartialFields?) -> BadgeDetailsPageResponse,
	encode: (self: BadgeDetailsPageResponse) -> buffer,
	decode: (input: buffer) -> BadgeDetailsPageResponse,
	jsonEncode: (self: BadgeDetailsPageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BadgeDetailsPageResponse,
	descriptor: proto.Descriptor,
}

type _BadgeDetailsPageResponseFields = {
	page_entries: { BadgeDetailsPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
}

type _BadgeDetailsPageResponsePartialFields = {
	page_entries: { BadgeDetailsPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
}

export type BadgeDetailsPageResponse = typeof(setmetatable(
	{} :: _BadgeDetailsPageResponseFields,
	{} :: _BadgeDetailsPageResponseImpl
))
type _BadgeDetailsPageResponseMessage = proto.Message<BadgeDetailsPageResponse, _BadgeDetailsPageResponsePartialFields>

type _BadgeDetailsPageResponse_TemplatesEntryImpl = {
	__index: _BadgeDetailsPageResponse_TemplatesEntryImpl,
	new: (fields: _BadgeDetailsPageResponse_TemplatesEntryPartialFields?) -> BadgeDetailsPageResponse_TemplatesEntry,
	encode: (self: BadgeDetailsPageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> BadgeDetailsPageResponse_TemplatesEntry,
	jsonEncode: (self: BadgeDetailsPageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BadgeDetailsPageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _BadgeDetailsPageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _BadgeDetailsPageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type BadgeDetailsPageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _BadgeDetailsPageResponse_TemplatesEntryFields,
	{} :: _BadgeDetailsPageResponse_TemplatesEntryImpl
))
type _BadgeDetailsPageResponse_TemplatesEntryMessage = proto.Message<
	BadgeDetailsPageResponse_TemplatesEntry,
	_BadgeDetailsPageResponse_TemplatesEntryPartialFields
>

type _BadgeDetailsPageEntryImpl = {
	__index: _BadgeDetailsPageEntryImpl,
	new: (fields: _BadgeDetailsPageEntryPartialFields?) -> BadgeDetailsPageEntry,
	encode: (self: BadgeDetailsPageEntry) -> buffer,
	decode: (input: buffer) -> BadgeDetailsPageEntry,
	jsonEncode: (self: BadgeDetailsPageEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BadgeDetailsPageEntry,
	descriptor: proto.Descriptor,
}

type _BadgeDetailsPageEntryFields = {
	page_entry: _roblox_apppageplatform_shared_v1beta1_page_entry.PageEntry?,
	kind: { type: "badge_details", value: BadgeDetailsContent }?,
}

type _BadgeDetailsPageEntryPartialFields = {
	page_entry: _roblox_apppageplatform_shared_v1beta1_page_entry.PageEntry?,
	kind: { type: "badge_details", value: BadgeDetailsContent }?,
}

export type BadgeDetailsPageEntry = typeof(setmetatable(
	{} :: _BadgeDetailsPageEntryFields,
	{} :: _BadgeDetailsPageEntryImpl
))
type _BadgeDetailsPageEntryMessage = proto.Message<BadgeDetailsPageEntry, _BadgeDetailsPageEntryPartialFields>

type _BadgeDetailsContentImpl = {
	__index: _BadgeDetailsContentImpl,
	new: (fields: _BadgeDetailsContentPartialFields?) -> BadgeDetailsContent,
	encode: (self: BadgeDetailsContent) -> buffer,
	decode: (input: buffer) -> BadgeDetailsContent,
	jsonEncode: (self: BadgeDetailsContent) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BadgeDetailsContent,
	descriptor: proto.Descriptor,
}

type _BadgeDetailsContentFields = {
	badge_id: string,
	universe_id: string,
	share_link_data: BadgeDetailsContent_ShareLinkData?,
	creator_key: string,
}

type _BadgeDetailsContentPartialFields = {
	badge_id: string?,
	universe_id: string?,
	share_link_data: BadgeDetailsContent_ShareLinkData?,
	creator_key: string?,
}

export type BadgeDetailsContent = typeof(setmetatable({} :: _BadgeDetailsContentFields, {} :: _BadgeDetailsContentImpl))
type _BadgeDetailsContentMessage = proto.Message<BadgeDetailsContent, _BadgeDetailsContentPartialFields>

type _BadgeDetailsContent_ShareLinkDataImpl = {
	__index: _BadgeDetailsContent_ShareLinkDataImpl,
	new: (fields: _BadgeDetailsContent_ShareLinkDataPartialFields?) -> BadgeDetailsContent_ShareLinkData,
	encode: (self: BadgeDetailsContent_ShareLinkData) -> buffer,
	decode: (input: buffer) -> BadgeDetailsContent_ShareLinkData,
	jsonEncode: (self: BadgeDetailsContent_ShareLinkData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BadgeDetailsContent_ShareLinkData,
	descriptor: proto.Descriptor,
}

type _BadgeDetailsContent_ShareLinkDataFields = {
	badge_id: string,
}

type _BadgeDetailsContent_ShareLinkDataPartialFields = {
	badge_id: string?,
}

export type BadgeDetailsContent_ShareLinkData = typeof(setmetatable(
	{} :: _BadgeDetailsContent_ShareLinkDataFields,
	{} :: _BadgeDetailsContent_ShareLinkDataImpl
))
type _BadgeDetailsContent_ShareLinkDataMessage = proto.Message<
	BadgeDetailsContent_ShareLinkData,
	_BadgeDetailsContent_ShareLinkDataPartialFields
>

do
	local _BadgeDetailsPageRequestImpl = {}
	_BadgeDetailsPageRequestImpl.__index = _BadgeDetailsPageRequestImpl

	function _BadgeDetailsPageRequestImpl.new(data: _BadgeDetailsPageRequestPartialFields?): BadgeDetailsPageRequest
		return setmetatable({
			badge_id = if data == nil or data.badge_id == nil then 0 else data.badge_id,
			user_id = if data == nil or data.user_id == nil then 0 else data.user_id,
		}, _BadgeDetailsPageRequestImpl :: _BadgeDetailsPageRequestImpl)
	end

	function _BadgeDetailsPageRequestImpl.encode(self: BadgeDetailsPageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.badge_id ~= nil and self.badge_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.badge_id)
		end

		if self.user_id ~= nil and self.user_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.user_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BadgeDetailsPageRequestImpl.decode(input: buffer): BadgeDetailsPageRequest
		local self = _BadgeDetailsPageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.badge_id = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.user_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BadgeDetailsPageRequestImpl.jsonEncode(self: BadgeDetailsPageRequest): any
		local output = {}

		if self.badge_id ~= nil and self.badge_id ~= 0 then
			output.badgeId = self.badge_id
		end

		if self.user_id ~= nil and self.user_id ~= 0 then
			output.userId = self.user_id
		end

		return output
	end

	function _BadgeDetailsPageRequestImpl.jsonDecode(input: { [string]: any }): BadgeDetailsPageRequest
		local self = _BadgeDetailsPageRequestImpl.new()

		if input.badge_id ~= nil then
			self.badge_id = input.badge_id
		end

		if input.badgeId ~= nil then
			self.badge_id = input.badgeId
		end

		if input.user_id ~= nil then
			self.user_id = input.user_id
		end

		if input.userId ~= nil then
			self.user_id = input.userId
		end

		return self
	end

	_BadgeDetailsPageRequestImpl.descriptor = {
		name = "BadgeDetailsPageRequest",
		fullName = "roblox.apppageplatform.badges.v1beta1.BadgeDetailsPageRequest",
	}

	messages.BadgeDetailsPageRequest = _BadgeDetailsPageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BadgeDetailsPageRequest)
end

do
	local _BadgeDetailsPageResponseImpl = {}
	_BadgeDetailsPageResponseImpl.__index = _BadgeDetailsPageResponseImpl

	function _BadgeDetailsPageResponseImpl.new(data: _BadgeDetailsPageResponsePartialFields?): BadgeDetailsPageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
		}, _BadgeDetailsPageResponseImpl :: _BadgeDetailsPageResponseImpl)
	end

	function _BadgeDetailsPageResponseImpl.encode(self: BadgeDetailsPageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BadgeDetailsPageResponseImpl.decode(input: buffer): BadgeDetailsPageResponse
		local self = _BadgeDetailsPageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.page_entries, messages.BadgeDetailsPageEntry.decode(value))
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.BadgeDetailsPageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BadgeDetailsPageResponseImpl.jsonEncode(self: BadgeDetailsPageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		return output
	end

	function _BadgeDetailsPageResponseImpl.jsonDecode(input: { [string]: any }): BadgeDetailsPageResponse
		local self = _BadgeDetailsPageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { BadgeDetailsPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(newOutput, messages.BadgeDetailsPageEntry.jsonDecode(value))
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { BadgeDetailsPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(newOutput, messages.BadgeDetailsPageEntry.jsonDecode(value))
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		return self
	end

	_BadgeDetailsPageResponseImpl.descriptor = {
		name = "BadgeDetailsPageResponse",
		fullName = "roblox.apppageplatform.badges.v1beta1.BadgeDetailsPageResponse",
	}

	messages.BadgeDetailsPageResponse = _BadgeDetailsPageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BadgeDetailsPageResponse)
end

do
	local _BadgeDetailsPageResponse_TemplatesEntryImpl = {}
	_BadgeDetailsPageResponse_TemplatesEntryImpl.__index = _BadgeDetailsPageResponse_TemplatesEntryImpl

	function _BadgeDetailsPageResponse_TemplatesEntryImpl.new(
		data: _BadgeDetailsPageResponse_TemplatesEntryPartialFields?
	): BadgeDetailsPageResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _BadgeDetailsPageResponse_TemplatesEntryImpl :: _BadgeDetailsPageResponse_TemplatesEntryImpl)
	end

	function _BadgeDetailsPageResponse_TemplatesEntryImpl.encode(self: BadgeDetailsPageResponse_TemplatesEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BadgeDetailsPageResponse_TemplatesEntryImpl.decode(input: buffer): BadgeDetailsPageResponse_TemplatesEntry
		local self = _BadgeDetailsPageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BadgeDetailsPageResponse_TemplatesEntryImpl.jsonEncode(self: BadgeDetailsPageResponse_TemplatesEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _BadgeDetailsPageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): BadgeDetailsPageResponse_TemplatesEntry
		local self = _BadgeDetailsPageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_BadgeDetailsPageResponse_TemplatesEntryImpl.descriptor = {
		name = "BadgeDetailsPageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.badges.v1beta1.TemplatesEntry",
	}

	messages.BadgeDetailsPageResponse_TemplatesEntry = _BadgeDetailsPageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BadgeDetailsPageResponse_TemplatesEntry)
end

do
	local _BadgeDetailsPageEntryImpl = {}
	_BadgeDetailsPageEntryImpl.__index = _BadgeDetailsPageEntryImpl

	function _BadgeDetailsPageEntryImpl.new(data: _BadgeDetailsPageEntryPartialFields?): BadgeDetailsPageEntry
		return setmetatable({
			page_entry = if data == nil or data.page_entry == nil then nil else data.page_entry,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _BadgeDetailsPageEntryImpl :: _BadgeDetailsPageEntryImpl)
	end

	function _BadgeDetailsPageEntryImpl.encode(self: BadgeDetailsPageEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entry ~= nil then
			local encoded = self.page_entry:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "badge_details" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BadgeDetailsPageEntryImpl.decode(input: buffer): BadgeDetailsPageEntry
		local self = _BadgeDetailsPageEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.page_entry = _roblox_apppageplatform_shared_v1beta1_page_entry.PageEntry.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "badge_details", value = messages.BadgeDetailsContent.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BadgeDetailsPageEntryImpl.jsonEncode(self: BadgeDetailsPageEntry): any
		local output = {}

		if self.page_entry ~= nil then
			output.pageEntry = self.page_entry:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "badge_details" then
				output.badgeDetails = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _BadgeDetailsPageEntryImpl.jsonDecode(input: { [string]: any }): BadgeDetailsPageEntry
		local self = _BadgeDetailsPageEntryImpl.new()

		if input.page_entry ~= nil then
			self.page_entry = _roblox_apppageplatform_shared_v1beta1_page_entry.PageEntry.jsonDecode(input.page_entry)
		end

		if input.pageEntry ~= nil then
			self.page_entry = _roblox_apppageplatform_shared_v1beta1_page_entry.PageEntry.jsonDecode(input.pageEntry)
		end

		if input.badge_details ~= nil then
			self.kind = { type = "badge_details", value = messages.BadgeDetailsContent.jsonDecode(input.badge_details) }
		end

		if input.badgeDetails ~= nil then
			self.kind = { type = "badge_details", value = messages.BadgeDetailsContent.jsonDecode(input.badgeDetails) }
		end

		return self
	end

	_BadgeDetailsPageEntryImpl.descriptor = {
		name = "BadgeDetailsPageEntry",
		fullName = "roblox.apppageplatform.badges.v1beta1.BadgeDetailsPageEntry",
	}

	messages.BadgeDetailsPageEntry = _BadgeDetailsPageEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BadgeDetailsPageEntry)
end

do
	local _BadgeDetailsContentImpl = {}
	_BadgeDetailsContentImpl.__index = _BadgeDetailsContentImpl

	function _BadgeDetailsContentImpl.new(data: _BadgeDetailsContentPartialFields?): BadgeDetailsContent
		return setmetatable({
			badge_id = if data == nil or data.badge_id == nil then "" else data.badge_id,
			universe_id = if data == nil or data.universe_id == nil then "" else data.universe_id,
			share_link_data = if data == nil or data.share_link_data == nil then nil else data.share_link_data,
			creator_key = if data == nil or data.creator_key == nil then "" else data.creator_key,
		}, _BadgeDetailsContentImpl :: _BadgeDetailsContentImpl)
	end

	function _BadgeDetailsContentImpl.encode(self: BadgeDetailsContent): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.badge_id ~= nil and self.badge_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.badge_id)
		end

		if self.universe_id ~= nil and self.universe_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.universe_id)
		end

		if self.share_link_data ~= nil then
			local encoded = self.share_link_data:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.creator_key ~= nil and self.creator_key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.creator_key)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BadgeDetailsContentImpl.decode(input: buffer): BadgeDetailsContent
		local self = _BadgeDetailsContentImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.badge_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.universe_id = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.share_link_data = messages.BadgeDetailsContent_ShareLinkData.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.creator_key = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BadgeDetailsContentImpl.jsonEncode(self: BadgeDetailsContent): any
		local output = {}

		if self.badge_id ~= nil and self.badge_id ~= "" then
			output.badgeId = self.badge_id
		end

		if self.universe_id ~= nil and self.universe_id ~= "" then
			output.universeId = self.universe_id
		end

		if self.share_link_data ~= nil then
			output.shareLinkData = self.share_link_data:jsonEncode()
		end

		if self.creator_key ~= nil and self.creator_key ~= "" then
			output.creatorKey = self.creator_key
		end

		return output
	end

	function _BadgeDetailsContentImpl.jsonDecode(input: { [string]: any }): BadgeDetailsContent
		local self = _BadgeDetailsContentImpl.new()

		if input.badge_id ~= nil then
			self.badge_id = input.badge_id
		end

		if input.badgeId ~= nil then
			self.badge_id = input.badgeId
		end

		if input.universe_id ~= nil then
			self.universe_id = input.universe_id
		end

		if input.universeId ~= nil then
			self.universe_id = input.universeId
		end

		if input.share_link_data ~= nil then
			self.share_link_data = messages.BadgeDetailsContent_ShareLinkData.jsonDecode(input.share_link_data)
		end

		if input.shareLinkData ~= nil then
			self.share_link_data = messages.BadgeDetailsContent_ShareLinkData.jsonDecode(input.shareLinkData)
		end

		if input.creator_key ~= nil then
			self.creator_key = input.creator_key
		end

		if input.creatorKey ~= nil then
			self.creator_key = input.creatorKey
		end

		return self
	end

	_BadgeDetailsContentImpl.descriptor = {
		name = "BadgeDetailsContent",
		fullName = "roblox.apppageplatform.badges.v1beta1.BadgeDetailsContent",
	}

	messages.BadgeDetailsContent = _BadgeDetailsContentImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BadgeDetailsContent)
end

do
	local _BadgeDetailsContent_ShareLinkDataImpl = {}
	_BadgeDetailsContent_ShareLinkDataImpl.__index = _BadgeDetailsContent_ShareLinkDataImpl

	function _BadgeDetailsContent_ShareLinkDataImpl.new(
		data: _BadgeDetailsContent_ShareLinkDataPartialFields?
	): BadgeDetailsContent_ShareLinkData
		return setmetatable({
			badge_id = if data == nil or data.badge_id == nil then "" else data.badge_id,
		}, _BadgeDetailsContent_ShareLinkDataImpl :: _BadgeDetailsContent_ShareLinkDataImpl)
	end

	function _BadgeDetailsContent_ShareLinkDataImpl.encode(self: BadgeDetailsContent_ShareLinkData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.badge_id ~= nil and self.badge_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.badge_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BadgeDetailsContent_ShareLinkDataImpl.decode(input: buffer): BadgeDetailsContent_ShareLinkData
		local self = _BadgeDetailsContent_ShareLinkDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.badge_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BadgeDetailsContent_ShareLinkDataImpl.jsonEncode(self: BadgeDetailsContent_ShareLinkData): any
		local output = {}

		if self.badge_id ~= nil and self.badge_id ~= "" then
			output.badgeId = self.badge_id
		end

		return output
	end

	function _BadgeDetailsContent_ShareLinkDataImpl.jsonDecode(
		input: { [string]: any }
	): BadgeDetailsContent_ShareLinkData
		local self = _BadgeDetailsContent_ShareLinkDataImpl.new()

		if input.badge_id ~= nil then
			self.badge_id = input.badge_id
		end

		if input.badgeId ~= nil then
			self.badge_id = input.badgeId
		end

		return self
	end

	_BadgeDetailsContent_ShareLinkDataImpl.descriptor = {
		name = "BadgeDetailsContent_ShareLinkData",
		fullName = "roblox.apppageplatform.badges.v1beta1.ShareLinkData",
	}

	messages.BadgeDetailsContent_ShareLinkData = _BadgeDetailsContent_ShareLinkDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BadgeDetailsContent_ShareLinkData)
end

return {
	BadgeDetailsPageRequest = messages.BadgeDetailsPageRequest,
	BadgeDetailsPageResponse = messages.BadgeDetailsPageResponse,
	BadgeDetailsPageEntry = messages.BadgeDetailsPageEntry,
	BadgeDetailsContent = messages.BadgeDetailsContent,
	BadgeDetailsContent_ShareLinkData = messages.BadgeDetailsContent_ShareLinkData,
}
