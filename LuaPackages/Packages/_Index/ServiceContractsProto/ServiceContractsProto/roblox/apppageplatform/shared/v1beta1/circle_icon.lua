--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CircleIconSchema: _CircleIconSchemaMessage,
	CircleIconSchema_Props: _CircleIconSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _CircleIconSchemaImpl = {
	__index: _CircleIconSchemaImpl,
	new: (fields: _CircleIconSchemaPartialFields?) -> CircleIconSchema,
	encode: (self: CircleIconSchema) -> buffer,
	decode: (input: buffer) -> CircleIconSchema,
	jsonEncode: (self: CircleIconSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CircleIconSchema,
	descriptor: proto.Descriptor,
}

type _CircleIconSchemaFields = {
	props: CircleIconSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _CircleIconSchemaPartialFields = {
	props: CircleIconSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type CircleIconSchema = typeof(setmetatable({} :: _CircleIconSchemaFields, {} :: _CircleIconSchemaImpl))
type _CircleIconSchemaMessage = proto.Message<CircleIconSchema, _CircleIconSchemaPartialFields>

type _CircleIconSchema_PropsImpl = {
	__index: _CircleIconSchema_PropsImpl,
	new: (fields: _CircleIconSchema_PropsPartialFields?) -> CircleIconSchema_Props,
	encode: (self: CircleIconSchema_Props) -> buffer,
	decode: (input: buffer) -> CircleIconSchema_Props,
	jsonEncode: (self: CircleIconSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CircleIconSchema_Props,
	descriptor: proto.Descriptor,
}

type _CircleIconSchema_PropsFields = {
	icon_name: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	icon_size: _roblox_apppageplatform_shared_v1beta1_prop_types.IconSizeProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

type _CircleIconSchema_PropsPartialFields = {
	icon_name: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	icon_size: _roblox_apppageplatform_shared_v1beta1_prop_types.IconSizeProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

export type CircleIconSchema_Props = typeof(setmetatable(
	{} :: _CircleIconSchema_PropsFields,
	{} :: _CircleIconSchema_PropsImpl
))
type _CircleIconSchema_PropsMessage = proto.Message<CircleIconSchema_Props, _CircleIconSchema_PropsPartialFields>

do
	local _CircleIconSchemaImpl = {}
	_CircleIconSchemaImpl.__index = _CircleIconSchemaImpl

	function _CircleIconSchemaImpl.new(data: _CircleIconSchemaPartialFields?): CircleIconSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _CircleIconSchemaImpl :: _CircleIconSchemaImpl)
	end

	function _CircleIconSchemaImpl.encode(self: CircleIconSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CircleIconSchemaImpl.decode(input: buffer): CircleIconSchema
		local self = _CircleIconSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.CircleIconSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CircleIconSchemaImpl.jsonEncode(self: CircleIconSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _CircleIconSchemaImpl.jsonDecode(input: { [string]: any }): CircleIconSchema
		local self = _CircleIconSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.CircleIconSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_CircleIconSchemaImpl.descriptor = {
		name = "CircleIconSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.CircleIconSchema",
	}

	messages.CircleIconSchema = _CircleIconSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CircleIconSchema)
end

do
	local _CircleIconSchema_PropsImpl = {}
	_CircleIconSchema_PropsImpl.__index = _CircleIconSchema_PropsImpl

	function _CircleIconSchema_PropsImpl.new(data: _CircleIconSchema_PropsPartialFields?): CircleIconSchema_Props
		return setmetatable({
			icon_name = if data == nil or data.icon_name == nil then nil else data.icon_name,
			icon_size = if data == nil or data.icon_size == nil then nil else data.icon_size,
			background_style = if data == nil or data.background_style == nil then nil else data.background_style,
			size = if data == nil or data.size == nil then nil else data.size,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
		}, _CircleIconSchema_PropsImpl :: _CircleIconSchema_PropsImpl)
	end

	function _CircleIconSchema_PropsImpl.encode(self: CircleIconSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.icon_name ~= nil then
			local encoded = self.icon_name:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon_size ~= nil then
			local encoded = self.icon_size:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_style ~= nil then
			local encoded = self.background_style:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CircleIconSchema_PropsImpl.decode(input: buffer): CircleIconSchema_Props
		local self = _CircleIconSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon_name = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon_size = _roblox_apppageplatform_shared_v1beta1_prop_types.IconSizeProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_style =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CircleIconSchema_PropsImpl.jsonEncode(self: CircleIconSchema_Props): any
		local output = {}

		if self.icon_name ~= nil then
			output.iconName = self.icon_name:jsonEncode()
		end

		if self.icon_size ~= nil then
			output.iconSize = self.icon_size:jsonEncode()
		end

		if self.background_style ~= nil then
			output.backgroundStyle = self.background_style:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		return output
	end

	function _CircleIconSchema_PropsImpl.jsonDecode(input: { [string]: any }): CircleIconSchema_Props
		local self = _CircleIconSchema_PropsImpl.new()

		if input.icon_name ~= nil then
			self.icon_name = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.icon_name)
		end

		if input.iconName ~= nil then
			self.icon_name = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.iconName)
		end

		if input.icon_size ~= nil then
			self.icon_size = _roblox_apppageplatform_shared_v1beta1_prop_types.IconSizeProp.jsonDecode(input.icon_size)
		end

		if input.iconSize ~= nil then
			self.icon_size = _roblox_apppageplatform_shared_v1beta1_prop_types.IconSizeProp.jsonDecode(input.iconSize)
		end

		if input.background_style ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.jsonDecode(input.background_style)
		end

		if input.backgroundStyle ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.jsonDecode(input.backgroundStyle)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		return self
	end

	_CircleIconSchema_PropsImpl.descriptor = {
		name = "CircleIconSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.CircleIconSchema_Props = _CircleIconSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CircleIconSchema_Props)
end

return {
	CircleIconSchema = messages.CircleIconSchema,
	CircleIconSchema_Props = messages.CircleIconSchema_Props,
}
