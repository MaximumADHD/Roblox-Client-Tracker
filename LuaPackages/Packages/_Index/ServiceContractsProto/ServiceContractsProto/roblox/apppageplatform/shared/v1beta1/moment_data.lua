--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages =
	{
		MomentData: _MomentDataMessage,
		MomentData_MomentMetadata: _MomentData_MomentMetadataMessage,
		MomentData_MomentMetadata_MomentEdits: _MomentData_MomentMetadata_MomentEditsMessage,
		MomentData_MomentMetadata_MomentEdits_MusicEdit: _MomentData_MomentMetadata_MomentEdits_MusicEditMessage,
		MomentData_MomentMetadata_MomentEdits_TrimEdit: _MomentData_MomentMetadata_MomentEdits_TrimEditMessage,
		MomentData_MomentMetadata_MomentEdits_TextOverlay: _MomentData_MomentMetadata_MomentEdits_TextOverlayMessage,
		MomentData_MomentMetadata_MomentEdits_TextOverlay_Style: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleMessage,
	}
local messages: _Messages = {} :: _Messages

type _MomentDataImpl = {
	__index: _MomentDataImpl,
	new: (fields: _MomentDataPartialFields?) -> MomentData,
	encode: (self: MomentData) -> buffer,
	decode: (input: buffer) -> MomentData,
	jsonEncode: (self: MomentData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData,
	descriptor: proto.Descriptor,
}

type _MomentDataFields = {
	moment_id: string?,
	asset_id: string?,
	creator_id: string?,
	status: string?,
	metadata: MomentData_MomentMetadata?,
}

type _MomentDataPartialFields = {
	moment_id: string?,
	asset_id: string?,
	creator_id: string?,
	status: string?,
	metadata: MomentData_MomentMetadata?,
}

export type MomentData = typeof(setmetatable({} :: _MomentDataFields, {} :: _MomentDataImpl))
type _MomentDataMessage = proto.Message<MomentData, _MomentDataPartialFields>

type _MomentData_MomentMetadataImpl = {
	__index: _MomentData_MomentMetadataImpl,
	new: (fields: _MomentData_MomentMetadataPartialFields?) -> MomentData_MomentMetadata,
	encode: (self: MomentData_MomentMetadata) -> buffer,
	decode: (input: buffer) -> MomentData_MomentMetadata,
	jsonEncode: (self: MomentData_MomentMetadata) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData_MomentMetadata,
	descriptor: proto.Descriptor,
}

type _MomentData_MomentMetadataFields = {
	asset_total_duration_seconds: number?,
	capture_type: string?,
	description: string?,
	capture_universe_id: string?,
	capture_place_id: string?,
	edits: MomentData_MomentMetadata_MomentEdits?,
}

type _MomentData_MomentMetadataPartialFields = {
	asset_total_duration_seconds: number?,
	capture_type: string?,
	description: string?,
	capture_universe_id: string?,
	capture_place_id: string?,
	edits: MomentData_MomentMetadata_MomentEdits?,
}

export type MomentData_MomentMetadata = typeof(setmetatable(
	{} :: _MomentData_MomentMetadataFields,
	{} :: _MomentData_MomentMetadataImpl
))
type _MomentData_MomentMetadataMessage = proto.Message<
	MomentData_MomentMetadata,
	_MomentData_MomentMetadataPartialFields
>

type _MomentData_MomentMetadata_MomentEditsImpl = {
	__index: _MomentData_MomentMetadata_MomentEditsImpl,
	new: (fields: _MomentData_MomentMetadata_MomentEditsPartialFields?) -> MomentData_MomentMetadata_MomentEdits,
	encode: (self: MomentData_MomentMetadata_MomentEdits) -> buffer,
	decode: (input: buffer) -> MomentData_MomentMetadata_MomentEdits,
	jsonEncode: (self: MomentData_MomentMetadata_MomentEdits) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData_MomentMetadata_MomentEdits,
	descriptor: proto.Descriptor,
}

type _MomentData_MomentMetadata_MomentEditsFields = {
	music: MomentData_MomentMetadata_MomentEdits_MusicEdit?,
	trim: MomentData_MomentMetadata_MomentEdits_TrimEdit?,
	text_overlays: { MomentData_MomentMetadata_MomentEdits_TextOverlay },
}

type _MomentData_MomentMetadata_MomentEditsPartialFields = {
	music: MomentData_MomentMetadata_MomentEdits_MusicEdit?,
	trim: MomentData_MomentMetadata_MomentEdits_TrimEdit?,
	text_overlays: { MomentData_MomentMetadata_MomentEdits_TextOverlay }?,
}

export type MomentData_MomentMetadata_MomentEdits = typeof(setmetatable(
	{} :: _MomentData_MomentMetadata_MomentEditsFields,
	{} :: _MomentData_MomentMetadata_MomentEditsImpl
))
type _MomentData_MomentMetadata_MomentEditsMessage = proto.Message<
	MomentData_MomentMetadata_MomentEdits,
	_MomentData_MomentMetadata_MomentEditsPartialFields
>

type _MomentData_MomentMetadata_MomentEdits_MusicEditImpl = {
	__index: _MomentData_MomentMetadata_MomentEdits_MusicEditImpl,
	new: (
		fields: _MomentData_MomentMetadata_MomentEdits_MusicEditPartialFields?
	) -> MomentData_MomentMetadata_MomentEdits_MusicEdit,
	encode: (self: MomentData_MomentMetadata_MomentEdits_MusicEdit) -> buffer,
	decode: (input: buffer) -> MomentData_MomentMetadata_MomentEdits_MusicEdit,
	jsonEncode: (self: MomentData_MomentMetadata_MomentEdits_MusicEdit) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData_MomentMetadata_MomentEdits_MusicEdit,
	descriptor: proto.Descriptor,
}

type _MomentData_MomentMetadata_MomentEdits_MusicEditFields = {
	asset_id: string?,
	start_time_seconds: number?,
}

type _MomentData_MomentMetadata_MomentEdits_MusicEditPartialFields = {
	asset_id: string?,
	start_time_seconds: number?,
}

export type MomentData_MomentMetadata_MomentEdits_MusicEdit = typeof(setmetatable(
	{} :: _MomentData_MomentMetadata_MomentEdits_MusicEditFields,
	{} :: _MomentData_MomentMetadata_MomentEdits_MusicEditImpl
))
type _MomentData_MomentMetadata_MomentEdits_MusicEditMessage = proto.Message<
	MomentData_MomentMetadata_MomentEdits_MusicEdit,
	_MomentData_MomentMetadata_MomentEdits_MusicEditPartialFields
>

type _MomentData_MomentMetadata_MomentEdits_TrimEditImpl = {
	__index: _MomentData_MomentMetadata_MomentEdits_TrimEditImpl,
	new: (
		fields: _MomentData_MomentMetadata_MomentEdits_TrimEditPartialFields?
	) -> MomentData_MomentMetadata_MomentEdits_TrimEdit,
	encode: (self: MomentData_MomentMetadata_MomentEdits_TrimEdit) -> buffer,
	decode: (input: buffer) -> MomentData_MomentMetadata_MomentEdits_TrimEdit,
	jsonEncode: (self: MomentData_MomentMetadata_MomentEdits_TrimEdit) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData_MomentMetadata_MomentEdits_TrimEdit,
	descriptor: proto.Descriptor,
}

type _MomentData_MomentMetadata_MomentEdits_TrimEditFields = {
	start_time_seconds: number?,
	end_time_seconds: number?,
}

type _MomentData_MomentMetadata_MomentEdits_TrimEditPartialFields = {
	start_time_seconds: number?,
	end_time_seconds: number?,
}

export type MomentData_MomentMetadata_MomentEdits_TrimEdit = typeof(setmetatable(
	{} :: _MomentData_MomentMetadata_MomentEdits_TrimEditFields,
	{} :: _MomentData_MomentMetadata_MomentEdits_TrimEditImpl
))
type _MomentData_MomentMetadata_MomentEdits_TrimEditMessage = proto.Message<
	MomentData_MomentMetadata_MomentEdits_TrimEdit,
	_MomentData_MomentMetadata_MomentEdits_TrimEditPartialFields
>

type _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl = {
	__index: _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl,
	new: (
		fields: _MomentData_MomentMetadata_MomentEdits_TextOverlayPartialFields?
	) -> MomentData_MomentMetadata_MomentEdits_TextOverlay,
	encode: (self: MomentData_MomentMetadata_MomentEdits_TextOverlay) -> buffer,
	decode: (input: buffer) -> MomentData_MomentMetadata_MomentEdits_TextOverlay,
	jsonEncode: (self: MomentData_MomentMetadata_MomentEdits_TextOverlay) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData_MomentMetadata_MomentEdits_TextOverlay,
	descriptor: proto.Descriptor,
}

type _MomentData_MomentMetadata_MomentEdits_TextOverlayFields = {
	position: { number },
	rotation: number?,
	scale: number?,
	text: string?,
	style: MomentData_MomentMetadata_MomentEdits_TextOverlay_Style?,
	z_index: number?,
}

type _MomentData_MomentMetadata_MomentEdits_TextOverlayPartialFields = {
	position: { number }?,
	rotation: number?,
	scale: number?,
	text: string?,
	style: MomentData_MomentMetadata_MomentEdits_TextOverlay_Style?,
	z_index: number?,
}

export type MomentData_MomentMetadata_MomentEdits_TextOverlay = typeof(setmetatable(
	{} :: _MomentData_MomentMetadata_MomentEdits_TextOverlayFields,
	{} :: _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl
))
type _MomentData_MomentMetadata_MomentEdits_TextOverlayMessage = proto.Message<
	MomentData_MomentMetadata_MomentEdits_TextOverlay,
	_MomentData_MomentMetadata_MomentEdits_TextOverlayPartialFields
>

type _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl = {
	__index: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl,
	new: (
		fields: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StylePartialFields?
	) -> MomentData_MomentMetadata_MomentEdits_TextOverlay_Style,
	encode: (self: MomentData_MomentMetadata_MomentEdits_TextOverlay_Style) -> buffer,
	decode: (input: buffer) -> MomentData_MomentMetadata_MomentEdits_TextOverlay_Style,
	jsonEncode: (self: MomentData_MomentMetadata_MomentEdits_TextOverlay_Style) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MomentData_MomentMetadata_MomentEdits_TextOverlay_Style,
	descriptor: proto.Descriptor,
}

type _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleFields = {
	font: string?,
	font_color: { number },
	font_size: number?,
	text_x_alignment: string?,
}

type _MomentData_MomentMetadata_MomentEdits_TextOverlay_StylePartialFields = {
	font: string?,
	font_color: { number }?,
	font_size: number?,
	text_x_alignment: string?,
}

export type MomentData_MomentMetadata_MomentEdits_TextOverlay_Style = typeof(setmetatable(
	{} :: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleFields,
	{} :: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl
))
type _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleMessage = proto.Message<
	MomentData_MomentMetadata_MomentEdits_TextOverlay_Style,
	_MomentData_MomentMetadata_MomentEdits_TextOverlay_StylePartialFields
>

do
	local _MomentDataImpl = {}
	_MomentDataImpl.__index = _MomentDataImpl

	function _MomentDataImpl.new(data: _MomentDataPartialFields?): MomentData
		return setmetatable({
			moment_id = if data == nil or data.moment_id == nil then nil else data.moment_id,
			asset_id = if data == nil or data.asset_id == nil then nil else data.asset_id,
			creator_id = if data == nil or data.creator_id == nil then nil else data.creator_id,
			status = if data == nil or data.status == nil then nil else data.status,
			metadata = if data == nil or data.metadata == nil then nil else data.metadata,
		}, _MomentDataImpl :: _MomentDataImpl)
	end

	function _MomentDataImpl.encode(self: MomentData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.moment_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.moment_id)
		end

		if self.asset_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.asset_id)
		end

		if self.creator_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.creator_id)
		end

		if self.status ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.status)
		end

		if self.metadata ~= nil then
			local encoded = self.metadata:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentDataImpl.decode(input: buffer): MomentData
		local self = _MomentDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.moment_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.asset_id = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.creator_id = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.status = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.metadata = messages.MomentData_MomentMetadata.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentDataImpl.jsonEncode(self: MomentData): any
		local output = {}

		if self.moment_id ~= nil then
			output.momentId = self.moment_id
		end

		if self.asset_id ~= nil then
			output.assetId = self.asset_id
		end

		if self.creator_id ~= nil then
			output.creatorId = self.creator_id
		end

		if self.status ~= nil then
			output.status = self.status
		end

		if self.metadata ~= nil then
			output.metadata = self.metadata:jsonEncode()
		end

		return output
	end

	function _MomentDataImpl.jsonDecode(input: { [string]: any }): MomentData
		local self = _MomentDataImpl.new()

		if input.moment_id ~= nil then
			self.moment_id = input.moment_id
		end

		if input.momentId ~= nil then
			self.moment_id = input.momentId
		end

		if input.asset_id ~= nil then
			self.asset_id = input.asset_id
		end

		if input.assetId ~= nil then
			self.asset_id = input.assetId
		end

		if input.creator_id ~= nil then
			self.creator_id = input.creator_id
		end

		if input.creatorId ~= nil then
			self.creator_id = input.creatorId
		end

		if input.status ~= nil then
			self.status = input.status
		end

		if input.metadata ~= nil then
			self.metadata = messages.MomentData_MomentMetadata.jsonDecode(input.metadata)
		end

		return self
	end

	_MomentDataImpl.descriptor = {
		name = "MomentData",
		fullName = "roblox.apppageplatform.shared.v1beta1.MomentData",
	}

	messages.MomentData = _MomentDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData)
end

do
	local _MomentData_MomentMetadataImpl = {}
	_MomentData_MomentMetadataImpl.__index = _MomentData_MomentMetadataImpl

	function _MomentData_MomentMetadataImpl.new(
		data: _MomentData_MomentMetadataPartialFields?
	): MomentData_MomentMetadata
		return setmetatable({
			asset_total_duration_seconds = if data == nil or data.asset_total_duration_seconds == nil
				then nil
				else data.asset_total_duration_seconds,
			capture_type = if data == nil or data.capture_type == nil then nil else data.capture_type,
			description = if data == nil or data.description == nil then nil else data.description,
			capture_universe_id = if data == nil or data.capture_universe_id == nil
				then nil
				else data.capture_universe_id,
			capture_place_id = if data == nil or data.capture_place_id == nil then nil else data.capture_place_id,
			edits = if data == nil or data.edits == nil then nil else data.edits,
		}, _MomentData_MomentMetadataImpl :: _MomentData_MomentMetadataImpl)
	end

	function _MomentData_MomentMetadataImpl.encode(self: MomentData_MomentMetadata): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.asset_total_duration_seconds ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.asset_total_duration_seconds)
		end

		if self.capture_type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.capture_type)
		end

		if self.description ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		if self.capture_universe_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.capture_universe_id)
		end

		if self.capture_place_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.capture_place_id)
		end

		if self.edits ~= nil then
			local encoded = self.edits:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentData_MomentMetadataImpl.decode(input: buffer): MomentData_MomentMetadata
		local self = _MomentData_MomentMetadataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.capture_type = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.capture_universe_id = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.capture_place_id = buffer.tostring(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.edits = messages.MomentData_MomentMetadata_MomentEdits.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 1 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.asset_total_duration_seconds = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentData_MomentMetadataImpl.jsonEncode(self: MomentData_MomentMetadata): any
		local output = {}

		if self.asset_total_duration_seconds ~= nil then
			output.assetTotalDurationSeconds = proto.json.serializeNumber(self.asset_total_duration_seconds)
		end

		if self.capture_type ~= nil then
			output.captureType = self.capture_type
		end

		if self.description ~= nil then
			output.description = self.description
		end

		if self.capture_universe_id ~= nil then
			output.captureUniverseId = self.capture_universe_id
		end

		if self.capture_place_id ~= nil then
			output.capturePlaceId = self.capture_place_id
		end

		if self.edits ~= nil then
			output.edits = self.edits:jsonEncode()
		end

		return output
	end

	function _MomentData_MomentMetadataImpl.jsonDecode(input: { [string]: any }): MomentData_MomentMetadata
		local self = _MomentData_MomentMetadataImpl.new()

		if input.asset_total_duration_seconds ~= nil then
			self.asset_total_duration_seconds = proto.json.deserializeNumber(input.asset_total_duration_seconds)
		end

		if input.assetTotalDurationSeconds ~= nil then
			self.asset_total_duration_seconds = proto.json.deserializeNumber(input.assetTotalDurationSeconds)
		end

		if input.capture_type ~= nil then
			self.capture_type = input.capture_type
		end

		if input.captureType ~= nil then
			self.capture_type = input.captureType
		end

		if input.description ~= nil then
			self.description = input.description
		end

		if input.capture_universe_id ~= nil then
			self.capture_universe_id = input.capture_universe_id
		end

		if input.captureUniverseId ~= nil then
			self.capture_universe_id = input.captureUniverseId
		end

		if input.capture_place_id ~= nil then
			self.capture_place_id = input.capture_place_id
		end

		if input.capturePlaceId ~= nil then
			self.capture_place_id = input.capturePlaceId
		end

		if input.edits ~= nil then
			self.edits = messages.MomentData_MomentMetadata_MomentEdits.jsonDecode(input.edits)
		end

		return self
	end

	_MomentData_MomentMetadataImpl.descriptor = {
		name = "MomentData_MomentMetadata",
		fullName = "roblox.apppageplatform.shared.v1beta1.MomentMetadata",
	}

	messages.MomentData_MomentMetadata = _MomentData_MomentMetadataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData_MomentMetadata)
end

do
	local _MomentData_MomentMetadata_MomentEditsImpl = {}
	_MomentData_MomentMetadata_MomentEditsImpl.__index = _MomentData_MomentMetadata_MomentEditsImpl

	function _MomentData_MomentMetadata_MomentEditsImpl.new(
		data: _MomentData_MomentMetadata_MomentEditsPartialFields?
	): MomentData_MomentMetadata_MomentEdits
		return setmetatable({
			music = if data == nil or data.music == nil then nil else data.music,
			trim = if data == nil or data.trim == nil then nil else data.trim,
			text_overlays = if data == nil or data.text_overlays == nil then {} else data.text_overlays,
		}, _MomentData_MomentMetadata_MomentEditsImpl :: _MomentData_MomentMetadata_MomentEditsImpl)
	end

	function _MomentData_MomentMetadata_MomentEditsImpl.encode(self: MomentData_MomentMetadata_MomentEdits): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.music ~= nil then
			local encoded = self.music:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.trim ~= nil then
			local encoded = self.trim:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.text_overlays ~= nil and #self.text_overlays > 0 then
			for _, value in self.text_overlays do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentData_MomentMetadata_MomentEditsImpl.decode(input: buffer): MomentData_MomentMetadata_MomentEdits
		local self = _MomentData_MomentMetadata_MomentEditsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.music = messages.MomentData_MomentMetadata_MomentEdits_MusicEdit.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.trim = messages.MomentData_MomentMetadata_MomentEdits_TrimEdit.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.text_overlays,
						messages.MomentData_MomentMetadata_MomentEdits_TextOverlay.decode(value)
					)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentData_MomentMetadata_MomentEditsImpl.jsonEncode(self: MomentData_MomentMetadata_MomentEdits): any
		local output = {}

		if self.music ~= nil then
			output.music = self.music:jsonEncode()
		end

		if self.trim ~= nil then
			output.trim = self.trim:jsonEncode()
		end

		if self.text_overlays ~= nil and #self.text_overlays > 0 then
			local newOutput = {}
			for _, value in self.text_overlays do
				table.insert(newOutput, value:jsonEncode())
			end
			output.textOverlays = newOutput
		end

		return output
	end

	function _MomentData_MomentMetadata_MomentEditsImpl.jsonDecode(
		input: { [string]: any }
	): MomentData_MomentMetadata_MomentEdits
		local self = _MomentData_MomentMetadata_MomentEditsImpl.new()

		if input.music ~= nil then
			self.music = messages.MomentData_MomentMetadata_MomentEdits_MusicEdit.jsonDecode(input.music)
		end

		if input.trim ~= nil then
			self.trim = messages.MomentData_MomentMetadata_MomentEdits_TrimEdit.jsonDecode(input.trim)
		end

		if input.text_overlays ~= nil then
			local newOutput: { MomentData_MomentMetadata_MomentEdits_TextOverlay } = {}
			for _, value in input.text_overlays do
				table.insert(newOutput, messages.MomentData_MomentMetadata_MomentEdits_TextOverlay.jsonDecode(value))
			end

			self.text_overlays = newOutput
		end

		if input.textOverlays ~= nil then
			local newOutput: { MomentData_MomentMetadata_MomentEdits_TextOverlay } = {}
			for _, value in input.textOverlays do
				table.insert(newOutput, messages.MomentData_MomentMetadata_MomentEdits_TextOverlay.jsonDecode(value))
			end

			self.text_overlays = newOutput
		end

		return self
	end

	_MomentData_MomentMetadata_MomentEditsImpl.descriptor = {
		name = "MomentData_MomentMetadata_MomentEdits",
		fullName = "roblox.apppageplatform.shared.v1beta1.MomentEdits",
	}

	messages.MomentData_MomentMetadata_MomentEdits = _MomentData_MomentMetadata_MomentEditsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData_MomentMetadata_MomentEdits)
end

do
	local _MomentData_MomentMetadata_MomentEdits_MusicEditImpl = {}
	_MomentData_MomentMetadata_MomentEdits_MusicEditImpl.__index = _MomentData_MomentMetadata_MomentEdits_MusicEditImpl

	function _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.new(
		data: _MomentData_MomentMetadata_MomentEdits_MusicEditPartialFields?
	): MomentData_MomentMetadata_MomentEdits_MusicEdit
		return setmetatable({
			asset_id = if data == nil or data.asset_id == nil then nil else data.asset_id,
			start_time_seconds = if data == nil or data.start_time_seconds == nil then nil else data.start_time_seconds,
		}, _MomentData_MomentMetadata_MomentEdits_MusicEditImpl :: _MomentData_MomentMetadata_MomentEdits_MusicEditImpl)
	end

	function _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.encode(
		self: MomentData_MomentMetadata_MomentEdits_MusicEdit
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.asset_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.asset_id)
		end

		if self.start_time_seconds ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.start_time_seconds)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.decode(
		input: buffer
	): MomentData_MomentMetadata_MomentEdits_MusicEdit
		local self = _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.asset_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.start_time_seconds = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.jsonEncode(
		self: MomentData_MomentMetadata_MomentEdits_MusicEdit
	): any
		local output = {}

		if self.asset_id ~= nil then
			output.assetId = self.asset_id
		end

		if self.start_time_seconds ~= nil then
			output.startTimeSeconds = proto.json.serializeNumber(self.start_time_seconds)
		end

		return output
	end

	function _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.jsonDecode(
		input: { [string]: any }
	): MomentData_MomentMetadata_MomentEdits_MusicEdit
		local self = _MomentData_MomentMetadata_MomentEdits_MusicEditImpl.new()

		if input.asset_id ~= nil then
			self.asset_id = input.asset_id
		end

		if input.assetId ~= nil then
			self.asset_id = input.assetId
		end

		if input.start_time_seconds ~= nil then
			self.start_time_seconds = proto.json.deserializeNumber(input.start_time_seconds)
		end

		if input.startTimeSeconds ~= nil then
			self.start_time_seconds = proto.json.deserializeNumber(input.startTimeSeconds)
		end

		return self
	end

	_MomentData_MomentMetadata_MomentEdits_MusicEditImpl.descriptor = {
		name = "MomentData_MomentMetadata_MomentEdits_MusicEdit",
		fullName = "roblox.apppageplatform.shared.v1beta1.MusicEdit",
	}

	messages.MomentData_MomentMetadata_MomentEdits_MusicEdit =
		_MomentData_MomentMetadata_MomentEdits_MusicEditImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData_MomentMetadata_MomentEdits_MusicEdit)
end

do
	local _MomentData_MomentMetadata_MomentEdits_TrimEditImpl = {}
	_MomentData_MomentMetadata_MomentEdits_TrimEditImpl.__index = _MomentData_MomentMetadata_MomentEdits_TrimEditImpl

	function _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.new(
		data: _MomentData_MomentMetadata_MomentEdits_TrimEditPartialFields?
	): MomentData_MomentMetadata_MomentEdits_TrimEdit
		return setmetatable({
			start_time_seconds = if data == nil or data.start_time_seconds == nil then nil else data.start_time_seconds,
			end_time_seconds = if data == nil or data.end_time_seconds == nil then nil else data.end_time_seconds,
		}, _MomentData_MomentMetadata_MomentEdits_TrimEditImpl :: _MomentData_MomentMetadata_MomentEdits_TrimEditImpl)
	end

	function _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.encode(
		self: MomentData_MomentMetadata_MomentEdits_TrimEdit
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.start_time_seconds ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.start_time_seconds)
		end

		if self.end_time_seconds ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.end_time_seconds)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.decode(
		input: buffer
	): MomentData_MomentMetadata_MomentEdits_TrimEdit
		local self = _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 1 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.start_time_seconds = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.end_time_seconds = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.jsonEncode(
		self: MomentData_MomentMetadata_MomentEdits_TrimEdit
	): any
		local output = {}

		if self.start_time_seconds ~= nil then
			output.startTimeSeconds = proto.json.serializeNumber(self.start_time_seconds)
		end

		if self.end_time_seconds ~= nil then
			output.endTimeSeconds = proto.json.serializeNumber(self.end_time_seconds)
		end

		return output
	end

	function _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.jsonDecode(
		input: { [string]: any }
	): MomentData_MomentMetadata_MomentEdits_TrimEdit
		local self = _MomentData_MomentMetadata_MomentEdits_TrimEditImpl.new()

		if input.start_time_seconds ~= nil then
			self.start_time_seconds = proto.json.deserializeNumber(input.start_time_seconds)
		end

		if input.startTimeSeconds ~= nil then
			self.start_time_seconds = proto.json.deserializeNumber(input.startTimeSeconds)
		end

		if input.end_time_seconds ~= nil then
			self.end_time_seconds = proto.json.deserializeNumber(input.end_time_seconds)
		end

		if input.endTimeSeconds ~= nil then
			self.end_time_seconds = proto.json.deserializeNumber(input.endTimeSeconds)
		end

		return self
	end

	_MomentData_MomentMetadata_MomentEdits_TrimEditImpl.descriptor = {
		name = "MomentData_MomentMetadata_MomentEdits_TrimEdit",
		fullName = "roblox.apppageplatform.shared.v1beta1.TrimEdit",
	}

	messages.MomentData_MomentMetadata_MomentEdits_TrimEdit = _MomentData_MomentMetadata_MomentEdits_TrimEditImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData_MomentMetadata_MomentEdits_TrimEdit)
end

do
	local _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl = {}
	_MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.__index =
		_MomentData_MomentMetadata_MomentEdits_TextOverlayImpl

	function _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.new(
		data: _MomentData_MomentMetadata_MomentEdits_TextOverlayPartialFields?
	): MomentData_MomentMetadata_MomentEdits_TextOverlay
		return setmetatable(
			{
				position = if data == nil or data.position == nil then {} else data.position,
				rotation = if data == nil or data.rotation == nil then nil else data.rotation,
				scale = if data == nil or data.scale == nil then nil else data.scale,
				text = if data == nil or data.text == nil then nil else data.text,
				style = if data == nil or data.style == nil then nil else data.style,
				z_index = if data == nil or data.z_index == nil then nil else data.z_index,
			},
			_MomentData_MomentMetadata_MomentEdits_TextOverlayImpl :: _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl
		)
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.encode(
		self: MomentData_MomentMetadata_MomentEdits_TextOverlay
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.position ~= nil and #self.position > 0 then
			local packedBuffer = buffer.create(0)
			local packedCursor = 0

			for _, value in self.position do
				packedBuffer, packedCursor = proto.writeFloat(packedBuffer, packedCursor, value)
			end

			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, packedBuffer, packedCursor)
		end

		if self.rotation ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.rotation)
		end

		if self.scale ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.scale)
		end

		if self.text ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.text)
		end

		if self.style ~= nil then
			local encoded = self.style:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.z_index ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.z_index)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.decode(
		input: buffer
	): MomentData_MomentMetadata_MomentEdits_TextOverlay
		local self = _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 6 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.z_index = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local length
					length, cursor = proto.readVarInt(input, cursor)

					local limit = cursor + length

					while cursor < limit do
						local value
						value, cursor = proto.readFloat(input, cursor)
						table.insert(self.position, value)
					end

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.style = messages.MomentData_MomentMetadata_MomentEdits_TextOverlay_Style.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 1 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					table.insert(self.position, value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.rotation = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.scale = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.jsonEncode(
		self: MomentData_MomentMetadata_MomentEdits_TextOverlay
	): any
		local output = {}

		if self.position ~= nil and #self.position > 0 then
			local newOutput = {}
			for _, value in self.position do
				table.insert(newOutput, proto.json.serializeNumber(value))
			end
			output.position = newOutput
		end

		if self.rotation ~= nil then
			output.rotation = proto.json.serializeNumber(self.rotation)
		end

		if self.scale ~= nil then
			output.scale = proto.json.serializeNumber(self.scale)
		end

		if self.text ~= nil then
			output.text = self.text
		end

		if self.style ~= nil then
			output.style = self.style:jsonEncode()
		end

		if self.z_index ~= nil then
			output.zIndex = self.z_index
		end

		return output
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.jsonDecode(
		input: { [string]: any }
	): MomentData_MomentMetadata_MomentEdits_TextOverlay
		local self = _MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.new()

		if input.position ~= nil then
			local newOutput: { number } = {}
			for _, value in input.position do
				table.insert(newOutput, proto.json.deserializeNumber(value))
			end

			self.position = newOutput
		end

		if input.rotation ~= nil then
			self.rotation = proto.json.deserializeNumber(input.rotation)
		end

		if input.scale ~= nil then
			self.scale = proto.json.deserializeNumber(input.scale)
		end

		if input.text ~= nil then
			self.text = input.text
		end

		if input.style ~= nil then
			self.style = messages.MomentData_MomentMetadata_MomentEdits_TextOverlay_Style.jsonDecode(input.style)
		end

		if input.z_index ~= nil then
			self.z_index = input.z_index
		end

		if input.zIndex ~= nil then
			self.z_index = input.zIndex
		end

		return self
	end

	_MomentData_MomentMetadata_MomentEdits_TextOverlayImpl.descriptor = {
		name = "MomentData_MomentMetadata_MomentEdits_TextOverlay",
		fullName = "roblox.apppageplatform.shared.v1beta1.TextOverlay",
	}

	messages.MomentData_MomentMetadata_MomentEdits_TextOverlay =
		_MomentData_MomentMetadata_MomentEdits_TextOverlayImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData_MomentMetadata_MomentEdits_TextOverlay)
end

do
	local _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl = {}
	_MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.__index =
		_MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl

	function _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.new(
		data: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StylePartialFields?
	): MomentData_MomentMetadata_MomentEdits_TextOverlay_Style
		return setmetatable(
			{
				font = if data == nil or data.font == nil then nil else data.font,
				font_color = if data == nil or data.font_color == nil then {} else data.font_color,
				font_size = if data == nil or data.font_size == nil then nil else data.font_size,
				text_x_alignment = if data == nil or data.text_x_alignment == nil then nil else data.text_x_alignment,
			},
			_MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl :: _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl
		)
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.encode(
		self: MomentData_MomentMetadata_MomentEdits_TextOverlay_Style
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.font ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.font)
		end

		if self.font_color ~= nil and #self.font_color > 0 then
			local packedBuffer = buffer.create(0)
			local packedCursor = 0

			for _, value in self.font_color do
				packedBuffer, packedCursor = proto.writeFloat(packedBuffer, packedCursor, value)
			end

			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, packedBuffer, packedCursor)
		end

		if self.font_size ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.font_size)
		end

		if self.text_x_alignment ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.text_x_alignment)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.decode(
		input: buffer
	): MomentData_MomentMetadata_MomentEdits_TextOverlay_Style
		local self = _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.font = buffer.tostring(value)
					continue
				elseif field == 2 then
					local length
					length, cursor = proto.readVarInt(input, cursor)

					local limit = cursor + length

					while cursor < limit do
						local value
						value, cursor = proto.readFloat(input, cursor)
						table.insert(self.font_color, value)
					end

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text_x_alignment = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					table.insert(self.font_color, value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.font_size = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.jsonEncode(
		self: MomentData_MomentMetadata_MomentEdits_TextOverlay_Style
	): any
		local output = {}

		if self.font ~= nil then
			output.font = self.font
		end

		if self.font_color ~= nil and #self.font_color > 0 then
			local newOutput = {}
			for _, value in self.font_color do
				table.insert(newOutput, proto.json.serializeNumber(value))
			end
			output.fontColor = newOutput
		end

		if self.font_size ~= nil then
			output.fontSize = proto.json.serializeNumber(self.font_size)
		end

		if self.text_x_alignment ~= nil then
			output.textXAlignment = self.text_x_alignment
		end

		return output
	end

	function _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.jsonDecode(
		input: { [string]: any }
	): MomentData_MomentMetadata_MomentEdits_TextOverlay_Style
		local self = _MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.new()

		if input.font ~= nil then
			self.font = input.font
		end

		if input.font_color ~= nil then
			local newOutput: { number } = {}
			for _, value in input.font_color do
				table.insert(newOutput, proto.json.deserializeNumber(value))
			end

			self.font_color = newOutput
		end

		if input.fontColor ~= nil then
			local newOutput: { number } = {}
			for _, value in input.fontColor do
				table.insert(newOutput, proto.json.deserializeNumber(value))
			end

			self.font_color = newOutput
		end

		if input.font_size ~= nil then
			self.font_size = proto.json.deserializeNumber(input.font_size)
		end

		if input.fontSize ~= nil then
			self.font_size = proto.json.deserializeNumber(input.fontSize)
		end

		if input.text_x_alignment ~= nil then
			self.text_x_alignment = input.text_x_alignment
		end

		if input.textXAlignment ~= nil then
			self.text_x_alignment = input.textXAlignment
		end

		return self
	end

	_MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl.descriptor = {
		name = "MomentData_MomentMetadata_MomentEdits_TextOverlay_Style",
		fullName = "roblox.apppageplatform.shared.v1beta1.Style",
	}

	messages.MomentData_MomentMetadata_MomentEdits_TextOverlay_Style =
		_MomentData_MomentMetadata_MomentEdits_TextOverlay_StyleImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MomentData_MomentMetadata_MomentEdits_TextOverlay_Style)
end

return {
	MomentData = messages.MomentData,
	MomentData_MomentMetadata = messages.MomentData_MomentMetadata,
	MomentData_MomentMetadata_MomentEdits = messages.MomentData_MomentMetadata_MomentEdits,
	MomentData_MomentMetadata_MomentEdits_MusicEdit = messages.MomentData_MomentMetadata_MomentEdits_MusicEdit,
	MomentData_MomentMetadata_MomentEdits_TrimEdit = messages.MomentData_MomentMetadata_MomentEdits_TrimEdit,
	MomentData_MomentMetadata_MomentEdits_TextOverlay = messages.MomentData_MomentMetadata_MomentEdits_TextOverlay,
	MomentData_MomentMetadata_MomentEdits_TextOverlay_Style = messages.MomentData_MomentMetadata_MomentEdits_TextOverlay_Style,
}
