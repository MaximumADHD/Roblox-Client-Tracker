--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GameSubscriptionData: _GameSubscriptionDataMessage,
}
local messages: _Messages = {} :: _Messages

type _GameSubscriptionDataImpl = {
	__index: _GameSubscriptionDataImpl,
	new: (fields: _GameSubscriptionDataPartialFields?) -> GameSubscriptionData,
	encode: (self: GameSubscriptionData) -> buffer,
	decode: (input: buffer) -> GameSubscriptionData,
	jsonEncode: (self: GameSubscriptionData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GameSubscriptionData,
	descriptor: proto.Descriptor,
}

type _GameSubscriptionDataFields = {
	id: string,
	name: string,
	description: string,
	image_asset_id: number,
	price_display: string,
	period_type: string,
	is_subscribed: boolean,
	price_in_robux: number,
	price_tier: number,
}

type _GameSubscriptionDataPartialFields = {
	id: string?,
	name: string?,
	description: string?,
	image_asset_id: number?,
	price_display: string?,
	period_type: string?,
	is_subscribed: boolean?,
	price_in_robux: number?,
	price_tier: number?,
}

export type GameSubscriptionData = typeof(setmetatable(
	{} :: _GameSubscriptionDataFields,
	{} :: _GameSubscriptionDataImpl
))
type _GameSubscriptionDataMessage = proto.Message<GameSubscriptionData, _GameSubscriptionDataPartialFields>

do
	local _GameSubscriptionDataImpl = {}
	_GameSubscriptionDataImpl.__index = _GameSubscriptionDataImpl

	function _GameSubscriptionDataImpl.new(data: _GameSubscriptionDataPartialFields?): GameSubscriptionData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			name = if data == nil or data.name == nil then "" else data.name,
			description = if data == nil or data.description == nil then "" else data.description,
			image_asset_id = if data == nil or data.image_asset_id == nil then 0 else data.image_asset_id,
			price_display = if data == nil or data.price_display == nil then "" else data.price_display,
			period_type = if data == nil or data.period_type == nil then "" else data.period_type,
			is_subscribed = if data == nil or data.is_subscribed == nil then false else data.is_subscribed,
			price_in_robux = if data == nil or data.price_in_robux == nil then 0 else data.price_in_robux,
			price_tier = if data == nil or data.price_tier == nil then 0 else data.price_tier,
		}, _GameSubscriptionDataImpl :: _GameSubscriptionDataImpl)
	end

	function _GameSubscriptionDataImpl.encode(self: GameSubscriptionData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.description ~= nil and self.description ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		if self.image_asset_id ~= nil and self.image_asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.image_asset_id)
		end

		if self.price_display ~= nil and self.price_display ~= "" then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.price_display)
		end

		if self.period_type ~= nil and self.period_type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.period_type)
		end

		if self.is_subscribed then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_subscribed then 1 else 0)
		end

		if self.price_in_robux ~= nil and self.price_in_robux ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.price_in_robux)
		end

		if self.price_tier ~= nil and self.price_tier ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.price_tier)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GameSubscriptionDataImpl.decode(input: buffer): GameSubscriptionData
		local self = _GameSubscriptionDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 4 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.image_asset_id = value
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_subscribed = value ~= 0
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.price_in_robux = value
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.price_tier = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.price_display = buffer.tostring(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.period_type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GameSubscriptionDataImpl.jsonEncode(self: GameSubscriptionData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.description ~= nil and self.description ~= "" then
			output.description = self.description
		end

		if self.image_asset_id ~= nil and self.image_asset_id ~= 0 then
			output.imageAssetId = self.image_asset_id
		end

		if self.price_display ~= nil and self.price_display ~= "" then
			output.priceDisplay = self.price_display
		end

		if self.period_type ~= nil and self.period_type ~= "" then
			output.periodType = self.period_type
		end

		if self.is_subscribed then
			output.isSubscribed = self.is_subscribed
		end

		if self.price_in_robux ~= nil and self.price_in_robux ~= 0 then
			output.priceInRobux = self.price_in_robux
		end

		if self.price_tier ~= nil and self.price_tier ~= 0 then
			output.priceTier = self.price_tier
		end

		return output
	end

	function _GameSubscriptionDataImpl.jsonDecode(input: { [string]: any }): GameSubscriptionData
		local self = _GameSubscriptionDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.description ~= nil then
			self.description = input.description
		end

		if input.image_asset_id ~= nil then
			self.image_asset_id = input.image_asset_id
		end

		if input.imageAssetId ~= nil then
			self.image_asset_id = input.imageAssetId
		end

		if input.price_display ~= nil then
			self.price_display = input.price_display
		end

		if input.priceDisplay ~= nil then
			self.price_display = input.priceDisplay
		end

		if input.period_type ~= nil then
			self.period_type = input.period_type
		end

		if input.periodType ~= nil then
			self.period_type = input.periodType
		end

		if input.is_subscribed ~= nil then
			self.is_subscribed = input.is_subscribed
		end

		if input.isSubscribed ~= nil then
			self.is_subscribed = input.isSubscribed
		end

		if input.price_in_robux ~= nil then
			self.price_in_robux = input.price_in_robux
		end

		if input.priceInRobux ~= nil then
			self.price_in_robux = input.priceInRobux
		end

		if input.price_tier ~= nil then
			self.price_tier = input.price_tier
		end

		if input.priceTier ~= nil then
			self.price_tier = input.priceTier
		end

		return self
	end

	_GameSubscriptionDataImpl.descriptor = {
		name = "GameSubscriptionData",
		fullName = "roblox.apppageplatform.shared.v1beta1.GameSubscriptionData",
	}

	messages.GameSubscriptionData = _GameSubscriptionDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GameSubscriptionData)
end

return {
	GameSubscriptionData = messages.GameSubscriptionData,
}
