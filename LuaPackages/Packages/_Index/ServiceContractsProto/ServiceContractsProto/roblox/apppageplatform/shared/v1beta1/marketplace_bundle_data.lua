--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	MarketplaceBundleData: _MarketplaceBundleDataMessage,
}
local messages: _Messages = {} :: _Messages

type _MarketplaceBundleDataImpl = {
	__index: _MarketplaceBundleDataImpl,
	new: (fields: _MarketplaceBundleDataPartialFields?) -> MarketplaceBundleData,
	encode: (self: MarketplaceBundleData) -> buffer,
	decode: (input: buffer) -> MarketplaceBundleData,
	jsonEncode: (self: MarketplaceBundleData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceBundleData,
	descriptor: proto.Descriptor,
}

type _MarketplaceBundleDataFields = {
	id: string,
}

type _MarketplaceBundleDataPartialFields = {
	id: string?,
}

export type MarketplaceBundleData = typeof(setmetatable(
	{} :: _MarketplaceBundleDataFields,
	{} :: _MarketplaceBundleDataImpl
))
type _MarketplaceBundleDataMessage = proto.Message<MarketplaceBundleData, _MarketplaceBundleDataPartialFields>

do
	local _MarketplaceBundleDataImpl = {}
	_MarketplaceBundleDataImpl.__index = _MarketplaceBundleDataImpl

	function _MarketplaceBundleDataImpl.new(data: _MarketplaceBundleDataPartialFields?): MarketplaceBundleData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
		}, _MarketplaceBundleDataImpl :: _MarketplaceBundleDataImpl)
	end

	function _MarketplaceBundleDataImpl.encode(self: MarketplaceBundleData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceBundleDataImpl.decode(input: buffer): MarketplaceBundleData
		local self = _MarketplaceBundleDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceBundleDataImpl.jsonEncode(self: MarketplaceBundleData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		return output
	end

	function _MarketplaceBundleDataImpl.jsonDecode(input: { [string]: any }): MarketplaceBundleData
		local self = _MarketplaceBundleDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		return self
	end

	_MarketplaceBundleDataImpl.descriptor = {
		name = "MarketplaceBundleData",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceBundleData",
	}

	messages.MarketplaceBundleData = _MarketplaceBundleDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceBundleData)
end

return {
	MarketplaceBundleData = messages.MarketplaceBundleData,
}
