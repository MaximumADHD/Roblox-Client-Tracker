--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	TextIconRowSchema: _TextIconRowSchemaMessage,
	TextIconRowSchema_Props: _TextIconRowSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _TextIconRowSchemaImpl = {
	__index: _TextIconRowSchemaImpl,
	new: (fields: _TextIconRowSchemaPartialFields?) -> TextIconRowSchema,
	encode: (self: TextIconRowSchema) -> buffer,
	decode: (input: buffer) -> TextIconRowSchema,
	jsonEncode: (self: TextIconRowSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextIconRowSchema,
	descriptor: proto.Descriptor,
}

type _TextIconRowSchemaFields = {
	props: TextIconRowSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _TextIconRowSchemaPartialFields = {
	props: TextIconRowSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type TextIconRowSchema = typeof(setmetatable({} :: _TextIconRowSchemaFields, {} :: _TextIconRowSchemaImpl))
type _TextIconRowSchemaMessage = proto.Message<TextIconRowSchema, _TextIconRowSchemaPartialFields>

type _TextIconRowSchema_PropsImpl = {
	__index: _TextIconRowSchema_PropsImpl,
	new: (fields: _TextIconRowSchema_PropsPartialFields?) -> TextIconRowSchema_Props,
	encode: (self: TextIconRowSchema_Props) -> buffer,
	decode: (input: buffer) -> TextIconRowSchema_Props,
	jsonEncode: (self: TextIconRowSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextIconRowSchema_Props,
	descriptor: proto.Descriptor,
}

type _TextIconRowSchema_PropsFields = {
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	selectable: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	icon_first: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	text_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	font_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	icon: _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp?,
	icon_width: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	icon_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	text_flex_mode: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	corner_radius: _roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp?,
}

type _TextIconRowSchema_PropsPartialFields = {
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	selectable: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	icon_first: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	text_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	font_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	icon: _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp?,
	icon_width: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	icon_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	text_flex_mode: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	corner_radius: _roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp?,
}

export type TextIconRowSchema_Props = typeof(setmetatable(
	{} :: _TextIconRowSchema_PropsFields,
	{} :: _TextIconRowSchema_PropsImpl
))
type _TextIconRowSchema_PropsMessage = proto.Message<TextIconRowSchema_Props, _TextIconRowSchema_PropsPartialFields>

do
	local _TextIconRowSchemaImpl = {}
	_TextIconRowSchemaImpl.__index = _TextIconRowSchemaImpl

	function _TextIconRowSchemaImpl.new(data: _TextIconRowSchemaPartialFields?): TextIconRowSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _TextIconRowSchemaImpl :: _TextIconRowSchemaImpl)
	end

	function _TextIconRowSchemaImpl.encode(self: TextIconRowSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextIconRowSchemaImpl.decode(input: buffer): TextIconRowSchema
		local self = _TextIconRowSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.TextIconRowSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextIconRowSchemaImpl.jsonEncode(self: TextIconRowSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _TextIconRowSchemaImpl.jsonDecode(input: { [string]: any }): TextIconRowSchema
		local self = _TextIconRowSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.TextIconRowSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_TextIconRowSchemaImpl.descriptor = {
		name = "TextIconRowSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.TextIconRowSchema",
	}

	messages.TextIconRowSchema = _TextIconRowSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextIconRowSchema)
end

do
	local _TextIconRowSchema_PropsImpl = {}
	_TextIconRowSchema_PropsImpl.__index = _TextIconRowSchema_PropsImpl

	function _TextIconRowSchema_PropsImpl.new(data: _TextIconRowSchema_PropsPartialFields?): TextIconRowSchema_Props
		return setmetatable({
			automatic_size = if data == nil or data.automatic_size == nil then nil else data.automatic_size,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			size = if data == nil or data.size == nil then nil else data.size,
			z_index = if data == nil or data.z_index == nil then nil else data.z_index,
			position = if data == nil or data.position == nil then nil else data.position,
			selectable = if data == nil or data.selectable == nil then nil else data.selectable,
			icon_first = if data == nil or data.icon_first == nil then nil else data.icon_first,
			on_activated = if data == nil or data.on_activated == nil then nil else data.on_activated,
			text = if data == nil or data.text == nil then nil else data.text,
			text_color = if data == nil or data.text_color == nil then nil else data.text_color,
			font_style = if data == nil or data.font_style == nil then nil else data.font_style,
			gap = if data == nil or data.gap == nil then nil else data.gap,
			icon = if data == nil or data.icon == nil then nil else data.icon,
			icon_width = if data == nil or data.icon_width == nil then nil else data.icon_width,
			icon_color = if data == nil or data.icon_color == nil then nil else data.icon_color,
			text_flex_mode = if data == nil or data.text_flex_mode == nil then nil else data.text_flex_mode,
			background_style = if data == nil or data.background_style == nil then nil else data.background_style,
			corner_radius = if data == nil or data.corner_radius == nil then nil else data.corner_radius,
		}, _TextIconRowSchema_PropsImpl :: _TextIconRowSchema_PropsImpl)
	end

	function _TextIconRowSchema_PropsImpl.encode(self: TextIconRowSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.automatic_size ~= nil then
			local encoded = self.automatic_size:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.z_index ~= nil then
			local encoded = self.z_index:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.selectable ~= nil then
			local encoded = self.selectable:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon_first ~= nil then
			local encoded = self.icon_first:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_activated ~= nil then
			local encoded = self.on_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.text ~= nil then
			local encoded = self.text:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.text_color ~= nil then
			local encoded = self.text_color:encode()
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.font_style ~= nil then
			local encoded = self.font_style:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.gap ~= nil then
			local encoded = self.gap:encode()
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon ~= nil then
			local encoded = self.icon:encode()
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon_width ~= nil then
			local encoded = self.icon_width:encode()
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon_color ~= nil then
			local encoded = self.icon_color:encode()
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.text_flex_mode ~= nil then
			local encoded = self.text_flex_mode:encode()
			output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_style ~= nil then
			local encoded = self.background_style:encode()
			output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.corner_radius ~= nil then
			local encoded = self.corner_radius:encode()
			output, cursor = proto.writeTag(output, cursor, 19, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextIconRowSchema_PropsImpl.decode(input: buffer): TextIconRowSchema_Props
		local self = _TextIconRowSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.automatic_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.selectable = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon_first = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.font_style = _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.decode(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon = _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.decode(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon_width = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text_flex_mode = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_style =
						_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.decode(value)
					continue
				elseif field == 19 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.corner_radius = _roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextIconRowSchema_PropsImpl.jsonEncode(self: TextIconRowSchema_Props): any
		local output = {}

		if self.automatic_size ~= nil then
			output.automaticSize = self.automatic_size:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.z_index ~= nil then
			output.zIndex = self.z_index:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.selectable ~= nil then
			output.selectable = self.selectable:jsonEncode()
		end

		if self.icon_first ~= nil then
			output.iconFirst = self.icon_first:jsonEncode()
		end

		if self.on_activated ~= nil then
			output.onActivated = self.on_activated:jsonEncode()
		end

		if self.text ~= nil then
			output.text = self.text:jsonEncode()
		end

		if self.text_color ~= nil then
			output.textColor = self.text_color:jsonEncode()
		end

		if self.font_style ~= nil then
			output.fontStyle = self.font_style:jsonEncode()
		end

		if self.gap ~= nil then
			output.gap = self.gap:jsonEncode()
		end

		if self.icon ~= nil then
			output.icon = self.icon:jsonEncode()
		end

		if self.icon_width ~= nil then
			output.iconWidth = self.icon_width:jsonEncode()
		end

		if self.icon_color ~= nil then
			output.iconColor = self.icon_color:jsonEncode()
		end

		if self.text_flex_mode ~= nil then
			output.textFlexMode = self.text_flex_mode:jsonEncode()
		end

		if self.background_style ~= nil then
			output.backgroundStyle = self.background_style:jsonEncode()
		end

		if self.corner_radius ~= nil then
			output.cornerRadius = self.corner_radius:jsonEncode()
		end

		return output
	end

	function _TextIconRowSchema_PropsImpl.jsonDecode(input: { [string]: any }): TextIconRowSchema_Props
		local self = _TextIconRowSchema_PropsImpl.new()

		if input.automatic_size ~= nil then
			self.automatic_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.automatic_size)
		end

		if input.automaticSize ~= nil then
			self.automatic_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.automaticSize)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.z_index ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.z_index)
		end

		if input.zIndex ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.zIndex)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.selectable ~= nil then
			self.selectable = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.selectable)
		end

		if input.icon_first ~= nil then
			self.icon_first = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.icon_first)
		end

		if input.iconFirst ~= nil then
			self.icon_first = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.iconFirst)
		end

		if input.on_activated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_activated)
		end

		if input.onActivated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onActivated)
		end

		if input.text ~= nil then
			self.text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.text)
		end

		if input.text_color ~= nil then
			self.text_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.text_color)
		end

		if input.textColor ~= nil then
			self.text_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.textColor)
		end

		if input.font_style ~= nil then
			self.font_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.font_style)
		end

		if input.fontStyle ~= nil then
			self.font_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.fontStyle)
		end

		if input.gap ~= nil then
			self.gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.gap)
		end

		if input.icon ~= nil then
			self.icon = _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.jsonDecode(input.icon)
		end

		if input.icon_width ~= nil then
			self.icon_width = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.icon_width)
		end

		if input.iconWidth ~= nil then
			self.icon_width = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.iconWidth)
		end

		if input.icon_color ~= nil then
			self.icon_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.icon_color)
		end

		if input.iconColor ~= nil then
			self.icon_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.iconColor)
		end

		if input.text_flex_mode ~= nil then
			self.text_flex_mode =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.text_flex_mode)
		end

		if input.textFlexMode ~= nil then
			self.text_flex_mode =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.textFlexMode)
		end

		if input.background_style ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.background_style)
		end

		if input.backgroundStyle ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.backgroundStyle)
		end

		if input.corner_radius ~= nil then
			self.corner_radius =
				_roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp.jsonDecode(input.corner_radius)
		end

		if input.cornerRadius ~= nil then
			self.corner_radius =
				_roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp.jsonDecode(input.cornerRadius)
		end

		return self
	end

	_TextIconRowSchema_PropsImpl.descriptor = {
		name = "TextIconRowSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.TextIconRowSchema_Props = _TextIconRowSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextIconRowSchema_Props)
end

return {
	TextIconRowSchema = messages.TextIconRowSchema,
	TextIconRowSchema_Props = messages.TextIconRowSchema_Props,
}
