--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	PlayabilityData: _PlayabilityDataMessage,
}
local messages: _Messages = {} :: _Messages

type _PlayabilityDataImpl = {
	__index: _PlayabilityDataImpl,
	new: (fields: _PlayabilityDataPartialFields?) -> PlayabilityData,
	encode: (self: PlayabilityData) -> buffer,
	decode: (input: buffer) -> PlayabilityData,
	jsonEncode: (self: PlayabilityData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PlayabilityData,
	descriptor: proto.Descriptor,
}

type _PlayabilityDataFields = {
	is_playable: boolean,
	playability_status: string,
}

type _PlayabilityDataPartialFields = {
	is_playable: boolean?,
	playability_status: string?,
}

export type PlayabilityData = typeof(setmetatable({} :: _PlayabilityDataFields, {} :: _PlayabilityDataImpl))
type _PlayabilityDataMessage = proto.Message<PlayabilityData, _PlayabilityDataPartialFields>

do
	local _PlayabilityDataImpl = {}
	_PlayabilityDataImpl.__index = _PlayabilityDataImpl

	function _PlayabilityDataImpl.new(data: _PlayabilityDataPartialFields?): PlayabilityData
		return setmetatable({
			is_playable = if data == nil or data.is_playable == nil then false else data.is_playable,
			playability_status = if data == nil or data.playability_status == nil then "" else data.playability_status,
		}, _PlayabilityDataImpl :: _PlayabilityDataImpl)
	end

	function _PlayabilityDataImpl.encode(self: PlayabilityData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.is_playable then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_playable then 1 else 0)
		end

		if self.playability_status ~= nil and self.playability_status ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.playability_status)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PlayabilityDataImpl.decode(input: buffer): PlayabilityData
		local self = _PlayabilityDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_playable = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.playability_status = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PlayabilityDataImpl.jsonEncode(self: PlayabilityData): any
		local output = {}

		if self.is_playable then
			output.isPlayable = self.is_playable
		end

		if self.playability_status ~= nil and self.playability_status ~= "" then
			output.playabilityStatus = self.playability_status
		end

		return output
	end

	function _PlayabilityDataImpl.jsonDecode(input: { [string]: any }): PlayabilityData
		local self = _PlayabilityDataImpl.new()

		if input.is_playable ~= nil then
			self.is_playable = input.is_playable
		end

		if input.isPlayable ~= nil then
			self.is_playable = input.isPlayable
		end

		if input.playability_status ~= nil then
			self.playability_status = input.playability_status
		end

		if input.playabilityStatus ~= nil then
			self.playability_status = input.playabilityStatus
		end

		return self
	end

	_PlayabilityDataImpl.descriptor = {
		name = "PlayabilityData",
		fullName = "roblox.apppageplatform.shared.v1beta1.PlayabilityData",
	}

	messages.PlayabilityData = _PlayabilityDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PlayabilityData)
end

return {
	PlayabilityData = messages.PlayabilityData,
}
