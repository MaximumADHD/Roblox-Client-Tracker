--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	ImageBackground: _ImageBackgroundMessage,
	BackgroundLayer: _BackgroundLayerMessage,
	ArrayOfBackgroundLayerProp: _ArrayOfBackgroundLayerPropMessage,
	ArrayOfBackgroundLayerProp_ConditionalOption: _ArrayOfBackgroundLayerProp_ConditionalOptionMessage,
	ArrayOfBackgroundLayerProp_ConditionalOptions: _ArrayOfBackgroundLayerProp_ConditionalOptionsMessage,
	ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_condition = require(script.Parent.prop_condition)

type _ImageBackgroundImpl = {
	__index: _ImageBackgroundImpl,
	new: (fields: _ImageBackgroundPartialFields?) -> ImageBackground,
	encode: (self: ImageBackground) -> buffer,
	decode: (input: buffer) -> ImageBackground,
	jsonEncode: (self: ImageBackground) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ImageBackground,
	descriptor: proto.Descriptor,
}

type _ImageBackgroundFields = {
	resource_identifier: string,
}

type _ImageBackgroundPartialFields = {
	resource_identifier: string?,
}

export type ImageBackground = typeof(setmetatable({} :: _ImageBackgroundFields, {} :: _ImageBackgroundImpl))
type _ImageBackgroundMessage = proto.Message<ImageBackground, _ImageBackgroundPartialFields>

type _BackgroundLayerImpl = {
	__index: _BackgroundLayerImpl,
	new: (fields: _BackgroundLayerPartialFields?) -> BackgroundLayer,
	encode: (self: BackgroundLayer) -> buffer,
	decode: (input: buffer) -> BackgroundLayer,
	jsonEncode: (self: BackgroundLayer) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> BackgroundLayer,
	descriptor: proto.Descriptor,
}

type _BackgroundLayerFields = {
	kind: { type: "image_background", value: ImageBackground }?,
}

type _BackgroundLayerPartialFields = {
	kind: { type: "image_background", value: ImageBackground }?,
}

export type BackgroundLayer = typeof(setmetatable({} :: _BackgroundLayerFields, {} :: _BackgroundLayerImpl))
type _BackgroundLayerMessage = proto.Message<BackgroundLayer, _BackgroundLayerPartialFields>

type _ArrayOfBackgroundLayerPropImpl = {
	__index: _ArrayOfBackgroundLayerPropImpl,
	new: (fields: _ArrayOfBackgroundLayerPropPartialFields?) -> ArrayOfBackgroundLayerProp,
	encode: (self: ArrayOfBackgroundLayerProp) -> buffer,
	decode: (input: buffer) -> ArrayOfBackgroundLayerProp,
	jsonEncode: (self: ArrayOfBackgroundLayerProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfBackgroundLayerProp,
	descriptor: proto.Descriptor,
}

type _ArrayOfBackgroundLayerPropFields = {
	kind: (
		{ type: "literal", value: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfBackgroundLayerProp_ConditionalOptions }
	)?,
}

type _ArrayOfBackgroundLayerPropPartialFields = {
	kind: (
		{ type: "literal", value: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfBackgroundLayerProp_ConditionalOptions }
	)?,
}

export type ArrayOfBackgroundLayerProp = typeof(setmetatable(
	{} :: _ArrayOfBackgroundLayerPropFields,
	{} :: _ArrayOfBackgroundLayerPropImpl
))
type _ArrayOfBackgroundLayerPropMessage = proto.Message<
	ArrayOfBackgroundLayerProp,
	_ArrayOfBackgroundLayerPropPartialFields
>

type _ArrayOfBackgroundLayerProp_ConditionalOptionImpl = {
	__index: _ArrayOfBackgroundLayerProp_ConditionalOptionImpl,
	new: (
		fields: _ArrayOfBackgroundLayerProp_ConditionalOptionPartialFields?
	) -> ArrayOfBackgroundLayerProp_ConditionalOption,
	encode: (self: ArrayOfBackgroundLayerProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> ArrayOfBackgroundLayerProp_ConditionalOption,
	jsonEncode: (self: ArrayOfBackgroundLayerProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfBackgroundLayerProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _ArrayOfBackgroundLayerProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: (
		{ type: "literal", value: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer }
		| { type: "binding_path", value: string }
	)?,
}

type _ArrayOfBackgroundLayerProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: (
		{ type: "literal", value: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer }
		| { type: "binding_path", value: string }
	)?,
}

export type ArrayOfBackgroundLayerProp_ConditionalOption = typeof(setmetatable(
	{} :: _ArrayOfBackgroundLayerProp_ConditionalOptionFields,
	{} :: _ArrayOfBackgroundLayerProp_ConditionalOptionImpl
))
type _ArrayOfBackgroundLayerProp_ConditionalOptionMessage = proto.Message<
	ArrayOfBackgroundLayerProp_ConditionalOption,
	_ArrayOfBackgroundLayerProp_ConditionalOptionPartialFields
>

type _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl = {
	__index: _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl,
	new: (
		fields: _ArrayOfBackgroundLayerProp_ConditionalOptionsPartialFields?
	) -> ArrayOfBackgroundLayerProp_ConditionalOptions,
	encode: (self: ArrayOfBackgroundLayerProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> ArrayOfBackgroundLayerProp_ConditionalOptions,
	jsonEncode: (self: ArrayOfBackgroundLayerProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfBackgroundLayerProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _ArrayOfBackgroundLayerProp_ConditionalOptionsFields = {
	options: { ArrayOfBackgroundLayerProp_ConditionalOption },
}

type _ArrayOfBackgroundLayerProp_ConditionalOptionsPartialFields = {
	options: { ArrayOfBackgroundLayerProp_ConditionalOption }?,
}

export type ArrayOfBackgroundLayerProp_ConditionalOptions = typeof(setmetatable(
	{} :: _ArrayOfBackgroundLayerProp_ConditionalOptionsFields,
	{} :: _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl
))
type _ArrayOfBackgroundLayerProp_ConditionalOptionsMessage = proto.Message<
	ArrayOfBackgroundLayerProp_ConditionalOptions,
	_ArrayOfBackgroundLayerProp_ConditionalOptionsPartialFields
>

type _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl = {
	__index: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl,
	new: (
		fields: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerPartialFields?
	) -> ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer,
	encode: (self: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer) -> buffer,
	decode: (input: buffer) -> ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer,
	jsonEncode: (self: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer,
	descriptor: proto.Descriptor,
}

type _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerFields = {
	array: { BackgroundLayer },
}

type _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerPartialFields = {
	array: { BackgroundLayer }?,
}

export type ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer = typeof(setmetatable(
	{} :: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerFields,
	{} :: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl
))
type _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerMessage = proto.Message<
	ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer,
	_ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerPartialFields
>

do
	local _ImageBackgroundImpl = {}
	_ImageBackgroundImpl.__index = _ImageBackgroundImpl

	function _ImageBackgroundImpl.new(data: _ImageBackgroundPartialFields?): ImageBackground
		return setmetatable({
			resource_identifier = if data == nil or data.resource_identifier == nil
				then ""
				else data.resource_identifier,
		}, _ImageBackgroundImpl :: _ImageBackgroundImpl)
	end

	function _ImageBackgroundImpl.encode(self: ImageBackground): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.resource_identifier ~= nil and self.resource_identifier ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.resource_identifier)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ImageBackgroundImpl.decode(input: buffer): ImageBackground
		local self = _ImageBackgroundImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.resource_identifier = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ImageBackgroundImpl.jsonEncode(self: ImageBackground): any
		local output = {}

		if self.resource_identifier ~= nil and self.resource_identifier ~= "" then
			output.resourceIdentifier = self.resource_identifier
		end

		return output
	end

	function _ImageBackgroundImpl.jsonDecode(input: { [string]: any }): ImageBackground
		local self = _ImageBackgroundImpl.new()

		if input.resource_identifier ~= nil then
			self.resource_identifier = input.resource_identifier
		end

		if input.resourceIdentifier ~= nil then
			self.resource_identifier = input.resourceIdentifier
		end

		return self
	end

	_ImageBackgroundImpl.descriptor = {
		name = "ImageBackground",
		fullName = "roblox.apppageplatform.shared.v1beta1.ImageBackground",
	}

	messages.ImageBackground = _ImageBackgroundImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ImageBackground)
end

do
	local _BackgroundLayerImpl = {}
	_BackgroundLayerImpl.__index = _BackgroundLayerImpl

	function _BackgroundLayerImpl.new(data: _BackgroundLayerPartialFields?): BackgroundLayer
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _BackgroundLayerImpl :: _BackgroundLayerImpl)
	end

	function _BackgroundLayerImpl.encode(self: BackgroundLayer): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "image_background" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _BackgroundLayerImpl.decode(input: buffer): BackgroundLayer
		local self = _BackgroundLayerImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "image_background", value = messages.ImageBackground.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _BackgroundLayerImpl.jsonEncode(self: BackgroundLayer): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "image_background" then
				output.imageBackground = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _BackgroundLayerImpl.jsonDecode(input: { [string]: any }): BackgroundLayer
		local self = _BackgroundLayerImpl.new()

		if input.image_background ~= nil then
			self.kind =
				{ type = "image_background", value = messages.ImageBackground.jsonDecode(input.image_background) }
		end

		if input.imageBackground ~= nil then
			self.kind =
				{ type = "image_background", value = messages.ImageBackground.jsonDecode(input.imageBackground) }
		end

		return self
	end

	_BackgroundLayerImpl.descriptor = {
		name = "BackgroundLayer",
		fullName = "roblox.apppageplatform.shared.v1beta1.BackgroundLayer",
	}

	messages.BackgroundLayer = _BackgroundLayerImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.BackgroundLayer)
end

do
	local _ArrayOfBackgroundLayerPropImpl = {}
	_ArrayOfBackgroundLayerPropImpl.__index = _ArrayOfBackgroundLayerPropImpl

	function _ArrayOfBackgroundLayerPropImpl.new(
		data: _ArrayOfBackgroundLayerPropPartialFields?
	): ArrayOfBackgroundLayerProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfBackgroundLayerPropImpl :: _ArrayOfBackgroundLayerPropImpl)
	end

	function _ArrayOfBackgroundLayerPropImpl.encode(self: ArrayOfBackgroundLayerProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfBackgroundLayerPropImpl.decode(input: buffer): ArrayOfBackgroundLayerProp
		local self = _ArrayOfBackgroundLayerPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "literal",
						value = messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer.decode(value),
					}
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "conditional",
						value = messages.ArrayOfBackgroundLayerProp_ConditionalOptions.decode(value),
					}
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfBackgroundLayerPropImpl.jsonEncode(self: ArrayOfBackgroundLayerProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _ArrayOfBackgroundLayerPropImpl.jsonDecode(input: { [string]: any }): ArrayOfBackgroundLayerProp
		local self = _ArrayOfBackgroundLayerPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer.jsonDecode(input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.ArrayOfBackgroundLayerProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_ArrayOfBackgroundLayerPropImpl.descriptor = {
		name = "ArrayOfBackgroundLayerProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfBackgroundLayerProp",
	}

	messages.ArrayOfBackgroundLayerProp = _ArrayOfBackgroundLayerPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfBackgroundLayerProp)
end

do
	local _ArrayOfBackgroundLayerProp_ConditionalOptionImpl = {}
	_ArrayOfBackgroundLayerProp_ConditionalOptionImpl.__index = _ArrayOfBackgroundLayerProp_ConditionalOptionImpl

	function _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.new(
		data: _ArrayOfBackgroundLayerProp_ConditionalOptionPartialFields?
	): ArrayOfBackgroundLayerProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfBackgroundLayerProp_ConditionalOptionImpl :: _ArrayOfBackgroundLayerProp_ConditionalOptionImpl)
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.encode(
		self: ArrayOfBackgroundLayerProp_ConditionalOption
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.decode(
		input: buffer
	): ArrayOfBackgroundLayerProp_ConditionalOption
		local self = _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "literal",
						value = messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer.decode(value),
					}
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.jsonEncode(
		self: ArrayOfBackgroundLayerProp_ConditionalOption
	): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfBackgroundLayerProp_ConditionalOption
		local self = _ArrayOfBackgroundLayerProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer.jsonDecode(input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_ArrayOfBackgroundLayerProp_ConditionalOptionImpl.descriptor = {
		name = "ArrayOfBackgroundLayerProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.ArrayOfBackgroundLayerProp_ConditionalOption = _ArrayOfBackgroundLayerProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfBackgroundLayerProp_ConditionalOption)
end

do
	local _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl = {}
	_ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.__index = _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl

	function _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.new(
		data: _ArrayOfBackgroundLayerProp_ConditionalOptionsPartialFields?
	): ArrayOfBackgroundLayerProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl :: _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl)
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.encode(
		self: ArrayOfBackgroundLayerProp_ConditionalOptions
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.decode(
		input: buffer
	): ArrayOfBackgroundLayerProp_ConditionalOptions
		local self = _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.ArrayOfBackgroundLayerProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.jsonEncode(
		self: ArrayOfBackgroundLayerProp_ConditionalOptions
	): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfBackgroundLayerProp_ConditionalOptions
		local self = _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { ArrayOfBackgroundLayerProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.ArrayOfBackgroundLayerProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_ArrayOfBackgroundLayerProp_ConditionalOptionsImpl.descriptor = {
		name = "ArrayOfBackgroundLayerProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.ArrayOfBackgroundLayerProp_ConditionalOptions = _ArrayOfBackgroundLayerProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfBackgroundLayerProp_ConditionalOptions)
end

do
	local _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl = {}
	_ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.__index =
		_ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl

	function _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.new(
		data: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerPartialFields?
	): ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer
		return setmetatable(
			{
				array = if data == nil or data.array == nil then {} else data.array,
			},
			_ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl :: _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl
		)
	end

	function _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.encode(
		self: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.array ~= nil and #self.array > 0 then
			for _, value in self.array do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.decode(
		input: buffer
	): ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer
		local self = _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.array, messages.BackgroundLayer.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.jsonEncode(
		self: ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer
	): any
		local output = {}

		if self.array ~= nil and #self.array > 0 then
			local newOutput = {}
			for _, value in self.array do
				table.insert(newOutput, value:jsonEncode())
			end
			output.array = newOutput
		end

		return output
	end

	function _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer
		local self = _ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.new()

		if input.array ~= nil then
			local newOutput: { BackgroundLayer } = {}
			for _, value in input.array do
				table.insert(newOutput, messages.BackgroundLayer.jsonDecode(value))
			end

			self.array = newOutput
		end

		return self
	end

	_ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl.descriptor = {
		name = "ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfBackgroundLayer",
	}

	messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer =
		_ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayerImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer)
end

return {
	ImageBackground = messages.ImageBackground,
	BackgroundLayer = messages.BackgroundLayer,
	ArrayOfBackgroundLayerProp = messages.ArrayOfBackgroundLayerProp,
	ArrayOfBackgroundLayerProp_ConditionalOption = messages.ArrayOfBackgroundLayerProp_ConditionalOption,
	ArrayOfBackgroundLayerProp_ConditionalOptions = messages.ArrayOfBackgroundLayerProp_ConditionalOptions,
	ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer = messages.ArrayOfBackgroundLayerProp_ArrayOfBackgroundLayer,
}
