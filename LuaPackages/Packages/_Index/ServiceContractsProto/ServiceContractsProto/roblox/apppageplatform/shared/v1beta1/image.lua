--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	ImageSchema: _ImageSchemaMessage,
	ImageSchema_Props: _ImageSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_prop_types_engine = require(script.Parent.prop_types_engine)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _ImageSchemaImpl = {
	__index: _ImageSchemaImpl,
	new: (fields: _ImageSchemaPartialFields?) -> ImageSchema,
	encode: (self: ImageSchema) -> buffer,
	decode: (input: buffer) -> ImageSchema,
	jsonEncode: (self: ImageSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ImageSchema,
	descriptor: proto.Descriptor,
}

type _ImageSchemaFields = {
	props: ImageSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _ImageSchemaPartialFields = {
	props: ImageSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type ImageSchema = typeof(setmetatable({} :: _ImageSchemaFields, {} :: _ImageSchemaImpl))
type _ImageSchemaMessage = proto.Message<ImageSchema, _ImageSchemaPartialFields>

type _ImageSchema_PropsImpl = {
	__index: _ImageSchema_PropsImpl,
	new: (fields: _ImageSchema_PropsPartialFields?) -> ImageSchema_Props,
	encode: (self: ImageSchema_Props) -> buffer,
	decode: (input: buffer) -> ImageSchema_Props,
	jsonEncode: (self: ImageSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ImageSchema_Props,
	descriptor: proto.Descriptor,
}

type _ImageSchema_PropsFields = {
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	clips_descendants: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
	scale_type: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.ScaleTypeProp?,
	resample_mode: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.ResamplerModeProp?,
	tile_size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	image_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp?,
	test_id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	cursor_type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	visible: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	children: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	size_constraint: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.SizeConstraintProp?,
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.AutomaticSizeProp?,
}

type _ImageSchema_PropsPartialFields = {
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	clips_descendants: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
	scale_type: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.ScaleTypeProp?,
	resample_mode: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.ResamplerModeProp?,
	tile_size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	image_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp?,
	test_id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	cursor_type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	visible: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	children: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	size_constraint: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.SizeConstraintProp?,
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.AutomaticSizeProp?,
}

export type ImageSchema_Props = typeof(setmetatable({} :: _ImageSchema_PropsFields, {} :: _ImageSchema_PropsImpl))
type _ImageSchema_PropsMessage = proto.Message<ImageSchema_Props, _ImageSchema_PropsPartialFields>

do
	local _ImageSchemaImpl = {}
	_ImageSchemaImpl.__index = _ImageSchemaImpl

	function _ImageSchemaImpl.new(data: _ImageSchemaPartialFields?): ImageSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _ImageSchemaImpl :: _ImageSchemaImpl)
	end

	function _ImageSchemaImpl.encode(self: ImageSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ImageSchemaImpl.decode(input: buffer): ImageSchema
		local self = _ImageSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.ImageSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ImageSchemaImpl.jsonEncode(self: ImageSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _ImageSchemaImpl.jsonDecode(input: { [string]: any }): ImageSchema
		local self = _ImageSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.ImageSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_ImageSchemaImpl.descriptor = {
		name = "ImageSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.ImageSchema",
	}

	messages.ImageSchema = _ImageSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ImageSchema)
end

do
	local _ImageSchema_PropsImpl = {}
	_ImageSchema_PropsImpl.__index = _ImageSchema_PropsImpl

	function _ImageSchema_PropsImpl.new(data: _ImageSchema_PropsPartialFields?): ImageSchema_Props
		return setmetatable({
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			clips_descendants = if data == nil or data.clips_descendants == nil then nil else data.clips_descendants,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			position = if data == nil or data.position == nil then nil else data.position,
			size = if data == nil or data.size == nil then nil else data.size,
			on_activated = if data == nil or data.on_activated == nil then nil else data.on_activated,
			image = if data == nil or data.image == nil then nil else data.image,
			scale_type = if data == nil or data.scale_type == nil then nil else data.scale_type,
			resample_mode = if data == nil or data.resample_mode == nil then nil else data.resample_mode,
			tile_size = if data == nil or data.tile_size == nil then nil else data.tile_size,
			image_style = if data == nil or data.image_style == nil then nil else data.image_style,
			test_id = if data == nil or data.test_id == nil then nil else data.test_id,
			tag = if data == nil or data.tag == nil then nil else data.tag,
			cursor_type = if data == nil or data.cursor_type == nil then nil else data.cursor_type,
			visible = if data == nil or data.visible == nil then nil else data.visible,
			z_index = if data == nil or data.z_index == nil then nil else data.z_index,
			children = if data == nil or data.children == nil then nil else data.children,
			size_constraint = if data == nil or data.size_constraint == nil then nil else data.size_constraint,
			automatic_size = if data == nil or data.automatic_size == nil then nil else data.automatic_size,
		}, _ImageSchema_PropsImpl :: _ImageSchema_PropsImpl)
	end

	function _ImageSchema_PropsImpl.encode(self: ImageSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.clips_descendants ~= nil then
			local encoded = self.clips_descendants:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_activated ~= nil then
			local encoded = self.on_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.image ~= nil then
			local encoded = self.image:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.scale_type ~= nil then
			local encoded = self.scale_type:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.resample_mode ~= nil then
			local encoded = self.resample_mode:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.tile_size ~= nil then
			local encoded = self.tile_size:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.image_style ~= nil then
			local encoded = self.image_style:encode()
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.test_id ~= nil then
			local encoded = self.test_id:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.tag ~= nil then
			local encoded = self.tag:encode()
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.cursor_type ~= nil then
			local encoded = self.cursor_type:encode()
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.visible ~= nil then
			local encoded = self.visible:encode()
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.z_index ~= nil then
			local encoded = self.z_index:encode()
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.children ~= nil then
			local encoded = self.children:encode()
			output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size_constraint ~= nil then
			local encoded = self.size_constraint:encode()
			output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.automatic_size ~= nil then
			local encoded = self.automatic_size:encode()
			output, cursor = proto.writeTag(output, cursor, 19, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ImageSchema_PropsImpl.decode(input: buffer): ImageSchema_Props
		local self = _ImageSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.clips_descendants = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.image = _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.scale_type =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.ScaleTypeProp.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.resample_mode =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.ResamplerModeProp.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tile_size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.image_style = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.decode(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.test_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.cursor_type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.visible = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.children =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size_constraint =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SizeConstraintProp.decode(value)
					continue
				elseif field == 19 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.automatic_size =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.AutomaticSizeProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ImageSchema_PropsImpl.jsonEncode(self: ImageSchema_Props): any
		local output = {}

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.clips_descendants ~= nil then
			output.clipsDescendants = self.clips_descendants:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.on_activated ~= nil then
			output.onActivated = self.on_activated:jsonEncode()
		end

		if self.image ~= nil then
			output.image = self.image:jsonEncode()
		end

		if self.scale_type ~= nil then
			output.scaleType = self.scale_type:jsonEncode()
		end

		if self.resample_mode ~= nil then
			output.resampleMode = self.resample_mode:jsonEncode()
		end

		if self.tile_size ~= nil then
			output.tileSize = self.tile_size:jsonEncode()
		end

		if self.image_style ~= nil then
			output.imageStyle = self.image_style:jsonEncode()
		end

		if self.test_id ~= nil then
			output.testId = self.test_id:jsonEncode()
		end

		if self.tag ~= nil then
			output.tag = self.tag:jsonEncode()
		end

		if self.cursor_type ~= nil then
			output.cursorType = self.cursor_type:jsonEncode()
		end

		if self.visible ~= nil then
			output.visible = self.visible:jsonEncode()
		end

		if self.z_index ~= nil then
			output.zIndex = self.z_index:jsonEncode()
		end

		if self.children ~= nil then
			output.children = self.children:jsonEncode()
		end

		if self.size_constraint ~= nil then
			output.sizeConstraint = self.size_constraint:jsonEncode()
		end

		if self.automatic_size ~= nil then
			output.automaticSize = self.automatic_size:jsonEncode()
		end

		return output
	end

	function _ImageSchema_PropsImpl.jsonDecode(input: { [string]: any }): ImageSchema_Props
		local self = _ImageSchema_PropsImpl.new()

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.clips_descendants ~= nil then
			self.clips_descendants =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.clips_descendants)
		end

		if input.clipsDescendants ~= nil then
			self.clips_descendants =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.clipsDescendants)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.on_activated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_activated)
		end

		if input.onActivated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onActivated)
		end

		if input.image ~= nil then
			self.image = _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.image)
		end

		if input.scale_type ~= nil then
			self.scale_type =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.ScaleTypeProp.jsonDecode(input.scale_type)
		end

		if input.scaleType ~= nil then
			self.scale_type =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.ScaleTypeProp.jsonDecode(input.scaleType)
		end

		if input.resample_mode ~= nil then
			self.resample_mode = _roblox_apppageplatform_shared_v1beta1_prop_types_engine.ResamplerModeProp.jsonDecode(
				input.resample_mode
			)
		end

		if input.resampleMode ~= nil then
			self.resample_mode = _roblox_apppageplatform_shared_v1beta1_prop_types_engine.ResamplerModeProp.jsonDecode(
				input.resampleMode
			)
		end

		if input.tile_size ~= nil then
			self.tile_size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.tile_size)
		end

		if input.tileSize ~= nil then
			self.tile_size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.tileSize)
		end

		if input.image_style ~= nil then
			self.image_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.jsonDecode(input.image_style)
		end

		if input.imageStyle ~= nil then
			self.image_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.jsonDecode(input.imageStyle)
		end

		if input.test_id ~= nil then
			self.test_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.test_id)
		end

		if input.testId ~= nil then
			self.test_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.testId)
		end

		if input.tag ~= nil then
			self.tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.tag)
		end

		if input.cursor_type ~= nil then
			self.cursor_type =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.cursor_type)
		end

		if input.cursorType ~= nil then
			self.cursor_type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.cursorType)
		end

		if input.visible ~= nil then
			self.visible = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.visible)
		end

		if input.z_index ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.z_index)
		end

		if input.zIndex ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.zIndex)
		end

		if input.children ~= nil then
			self.children =
				_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(input.children)
		end

		if input.size_constraint ~= nil then
			self.size_constraint =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SizeConstraintProp.jsonDecode(
					input.size_constraint
				)
		end

		if input.sizeConstraint ~= nil then
			self.size_constraint =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SizeConstraintProp.jsonDecode(
					input.sizeConstraint
				)
		end

		if input.automatic_size ~= nil then
			self.automatic_size = _roblox_apppageplatform_shared_v1beta1_prop_types_engine.AutomaticSizeProp.jsonDecode(
				input.automatic_size
			)
		end

		if input.automaticSize ~= nil then
			self.automatic_size = _roblox_apppageplatform_shared_v1beta1_prop_types_engine.AutomaticSizeProp.jsonDecode(
				input.automaticSize
			)
		end

		return self
	end

	_ImageSchema_PropsImpl.descriptor = {
		name = "ImageSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.ImageSchema_Props = _ImageSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ImageSchema_Props)
end

return {
	ImageSchema = messages.ImageSchema,
	ImageSchema_Props = messages.ImageSchema_Props,
}
