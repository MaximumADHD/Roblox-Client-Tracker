--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CatalogVirtualizedFeedSchema: _CatalogVirtualizedFeedSchemaMessage,
	CatalogVirtualizedFeedSchema_Props: _CatalogVirtualizedFeedSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _CatalogVirtualizedFeedSchemaImpl = {
	__index: _CatalogVirtualizedFeedSchemaImpl,
	new: (fields: _CatalogVirtualizedFeedSchemaPartialFields?) -> CatalogVirtualizedFeedSchema,
	encode: (self: CatalogVirtualizedFeedSchema) -> buffer,
	decode: (input: buffer) -> CatalogVirtualizedFeedSchema,
	jsonEncode: (self: CatalogVirtualizedFeedSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogVirtualizedFeedSchema,
	descriptor: proto.Descriptor,
}

type _CatalogVirtualizedFeedSchemaFields = {
	props: CatalogVirtualizedFeedSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _CatalogVirtualizedFeedSchemaPartialFields = {
	props: CatalogVirtualizedFeedSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type CatalogVirtualizedFeedSchema = typeof(setmetatable(
	{} :: _CatalogVirtualizedFeedSchemaFields,
	{} :: _CatalogVirtualizedFeedSchemaImpl
))
type _CatalogVirtualizedFeedSchemaMessage = proto.Message<
	CatalogVirtualizedFeedSchema,
	_CatalogVirtualizedFeedSchemaPartialFields
>

type _CatalogVirtualizedFeedSchema_PropsImpl = {
	__index: _CatalogVirtualizedFeedSchema_PropsImpl,
	new: (fields: _CatalogVirtualizedFeedSchema_PropsPartialFields?) -> CatalogVirtualizedFeedSchema_Props,
	encode: (self: CatalogVirtualizedFeedSchema_Props) -> buffer,
	decode: (input: buffer) -> CatalogVirtualizedFeedSchema_Props,
	jsonEncode: (self: CatalogVirtualizedFeedSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogVirtualizedFeedSchema_Props,
	descriptor: proto.Descriptor,
}

type _CatalogVirtualizedFeedSchema_PropsFields = {
	feed_items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	on_load_more: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	on_resize: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	tags: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop?,
}

type _CatalogVirtualizedFeedSchema_PropsPartialFields = {
	feed_items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	on_load_more: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	on_resize: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	tags: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop?,
}

export type CatalogVirtualizedFeedSchema_Props = typeof(setmetatable(
	{} :: _CatalogVirtualizedFeedSchema_PropsFields,
	{} :: _CatalogVirtualizedFeedSchema_PropsImpl
))
type _CatalogVirtualizedFeedSchema_PropsMessage = proto.Message<
	CatalogVirtualizedFeedSchema_Props,
	_CatalogVirtualizedFeedSchema_PropsPartialFields
>

do
	local _CatalogVirtualizedFeedSchemaImpl = {}
	_CatalogVirtualizedFeedSchemaImpl.__index = _CatalogVirtualizedFeedSchemaImpl

	function _CatalogVirtualizedFeedSchemaImpl.new(
		data: _CatalogVirtualizedFeedSchemaPartialFields?
	): CatalogVirtualizedFeedSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _CatalogVirtualizedFeedSchemaImpl :: _CatalogVirtualizedFeedSchemaImpl)
	end

	function _CatalogVirtualizedFeedSchemaImpl.encode(self: CatalogVirtualizedFeedSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogVirtualizedFeedSchemaImpl.decode(input: buffer): CatalogVirtualizedFeedSchema
		local self = _CatalogVirtualizedFeedSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.CatalogVirtualizedFeedSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogVirtualizedFeedSchemaImpl.jsonEncode(self: CatalogVirtualizedFeedSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _CatalogVirtualizedFeedSchemaImpl.jsonDecode(input: { [string]: any }): CatalogVirtualizedFeedSchema
		local self = _CatalogVirtualizedFeedSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.CatalogVirtualizedFeedSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_CatalogVirtualizedFeedSchemaImpl.descriptor = {
		name = "CatalogVirtualizedFeedSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.CatalogVirtualizedFeedSchema",
	}

	messages.CatalogVirtualizedFeedSchema = _CatalogVirtualizedFeedSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogVirtualizedFeedSchema)
end

do
	local _CatalogVirtualizedFeedSchema_PropsImpl = {}
	_CatalogVirtualizedFeedSchema_PropsImpl.__index = _CatalogVirtualizedFeedSchema_PropsImpl

	function _CatalogVirtualizedFeedSchema_PropsImpl.new(
		data: _CatalogVirtualizedFeedSchema_PropsPartialFields?
	): CatalogVirtualizedFeedSchema_Props
		return setmetatable({
			feed_items = if data == nil or data.feed_items == nil then nil else data.feed_items,
			on_load_more = if data == nil or data.on_load_more == nil then nil else data.on_load_more,
			on_resize = if data == nil or data.on_resize == nil then nil else data.on_resize,
			tags = if data == nil or data.tags == nil then nil else data.tags,
			position = if data == nil or data.position == nil then nil else data.position,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			size = if data == nil or data.size == nil then nil else data.size,
		}, _CatalogVirtualizedFeedSchema_PropsImpl :: _CatalogVirtualizedFeedSchema_PropsImpl)
	end

	function _CatalogVirtualizedFeedSchema_PropsImpl.encode(self: CatalogVirtualizedFeedSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.feed_items ~= nil then
			local encoded = self.feed_items:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_load_more ~= nil then
			local encoded = self.on_load_more:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_resize ~= nil then
			local encoded = self.on_resize:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.tags ~= nil then
			local encoded = self.tags:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogVirtualizedFeedSchema_PropsImpl.decode(input: buffer): CatalogVirtualizedFeedSchema_Props
		local self = _CatalogVirtualizedFeedSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.feed_items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_load_more = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_resize = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tags = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogVirtualizedFeedSchema_PropsImpl.jsonEncode(self: CatalogVirtualizedFeedSchema_Props): any
		local output = {}

		if self.feed_items ~= nil then
			output.feedItems = self.feed_items:jsonEncode()
		end

		if self.on_load_more ~= nil then
			output.onLoadMore = self.on_load_more:jsonEncode()
		end

		if self.on_resize ~= nil then
			output.onResize = self.on_resize:jsonEncode()
		end

		if self.tags ~= nil then
			output.tags = self.tags:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		return output
	end

	function _CatalogVirtualizedFeedSchema_PropsImpl.jsonDecode(
		input: { [string]: any }
	): CatalogVirtualizedFeedSchema_Props
		local self = _CatalogVirtualizedFeedSchema_PropsImpl.new()

		if input.feed_items ~= nil then
			self.feed_items = _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(
				input.feed_items
			)
		end

		if input.feedItems ~= nil then
			self.feed_items = _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(
				input.feedItems
			)
		end

		if input.on_load_more ~= nil then
			self.on_load_more = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_load_more)
		end

		if input.onLoadMore ~= nil then
			self.on_load_more = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onLoadMore)
		end

		if input.on_resize ~= nil then
			self.on_resize = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_resize)
		end

		if input.onResize ~= nil then
			self.on_resize = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onResize)
		end

		if input.tags ~= nil then
			self.tags = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.tags)
		end

		if input.position ~= nil then
			self.position =
				_roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop.jsonDecode(input.position)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.layoutOrder)
		end

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UiScaledUDim2Prop.jsonDecode(input.size)
		end

		return self
	end

	_CatalogVirtualizedFeedSchema_PropsImpl.descriptor = {
		name = "CatalogVirtualizedFeedSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.CatalogVirtualizedFeedSchema_Props = _CatalogVirtualizedFeedSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogVirtualizedFeedSchema_Props)
end

return {
	CatalogVirtualizedFeedSchema = messages.CatalogVirtualizedFeedSchema,
	CatalogVirtualizedFeedSchema_Props = messages.CatalogVirtualizedFeedSchema_Props,
}
