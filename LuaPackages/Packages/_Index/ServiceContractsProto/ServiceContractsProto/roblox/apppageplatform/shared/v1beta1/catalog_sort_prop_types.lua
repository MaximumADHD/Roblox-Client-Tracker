--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages =
	{
		ItemFooter: _ItemFooterMessage,
		ArrayOfItemFooterProp: _ArrayOfItemFooterPropMessage,
		ArrayOfItemFooterProp_ConditionalOption: _ArrayOfItemFooterProp_ConditionalOptionMessage,
		ArrayOfItemFooterProp_ConditionalOptions: _ArrayOfItemFooterProp_ConditionalOptionsMessage,
		ArrayOfItemFooterProp_ArrayOfItemFooter: _ArrayOfItemFooterProp_ArrayOfItemFooterMessage,
		ArrayOfItemFooterProp_ArrayMap: _ArrayOfItemFooterProp_ArrayMapMessage,
		ArrayOfItemFooterProp_ArrayMap_LiteralItems: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsMessage,
		CardStyle: _CardStyleMessage,
		CardStyleProp: _CardStylePropMessage,
		CardStyleProp_ConditionalOption: _CardStyleProp_ConditionalOptionMessage,
		CardStyleProp_ConditionalOptions: _CardStyleProp_ConditionalOptionsMessage,
		CatalogSortContent: _CatalogSortContentMessage,
		ArrayOfCatalogSortContentProp: _ArrayOfCatalogSortContentPropMessage,
		ArrayOfCatalogSortContentProp_ConditionalOption: _ArrayOfCatalogSortContentProp_ConditionalOptionMessage,
		ArrayOfCatalogSortContentProp_ConditionalOptions: _ArrayOfCatalogSortContentProp_ConditionalOptionsMessage,
		ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentMessage,
		ArrayOfCatalogSortContentProp_ArrayMap: _ArrayOfCatalogSortContentProp_ArrayMapMessage,
		ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsMessage,
		WidgetStyle: _WidgetStyleMessage,
		WidgetStyleProp: _WidgetStylePropMessage,
		WidgetStyleProp_ConditionalOption: _WidgetStyleProp_ConditionalOptionMessage,
		WidgetStyleProp_ConditionalOptions: _WidgetStyleProp_ConditionalOptionsMessage,
	}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_background_layer = require(script.Parent.background_layer)
local _roblox_apppageplatform_shared_v1beta1_catalog_sort_data = require(script.Parent.catalog_sort_data)
local _roblox_apppageplatform_shared_v1beta1_hydration_data_spec = require(script.Parent.hydration_data_spec)
local _roblox_apppageplatform_shared_v1beta1_prop_condition = require(script.Parent.prop_condition)
local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)

type _ItemFooterImpl = {
	__index: _ItemFooterImpl,
	new: (fields: _ItemFooterPartialFields?) -> ItemFooter,
	encode: (self: ItemFooter) -> buffer,
	decode: (input: buffer) -> ItemFooter,
	jsonEncode: (self: ItemFooter) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ItemFooter,
	descriptor: proto.Descriptor,
}

type _ItemFooterFields = {
	content_type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _ItemFooterPartialFields = {
	content_type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type ItemFooter = typeof(setmetatable({} :: _ItemFooterFields, {} :: _ItemFooterImpl))
type _ItemFooterMessage = proto.Message<ItemFooter, _ItemFooterPartialFields>

type _ArrayOfItemFooterPropImpl = {
	__index: _ArrayOfItemFooterPropImpl,
	new: (fields: _ArrayOfItemFooterPropPartialFields?) -> ArrayOfItemFooterProp,
	encode: (self: ArrayOfItemFooterProp) -> buffer,
	decode: (input: buffer) -> ArrayOfItemFooterProp,
	jsonEncode: (self: ArrayOfItemFooterProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfItemFooterProp,
	descriptor: proto.Descriptor,
}

type _ArrayOfItemFooterPropFields = {
	kind: (
		{ type: "literal", value: ArrayOfItemFooterProp_ArrayOfItemFooter }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfItemFooterProp_ConditionalOptions }
		| { type: "array_map", value: ArrayOfItemFooterProp_ArrayMap }
	)?,
}

type _ArrayOfItemFooterPropPartialFields = {
	kind: (
		{ type: "literal", value: ArrayOfItemFooterProp_ArrayOfItemFooter }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfItemFooterProp_ConditionalOptions }
		| { type: "array_map", value: ArrayOfItemFooterProp_ArrayMap }
	)?,
}

export type ArrayOfItemFooterProp = typeof(setmetatable(
	{} :: _ArrayOfItemFooterPropFields,
	{} :: _ArrayOfItemFooterPropImpl
))
type _ArrayOfItemFooterPropMessage = proto.Message<ArrayOfItemFooterProp, _ArrayOfItemFooterPropPartialFields>

type _ArrayOfItemFooterProp_ConditionalOptionImpl = {
	__index: _ArrayOfItemFooterProp_ConditionalOptionImpl,
	new: (fields: _ArrayOfItemFooterProp_ConditionalOptionPartialFields?) -> ArrayOfItemFooterProp_ConditionalOption,
	encode: (self: ArrayOfItemFooterProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> ArrayOfItemFooterProp_ConditionalOption,
	jsonEncode: (self: ArrayOfItemFooterProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfItemFooterProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _ArrayOfItemFooterProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: (
		{ type: "literal", value: ArrayOfItemFooterProp_ArrayOfItemFooter } | { type: "binding_path", value: string }
	)?,
}

type _ArrayOfItemFooterProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: (
		{ type: "literal", value: ArrayOfItemFooterProp_ArrayOfItemFooter } | { type: "binding_path", value: string }
	)?,
}

export type ArrayOfItemFooterProp_ConditionalOption = typeof(setmetatable(
	{} :: _ArrayOfItemFooterProp_ConditionalOptionFields,
	{} :: _ArrayOfItemFooterProp_ConditionalOptionImpl
))
type _ArrayOfItemFooterProp_ConditionalOptionMessage = proto.Message<
	ArrayOfItemFooterProp_ConditionalOption,
	_ArrayOfItemFooterProp_ConditionalOptionPartialFields
>

type _ArrayOfItemFooterProp_ConditionalOptionsImpl = {
	__index: _ArrayOfItemFooterProp_ConditionalOptionsImpl,
	new: (fields: _ArrayOfItemFooterProp_ConditionalOptionsPartialFields?) -> ArrayOfItemFooterProp_ConditionalOptions,
	encode: (self: ArrayOfItemFooterProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> ArrayOfItemFooterProp_ConditionalOptions,
	jsonEncode: (self: ArrayOfItemFooterProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfItemFooterProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _ArrayOfItemFooterProp_ConditionalOptionsFields = {
	options: { ArrayOfItemFooterProp_ConditionalOption },
}

type _ArrayOfItemFooterProp_ConditionalOptionsPartialFields = {
	options: { ArrayOfItemFooterProp_ConditionalOption }?,
}

export type ArrayOfItemFooterProp_ConditionalOptions = typeof(setmetatable(
	{} :: _ArrayOfItemFooterProp_ConditionalOptionsFields,
	{} :: _ArrayOfItemFooterProp_ConditionalOptionsImpl
))
type _ArrayOfItemFooterProp_ConditionalOptionsMessage = proto.Message<
	ArrayOfItemFooterProp_ConditionalOptions,
	_ArrayOfItemFooterProp_ConditionalOptionsPartialFields
>

type _ArrayOfItemFooterProp_ArrayOfItemFooterImpl = {
	__index: _ArrayOfItemFooterProp_ArrayOfItemFooterImpl,
	new: (fields: _ArrayOfItemFooterProp_ArrayOfItemFooterPartialFields?) -> ArrayOfItemFooterProp_ArrayOfItemFooter,
	encode: (self: ArrayOfItemFooterProp_ArrayOfItemFooter) -> buffer,
	decode: (input: buffer) -> ArrayOfItemFooterProp_ArrayOfItemFooter,
	jsonEncode: (self: ArrayOfItemFooterProp_ArrayOfItemFooter) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfItemFooterProp_ArrayOfItemFooter,
	descriptor: proto.Descriptor,
}

type _ArrayOfItemFooterProp_ArrayOfItemFooterFields = {
	array: { ItemFooter },
}

type _ArrayOfItemFooterProp_ArrayOfItemFooterPartialFields = {
	array: { ItemFooter }?,
}

export type ArrayOfItemFooterProp_ArrayOfItemFooter = typeof(setmetatable(
	{} :: _ArrayOfItemFooterProp_ArrayOfItemFooterFields,
	{} :: _ArrayOfItemFooterProp_ArrayOfItemFooterImpl
))
type _ArrayOfItemFooterProp_ArrayOfItemFooterMessage = proto.Message<
	ArrayOfItemFooterProp_ArrayOfItemFooter,
	_ArrayOfItemFooterProp_ArrayOfItemFooterPartialFields
>

type _ArrayOfItemFooterProp_ArrayMapImpl = {
	__index: _ArrayOfItemFooterProp_ArrayMapImpl,
	new: (fields: _ArrayOfItemFooterProp_ArrayMapPartialFields?) -> ArrayOfItemFooterProp_ArrayMap,
	encode: (self: ArrayOfItemFooterProp_ArrayMap) -> buffer,
	decode: (input: buffer) -> ArrayOfItemFooterProp_ArrayMap,
	jsonEncode: (self: ArrayOfItemFooterProp_ArrayMap) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfItemFooterProp_ArrayMap,
	descriptor: proto.Descriptor,
}

type _ArrayOfItemFooterProp_ArrayMapFields = {
	kind: (
		{ type: "binding_path", value: string }
		| { type: "literal", value: ArrayOfItemFooterProp_ArrayMap_LiteralItems }
	)?,
	item_hydration_specs: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec },
	field_map: ItemFooter?,
}

type _ArrayOfItemFooterProp_ArrayMapPartialFields = {
	kind: (
		{ type: "binding_path", value: string }
		| { type: "literal", value: ArrayOfItemFooterProp_ArrayMap_LiteralItems }
	)?,
	item_hydration_specs: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec }?,
	field_map: ItemFooter?,
}

export type ArrayOfItemFooterProp_ArrayMap = typeof(setmetatable(
	{} :: _ArrayOfItemFooterProp_ArrayMapFields,
	{} :: _ArrayOfItemFooterProp_ArrayMapImpl
))
type _ArrayOfItemFooterProp_ArrayMapMessage = proto.Message<
	ArrayOfItemFooterProp_ArrayMap,
	_ArrayOfItemFooterProp_ArrayMapPartialFields
>

type _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl = {
	__index: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl,
	new: (
		fields: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsPartialFields?
	) -> ArrayOfItemFooterProp_ArrayMap_LiteralItems,
	encode: (self: ArrayOfItemFooterProp_ArrayMap_LiteralItems) -> buffer,
	decode: (input: buffer) -> ArrayOfItemFooterProp_ArrayMap_LiteralItems,
	jsonEncode: (self: ArrayOfItemFooterProp_ArrayMap_LiteralItems) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfItemFooterProp_ArrayMap_LiteralItems,
	descriptor: proto.Descriptor,
}

type _ArrayOfItemFooterProp_ArrayMap_LiteralItemsFields = {
	items: { _roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortCardItemFooter },
}

type _ArrayOfItemFooterProp_ArrayMap_LiteralItemsPartialFields = {
	items: { _roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortCardItemFooter }?,
}

export type ArrayOfItemFooterProp_ArrayMap_LiteralItems = typeof(setmetatable(
	{} :: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsFields,
	{} :: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl
))
type _ArrayOfItemFooterProp_ArrayMap_LiteralItemsMessage = proto.Message<
	ArrayOfItemFooterProp_ArrayMap_LiteralItems,
	_ArrayOfItemFooterProp_ArrayMap_LiteralItemsPartialFields
>

type _CardStyleImpl = {
	__index: _CardStyleImpl,
	new: (fields: _CardStylePartialFields?) -> CardStyle,
	encode: (self: CardStyle) -> buffer,
	decode: (input: buffer) -> CardStyle,
	jsonEncode: (self: CardStyle) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CardStyle,
	descriptor: proto.Descriptor,
}

type _CardStyleFields = {
	thumbnail_aspect_ratio: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	background_layers: _roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp?,
}

type _CardStylePartialFields = {
	thumbnail_aspect_ratio: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	background_layers: _roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp?,
}

export type CardStyle = typeof(setmetatable({} :: _CardStyleFields, {} :: _CardStyleImpl))
type _CardStyleMessage = proto.Message<CardStyle, _CardStylePartialFields>

type _CardStylePropImpl = {
	__index: _CardStylePropImpl,
	new: (fields: _CardStylePropPartialFields?) -> CardStyleProp,
	encode: (self: CardStyleProp) -> buffer,
	decode: (input: buffer) -> CardStyleProp,
	jsonEncode: (self: CardStyleProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CardStyleProp,
	descriptor: proto.Descriptor,
}

type _CardStylePropFields = {
	kind: (
		{ type: "literal", value: CardStyle }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: CardStyleProp_ConditionalOptions }
	)?,
}

type _CardStylePropPartialFields = {
	kind: (
		{ type: "literal", value: CardStyle }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: CardStyleProp_ConditionalOptions }
	)?,
}

export type CardStyleProp = typeof(setmetatable({} :: _CardStylePropFields, {} :: _CardStylePropImpl))
type _CardStylePropMessage = proto.Message<CardStyleProp, _CardStylePropPartialFields>

type _CardStyleProp_ConditionalOptionImpl = {
	__index: _CardStyleProp_ConditionalOptionImpl,
	new: (fields: _CardStyleProp_ConditionalOptionPartialFields?) -> CardStyleProp_ConditionalOption,
	encode: (self: CardStyleProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> CardStyleProp_ConditionalOption,
	jsonEncode: (self: CardStyleProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CardStyleProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _CardStyleProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: CardStyle } | { type: "binding_path", value: string })?,
}

type _CardStyleProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: CardStyle } | { type: "binding_path", value: string })?,
}

export type CardStyleProp_ConditionalOption = typeof(setmetatable(
	{} :: _CardStyleProp_ConditionalOptionFields,
	{} :: _CardStyleProp_ConditionalOptionImpl
))
type _CardStyleProp_ConditionalOptionMessage = proto.Message<
	CardStyleProp_ConditionalOption,
	_CardStyleProp_ConditionalOptionPartialFields
>

type _CardStyleProp_ConditionalOptionsImpl = {
	__index: _CardStyleProp_ConditionalOptionsImpl,
	new: (fields: _CardStyleProp_ConditionalOptionsPartialFields?) -> CardStyleProp_ConditionalOptions,
	encode: (self: CardStyleProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> CardStyleProp_ConditionalOptions,
	jsonEncode: (self: CardStyleProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CardStyleProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _CardStyleProp_ConditionalOptionsFields = {
	options: { CardStyleProp_ConditionalOption },
}

type _CardStyleProp_ConditionalOptionsPartialFields = {
	options: { CardStyleProp_ConditionalOption }?,
}

export type CardStyleProp_ConditionalOptions = typeof(setmetatable(
	{} :: _CardStyleProp_ConditionalOptionsFields,
	{} :: _CardStyleProp_ConditionalOptionsImpl
))
type _CardStyleProp_ConditionalOptionsMessage = proto.Message<
	CardStyleProp_ConditionalOptions,
	_CardStyleProp_ConditionalOptionsPartialFields
>

type _CatalogSortContentImpl = {
	__index: _CatalogSortContentImpl,
	new: (fields: _CatalogSortContentPartialFields?) -> CatalogSortContent,
	encode: (self: CatalogSortContent) -> buffer,
	decode: (input: buffer) -> CatalogSortContent,
	jsonEncode: (self: CatalogSortContent) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogSortContent,
	descriptor: proto.Descriptor,
}

type _CatalogSortContentFields = {
	id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _CatalogSortContentPartialFields = {
	id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type CatalogSortContent = typeof(setmetatable({} :: _CatalogSortContentFields, {} :: _CatalogSortContentImpl))
type _CatalogSortContentMessage = proto.Message<CatalogSortContent, _CatalogSortContentPartialFields>

type _ArrayOfCatalogSortContentPropImpl = {
	__index: _ArrayOfCatalogSortContentPropImpl,
	new: (fields: _ArrayOfCatalogSortContentPropPartialFields?) -> ArrayOfCatalogSortContentProp,
	encode: (self: ArrayOfCatalogSortContentProp) -> buffer,
	decode: (input: buffer) -> ArrayOfCatalogSortContentProp,
	jsonEncode: (self: ArrayOfCatalogSortContentProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfCatalogSortContentProp,
	descriptor: proto.Descriptor,
}

type _ArrayOfCatalogSortContentPropFields = {
	kind: (
		{ type: "literal", value: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfCatalogSortContentProp_ConditionalOptions }
		| { type: "array_map", value: ArrayOfCatalogSortContentProp_ArrayMap }
	)?,
}

type _ArrayOfCatalogSortContentPropPartialFields = {
	kind: (
		{ type: "literal", value: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfCatalogSortContentProp_ConditionalOptions }
		| { type: "array_map", value: ArrayOfCatalogSortContentProp_ArrayMap }
	)?,
}

export type ArrayOfCatalogSortContentProp = typeof(setmetatable(
	{} :: _ArrayOfCatalogSortContentPropFields,
	{} :: _ArrayOfCatalogSortContentPropImpl
))
type _ArrayOfCatalogSortContentPropMessage = proto.Message<
	ArrayOfCatalogSortContentProp,
	_ArrayOfCatalogSortContentPropPartialFields
>

type _ArrayOfCatalogSortContentProp_ConditionalOptionImpl = {
	__index: _ArrayOfCatalogSortContentProp_ConditionalOptionImpl,
	new: (
		fields: _ArrayOfCatalogSortContentProp_ConditionalOptionPartialFields?
	) -> ArrayOfCatalogSortContentProp_ConditionalOption,
	encode: (self: ArrayOfCatalogSortContentProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> ArrayOfCatalogSortContentProp_ConditionalOption,
	jsonEncode: (self: ArrayOfCatalogSortContentProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfCatalogSortContentProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _ArrayOfCatalogSortContentProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: (
		{ type: "literal", value: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent }
		| { type: "binding_path", value: string }
	)?,
}

type _ArrayOfCatalogSortContentProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: (
		{ type: "literal", value: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent }
		| { type: "binding_path", value: string }
	)?,
}

export type ArrayOfCatalogSortContentProp_ConditionalOption = typeof(setmetatable(
	{} :: _ArrayOfCatalogSortContentProp_ConditionalOptionFields,
	{} :: _ArrayOfCatalogSortContentProp_ConditionalOptionImpl
))
type _ArrayOfCatalogSortContentProp_ConditionalOptionMessage = proto.Message<
	ArrayOfCatalogSortContentProp_ConditionalOption,
	_ArrayOfCatalogSortContentProp_ConditionalOptionPartialFields
>

type _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl = {
	__index: _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl,
	new: (
		fields: _ArrayOfCatalogSortContentProp_ConditionalOptionsPartialFields?
	) -> ArrayOfCatalogSortContentProp_ConditionalOptions,
	encode: (self: ArrayOfCatalogSortContentProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> ArrayOfCatalogSortContentProp_ConditionalOptions,
	jsonEncode: (self: ArrayOfCatalogSortContentProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfCatalogSortContentProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _ArrayOfCatalogSortContentProp_ConditionalOptionsFields = {
	options: { ArrayOfCatalogSortContentProp_ConditionalOption },
}

type _ArrayOfCatalogSortContentProp_ConditionalOptionsPartialFields = {
	options: { ArrayOfCatalogSortContentProp_ConditionalOption }?,
}

export type ArrayOfCatalogSortContentProp_ConditionalOptions = typeof(setmetatable(
	{} :: _ArrayOfCatalogSortContentProp_ConditionalOptionsFields,
	{} :: _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl
))
type _ArrayOfCatalogSortContentProp_ConditionalOptionsMessage = proto.Message<
	ArrayOfCatalogSortContentProp_ConditionalOptions,
	_ArrayOfCatalogSortContentProp_ConditionalOptionsPartialFields
>

type _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl = {
	__index: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl,
	new: (
		fields: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentPartialFields?
	) -> ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent,
	encode: (self: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent) -> buffer,
	decode: (input: buffer) -> ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent,
	jsonEncode: (self: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent,
	descriptor: proto.Descriptor,
}

type _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentFields = {
	array: { CatalogSortContent },
}

type _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentPartialFields = {
	array: { CatalogSortContent }?,
}

export type ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent = typeof(setmetatable(
	{} :: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentFields,
	{} :: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl
))
type _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentMessage = proto.Message<
	ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent,
	_ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentPartialFields
>

type _ArrayOfCatalogSortContentProp_ArrayMapImpl = {
	__index: _ArrayOfCatalogSortContentProp_ArrayMapImpl,
	new: (fields: _ArrayOfCatalogSortContentProp_ArrayMapPartialFields?) -> ArrayOfCatalogSortContentProp_ArrayMap,
	encode: (self: ArrayOfCatalogSortContentProp_ArrayMap) -> buffer,
	decode: (input: buffer) -> ArrayOfCatalogSortContentProp_ArrayMap,
	jsonEncode: (self: ArrayOfCatalogSortContentProp_ArrayMap) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfCatalogSortContentProp_ArrayMap,
	descriptor: proto.Descriptor,
}

type _ArrayOfCatalogSortContentProp_ArrayMapFields = {
	kind: (
		{ type: "binding_path", value: string }
		| { type: "literal", value: ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems }
	)?,
	item_hydration_specs: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec },
	field_map: CatalogSortContent?,
}

type _ArrayOfCatalogSortContentProp_ArrayMapPartialFields = {
	kind: (
		{ type: "binding_path", value: string }
		| { type: "literal", value: ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems }
	)?,
	item_hydration_specs: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec }?,
	field_map: CatalogSortContent?,
}

export type ArrayOfCatalogSortContentProp_ArrayMap = typeof(setmetatable(
	{} :: _ArrayOfCatalogSortContentProp_ArrayMapFields,
	{} :: _ArrayOfCatalogSortContentProp_ArrayMapImpl
))
type _ArrayOfCatalogSortContentProp_ArrayMapMessage = proto.Message<
	ArrayOfCatalogSortContentProp_ArrayMap,
	_ArrayOfCatalogSortContentProp_ArrayMapPartialFields
>

type _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl = {
	__index: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl,
	new: (
		fields: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsPartialFields?
	) -> ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems,
	encode: (self: ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems) -> buffer,
	decode: (input: buffer) -> ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems,
	jsonEncode: (self: ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems,
	descriptor: proto.Descriptor,
}

type _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsFields = {
	items: { _roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortContentData },
}

type _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsPartialFields = {
	items: { _roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortContentData }?,
}

export type ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems = typeof(setmetatable(
	{} :: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsFields,
	{} :: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl
))
type _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsMessage = proto.Message<
	ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems,
	_ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsPartialFields
>

type _WidgetStyleImpl = {
	__index: _WidgetStyleImpl,
	new: (fields: _WidgetStylePartialFields?) -> WidgetStyle,
	encode: (self: WidgetStyle) -> buffer,
	decode: (input: buffer) -> WidgetStyle,
	jsonEncode: (self: WidgetStyle) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> WidgetStyle,
	descriptor: proto.Descriptor,
}

type _WidgetStyleFields = {
	background_layers: _roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp?,
}

type _WidgetStylePartialFields = {
	background_layers: _roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp?,
}

export type WidgetStyle = typeof(setmetatable({} :: _WidgetStyleFields, {} :: _WidgetStyleImpl))
type _WidgetStyleMessage = proto.Message<WidgetStyle, _WidgetStylePartialFields>

type _WidgetStylePropImpl = {
	__index: _WidgetStylePropImpl,
	new: (fields: _WidgetStylePropPartialFields?) -> WidgetStyleProp,
	encode: (self: WidgetStyleProp) -> buffer,
	decode: (input: buffer) -> WidgetStyleProp,
	jsonEncode: (self: WidgetStyleProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> WidgetStyleProp,
	descriptor: proto.Descriptor,
}

type _WidgetStylePropFields = {
	kind: (
		{ type: "literal", value: WidgetStyle }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: WidgetStyleProp_ConditionalOptions }
	)?,
}

type _WidgetStylePropPartialFields = {
	kind: (
		{ type: "literal", value: WidgetStyle }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: WidgetStyleProp_ConditionalOptions }
	)?,
}

export type WidgetStyleProp = typeof(setmetatable({} :: _WidgetStylePropFields, {} :: _WidgetStylePropImpl))
type _WidgetStylePropMessage = proto.Message<WidgetStyleProp, _WidgetStylePropPartialFields>

type _WidgetStyleProp_ConditionalOptionImpl = {
	__index: _WidgetStyleProp_ConditionalOptionImpl,
	new: (fields: _WidgetStyleProp_ConditionalOptionPartialFields?) -> WidgetStyleProp_ConditionalOption,
	encode: (self: WidgetStyleProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> WidgetStyleProp_ConditionalOption,
	jsonEncode: (self: WidgetStyleProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> WidgetStyleProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _WidgetStyleProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: WidgetStyle } | { type: "binding_path", value: string })?,
}

type _WidgetStyleProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: WidgetStyle } | { type: "binding_path", value: string })?,
}

export type WidgetStyleProp_ConditionalOption = typeof(setmetatable(
	{} :: _WidgetStyleProp_ConditionalOptionFields,
	{} :: _WidgetStyleProp_ConditionalOptionImpl
))
type _WidgetStyleProp_ConditionalOptionMessage = proto.Message<
	WidgetStyleProp_ConditionalOption,
	_WidgetStyleProp_ConditionalOptionPartialFields
>

type _WidgetStyleProp_ConditionalOptionsImpl = {
	__index: _WidgetStyleProp_ConditionalOptionsImpl,
	new: (fields: _WidgetStyleProp_ConditionalOptionsPartialFields?) -> WidgetStyleProp_ConditionalOptions,
	encode: (self: WidgetStyleProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> WidgetStyleProp_ConditionalOptions,
	jsonEncode: (self: WidgetStyleProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> WidgetStyleProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _WidgetStyleProp_ConditionalOptionsFields = {
	options: { WidgetStyleProp_ConditionalOption },
}

type _WidgetStyleProp_ConditionalOptionsPartialFields = {
	options: { WidgetStyleProp_ConditionalOption }?,
}

export type WidgetStyleProp_ConditionalOptions = typeof(setmetatable(
	{} :: _WidgetStyleProp_ConditionalOptionsFields,
	{} :: _WidgetStyleProp_ConditionalOptionsImpl
))
type _WidgetStyleProp_ConditionalOptionsMessage = proto.Message<
	WidgetStyleProp_ConditionalOptions,
	_WidgetStyleProp_ConditionalOptionsPartialFields
>

do
	local _ItemFooterImpl = {}
	_ItemFooterImpl.__index = _ItemFooterImpl

	function _ItemFooterImpl.new(data: _ItemFooterPartialFields?): ItemFooter
		return setmetatable({
			content_type = if data == nil or data.content_type == nil then nil else data.content_type,
		}, _ItemFooterImpl :: _ItemFooterImpl)
	end

	function _ItemFooterImpl.encode(self: ItemFooter): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.content_type ~= nil then
			local encoded = self.content_type:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ItemFooterImpl.decode(input: buffer): ItemFooter
		local self = _ItemFooterImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content_type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ItemFooterImpl.jsonEncode(self: ItemFooter): any
		local output = {}

		if self.content_type ~= nil then
			output.contentType = self.content_type:jsonEncode()
		end

		return output
	end

	function _ItemFooterImpl.jsonDecode(input: { [string]: any }): ItemFooter
		local self = _ItemFooterImpl.new()

		if input.content_type ~= nil then
			self.content_type =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.content_type)
		end

		if input.contentType ~= nil then
			self.content_type =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.contentType)
		end

		return self
	end

	_ItemFooterImpl.descriptor = {
		name = "ItemFooter",
		fullName = "roblox.apppageplatform.shared.v1beta1.ItemFooter",
	}

	messages.ItemFooter = _ItemFooterImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ItemFooter)
end

do
	local _ArrayOfItemFooterPropImpl = {}
	_ArrayOfItemFooterPropImpl.__index = _ArrayOfItemFooterPropImpl

	function _ArrayOfItemFooterPropImpl.new(data: _ArrayOfItemFooterPropPartialFields?): ArrayOfItemFooterProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfItemFooterPropImpl :: _ArrayOfItemFooterPropImpl)
	end

	function _ArrayOfItemFooterPropImpl.encode(self: ArrayOfItemFooterProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "array_map" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfItemFooterPropImpl.decode(input: buffer): ArrayOfItemFooterProp
		local self = _ArrayOfItemFooterPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "literal", value = messages.ArrayOfItemFooterProp_ArrayOfItemFooter.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "conditional",
						value = messages.ArrayOfItemFooterProp_ConditionalOptions.decode(value),
					}
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "array_map", value = messages.ArrayOfItemFooterProp_ArrayMap.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfItemFooterPropImpl.jsonEncode(self: ArrayOfItemFooterProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			elseif self.kind.type == "array_map" then
				output.arrayMap = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _ArrayOfItemFooterPropImpl.jsonDecode(input: { [string]: any }): ArrayOfItemFooterProp
		local self = _ArrayOfItemFooterPropImpl.new()

		if input.literal ~= nil then
			self.kind =
				{ type = "literal", value = messages.ArrayOfItemFooterProp_ArrayOfItemFooter.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.ArrayOfItemFooterProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		if input.array_map ~= nil then
			self.kind =
				{ type = "array_map", value = messages.ArrayOfItemFooterProp_ArrayMap.jsonDecode(input.array_map) }
		end

		if input.arrayMap ~= nil then
			self.kind =
				{ type = "array_map", value = messages.ArrayOfItemFooterProp_ArrayMap.jsonDecode(input.arrayMap) }
		end

		return self
	end

	_ArrayOfItemFooterPropImpl.descriptor = {
		name = "ArrayOfItemFooterProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfItemFooterProp",
	}

	messages.ArrayOfItemFooterProp = _ArrayOfItemFooterPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfItemFooterProp)
end

do
	local _ArrayOfItemFooterProp_ConditionalOptionImpl = {}
	_ArrayOfItemFooterProp_ConditionalOptionImpl.__index = _ArrayOfItemFooterProp_ConditionalOptionImpl

	function _ArrayOfItemFooterProp_ConditionalOptionImpl.new(
		data: _ArrayOfItemFooterProp_ConditionalOptionPartialFields?
	): ArrayOfItemFooterProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfItemFooterProp_ConditionalOptionImpl :: _ArrayOfItemFooterProp_ConditionalOptionImpl)
	end

	function _ArrayOfItemFooterProp_ConditionalOptionImpl.encode(self: ArrayOfItemFooterProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfItemFooterProp_ConditionalOptionImpl.decode(input: buffer): ArrayOfItemFooterProp_ConditionalOption
		local self = _ArrayOfItemFooterProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "literal", value = messages.ArrayOfItemFooterProp_ArrayOfItemFooter.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfItemFooterProp_ConditionalOptionImpl.jsonEncode(self: ArrayOfItemFooterProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _ArrayOfItemFooterProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfItemFooterProp_ConditionalOption
		local self = _ArrayOfItemFooterProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind =
				{ type = "literal", value = messages.ArrayOfItemFooterProp_ArrayOfItemFooter.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_ArrayOfItemFooterProp_ConditionalOptionImpl.descriptor = {
		name = "ArrayOfItemFooterProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.ArrayOfItemFooterProp_ConditionalOption = _ArrayOfItemFooterProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfItemFooterProp_ConditionalOption)
end

do
	local _ArrayOfItemFooterProp_ConditionalOptionsImpl = {}
	_ArrayOfItemFooterProp_ConditionalOptionsImpl.__index = _ArrayOfItemFooterProp_ConditionalOptionsImpl

	function _ArrayOfItemFooterProp_ConditionalOptionsImpl.new(
		data: _ArrayOfItemFooterProp_ConditionalOptionsPartialFields?
	): ArrayOfItemFooterProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _ArrayOfItemFooterProp_ConditionalOptionsImpl :: _ArrayOfItemFooterProp_ConditionalOptionsImpl)
	end

	function _ArrayOfItemFooterProp_ConditionalOptionsImpl.encode(
		self: ArrayOfItemFooterProp_ConditionalOptions
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfItemFooterProp_ConditionalOptionsImpl.decode(
		input: buffer
	): ArrayOfItemFooterProp_ConditionalOptions
		local self = _ArrayOfItemFooterProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.ArrayOfItemFooterProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfItemFooterProp_ConditionalOptionsImpl.jsonEncode(
		self: ArrayOfItemFooterProp_ConditionalOptions
	): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _ArrayOfItemFooterProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfItemFooterProp_ConditionalOptions
		local self = _ArrayOfItemFooterProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { ArrayOfItemFooterProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.ArrayOfItemFooterProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_ArrayOfItemFooterProp_ConditionalOptionsImpl.descriptor = {
		name = "ArrayOfItemFooterProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.ArrayOfItemFooterProp_ConditionalOptions = _ArrayOfItemFooterProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfItemFooterProp_ConditionalOptions)
end

do
	local _ArrayOfItemFooterProp_ArrayOfItemFooterImpl = {}
	_ArrayOfItemFooterProp_ArrayOfItemFooterImpl.__index = _ArrayOfItemFooterProp_ArrayOfItemFooterImpl

	function _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.new(
		data: _ArrayOfItemFooterProp_ArrayOfItemFooterPartialFields?
	): ArrayOfItemFooterProp_ArrayOfItemFooter
		return setmetatable({
			array = if data == nil or data.array == nil then {} else data.array,
		}, _ArrayOfItemFooterProp_ArrayOfItemFooterImpl :: _ArrayOfItemFooterProp_ArrayOfItemFooterImpl)
	end

	function _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.encode(self: ArrayOfItemFooterProp_ArrayOfItemFooter): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.array ~= nil and #self.array > 0 then
			for _, value in self.array do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.decode(input: buffer): ArrayOfItemFooterProp_ArrayOfItemFooter
		local self = _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.array, messages.ItemFooter.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.jsonEncode(self: ArrayOfItemFooterProp_ArrayOfItemFooter): any
		local output = {}

		if self.array ~= nil and #self.array > 0 then
			local newOutput = {}
			for _, value in self.array do
				table.insert(newOutput, value:jsonEncode())
			end
			output.array = newOutput
		end

		return output
	end

	function _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfItemFooterProp_ArrayOfItemFooter
		local self = _ArrayOfItemFooterProp_ArrayOfItemFooterImpl.new()

		if input.array ~= nil then
			local newOutput: { ItemFooter } = {}
			for _, value in input.array do
				table.insert(newOutput, messages.ItemFooter.jsonDecode(value))
			end

			self.array = newOutput
		end

		return self
	end

	_ArrayOfItemFooterProp_ArrayOfItemFooterImpl.descriptor = {
		name = "ArrayOfItemFooterProp_ArrayOfItemFooter",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfItemFooter",
	}

	messages.ArrayOfItemFooterProp_ArrayOfItemFooter = _ArrayOfItemFooterProp_ArrayOfItemFooterImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfItemFooterProp_ArrayOfItemFooter)
end

do
	local _ArrayOfItemFooterProp_ArrayMapImpl = {}
	_ArrayOfItemFooterProp_ArrayMapImpl.__index = _ArrayOfItemFooterProp_ArrayMapImpl

	function _ArrayOfItemFooterProp_ArrayMapImpl.new(
		data: _ArrayOfItemFooterProp_ArrayMapPartialFields?
	): ArrayOfItemFooterProp_ArrayMap
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
			item_hydration_specs = if data == nil or data.item_hydration_specs == nil
				then {}
				else data.item_hydration_specs,
			field_map = if data == nil or data.field_map == nil then nil else data.field_map,
		}, _ArrayOfItemFooterProp_ArrayMapImpl :: _ArrayOfItemFooterProp_ArrayMapImpl)
	end

	function _ArrayOfItemFooterProp_ArrayMapImpl.encode(self: ArrayOfItemFooterProp_ArrayMap): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.item_hydration_specs ~= nil and #self.item_hydration_specs > 0 then
			for _, value in self.item_hydration_specs do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.field_map ~= nil then
			local encoded = self.field_map:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfItemFooterProp_ArrayMapImpl.decode(input: buffer): ArrayOfItemFooterProp_ArrayMap
		local self = _ArrayOfItemFooterProp_ArrayMapImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "literal", value = messages.ArrayOfItemFooterProp_ArrayMap_LiteralItems.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.item_hydration_specs,
						_roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec.decode(value)
					)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.field_map = messages.ItemFooter.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfItemFooterProp_ArrayMapImpl.jsonEncode(self: ArrayOfItemFooterProp_ArrayMap): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			end
		end

		if self.item_hydration_specs ~= nil and #self.item_hydration_specs > 0 then
			local newOutput = {}
			for _, value in self.item_hydration_specs do
				table.insert(newOutput, value:jsonEncode())
			end
			output.itemHydrationSpecs = newOutput
		end

		if self.field_map ~= nil then
			output.fieldMap = self.field_map:jsonEncode()
		end

		return output
	end

	function _ArrayOfItemFooterProp_ArrayMapImpl.jsonDecode(input: { [string]: any }): ArrayOfItemFooterProp_ArrayMap
		local self = _ArrayOfItemFooterProp_ArrayMapImpl.new()

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.ArrayOfItemFooterProp_ArrayMap_LiteralItems.jsonDecode(input.literal),
			}
		end

		if input.item_hydration_specs ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec } = {}
			for _, value in input.item_hydration_specs do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec.jsonDecode(value)
				)
			end

			self.item_hydration_specs = newOutput
		end

		if input.itemHydrationSpecs ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec } = {}
			for _, value in input.itemHydrationSpecs do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec.jsonDecode(value)
				)
			end

			self.item_hydration_specs = newOutput
		end

		if input.field_map ~= nil then
			self.field_map = messages.ItemFooter.jsonDecode(input.field_map)
		end

		if input.fieldMap ~= nil then
			self.field_map = messages.ItemFooter.jsonDecode(input.fieldMap)
		end

		return self
	end

	_ArrayOfItemFooterProp_ArrayMapImpl.descriptor = {
		name = "ArrayOfItemFooterProp_ArrayMap",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayMap",
	}

	messages.ArrayOfItemFooterProp_ArrayMap = _ArrayOfItemFooterProp_ArrayMapImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfItemFooterProp_ArrayMap)
end

do
	local _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl = {}
	_ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.__index = _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl

	function _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.new(
		data: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsPartialFields?
	): ArrayOfItemFooterProp_ArrayMap_LiteralItems
		return setmetatable({
			items = if data == nil or data.items == nil then {} else data.items,
		}, _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl :: _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl)
	end

	function _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.encode(
		self: ArrayOfItemFooterProp_ArrayMap_LiteralItems
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.items ~= nil and #self.items > 0 then
			for _, value in self.items do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.decode(
		input: buffer
	): ArrayOfItemFooterProp_ArrayMap_LiteralItems
		local self = _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.items,
						_roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortCardItemFooter.decode(value)
					)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.jsonEncode(
		self: ArrayOfItemFooterProp_ArrayMap_LiteralItems
	): any
		local output = {}

		if self.items ~= nil and #self.items > 0 then
			local newOutput = {}
			for _, value in self.items do
				table.insert(newOutput, value:jsonEncode())
			end
			output.items = newOutput
		end

		return output
	end

	function _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfItemFooterProp_ArrayMap_LiteralItems
		local self = _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.new()

		if input.items ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortCardItemFooter } = {}
			for _, value in input.items do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortCardItemFooter.jsonDecode(value)
				)
			end

			self.items = newOutput
		end

		return self
	end

	_ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl.descriptor = {
		name = "ArrayOfItemFooterProp_ArrayMap_LiteralItems",
		fullName = "roblox.apppageplatform.shared.v1beta1.LiteralItems",
	}

	messages.ArrayOfItemFooterProp_ArrayMap_LiteralItems = _ArrayOfItemFooterProp_ArrayMap_LiteralItemsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfItemFooterProp_ArrayMap_LiteralItems)
end

do
	local _CardStyleImpl = {}
	_CardStyleImpl.__index = _CardStyleImpl

	function _CardStyleImpl.new(data: _CardStylePartialFields?): CardStyle
		return setmetatable({
			thumbnail_aspect_ratio = if data == nil or data.thumbnail_aspect_ratio == nil
				then nil
				else data.thumbnail_aspect_ratio,
			background_layers = if data == nil or data.background_layers == nil then nil else data.background_layers,
		}, _CardStyleImpl :: _CardStyleImpl)
	end

	function _CardStyleImpl.encode(self: CardStyle): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.thumbnail_aspect_ratio ~= nil then
			local encoded = self.thumbnail_aspect_ratio:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_layers ~= nil then
			local encoded = self.background_layers:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CardStyleImpl.decode(input: buffer): CardStyle
		local self = _CardStyleImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.thumbnail_aspect_ratio =
						_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_layers =
						_roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CardStyleImpl.jsonEncode(self: CardStyle): any
		local output = {}

		if self.thumbnail_aspect_ratio ~= nil then
			output.thumbnailAspectRatio = self.thumbnail_aspect_ratio:jsonEncode()
		end

		if self.background_layers ~= nil then
			output.backgroundLayers = self.background_layers:jsonEncode()
		end

		return output
	end

	function _CardStyleImpl.jsonDecode(input: { [string]: any }): CardStyle
		local self = _CardStyleImpl.new()

		if input.thumbnail_aspect_ratio ~= nil then
			self.thumbnail_aspect_ratio =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.thumbnail_aspect_ratio)
		end

		if input.thumbnailAspectRatio ~= nil then
			self.thumbnail_aspect_ratio =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.thumbnailAspectRatio)
		end

		if input.background_layers ~= nil then
			self.background_layers =
				_roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp.jsonDecode(
					input.background_layers
				)
		end

		if input.backgroundLayers ~= nil then
			self.background_layers =
				_roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp.jsonDecode(
					input.backgroundLayers
				)
		end

		return self
	end

	_CardStyleImpl.descriptor = {
		name = "CardStyle",
		fullName = "roblox.apppageplatform.shared.v1beta1.CardStyle",
	}

	messages.CardStyle = _CardStyleImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CardStyle)
end

do
	local _CardStylePropImpl = {}
	_CardStylePropImpl.__index = _CardStylePropImpl

	function _CardStylePropImpl.new(data: _CardStylePropPartialFields?): CardStyleProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _CardStylePropImpl :: _CardStylePropImpl)
	end

	function _CardStylePropImpl.encode(self: CardStyleProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CardStylePropImpl.decode(input: buffer): CardStyleProp
		local self = _CardStylePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "literal", value = messages.CardStyle.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.CardStyleProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CardStylePropImpl.jsonEncode(self: CardStyleProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _CardStylePropImpl.jsonDecode(input: { [string]: any }): CardStyleProp
		local self = _CardStylePropImpl.new()

		if input.literal ~= nil then
			self.kind = { type = "literal", value = messages.CardStyle.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.CardStyleProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_CardStylePropImpl.descriptor = {
		name = "CardStyleProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.CardStyleProp",
	}

	messages.CardStyleProp = _CardStylePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CardStyleProp)
end

do
	local _CardStyleProp_ConditionalOptionImpl = {}
	_CardStyleProp_ConditionalOptionImpl.__index = _CardStyleProp_ConditionalOptionImpl

	function _CardStyleProp_ConditionalOptionImpl.new(
		data: _CardStyleProp_ConditionalOptionPartialFields?
	): CardStyleProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _CardStyleProp_ConditionalOptionImpl :: _CardStyleProp_ConditionalOptionImpl)
	end

	function _CardStyleProp_ConditionalOptionImpl.encode(self: CardStyleProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CardStyleProp_ConditionalOptionImpl.decode(input: buffer): CardStyleProp_ConditionalOption
		local self = _CardStyleProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "literal", value = messages.CardStyle.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CardStyleProp_ConditionalOptionImpl.jsonEncode(self: CardStyleProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _CardStyleProp_ConditionalOptionImpl.jsonDecode(input: { [string]: any }): CardStyleProp_ConditionalOption
		local self = _CardStyleProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = { type = "literal", value = messages.CardStyle.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_CardStyleProp_ConditionalOptionImpl.descriptor = {
		name = "CardStyleProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.CardStyleProp_ConditionalOption = _CardStyleProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CardStyleProp_ConditionalOption)
end

do
	local _CardStyleProp_ConditionalOptionsImpl = {}
	_CardStyleProp_ConditionalOptionsImpl.__index = _CardStyleProp_ConditionalOptionsImpl

	function _CardStyleProp_ConditionalOptionsImpl.new(
		data: _CardStyleProp_ConditionalOptionsPartialFields?
	): CardStyleProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _CardStyleProp_ConditionalOptionsImpl :: _CardStyleProp_ConditionalOptionsImpl)
	end

	function _CardStyleProp_ConditionalOptionsImpl.encode(self: CardStyleProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CardStyleProp_ConditionalOptionsImpl.decode(input: buffer): CardStyleProp_ConditionalOptions
		local self = _CardStyleProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.CardStyleProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CardStyleProp_ConditionalOptionsImpl.jsonEncode(self: CardStyleProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _CardStyleProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): CardStyleProp_ConditionalOptions
		local self = _CardStyleProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { CardStyleProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.CardStyleProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_CardStyleProp_ConditionalOptionsImpl.descriptor = {
		name = "CardStyleProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.CardStyleProp_ConditionalOptions = _CardStyleProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CardStyleProp_ConditionalOptions)
end

do
	local _CatalogSortContentImpl = {}
	_CatalogSortContentImpl.__index = _CatalogSortContentImpl

	function _CatalogSortContentImpl.new(data: _CatalogSortContentPartialFields?): CatalogSortContent
		return setmetatable({
			id = if data == nil or data.id == nil then nil else data.id,
			type = if data == nil or data.type == nil then nil else data.type,
		}, _CatalogSortContentImpl :: _CatalogSortContentImpl)
	end

	function _CatalogSortContentImpl.encode(self: CatalogSortContent): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil then
			local encoded = self.id:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.type ~= nil then
			local encoded = self.type:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogSortContentImpl.decode(input: buffer): CatalogSortContent
		local self = _CatalogSortContentImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogSortContentImpl.jsonEncode(self: CatalogSortContent): any
		local output = {}

		if self.id ~= nil then
			output.id = self.id:jsonEncode()
		end

		if self.type ~= nil then
			output.type = self.type:jsonEncode()
		end

		return output
	end

	function _CatalogSortContentImpl.jsonDecode(input: { [string]: any }): CatalogSortContent
		local self = _CatalogSortContentImpl.new()

		if input.id ~= nil then
			self.id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.id)
		end

		if input.type ~= nil then
			self.type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.type)
		end

		return self
	end

	_CatalogSortContentImpl.descriptor = {
		name = "CatalogSortContent",
		fullName = "roblox.apppageplatform.shared.v1beta1.CatalogSortContent",
	}

	messages.CatalogSortContent = _CatalogSortContentImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogSortContent)
end

do
	local _ArrayOfCatalogSortContentPropImpl = {}
	_ArrayOfCatalogSortContentPropImpl.__index = _ArrayOfCatalogSortContentPropImpl

	function _ArrayOfCatalogSortContentPropImpl.new(
		data: _ArrayOfCatalogSortContentPropPartialFields?
	): ArrayOfCatalogSortContentProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfCatalogSortContentPropImpl :: _ArrayOfCatalogSortContentPropImpl)
	end

	function _ArrayOfCatalogSortContentPropImpl.encode(self: ArrayOfCatalogSortContentProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "array_map" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfCatalogSortContentPropImpl.decode(input: buffer): ArrayOfCatalogSortContentProp
		local self = _ArrayOfCatalogSortContentPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "literal",
						value = messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent.decode(value),
					}
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "conditional",
						value = messages.ArrayOfCatalogSortContentProp_ConditionalOptions.decode(value),
					}
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "array_map", value = messages.ArrayOfCatalogSortContentProp_ArrayMap.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfCatalogSortContentPropImpl.jsonEncode(self: ArrayOfCatalogSortContentProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			elseif self.kind.type == "array_map" then
				output.arrayMap = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _ArrayOfCatalogSortContentPropImpl.jsonDecode(input: { [string]: any }): ArrayOfCatalogSortContentProp
		local self = _ArrayOfCatalogSortContentPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent.jsonDecode(input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.ArrayOfCatalogSortContentProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		if input.array_map ~= nil then
			self.kind = {
				type = "array_map",
				value = messages.ArrayOfCatalogSortContentProp_ArrayMap.jsonDecode(input.array_map),
			}
		end

		if input.arrayMap ~= nil then
			self.kind = {
				type = "array_map",
				value = messages.ArrayOfCatalogSortContentProp_ArrayMap.jsonDecode(input.arrayMap),
			}
		end

		return self
	end

	_ArrayOfCatalogSortContentPropImpl.descriptor = {
		name = "ArrayOfCatalogSortContentProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfCatalogSortContentProp",
	}

	messages.ArrayOfCatalogSortContentProp = _ArrayOfCatalogSortContentPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfCatalogSortContentProp)
end

do
	local _ArrayOfCatalogSortContentProp_ConditionalOptionImpl = {}
	_ArrayOfCatalogSortContentProp_ConditionalOptionImpl.__index = _ArrayOfCatalogSortContentProp_ConditionalOptionImpl

	function _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.new(
		data: _ArrayOfCatalogSortContentProp_ConditionalOptionPartialFields?
	): ArrayOfCatalogSortContentProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfCatalogSortContentProp_ConditionalOptionImpl :: _ArrayOfCatalogSortContentProp_ConditionalOptionImpl)
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.encode(
		self: ArrayOfCatalogSortContentProp_ConditionalOption
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.decode(
		input: buffer
	): ArrayOfCatalogSortContentProp_ConditionalOption
		local self = _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "literal",
						value = messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent.decode(value),
					}
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.jsonEncode(
		self: ArrayOfCatalogSortContentProp_ConditionalOption
	): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfCatalogSortContentProp_ConditionalOption
		local self = _ArrayOfCatalogSortContentProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent.jsonDecode(input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_ArrayOfCatalogSortContentProp_ConditionalOptionImpl.descriptor = {
		name = "ArrayOfCatalogSortContentProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.ArrayOfCatalogSortContentProp_ConditionalOption =
		_ArrayOfCatalogSortContentProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfCatalogSortContentProp_ConditionalOption)
end

do
	local _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl = {}
	_ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.__index =
		_ArrayOfCatalogSortContentProp_ConditionalOptionsImpl

	function _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.new(
		data: _ArrayOfCatalogSortContentProp_ConditionalOptionsPartialFields?
	): ArrayOfCatalogSortContentProp_ConditionalOptions
		return setmetatable(
			{
				options = if data == nil or data.options == nil then {} else data.options,
			},
			_ArrayOfCatalogSortContentProp_ConditionalOptionsImpl :: _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl
		)
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.encode(
		self: ArrayOfCatalogSortContentProp_ConditionalOptions
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.decode(
		input: buffer
	): ArrayOfCatalogSortContentProp_ConditionalOptions
		local self = _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.ArrayOfCatalogSortContentProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.jsonEncode(
		self: ArrayOfCatalogSortContentProp_ConditionalOptions
	): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfCatalogSortContentProp_ConditionalOptions
		local self = _ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { ArrayOfCatalogSortContentProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.ArrayOfCatalogSortContentProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_ArrayOfCatalogSortContentProp_ConditionalOptionsImpl.descriptor = {
		name = "ArrayOfCatalogSortContentProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.ArrayOfCatalogSortContentProp_ConditionalOptions =
		_ArrayOfCatalogSortContentProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfCatalogSortContentProp_ConditionalOptions)
end

do
	local _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl = {}
	_ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.__index =
		_ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl

	function _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.new(
		data: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentPartialFields?
	): ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent
		return setmetatable(
			{
				array = if data == nil or data.array == nil then {} else data.array,
			},
			_ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl :: _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl
		)
	end

	function _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.encode(
		self: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.array ~= nil and #self.array > 0 then
			for _, value in self.array do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.decode(
		input: buffer
	): ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent
		local self = _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.array, messages.CatalogSortContent.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.jsonEncode(
		self: ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent
	): any
		local output = {}

		if self.array ~= nil and #self.array > 0 then
			local newOutput = {}
			for _, value in self.array do
				table.insert(newOutput, value:jsonEncode())
			end
			output.array = newOutput
		end

		return output
	end

	function _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent
		local self = _ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.new()

		if input.array ~= nil then
			local newOutput: { CatalogSortContent } = {}
			for _, value in input.array do
				table.insert(newOutput, messages.CatalogSortContent.jsonDecode(value))
			end

			self.array = newOutput
		end

		return self
	end

	_ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl.descriptor = {
		name = "ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfCatalogSortContent",
	}

	messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent =
		_ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContentImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent)
end

do
	local _ArrayOfCatalogSortContentProp_ArrayMapImpl = {}
	_ArrayOfCatalogSortContentProp_ArrayMapImpl.__index = _ArrayOfCatalogSortContentProp_ArrayMapImpl

	function _ArrayOfCatalogSortContentProp_ArrayMapImpl.new(
		data: _ArrayOfCatalogSortContentProp_ArrayMapPartialFields?
	): ArrayOfCatalogSortContentProp_ArrayMap
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
			item_hydration_specs = if data == nil or data.item_hydration_specs == nil
				then {}
				else data.item_hydration_specs,
			field_map = if data == nil or data.field_map == nil then nil else data.field_map,
		}, _ArrayOfCatalogSortContentProp_ArrayMapImpl :: _ArrayOfCatalogSortContentProp_ArrayMapImpl)
	end

	function _ArrayOfCatalogSortContentProp_ArrayMapImpl.encode(self: ArrayOfCatalogSortContentProp_ArrayMap): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.item_hydration_specs ~= nil and #self.item_hydration_specs > 0 then
			for _, value in self.item_hydration_specs do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.field_map ~= nil then
			local encoded = self.field_map:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfCatalogSortContentProp_ArrayMapImpl.decode(input: buffer): ArrayOfCatalogSortContentProp_ArrayMap
		local self = _ArrayOfCatalogSortContentProp_ArrayMapImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "literal",
						value = messages.ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems.decode(value),
					}
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.item_hydration_specs,
						_roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec.decode(value)
					)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.field_map = messages.CatalogSortContent.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfCatalogSortContentProp_ArrayMapImpl.jsonEncode(self: ArrayOfCatalogSortContentProp_ArrayMap): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			end
		end

		if self.item_hydration_specs ~= nil and #self.item_hydration_specs > 0 then
			local newOutput = {}
			for _, value in self.item_hydration_specs do
				table.insert(newOutput, value:jsonEncode())
			end
			output.itemHydrationSpecs = newOutput
		end

		if self.field_map ~= nil then
			output.fieldMap = self.field_map:jsonEncode()
		end

		return output
	end

	function _ArrayOfCatalogSortContentProp_ArrayMapImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfCatalogSortContentProp_ArrayMap
		local self = _ArrayOfCatalogSortContentProp_ArrayMapImpl.new()

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems.jsonDecode(input.literal),
			}
		end

		if input.item_hydration_specs ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec } = {}
			for _, value in input.item_hydration_specs do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec.jsonDecode(value)
				)
			end

			self.item_hydration_specs = newOutput
		end

		if input.itemHydrationSpecs ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec } = {}
			for _, value in input.itemHydrationSpecs do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_hydration_data_spec.HydrationDataSpec.jsonDecode(value)
				)
			end

			self.item_hydration_specs = newOutput
		end

		if input.field_map ~= nil then
			self.field_map = messages.CatalogSortContent.jsonDecode(input.field_map)
		end

		if input.fieldMap ~= nil then
			self.field_map = messages.CatalogSortContent.jsonDecode(input.fieldMap)
		end

		return self
	end

	_ArrayOfCatalogSortContentProp_ArrayMapImpl.descriptor = {
		name = "ArrayOfCatalogSortContentProp_ArrayMap",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayMap",
	}

	messages.ArrayOfCatalogSortContentProp_ArrayMap = _ArrayOfCatalogSortContentProp_ArrayMapImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfCatalogSortContentProp_ArrayMap)
end

do
	local _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl = {}
	_ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.__index =
		_ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl

	function _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.new(
		data: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsPartialFields?
	): ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems
		return setmetatable(
			{
				items = if data == nil or data.items == nil then {} else data.items,
			},
			_ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl :: _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl
		)
	end

	function _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.encode(
		self: ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.items ~= nil and #self.items > 0 then
			for _, value in self.items do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.decode(
		input: buffer
	): ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems
		local self = _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.items,
						_roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortContentData.decode(value)
					)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.jsonEncode(
		self: ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems
	): any
		local output = {}

		if self.items ~= nil and #self.items > 0 then
			local newOutput = {}
			for _, value in self.items do
				table.insert(newOutput, value:jsonEncode())
			end
			output.items = newOutput
		end

		return output
	end

	function _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems
		local self = _ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.new()

		if input.items ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortContentData } = {}
			for _, value in input.items do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_catalog_sort_data.CatalogSortContentData.jsonDecode(value)
				)
			end

			self.items = newOutput
		end

		return self
	end

	_ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl.descriptor = {
		name = "ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems",
		fullName = "roblox.apppageplatform.shared.v1beta1.LiteralItems",
	}

	messages.ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems =
		_ArrayOfCatalogSortContentProp_ArrayMap_LiteralItemsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems)
end

do
	local _WidgetStyleImpl = {}
	_WidgetStyleImpl.__index = _WidgetStyleImpl

	function _WidgetStyleImpl.new(data: _WidgetStylePartialFields?): WidgetStyle
		return setmetatable({
			background_layers = if data == nil or data.background_layers == nil then nil else data.background_layers,
		}, _WidgetStyleImpl :: _WidgetStyleImpl)
	end

	function _WidgetStyleImpl.encode(self: WidgetStyle): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.background_layers ~= nil then
			local encoded = self.background_layers:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _WidgetStyleImpl.decode(input: buffer): WidgetStyle
		local self = _WidgetStyleImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_layers =
						_roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _WidgetStyleImpl.jsonEncode(self: WidgetStyle): any
		local output = {}

		if self.background_layers ~= nil then
			output.backgroundLayers = self.background_layers:jsonEncode()
		end

		return output
	end

	function _WidgetStyleImpl.jsonDecode(input: { [string]: any }): WidgetStyle
		local self = _WidgetStyleImpl.new()

		if input.background_layers ~= nil then
			self.background_layers =
				_roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp.jsonDecode(
					input.background_layers
				)
		end

		if input.backgroundLayers ~= nil then
			self.background_layers =
				_roblox_apppageplatform_shared_v1beta1_background_layer.ArrayOfBackgroundLayerProp.jsonDecode(
					input.backgroundLayers
				)
		end

		return self
	end

	_WidgetStyleImpl.descriptor = {
		name = "WidgetStyle",
		fullName = "roblox.apppageplatform.shared.v1beta1.WidgetStyle",
	}

	messages.WidgetStyle = _WidgetStyleImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.WidgetStyle)
end

do
	local _WidgetStylePropImpl = {}
	_WidgetStylePropImpl.__index = _WidgetStylePropImpl

	function _WidgetStylePropImpl.new(data: _WidgetStylePropPartialFields?): WidgetStyleProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _WidgetStylePropImpl :: _WidgetStylePropImpl)
	end

	function _WidgetStylePropImpl.encode(self: WidgetStyleProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _WidgetStylePropImpl.decode(input: buffer): WidgetStyleProp
		local self = _WidgetStylePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "literal", value = messages.WidgetStyle.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.WidgetStyleProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _WidgetStylePropImpl.jsonEncode(self: WidgetStyleProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _WidgetStylePropImpl.jsonDecode(input: { [string]: any }): WidgetStyleProp
		local self = _WidgetStylePropImpl.new()

		if input.literal ~= nil then
			self.kind = { type = "literal", value = messages.WidgetStyle.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.WidgetStyleProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_WidgetStylePropImpl.descriptor = {
		name = "WidgetStyleProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.WidgetStyleProp",
	}

	messages.WidgetStyleProp = _WidgetStylePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.WidgetStyleProp)
end

do
	local _WidgetStyleProp_ConditionalOptionImpl = {}
	_WidgetStyleProp_ConditionalOptionImpl.__index = _WidgetStyleProp_ConditionalOptionImpl

	function _WidgetStyleProp_ConditionalOptionImpl.new(
		data: _WidgetStyleProp_ConditionalOptionPartialFields?
	): WidgetStyleProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _WidgetStyleProp_ConditionalOptionImpl :: _WidgetStyleProp_ConditionalOptionImpl)
	end

	function _WidgetStyleProp_ConditionalOptionImpl.encode(self: WidgetStyleProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _WidgetStyleProp_ConditionalOptionImpl.decode(input: buffer): WidgetStyleProp_ConditionalOption
		local self = _WidgetStyleProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "literal", value = messages.WidgetStyle.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _WidgetStyleProp_ConditionalOptionImpl.jsonEncode(self: WidgetStyleProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _WidgetStyleProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): WidgetStyleProp_ConditionalOption
		local self = _WidgetStyleProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = { type = "literal", value = messages.WidgetStyle.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_WidgetStyleProp_ConditionalOptionImpl.descriptor = {
		name = "WidgetStyleProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.WidgetStyleProp_ConditionalOption = _WidgetStyleProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.WidgetStyleProp_ConditionalOption)
end

do
	local _WidgetStyleProp_ConditionalOptionsImpl = {}
	_WidgetStyleProp_ConditionalOptionsImpl.__index = _WidgetStyleProp_ConditionalOptionsImpl

	function _WidgetStyleProp_ConditionalOptionsImpl.new(
		data: _WidgetStyleProp_ConditionalOptionsPartialFields?
	): WidgetStyleProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _WidgetStyleProp_ConditionalOptionsImpl :: _WidgetStyleProp_ConditionalOptionsImpl)
	end

	function _WidgetStyleProp_ConditionalOptionsImpl.encode(self: WidgetStyleProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _WidgetStyleProp_ConditionalOptionsImpl.decode(input: buffer): WidgetStyleProp_ConditionalOptions
		local self = _WidgetStyleProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.WidgetStyleProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _WidgetStyleProp_ConditionalOptionsImpl.jsonEncode(self: WidgetStyleProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _WidgetStyleProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): WidgetStyleProp_ConditionalOptions
		local self = _WidgetStyleProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { WidgetStyleProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.WidgetStyleProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_WidgetStyleProp_ConditionalOptionsImpl.descriptor = {
		name = "WidgetStyleProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.WidgetStyleProp_ConditionalOptions = _WidgetStyleProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.WidgetStyleProp_ConditionalOptions)
end

return {
	ItemFooter = messages.ItemFooter,
	ArrayOfItemFooterProp = messages.ArrayOfItemFooterProp,
	ArrayOfItemFooterProp_ConditionalOption = messages.ArrayOfItemFooterProp_ConditionalOption,
	ArrayOfItemFooterProp_ConditionalOptions = messages.ArrayOfItemFooterProp_ConditionalOptions,
	ArrayOfItemFooterProp_ArrayOfItemFooter = messages.ArrayOfItemFooterProp_ArrayOfItemFooter,
	ArrayOfItemFooterProp_ArrayMap = messages.ArrayOfItemFooterProp_ArrayMap,
	ArrayOfItemFooterProp_ArrayMap_LiteralItems = messages.ArrayOfItemFooterProp_ArrayMap_LiteralItems,
	CardStyle = messages.CardStyle,
	CardStyleProp = messages.CardStyleProp,
	CardStyleProp_ConditionalOption = messages.CardStyleProp_ConditionalOption,
	CardStyleProp_ConditionalOptions = messages.CardStyleProp_ConditionalOptions,
	CatalogSortContent = messages.CatalogSortContent,
	ArrayOfCatalogSortContentProp = messages.ArrayOfCatalogSortContentProp,
	ArrayOfCatalogSortContentProp_ConditionalOption = messages.ArrayOfCatalogSortContentProp_ConditionalOption,
	ArrayOfCatalogSortContentProp_ConditionalOptions = messages.ArrayOfCatalogSortContentProp_ConditionalOptions,
	ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent = messages.ArrayOfCatalogSortContentProp_ArrayOfCatalogSortContent,
	ArrayOfCatalogSortContentProp_ArrayMap = messages.ArrayOfCatalogSortContentProp_ArrayMap,
	ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems = messages.ArrayOfCatalogSortContentProp_ArrayMap_LiteralItems,
	WidgetStyle = messages.WidgetStyle,
	WidgetStyleProp = messages.WidgetStyleProp,
	WidgetStyleProp_ConditionalOption = messages.WidgetStyleProp_ConditionalOption,
	WidgetStyleProp_ConditionalOptions = messages.WidgetStyleProp_ConditionalOptions,
}
