--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	FriendRecommendationData: _FriendRecommendationDataMessage,
	FriendRecommendationData_RecommendationType: _FriendRecommendationData_RecommendationTypeMessage,
}
local messages: _Messages = {} :: _Messages

type _FriendRecommendationDataImpl = {
	__index: _FriendRecommendationDataImpl,
	new: (fields: _FriendRecommendationDataPartialFields?) -> FriendRecommendationData,
	encode: (self: FriendRecommendationData) -> buffer,
	decode: (input: buffer) -> FriendRecommendationData,
	jsonEncode: (self: FriendRecommendationData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FriendRecommendationData,
	descriptor: proto.Descriptor,
}

type _FriendRecommendationDataFields = {
	recommendation_type: FriendRecommendationData_RecommendationType,
	context_type: string?,
	mutual_friends_count: number?,
	contact_name: string?,
	avatar_thumbnail_url: string?,
	headshot_thumbnail_url: string?,
}

type _FriendRecommendationDataPartialFields = {
	recommendation_type: FriendRecommendationData_RecommendationType?,
	context_type: string?,
	mutual_friends_count: number?,
	contact_name: string?,
	avatar_thumbnail_url: string?,
	headshot_thumbnail_url: string?,
}

export type FriendRecommendationData = typeof(setmetatable(
	{} :: _FriendRecommendationDataFields,
	{} :: _FriendRecommendationDataImpl
))
type _FriendRecommendationDataMessage = proto.Message<FriendRecommendationData, _FriendRecommendationDataPartialFields>

type _FriendRecommendationData_RecommendationTypeMessage = proto.Enum<FriendRecommendationData_RecommendationType>
export type FriendRecommendationData_RecommendationType =
	"RECOMMENDATION_TYPE_INVALID"
	| "RECOMMENDATION_TYPE_USER"
	| "RECOMMENDATION_TYPE_CONTACT"
	| number -- Unknown

do
	local _FriendRecommendationDataImpl = {}
	_FriendRecommendationDataImpl.__index = _FriendRecommendationDataImpl

	function _FriendRecommendationDataImpl.new(data: _FriendRecommendationDataPartialFields?): FriendRecommendationData
		return setmetatable({
			recommendation_type = if data == nil or data.recommendation_type == nil
				then assert(messages.FriendRecommendationData_RecommendationType.fromNumber(0), "Enum has no 0 default")
				else data.recommendation_type,
			context_type = if data == nil or data.context_type == nil then nil else data.context_type,
			mutual_friends_count = if data == nil or data.mutual_friends_count == nil
				then nil
				else data.mutual_friends_count,
			contact_name = if data == nil or data.contact_name == nil then nil else data.contact_name,
			avatar_thumbnail_url = if data == nil or data.avatar_thumbnail_url == nil
				then nil
				else data.avatar_thumbnail_url,
			headshot_thumbnail_url = if data == nil or data.headshot_thumbnail_url == nil
				then nil
				else data.headshot_thumbnail_url,
		}, _FriendRecommendationDataImpl :: _FriendRecommendationDataImpl)
	end

	function _FriendRecommendationDataImpl.encode(self: FriendRecommendationData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if
			self.recommendation_type ~= nil
			and (
				self.recommendation_type ~= nil and self.recommendation_type ~= 0
				or self.recommendation_type ~= messages.FriendRecommendationData_RecommendationType.fromNumber(0)
			)
		then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(
				output,
				cursor,
				messages.FriendRecommendationData_RecommendationType.toNumber(self.recommendation_type :: any)
			)
		end

		if self.context_type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.context_type)
		end

		if self.mutual_friends_count ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.mutual_friends_count)
		end

		if self.contact_name ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.contact_name)
		end

		if self.avatar_thumbnail_url ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.avatar_thumbnail_url)
		end

		if self.headshot_thumbnail_url ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.headshot_thumbnail_url)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FriendRecommendationDataImpl.decode(input: buffer): FriendRecommendationData
		local self = _FriendRecommendationDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.recommendation_type = (
						messages.FriendRecommendationData_RecommendationType.fromNumber(value) or value
					) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.mutual_friends_count = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.context_type = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.contact_name = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.avatar_thumbnail_url = buffer.tostring(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.headshot_thumbnail_url = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FriendRecommendationDataImpl.jsonEncode(self: FriendRecommendationData): any
		local output = {}

		if
			self.recommendation_type ~= nil
			and (
				self.recommendation_type ~= nil and self.recommendation_type ~= 0
				or self.recommendation_type ~= messages.FriendRecommendationData_RecommendationType.fromNumber(0)
			)
		then
			output.recommendationType = if typeof(self.recommendation_type) == "number"
				then self.recommendation_type
				else messages.FriendRecommendationData_RecommendationType.toNumber(self.recommendation_type :: any)
		end

		if self.context_type ~= nil then
			output.contextType = self.context_type
		end

		if self.mutual_friends_count ~= nil then
			output.mutualFriendsCount = self.mutual_friends_count
		end

		if self.contact_name ~= nil then
			output.contactName = self.contact_name
		end

		if self.avatar_thumbnail_url ~= nil then
			output.avatarThumbnailUrl = self.avatar_thumbnail_url
		end

		if self.headshot_thumbnail_url ~= nil then
			output.headshotThumbnailUrl = self.headshot_thumbnail_url
		end

		return output
	end

	function _FriendRecommendationDataImpl.jsonDecode(input: { [string]: any }): FriendRecommendationData
		local self = _FriendRecommendationDataImpl.new()

		if input.recommendation_type ~= nil then
			self.recommendation_type = if typeof(input.recommendation_type) == "number"
				then (
					messages.FriendRecommendationData_RecommendationType.fromNumber(input.recommendation_type)
					or input.recommendation_type
				)
				else (
					messages.FriendRecommendationData_RecommendationType.fromName(input.recommendation_type)
					or input.recommendation_type
				)
		end

		if input.recommendationType ~= nil then
			self.recommendation_type = if typeof(input.recommendationType) == "number"
				then (
					messages.FriendRecommendationData_RecommendationType.fromNumber(input.recommendationType)
					or input.recommendationType
				)
				else (
					messages.FriendRecommendationData_RecommendationType.fromName(input.recommendationType)
					or input.recommendationType
				)
		end

		if input.context_type ~= nil then
			self.context_type = input.context_type
		end

		if input.contextType ~= nil then
			self.context_type = input.contextType
		end

		if input.mutual_friends_count ~= nil then
			self.mutual_friends_count = input.mutual_friends_count
		end

		if input.mutualFriendsCount ~= nil then
			self.mutual_friends_count = input.mutualFriendsCount
		end

		if input.contact_name ~= nil then
			self.contact_name = input.contact_name
		end

		if input.contactName ~= nil then
			self.contact_name = input.contactName
		end

		if input.avatar_thumbnail_url ~= nil then
			self.avatar_thumbnail_url = input.avatar_thumbnail_url
		end

		if input.avatarThumbnailUrl ~= nil then
			self.avatar_thumbnail_url = input.avatarThumbnailUrl
		end

		if input.headshot_thumbnail_url ~= nil then
			self.headshot_thumbnail_url = input.headshot_thumbnail_url
		end

		if input.headshotThumbnailUrl ~= nil then
			self.headshot_thumbnail_url = input.headshotThumbnailUrl
		end

		return self
	end

	_FriendRecommendationDataImpl.descriptor = {
		name = "FriendRecommendationData",
		fullName = "roblox.apppageplatform.shared.v1beta1.FriendRecommendationData",
	}

	messages.FriendRecommendationData = _FriendRecommendationDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FriendRecommendationData)
end

messages.FriendRecommendationData_RecommendationType = {
	fromNumber = function(value: number): FriendRecommendationData_RecommendationType?
		if value == 0 then
			return "RECOMMENDATION_TYPE_INVALID"
		elseif value == 1 then
			return "RECOMMENDATION_TYPE_USER"
		elseif value == 2 then
			return "RECOMMENDATION_TYPE_CONTACT"
		else
			return nil
		end
	end,

	toNumber = function(self: FriendRecommendationData_RecommendationType): number
		if self == "RECOMMENDATION_TYPE_INVALID" then
			return 0
		elseif self == "RECOMMENDATION_TYPE_USER" then
			return 1
		elseif self == "RECOMMENDATION_TYPE_CONTACT" then
			return 2
		else
			return self
		end
	end,

	fromName = function(name: string): FriendRecommendationData_RecommendationType?
		if name == "RECOMMENDATION_TYPE_INVALID" then
			return "RECOMMENDATION_TYPE_INVALID"
		elseif name == "RECOMMENDATION_TYPE_USER" then
			return "RECOMMENDATION_TYPE_USER"
		elseif name == "RECOMMENDATION_TYPE_CONTACT" then
			return "RECOMMENDATION_TYPE_CONTACT"
		else
			return nil
		end
	end,
}

return {
	FriendRecommendationData = messages.FriendRecommendationData,
	FriendRecommendationData_RecommendationType = messages.FriendRecommendationData_RecommendationType,
}
