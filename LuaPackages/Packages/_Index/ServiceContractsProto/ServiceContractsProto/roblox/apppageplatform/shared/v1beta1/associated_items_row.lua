--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	AssociatedItemsRowSchema: _AssociatedItemsRowSchemaMessage,
	AssociatedItemsRowSchema_Props: _AssociatedItemsRowSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _AssociatedItemsRowSchemaImpl = {
	__index: _AssociatedItemsRowSchemaImpl,
	new: (fields: _AssociatedItemsRowSchemaPartialFields?) -> AssociatedItemsRowSchema,
	encode: (self: AssociatedItemsRowSchema) -> buffer,
	decode: (input: buffer) -> AssociatedItemsRowSchema,
	jsonEncode: (self: AssociatedItemsRowSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AssociatedItemsRowSchema,
	descriptor: proto.Descriptor,
}

type _AssociatedItemsRowSchemaFields = {
	props: AssociatedItemsRowSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _AssociatedItemsRowSchemaPartialFields = {
	props: AssociatedItemsRowSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type AssociatedItemsRowSchema = typeof(setmetatable(
	{} :: _AssociatedItemsRowSchemaFields,
	{} :: _AssociatedItemsRowSchemaImpl
))
type _AssociatedItemsRowSchemaMessage = proto.Message<AssociatedItemsRowSchema, _AssociatedItemsRowSchemaPartialFields>

type _AssociatedItemsRowSchema_PropsImpl = {
	__index: _AssociatedItemsRowSchema_PropsImpl,
	new: (fields: _AssociatedItemsRowSchema_PropsPartialFields?) -> AssociatedItemsRowSchema_Props,
	encode: (self: AssociatedItemsRowSchema_Props) -> buffer,
	decode: (input: buffer) -> AssociatedItemsRowSchema_Props,
	jsonEncode: (self: AssociatedItemsRowSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AssociatedItemsRowSchema_Props,
	descriptor: proto.Descriptor,
}

type _AssociatedItemsRowSchema_PropsFields = {
	items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

type _AssociatedItemsRowSchema_PropsPartialFields = {
	items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

export type AssociatedItemsRowSchema_Props = typeof(setmetatable(
	{} :: _AssociatedItemsRowSchema_PropsFields,
	{} :: _AssociatedItemsRowSchema_PropsImpl
))
type _AssociatedItemsRowSchema_PropsMessage = proto.Message<
	AssociatedItemsRowSchema_Props,
	_AssociatedItemsRowSchema_PropsPartialFields
>

do
	local _AssociatedItemsRowSchemaImpl = {}
	_AssociatedItemsRowSchemaImpl.__index = _AssociatedItemsRowSchemaImpl

	function _AssociatedItemsRowSchemaImpl.new(data: _AssociatedItemsRowSchemaPartialFields?): AssociatedItemsRowSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _AssociatedItemsRowSchemaImpl :: _AssociatedItemsRowSchemaImpl)
	end

	function _AssociatedItemsRowSchemaImpl.encode(self: AssociatedItemsRowSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AssociatedItemsRowSchemaImpl.decode(input: buffer): AssociatedItemsRowSchema
		local self = _AssociatedItemsRowSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.AssociatedItemsRowSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AssociatedItemsRowSchemaImpl.jsonEncode(self: AssociatedItemsRowSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _AssociatedItemsRowSchemaImpl.jsonDecode(input: { [string]: any }): AssociatedItemsRowSchema
		local self = _AssociatedItemsRowSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.AssociatedItemsRowSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_AssociatedItemsRowSchemaImpl.descriptor = {
		name = "AssociatedItemsRowSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.AssociatedItemsRowSchema",
	}

	messages.AssociatedItemsRowSchema = _AssociatedItemsRowSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AssociatedItemsRowSchema)
end

do
	local _AssociatedItemsRowSchema_PropsImpl = {}
	_AssociatedItemsRowSchema_PropsImpl.__index = _AssociatedItemsRowSchema_PropsImpl

	function _AssociatedItemsRowSchema_PropsImpl.new(
		data: _AssociatedItemsRowSchema_PropsPartialFields?
	): AssociatedItemsRowSchema_Props
		return setmetatable({
			items = if data == nil or data.items == nil then nil else data.items,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
		}, _AssociatedItemsRowSchema_PropsImpl :: _AssociatedItemsRowSchema_PropsImpl)
	end

	function _AssociatedItemsRowSchema_PropsImpl.encode(self: AssociatedItemsRowSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.items ~= nil then
			local encoded = self.items:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AssociatedItemsRowSchema_PropsImpl.decode(input: buffer): AssociatedItemsRowSchema_Props
		local self = _AssociatedItemsRowSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AssociatedItemsRowSchema_PropsImpl.jsonEncode(self: AssociatedItemsRowSchema_Props): any
		local output = {}

		if self.items ~= nil then
			output.items = self.items:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		return output
	end

	function _AssociatedItemsRowSchema_PropsImpl.jsonDecode(input: { [string]: any }): AssociatedItemsRowSchema_Props
		local self = _AssociatedItemsRowSchema_PropsImpl.new()

		if input.items ~= nil then
			self.items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(input.items)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		return self
	end

	_AssociatedItemsRowSchema_PropsImpl.descriptor = {
		name = "AssociatedItemsRowSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.AssociatedItemsRowSchema_Props = _AssociatedItemsRowSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AssociatedItemsRowSchema_Props)
end

return {
	AssociatedItemsRowSchema = messages.AssociatedItemsRowSchema,
	AssociatedItemsRowSchema_Props = messages.AssociatedItemsRowSchema_Props,
}
