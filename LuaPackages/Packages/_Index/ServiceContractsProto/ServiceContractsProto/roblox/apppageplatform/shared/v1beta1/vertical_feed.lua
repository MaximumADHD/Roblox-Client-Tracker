--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	VerticalFeedSchema: _VerticalFeedSchemaMessage,
	VerticalFeedSchema_Props: _VerticalFeedSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_prop_types_engine = require(script.Parent.prop_types_engine)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _VerticalFeedSchemaImpl = {
	__index: _VerticalFeedSchemaImpl,
	new: (fields: _VerticalFeedSchemaPartialFields?) -> VerticalFeedSchema,
	encode: (self: VerticalFeedSchema) -> buffer,
	decode: (input: buffer) -> VerticalFeedSchema,
	jsonEncode: (self: VerticalFeedSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VerticalFeedSchema,
	descriptor: proto.Descriptor,
}

type _VerticalFeedSchemaFields = {
	props: VerticalFeedSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _VerticalFeedSchemaPartialFields = {
	props: VerticalFeedSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type VerticalFeedSchema = typeof(setmetatable({} :: _VerticalFeedSchemaFields, {} :: _VerticalFeedSchemaImpl))
type _VerticalFeedSchemaMessage = proto.Message<VerticalFeedSchema, _VerticalFeedSchemaPartialFields>

type _VerticalFeedSchema_PropsImpl = {
	__index: _VerticalFeedSchema_PropsImpl,
	new: (fields: _VerticalFeedSchema_PropsPartialFields?) -> VerticalFeedSchema_Props,
	encode: (self: VerticalFeedSchema_Props) -> buffer,
	decode: (input: buffer) -> VerticalFeedSchema_Props,
	jsonEncode: (self: VerticalFeedSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VerticalFeedSchema_Props,
	descriptor: proto.Descriptor,
}

type _VerticalFeedSchema_PropsFields = {
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	feed_items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	threshold_from_end: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	on_scroll_to_end: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	horizontal_alignment: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.HorizontalAlignmentProp?,
	horizontal_padding: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	gap_between_feed_items: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	impressions_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	selection_behavior_left: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp?,
	selection_behavior_right: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp?,
	on_refresh: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	sticky_items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	gap_after_sticky_items: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	gap_between_sticky_items: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	sticky_padding_top: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	sticky_padding_bottom: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	sticky_content_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _VerticalFeedSchema_PropsPartialFields = {
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	feed_items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	threshold_from_end: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	on_scroll_to_end: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	horizontal_alignment: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.HorizontalAlignmentProp?,
	horizontal_padding: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	gap_between_feed_items: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	impressions_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	selection_behavior_left: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp?,
	selection_behavior_right: _roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp?,
	on_refresh: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	sticky_items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	gap_after_sticky_items: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	gap_between_sticky_items: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	sticky_padding_top: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	sticky_padding_bottom: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	sticky_content_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type VerticalFeedSchema_Props = typeof(setmetatable(
	{} :: _VerticalFeedSchema_PropsFields,
	{} :: _VerticalFeedSchema_PropsImpl
))
type _VerticalFeedSchema_PropsMessage = proto.Message<VerticalFeedSchema_Props, _VerticalFeedSchema_PropsPartialFields>

do
	local _VerticalFeedSchemaImpl = {}
	_VerticalFeedSchemaImpl.__index = _VerticalFeedSchemaImpl

	function _VerticalFeedSchemaImpl.new(data: _VerticalFeedSchemaPartialFields?): VerticalFeedSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _VerticalFeedSchemaImpl :: _VerticalFeedSchemaImpl)
	end

	function _VerticalFeedSchemaImpl.encode(self: VerticalFeedSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VerticalFeedSchemaImpl.decode(input: buffer): VerticalFeedSchema
		local self = _VerticalFeedSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.VerticalFeedSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VerticalFeedSchemaImpl.jsonEncode(self: VerticalFeedSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _VerticalFeedSchemaImpl.jsonDecode(input: { [string]: any }): VerticalFeedSchema
		local self = _VerticalFeedSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.VerticalFeedSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_VerticalFeedSchemaImpl.descriptor = {
		name = "VerticalFeedSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.VerticalFeedSchema",
	}

	messages.VerticalFeedSchema = _VerticalFeedSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VerticalFeedSchema)
end

do
	local _VerticalFeedSchema_PropsImpl = {}
	_VerticalFeedSchema_PropsImpl.__index = _VerticalFeedSchema_PropsImpl

	function _VerticalFeedSchema_PropsImpl.new(data: _VerticalFeedSchema_PropsPartialFields?): VerticalFeedSchema_Props
		return setmetatable({
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			feed_items = if data == nil or data.feed_items == nil then nil else data.feed_items,
			threshold_from_end = if data == nil or data.threshold_from_end == nil then nil else data.threshold_from_end,
			on_scroll_to_end = if data == nil or data.on_scroll_to_end == nil then nil else data.on_scroll_to_end,
			background_style = if data == nil or data.background_style == nil then nil else data.background_style,
			horizontal_alignment = if data == nil or data.horizontal_alignment == nil
				then nil
				else data.horizontal_alignment,
			horizontal_padding = if data == nil or data.horizontal_padding == nil then nil else data.horizontal_padding,
			gap_between_feed_items = if data == nil or data.gap_between_feed_items == nil
				then nil
				else data.gap_between_feed_items,
			impressions_tag = if data == nil or data.impressions_tag == nil then nil else data.impressions_tag,
			selection_behavior_left = if data == nil or data.selection_behavior_left == nil
				then nil
				else data.selection_behavior_left,
			selection_behavior_right = if data == nil or data.selection_behavior_right == nil
				then nil
				else data.selection_behavior_right,
			on_refresh = if data == nil or data.on_refresh == nil then nil else data.on_refresh,
			sticky_items = if data == nil or data.sticky_items == nil then nil else data.sticky_items,
			gap_after_sticky_items = if data == nil or data.gap_after_sticky_items == nil
				then nil
				else data.gap_after_sticky_items,
			gap_between_sticky_items = if data == nil or data.gap_between_sticky_items == nil
				then nil
				else data.gap_between_sticky_items,
			sticky_padding_top = if data == nil or data.sticky_padding_top == nil then nil else data.sticky_padding_top,
			sticky_padding_bottom = if data == nil or data.sticky_padding_bottom == nil
				then nil
				else data.sticky_padding_bottom,
			sticky_content_tag = if data == nil or data.sticky_content_tag == nil then nil else data.sticky_content_tag,
		}, _VerticalFeedSchema_PropsImpl :: _VerticalFeedSchema_PropsImpl)
	end

	function _VerticalFeedSchema_PropsImpl.encode(self: VerticalFeedSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.feed_items ~= nil then
			local encoded = self.feed_items:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.threshold_from_end ~= nil then
			local encoded = self.threshold_from_end:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_scroll_to_end ~= nil then
			local encoded = self.on_scroll_to_end:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_style ~= nil then
			local encoded = self.background_style:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.horizontal_alignment ~= nil then
			local encoded = self.horizontal_alignment:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.horizontal_padding ~= nil then
			local encoded = self.horizontal_padding:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.gap_between_feed_items ~= nil then
			local encoded = self.gap_between_feed_items:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.impressions_tag ~= nil then
			local encoded = self.impressions_tag:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.selection_behavior_left ~= nil then
			local encoded = self.selection_behavior_left:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.selection_behavior_right ~= nil then
			local encoded = self.selection_behavior_right:encode()
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_refresh ~= nil then
			local encoded = self.on_refresh:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.sticky_items ~= nil then
			local encoded = self.sticky_items:encode()
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.gap_after_sticky_items ~= nil then
			local encoded = self.gap_after_sticky_items:encode()
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.gap_between_sticky_items ~= nil then
			local encoded = self.gap_between_sticky_items:encode()
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.sticky_padding_top ~= nil then
			local encoded = self.sticky_padding_top:encode()
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.sticky_padding_bottom ~= nil then
			local encoded = self.sticky_padding_bottom:encode()
			output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.sticky_content_tag ~= nil then
			local encoded = self.sticky_content_tag:encode()
			output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VerticalFeedSchema_PropsImpl.decode(input: buffer): VerticalFeedSchema_Props
		local self = _VerticalFeedSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.feed_items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.threshold_from_end = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_scroll_to_end = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_style = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.horizontal_alignment =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.HorizontalAlignmentProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.horizontal_padding = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gap_between_feed_items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.impressions_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.selection_behavior_left =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp.decode(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.selection_behavior_right =
						_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp.decode(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_refresh = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sticky_items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gap_after_sticky_items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gap_between_sticky_items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sticky_padding_top = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sticky_padding_bottom =
						_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sticky_content_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VerticalFeedSchema_PropsImpl.jsonEncode(self: VerticalFeedSchema_Props): any
		local output = {}

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.feed_items ~= nil then
			output.feedItems = self.feed_items:jsonEncode()
		end

		if self.threshold_from_end ~= nil then
			output.thresholdFromEnd = self.threshold_from_end:jsonEncode()
		end

		if self.on_scroll_to_end ~= nil then
			output.onScrollToEnd = self.on_scroll_to_end:jsonEncode()
		end

		if self.background_style ~= nil then
			output.backgroundStyle = self.background_style:jsonEncode()
		end

		if self.horizontal_alignment ~= nil then
			output.horizontalAlignment = self.horizontal_alignment:jsonEncode()
		end

		if self.horizontal_padding ~= nil then
			output.horizontalPadding = self.horizontal_padding:jsonEncode()
		end

		if self.gap_between_feed_items ~= nil then
			output.gapBetweenFeedItems = self.gap_between_feed_items:jsonEncode()
		end

		if self.impressions_tag ~= nil then
			output.impressionsTag = self.impressions_tag:jsonEncode()
		end

		if self.selection_behavior_left ~= nil then
			output.selectionBehaviorLeft = self.selection_behavior_left:jsonEncode()
		end

		if self.selection_behavior_right ~= nil then
			output.selectionBehaviorRight = self.selection_behavior_right:jsonEncode()
		end

		if self.on_refresh ~= nil then
			output.onRefresh = self.on_refresh:jsonEncode()
		end

		if self.sticky_items ~= nil then
			output.stickyItems = self.sticky_items:jsonEncode()
		end

		if self.gap_after_sticky_items ~= nil then
			output.gapAfterStickyItems = self.gap_after_sticky_items:jsonEncode()
		end

		if self.gap_between_sticky_items ~= nil then
			output.gapBetweenStickyItems = self.gap_between_sticky_items:jsonEncode()
		end

		if self.sticky_padding_top ~= nil then
			output.stickyPaddingTop = self.sticky_padding_top:jsonEncode()
		end

		if self.sticky_padding_bottom ~= nil then
			output.stickyPaddingBottom = self.sticky_padding_bottom:jsonEncode()
		end

		if self.sticky_content_tag ~= nil then
			output.stickyContentTag = self.sticky_content_tag:jsonEncode()
		end

		return output
	end

	function _VerticalFeedSchema_PropsImpl.jsonDecode(input: { [string]: any }): VerticalFeedSchema_Props
		local self = _VerticalFeedSchema_PropsImpl.new()

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		if input.feed_items ~= nil then
			self.feed_items = _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(
				input.feed_items
			)
		end

		if input.feedItems ~= nil then
			self.feed_items = _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(
				input.feedItems
			)
		end

		if input.threshold_from_end ~= nil then
			self.threshold_from_end =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.threshold_from_end)
		end

		if input.thresholdFromEnd ~= nil then
			self.threshold_from_end =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.thresholdFromEnd)
		end

		if input.on_scroll_to_end ~= nil then
			self.on_scroll_to_end =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_scroll_to_end)
		end

		if input.onScrollToEnd ~= nil then
			self.on_scroll_to_end =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onScrollToEnd)
		end

		if input.background_style ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.background_style)
		end

		if input.backgroundStyle ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.backgroundStyle)
		end

		if input.horizontal_alignment ~= nil then
			self.horizontal_alignment =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.HorizontalAlignmentProp.jsonDecode(
					input.horizontal_alignment
				)
		end

		if input.horizontalAlignment ~= nil then
			self.horizontal_alignment =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.HorizontalAlignmentProp.jsonDecode(
					input.horizontalAlignment
				)
		end

		if input.horizontal_padding ~= nil then
			self.horizontal_padding =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.horizontal_padding)
		end

		if input.horizontalPadding ~= nil then
			self.horizontal_padding =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.horizontalPadding)
		end

		if input.gap_between_feed_items ~= nil then
			self.gap_between_feed_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.gap_between_feed_items)
		end

		if input.gapBetweenFeedItems ~= nil then
			self.gap_between_feed_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.gapBetweenFeedItems)
		end

		if input.impressions_tag ~= nil then
			self.impressions_tag =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.impressions_tag)
		end

		if input.impressionsTag ~= nil then
			self.impressions_tag =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.impressionsTag)
		end

		if input.selection_behavior_left ~= nil then
			self.selection_behavior_left =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp.jsonDecode(
					input.selection_behavior_left
				)
		end

		if input.selectionBehaviorLeft ~= nil then
			self.selection_behavior_left =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp.jsonDecode(
					input.selectionBehaviorLeft
				)
		end

		if input.selection_behavior_right ~= nil then
			self.selection_behavior_right =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp.jsonDecode(
					input.selection_behavior_right
				)
		end

		if input.selectionBehaviorRight ~= nil then
			self.selection_behavior_right =
				_roblox_apppageplatform_shared_v1beta1_prop_types_engine.SelectionBehaviorProp.jsonDecode(
					input.selectionBehaviorRight
				)
		end

		if input.on_refresh ~= nil then
			self.on_refresh = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_refresh)
		end

		if input.onRefresh ~= nil then
			self.on_refresh = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onRefresh)
		end

		if input.sticky_items ~= nil then
			self.sticky_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(
					input.sticky_items
				)
		end

		if input.stickyItems ~= nil then
			self.sticky_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(
					input.stickyItems
				)
		end

		if input.gap_after_sticky_items ~= nil then
			self.gap_after_sticky_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.gap_after_sticky_items)
		end

		if input.gapAfterStickyItems ~= nil then
			self.gap_after_sticky_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.gapAfterStickyItems)
		end

		if input.gap_between_sticky_items ~= nil then
			self.gap_between_sticky_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.gap_between_sticky_items)
		end

		if input.gapBetweenStickyItems ~= nil then
			self.gap_between_sticky_items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.gapBetweenStickyItems)
		end

		if input.sticky_padding_top ~= nil then
			self.sticky_padding_top =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.sticky_padding_top)
		end

		if input.stickyPaddingTop ~= nil then
			self.sticky_padding_top =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.stickyPaddingTop)
		end

		if input.sticky_padding_bottom ~= nil then
			self.sticky_padding_bottom =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.sticky_padding_bottom)
		end

		if input.stickyPaddingBottom ~= nil then
			self.sticky_padding_bottom =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.stickyPaddingBottom)
		end

		if input.sticky_content_tag ~= nil then
			self.sticky_content_tag =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.sticky_content_tag)
		end

		if input.stickyContentTag ~= nil then
			self.sticky_content_tag =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.stickyContentTag)
		end

		return self
	end

	_VerticalFeedSchema_PropsImpl.descriptor = {
		name = "VerticalFeedSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.VerticalFeedSchema_Props = _VerticalFeedSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VerticalFeedSchema_Props)
end

return {
	VerticalFeedSchema = messages.VerticalFeedSchema,
	VerticalFeedSchema_Props = messages.VerticalFeedSchema_Props,
}
