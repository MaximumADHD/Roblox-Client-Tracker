--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	UniverseUserVoteData: _UniverseUserVoteDataMessage,
}
local messages: _Messages = {} :: _Messages

type _UniverseUserVoteDataImpl = {
	__index: _UniverseUserVoteDataImpl,
	new: (fields: _UniverseUserVoteDataPartialFields?) -> UniverseUserVoteData,
	encode: (self: UniverseUserVoteData) -> buffer,
	decode: (input: buffer) -> UniverseUserVoteData,
	jsonEncode: (self: UniverseUserVoteData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UniverseUserVoteData,
	descriptor: proto.Descriptor,
}

type _UniverseUserVoteDataFields = {
	user_vote: boolean?,
	can_vote: boolean,
	reason_for_not_voteable: string,
}

type _UniverseUserVoteDataPartialFields = {
	user_vote: boolean?,
	can_vote: boolean?,
	reason_for_not_voteable: string?,
}

export type UniverseUserVoteData = typeof(setmetatable(
	{} :: _UniverseUserVoteDataFields,
	{} :: _UniverseUserVoteDataImpl
))
type _UniverseUserVoteDataMessage = proto.Message<UniverseUserVoteData, _UniverseUserVoteDataPartialFields>

do
	local _UniverseUserVoteDataImpl = {}
	_UniverseUserVoteDataImpl.__index = _UniverseUserVoteDataImpl

	function _UniverseUserVoteDataImpl.new(data: _UniverseUserVoteDataPartialFields?): UniverseUserVoteData
		return setmetatable({
			user_vote = if data == nil or data.user_vote == nil then nil else data.user_vote,
			can_vote = if data == nil or data.can_vote == nil then false else data.can_vote,
			reason_for_not_voteable = if data == nil or data.reason_for_not_voteable == nil
				then ""
				else data.reason_for_not_voteable,
		}, _UniverseUserVoteDataImpl :: _UniverseUserVoteDataImpl)
	end

	function _UniverseUserVoteDataImpl.encode(self: UniverseUserVoteData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.user_vote ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.user_vote then 1 else 0)
		end

		if self.can_vote then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.can_vote then 1 else 0)
		end

		if self.reason_for_not_voteable ~= nil and self.reason_for_not_voteable ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.reason_for_not_voteable)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UniverseUserVoteDataImpl.decode(input: buffer): UniverseUserVoteData
		local self = _UniverseUserVoteDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.user_vote = value ~= 0
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.can_vote = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.reason_for_not_voteable = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UniverseUserVoteDataImpl.jsonEncode(self: UniverseUserVoteData): any
		local output = {}

		if self.user_vote ~= nil then
			output.userVote = self.user_vote
		end

		if self.can_vote then
			output.canVote = self.can_vote
		end

		if self.reason_for_not_voteable ~= nil and self.reason_for_not_voteable ~= "" then
			output.reasonForNotVoteable = self.reason_for_not_voteable
		end

		return output
	end

	function _UniverseUserVoteDataImpl.jsonDecode(input: { [string]: any }): UniverseUserVoteData
		local self = _UniverseUserVoteDataImpl.new()

		if input.user_vote ~= nil then
			self.user_vote = input.user_vote
		end

		if input.userVote ~= nil then
			self.user_vote = input.userVote
		end

		if input.can_vote ~= nil then
			self.can_vote = input.can_vote
		end

		if input.canVote ~= nil then
			self.can_vote = input.canVote
		end

		if input.reason_for_not_voteable ~= nil then
			self.reason_for_not_voteable = input.reason_for_not_voteable
		end

		if input.reasonForNotVoteable ~= nil then
			self.reason_for_not_voteable = input.reasonForNotVoteable
		end

		return self
	end

	_UniverseUserVoteDataImpl.descriptor = {
		name = "UniverseUserVoteData",
		fullName = "roblox.apppageplatform.shared.v1beta1.UniverseUserVoteData",
	}

	messages.UniverseUserVoteData = _UniverseUserVoteDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UniverseUserVoteData)
end

return {
	UniverseUserVoteData = messages.UniverseUserVoteData,
}
