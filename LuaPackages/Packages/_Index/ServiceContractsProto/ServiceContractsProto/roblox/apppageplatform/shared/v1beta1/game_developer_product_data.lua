--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GameDeveloperProductData: _GameDeveloperProductDataMessage,
}
local messages: _Messages = {} :: _Messages

type _GameDeveloperProductDataImpl = {
	__index: _GameDeveloperProductDataImpl,
	new: (fields: _GameDeveloperProductDataPartialFields?) -> GameDeveloperProductData,
	encode: (self: GameDeveloperProductData) -> buffer,
	decode: (input: buffer) -> GameDeveloperProductData,
	jsonEncode: (self: GameDeveloperProductData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GameDeveloperProductData,
	descriptor: proto.Descriptor,
}

type _GameDeveloperProductDataFields = {
	id: string,
	name: string,
	description: string,
	image_asset_id: number,
	price_in_robux: number,
	pending_receipt_count: number,
	status_tooltip: string,
}

type _GameDeveloperProductDataPartialFields = {
	id: string?,
	name: string?,
	description: string?,
	image_asset_id: number?,
	price_in_robux: number?,
	pending_receipt_count: number?,
	status_tooltip: string?,
}

export type GameDeveloperProductData = typeof(setmetatable(
	{} :: _GameDeveloperProductDataFields,
	{} :: _GameDeveloperProductDataImpl
))
type _GameDeveloperProductDataMessage = proto.Message<GameDeveloperProductData, _GameDeveloperProductDataPartialFields>

do
	local _GameDeveloperProductDataImpl = {}
	_GameDeveloperProductDataImpl.__index = _GameDeveloperProductDataImpl

	function _GameDeveloperProductDataImpl.new(data: _GameDeveloperProductDataPartialFields?): GameDeveloperProductData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			name = if data == nil or data.name == nil then "" else data.name,
			description = if data == nil or data.description == nil then "" else data.description,
			image_asset_id = if data == nil or data.image_asset_id == nil then 0 else data.image_asset_id,
			price_in_robux = if data == nil or data.price_in_robux == nil then 0 else data.price_in_robux,
			pending_receipt_count = if data == nil or data.pending_receipt_count == nil
				then 0
				else data.pending_receipt_count,
			status_tooltip = if data == nil or data.status_tooltip == nil then "" else data.status_tooltip,
		}, _GameDeveloperProductDataImpl :: _GameDeveloperProductDataImpl)
	end

	function _GameDeveloperProductDataImpl.encode(self: GameDeveloperProductData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.description ~= nil and self.description ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		if self.image_asset_id ~= nil and self.image_asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.image_asset_id)
		end

		if self.price_in_robux ~= nil and self.price_in_robux ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.price_in_robux)
		end

		if self.pending_receipt_count ~= nil and self.pending_receipt_count ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.pending_receipt_count)
		end

		if self.status_tooltip ~= nil and self.status_tooltip ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.status_tooltip)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GameDeveloperProductDataImpl.decode(input: buffer): GameDeveloperProductData
		local self = _GameDeveloperProductDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 4 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.image_asset_id = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.price_in_robux = value
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.pending_receipt_count = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.status_tooltip = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GameDeveloperProductDataImpl.jsonEncode(self: GameDeveloperProductData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.description ~= nil and self.description ~= "" then
			output.description = self.description
		end

		if self.image_asset_id ~= nil and self.image_asset_id ~= 0 then
			output.imageAssetId = self.image_asset_id
		end

		if self.price_in_robux ~= nil and self.price_in_robux ~= 0 then
			output.priceInRobux = self.price_in_robux
		end

		if self.pending_receipt_count ~= nil and self.pending_receipt_count ~= 0 then
			output.pendingReceiptCount = self.pending_receipt_count
		end

		if self.status_tooltip ~= nil and self.status_tooltip ~= "" then
			output.statusTooltip = self.status_tooltip
		end

		return output
	end

	function _GameDeveloperProductDataImpl.jsonDecode(input: { [string]: any }): GameDeveloperProductData
		local self = _GameDeveloperProductDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.description ~= nil then
			self.description = input.description
		end

		if input.image_asset_id ~= nil then
			self.image_asset_id = input.image_asset_id
		end

		if input.imageAssetId ~= nil then
			self.image_asset_id = input.imageAssetId
		end

		if input.price_in_robux ~= nil then
			self.price_in_robux = input.price_in_robux
		end

		if input.priceInRobux ~= nil then
			self.price_in_robux = input.priceInRobux
		end

		if input.pending_receipt_count ~= nil then
			self.pending_receipt_count = input.pending_receipt_count
		end

		if input.pendingReceiptCount ~= nil then
			self.pending_receipt_count = input.pendingReceiptCount
		end

		if input.status_tooltip ~= nil then
			self.status_tooltip = input.status_tooltip
		end

		if input.statusTooltip ~= nil then
			self.status_tooltip = input.statusTooltip
		end

		return self
	end

	_GameDeveloperProductDataImpl.descriptor = {
		name = "GameDeveloperProductData",
		fullName = "roblox.apppageplatform.shared.v1beta1.GameDeveloperProductData",
	}

	messages.GameDeveloperProductData = _GameDeveloperProductDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GameDeveloperProductData)
end

return {
	GameDeveloperProductData = messages.GameDeveloperProductData,
}
