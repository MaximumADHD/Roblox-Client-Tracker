--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	HeroUnitSchema: _HeroUnitSchemaMessage,
	HeroUnitSchema_Props: _HeroUnitSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _HeroUnitSchemaImpl = {
	__index: _HeroUnitSchemaImpl,
	new: (fields: _HeroUnitSchemaPartialFields?) -> HeroUnitSchema,
	encode: (self: HeroUnitSchema) -> buffer,
	decode: (input: buffer) -> HeroUnitSchema,
	jsonEncode: (self: HeroUnitSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HeroUnitSchema,
	descriptor: proto.Descriptor,
}

type _HeroUnitSchemaFields = {
	props: HeroUnitSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _HeroUnitSchemaPartialFields = {
	props: HeroUnitSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type HeroUnitSchema = typeof(setmetatable({} :: _HeroUnitSchemaFields, {} :: _HeroUnitSchemaImpl))
type _HeroUnitSchemaMessage = proto.Message<HeroUnitSchema, _HeroUnitSchemaPartialFields>

type _HeroUnitSchema_PropsImpl = {
	__index: _HeroUnitSchema_PropsImpl,
	new: (fields: _HeroUnitSchema_PropsPartialFields?) -> HeroUnitSchema_Props,
	encode: (self: HeroUnitSchema_Props) -> buffer,
	decode: (input: buffer) -> HeroUnitSchema_Props,
	jsonEncode: (self: HeroUnitSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HeroUnitSchema_Props,
	descriptor: proto.Descriptor,
}

type _HeroUnitSchema_PropsFields = {
	background_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	bottom_row_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	cta_button_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	gradient: _roblox_apppageplatform_shared_v1beta1_prop_types.GradientProp?,
	header_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	overlay_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	background_image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
	foreground_image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
	title_image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
}

type _HeroUnitSchema_PropsPartialFields = {
	background_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	bottom_row_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	cta_button_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	gradient: _roblox_apppageplatform_shared_v1beta1_prop_types.GradientProp?,
	header_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	overlay_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	background_image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
	foreground_image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
	title_image: _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp?,
}

export type HeroUnitSchema_Props = typeof(setmetatable(
	{} :: _HeroUnitSchema_PropsFields,
	{} :: _HeroUnitSchema_PropsImpl
))
type _HeroUnitSchema_PropsMessage = proto.Message<HeroUnitSchema_Props, _HeroUnitSchema_PropsPartialFields>

do
	local _HeroUnitSchemaImpl = {}
	_HeroUnitSchemaImpl.__index = _HeroUnitSchemaImpl

	function _HeroUnitSchemaImpl.new(data: _HeroUnitSchemaPartialFields?): HeroUnitSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _HeroUnitSchemaImpl :: _HeroUnitSchemaImpl)
	end

	function _HeroUnitSchemaImpl.encode(self: HeroUnitSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HeroUnitSchemaImpl.decode(input: buffer): HeroUnitSchema
		local self = _HeroUnitSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.HeroUnitSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HeroUnitSchemaImpl.jsonEncode(self: HeroUnitSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _HeroUnitSchemaImpl.jsonDecode(input: { [string]: any }): HeroUnitSchema
		local self = _HeroUnitSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.HeroUnitSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_HeroUnitSchemaImpl.descriptor = {
		name = "HeroUnitSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.HeroUnitSchema",
	}

	messages.HeroUnitSchema = _HeroUnitSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HeroUnitSchema)
end

do
	local _HeroUnitSchema_PropsImpl = {}
	_HeroUnitSchema_PropsImpl.__index = _HeroUnitSchema_PropsImpl

	function _HeroUnitSchema_PropsImpl.new(data: _HeroUnitSchema_PropsPartialFields?): HeroUnitSchema_Props
		return setmetatable({
			background_component = if data == nil or data.background_component == nil
				then nil
				else data.background_component,
			bottom_row_component = if data == nil or data.bottom_row_component == nil
				then nil
				else data.bottom_row_component,
			cta_button_component = if data == nil or data.cta_button_component == nil
				then nil
				else data.cta_button_component,
			gradient = if data == nil or data.gradient == nil then nil else data.gradient,
			header_component = if data == nil or data.header_component == nil then nil else data.header_component,
			on_activated = if data == nil or data.on_activated == nil then nil else data.on_activated,
			overlay_component = if data == nil or data.overlay_component == nil then nil else data.overlay_component,
			background_image = if data == nil or data.background_image == nil then nil else data.background_image,
			foreground_image = if data == nil or data.foreground_image == nil then nil else data.foreground_image,
			title_image = if data == nil or data.title_image == nil then nil else data.title_image,
		}, _HeroUnitSchema_PropsImpl :: _HeroUnitSchema_PropsImpl)
	end

	function _HeroUnitSchema_PropsImpl.encode(self: HeroUnitSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.background_component ~= nil then
			local encoded = self.background_component:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.bottom_row_component ~= nil then
			local encoded = self.bottom_row_component:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.cta_button_component ~= nil then
			local encoded = self.cta_button_component:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.gradient ~= nil then
			local encoded = self.gradient:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.header_component ~= nil then
			local encoded = self.header_component:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_activated ~= nil then
			local encoded = self.on_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.overlay_component ~= nil then
			local encoded = self.overlay_component:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_image ~= nil then
			local encoded = self.background_image:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.foreground_image ~= nil then
			local encoded = self.foreground_image:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_image ~= nil then
			local encoded = self.title_image:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HeroUnitSchema_PropsImpl.decode(input: buffer): HeroUnitSchema_Props
		local self = _HeroUnitSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.bottom_row_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.cta_button_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.gradient = _roblox_apppageplatform_shared_v1beta1_prop_types.GradientProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.header_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.overlay_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_image =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.foreground_image =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_image = _roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HeroUnitSchema_PropsImpl.jsonEncode(self: HeroUnitSchema_Props): any
		local output = {}

		if self.background_component ~= nil then
			output.backgroundComponent = self.background_component:jsonEncode()
		end

		if self.bottom_row_component ~= nil then
			output.bottomRowComponent = self.bottom_row_component:jsonEncode()
		end

		if self.cta_button_component ~= nil then
			output.ctaButtonComponent = self.cta_button_component:jsonEncode()
		end

		if self.gradient ~= nil then
			output.gradient = self.gradient:jsonEncode()
		end

		if self.header_component ~= nil then
			output.headerComponent = self.header_component:jsonEncode()
		end

		if self.on_activated ~= nil then
			output.onActivated = self.on_activated:jsonEncode()
		end

		if self.overlay_component ~= nil then
			output.overlayComponent = self.overlay_component:jsonEncode()
		end

		if self.background_image ~= nil then
			output.backgroundImage = self.background_image:jsonEncode()
		end

		if self.foreground_image ~= nil then
			output.foregroundImage = self.foreground_image:jsonEncode()
		end

		if self.title_image ~= nil then
			output.titleImage = self.title_image:jsonEncode()
		end

		return output
	end

	function _HeroUnitSchema_PropsImpl.jsonDecode(input: { [string]: any }): HeroUnitSchema_Props
		local self = _HeroUnitSchema_PropsImpl.new()

		if input.background_component ~= nil then
			self.background_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.background_component
				)
		end

		if input.backgroundComponent ~= nil then
			self.background_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.backgroundComponent
				)
		end

		if input.bottom_row_component ~= nil then
			self.bottom_row_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.bottom_row_component
				)
		end

		if input.bottomRowComponent ~= nil then
			self.bottom_row_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.bottomRowComponent
				)
		end

		if input.cta_button_component ~= nil then
			self.cta_button_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.cta_button_component
				)
		end

		if input.ctaButtonComponent ~= nil then
			self.cta_button_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.ctaButtonComponent
				)
		end

		if input.gradient ~= nil then
			self.gradient = _roblox_apppageplatform_shared_v1beta1_prop_types.GradientProp.jsonDecode(input.gradient)
		end

		if input.header_component ~= nil then
			self.header_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.header_component)
		end

		if input.headerComponent ~= nil then
			self.header_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.headerComponent)
		end

		if input.on_activated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_activated)
		end

		if input.onActivated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onActivated)
		end

		if input.overlay_component ~= nil then
			self.overlay_component = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
				input.overlay_component
			)
		end

		if input.overlayComponent ~= nil then
			self.overlay_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.overlayComponent)
		end

		if input.background_image ~= nil then
			self.background_image =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.background_image)
		end

		if input.backgroundImage ~= nil then
			self.background_image =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.backgroundImage)
		end

		if input.foreground_image ~= nil then
			self.foreground_image =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.foreground_image)
		end

		if input.foregroundImage ~= nil then
			self.foreground_image =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.foregroundImage)
		end

		if input.title_image ~= nil then
			self.title_image =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.title_image)
		end

		if input.titleImage ~= nil then
			self.title_image =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ImageStringProp.jsonDecode(input.titleImage)
		end

		return self
	end

	_HeroUnitSchema_PropsImpl.descriptor = {
		name = "HeroUnitSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.HeroUnitSchema_Props = _HeroUnitSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HeroUnitSchema_Props)
end

return {
	HeroUnitSchema = messages.HeroUnitSchema,
	HeroUnitSchema_Props = messages.HeroUnitSchema_Props,
}
