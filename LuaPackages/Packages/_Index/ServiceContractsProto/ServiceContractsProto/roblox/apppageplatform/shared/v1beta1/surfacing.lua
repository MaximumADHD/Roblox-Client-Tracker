--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	Surfacing: _SurfacingMessage,
	PeekView: _PeekViewMessage,
}
local messages: _Messages = {} :: _Messages

type _SurfacingImpl = {
	__index: _SurfacingImpl,
	new: (fields: _SurfacingPartialFields?) -> Surfacing,
	encode: (self: Surfacing) -> buffer,
	decode: (input: buffer) -> Surfacing,
	jsonEncode: (self: Surfacing) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Surfacing,
	descriptor: proto.Descriptor,
}

type _SurfacingFields = {
	kind: { type: "peek_view", value: PeekView }?,
}

type _SurfacingPartialFields = {
	kind: { type: "peek_view", value: PeekView }?,
}

export type Surfacing = typeof(setmetatable({} :: _SurfacingFields, {} :: _SurfacingImpl))
type _SurfacingMessage = proto.Message<Surfacing, _SurfacingPartialFields>

type _PeekViewImpl = {
	__index: _PeekViewImpl,
	new: (fields: _PeekViewPartialFields?) -> PeekView,
	encode: (self: PeekView) -> buffer,
	decode: (input: buffer) -> PeekView,
	jsonEncode: (self: PeekView) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PeekView,
	descriptor: proto.Descriptor,
}

type _PeekViewFields = {
	height_offset: number?,
}

type _PeekViewPartialFields = {
	height_offset: number?,
}

export type PeekView = typeof(setmetatable({} :: _PeekViewFields, {} :: _PeekViewImpl))
type _PeekViewMessage = proto.Message<PeekView, _PeekViewPartialFields>

do
	local _SurfacingImpl = {}
	_SurfacingImpl.__index = _SurfacingImpl

	function _SurfacingImpl.new(data: _SurfacingPartialFields?): Surfacing
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _SurfacingImpl :: _SurfacingImpl)
	end

	function _SurfacingImpl.encode(self: Surfacing): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "peek_view" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SurfacingImpl.decode(input: buffer): Surfacing
		local self = _SurfacingImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "peek_view", value = messages.PeekView.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SurfacingImpl.jsonEncode(self: Surfacing): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "peek_view" then
				output.peekView = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _SurfacingImpl.jsonDecode(input: { [string]: any }): Surfacing
		local self = _SurfacingImpl.new()

		if input.peek_view ~= nil then
			self.kind = { type = "peek_view", value = messages.PeekView.jsonDecode(input.peek_view) }
		end

		if input.peekView ~= nil then
			self.kind = { type = "peek_view", value = messages.PeekView.jsonDecode(input.peekView) }
		end

		return self
	end

	_SurfacingImpl.descriptor = {
		name = "Surfacing",
		fullName = "roblox.apppageplatform.shared.v1beta1.Surfacing",
	}

	messages.Surfacing = _SurfacingImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Surfacing)
end

do
	local _PeekViewImpl = {}
	_PeekViewImpl.__index = _PeekViewImpl

	function _PeekViewImpl.new(data: _PeekViewPartialFields?): PeekView
		return setmetatable({
			height_offset = if data == nil or data.height_offset == nil then nil else data.height_offset,
		}, _PeekViewImpl :: _PeekViewImpl)
	end

	function _PeekViewImpl.encode(self: PeekView): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.height_offset ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.height_offset)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PeekViewImpl.decode(input: buffer): PeekView
		local self = _PeekViewImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.height_offset = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PeekViewImpl.jsonEncode(self: PeekView): any
		local output = {}

		if self.height_offset ~= nil then
			output.heightOffset = self.height_offset
		end

		return output
	end

	function _PeekViewImpl.jsonDecode(input: { [string]: any }): PeekView
		local self = _PeekViewImpl.new()

		if input.height_offset ~= nil then
			self.height_offset = input.height_offset
		end

		if input.heightOffset ~= nil then
			self.height_offset = input.heightOffset
		end

		return self
	end

	_PeekViewImpl.descriptor = {
		name = "PeekView",
		fullName = "roblox.apppageplatform.shared.v1beta1.PeekView",
	}

	messages.PeekView = _PeekViewImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PeekView)
end

return {
	Surfacing = messages.Surfacing,
	PeekView = messages.PeekView,
}
