--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	UserRecommendationData: _UserRecommendationDataMessage,
}
local messages: _Messages = {} :: _Messages

type _UserRecommendationDataImpl = {
	__index: _UserRecommendationDataImpl,
	new: (fields: _UserRecommendationDataPartialFields?) -> UserRecommendationData,
	encode: (self: UserRecommendationData) -> buffer,
	decode: (input: buffer) -> UserRecommendationData,
	jsonEncode: (self: UserRecommendationData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UserRecommendationData,
	descriptor: proto.Descriptor,
}

type _UserRecommendationDataFields = {
	context_type: string?,
	mutual_friends_count: number?,
}

type _UserRecommendationDataPartialFields = {
	context_type: string?,
	mutual_friends_count: number?,
}

export type UserRecommendationData = typeof(setmetatable(
	{} :: _UserRecommendationDataFields,
	{} :: _UserRecommendationDataImpl
))
type _UserRecommendationDataMessage = proto.Message<UserRecommendationData, _UserRecommendationDataPartialFields>

do
	local _UserRecommendationDataImpl = {}
	_UserRecommendationDataImpl.__index = _UserRecommendationDataImpl

	function _UserRecommendationDataImpl.new(data: _UserRecommendationDataPartialFields?): UserRecommendationData
		return setmetatable({
			context_type = if data == nil or data.context_type == nil then nil else data.context_type,
			mutual_friends_count = if data == nil or data.mutual_friends_count == nil
				then nil
				else data.mutual_friends_count,
		}, _UserRecommendationDataImpl :: _UserRecommendationDataImpl)
	end

	function _UserRecommendationDataImpl.encode(self: UserRecommendationData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.context_type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.context_type)
		end

		if self.mutual_friends_count ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.mutual_friends_count)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UserRecommendationDataImpl.decode(input: buffer): UserRecommendationData
		local self = _UserRecommendationDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.mutual_friends_count = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.context_type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UserRecommendationDataImpl.jsonEncode(self: UserRecommendationData): any
		local output = {}

		if self.context_type ~= nil then
			output.contextType = self.context_type
		end

		if self.mutual_friends_count ~= nil then
			output.mutualFriendsCount = self.mutual_friends_count
		end

		return output
	end

	function _UserRecommendationDataImpl.jsonDecode(input: { [string]: any }): UserRecommendationData
		local self = _UserRecommendationDataImpl.new()

		if input.context_type ~= nil then
			self.context_type = input.context_type
		end

		if input.contextType ~= nil then
			self.context_type = input.contextType
		end

		if input.mutual_friends_count ~= nil then
			self.mutual_friends_count = input.mutual_friends_count
		end

		if input.mutualFriendsCount ~= nil then
			self.mutual_friends_count = input.mutualFriendsCount
		end

		return self
	end

	_UserRecommendationDataImpl.descriptor = {
		name = "UserRecommendationData",
		fullName = "roblox.apppageplatform.shared.v1beta1.UserRecommendationData",
	}

	messages.UserRecommendationData = _UserRecommendationDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UserRecommendationData)
end

return {
	UserRecommendationData = messages.UserRecommendationData,
}
