--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	SubscriptionCellSchema: _SubscriptionCellSchemaMessage,
	SubscriptionCellSchema_Props: _SubscriptionCellSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _SubscriptionCellSchemaImpl = {
	__index: _SubscriptionCellSchemaImpl,
	new: (fields: _SubscriptionCellSchemaPartialFields?) -> SubscriptionCellSchema,
	encode: (self: SubscriptionCellSchema) -> buffer,
	decode: (input: buffer) -> SubscriptionCellSchema,
	jsonEncode: (self: SubscriptionCellSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SubscriptionCellSchema,
	descriptor: proto.Descriptor,
}

type _SubscriptionCellSchemaFields = {
	props: SubscriptionCellSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _SubscriptionCellSchemaPartialFields = {
	props: SubscriptionCellSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type SubscriptionCellSchema = typeof(setmetatable(
	{} :: _SubscriptionCellSchemaFields,
	{} :: _SubscriptionCellSchemaImpl
))
type _SubscriptionCellSchemaMessage = proto.Message<SubscriptionCellSchema, _SubscriptionCellSchemaPartialFields>

type _SubscriptionCellSchema_PropsImpl = {
	__index: _SubscriptionCellSchema_PropsImpl,
	new: (fields: _SubscriptionCellSchema_PropsPartialFields?) -> SubscriptionCellSchema_Props,
	encode: (self: SubscriptionCellSchema_Props) -> buffer,
	decode: (input: buffer) -> SubscriptionCellSchema_Props,
	jsonEncode: (self: SubscriptionCellSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SubscriptionCellSchema_Props,
	descriptor: proto.Descriptor,
}

type _SubscriptionCellSchema_PropsFields = {
	content_row: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	button: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

type _SubscriptionCellSchema_PropsPartialFields = {
	content_row: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	button: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

export type SubscriptionCellSchema_Props = typeof(setmetatable(
	{} :: _SubscriptionCellSchema_PropsFields,
	{} :: _SubscriptionCellSchema_PropsImpl
))
type _SubscriptionCellSchema_PropsMessage = proto.Message<
	SubscriptionCellSchema_Props,
	_SubscriptionCellSchema_PropsPartialFields
>

do
	local _SubscriptionCellSchemaImpl = {}
	_SubscriptionCellSchemaImpl.__index = _SubscriptionCellSchemaImpl

	function _SubscriptionCellSchemaImpl.new(data: _SubscriptionCellSchemaPartialFields?): SubscriptionCellSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _SubscriptionCellSchemaImpl :: _SubscriptionCellSchemaImpl)
	end

	function _SubscriptionCellSchemaImpl.encode(self: SubscriptionCellSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SubscriptionCellSchemaImpl.decode(input: buffer): SubscriptionCellSchema
		local self = _SubscriptionCellSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.SubscriptionCellSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SubscriptionCellSchemaImpl.jsonEncode(self: SubscriptionCellSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _SubscriptionCellSchemaImpl.jsonDecode(input: { [string]: any }): SubscriptionCellSchema
		local self = _SubscriptionCellSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.SubscriptionCellSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_SubscriptionCellSchemaImpl.descriptor = {
		name = "SubscriptionCellSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.SubscriptionCellSchema",
	}

	messages.SubscriptionCellSchema = _SubscriptionCellSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SubscriptionCellSchema)
end

do
	local _SubscriptionCellSchema_PropsImpl = {}
	_SubscriptionCellSchema_PropsImpl.__index = _SubscriptionCellSchema_PropsImpl

	function _SubscriptionCellSchema_PropsImpl.new(
		data: _SubscriptionCellSchema_PropsPartialFields?
	): SubscriptionCellSchema_Props
		return setmetatable({
			content_row = if data == nil or data.content_row == nil then nil else data.content_row,
			button = if data == nil or data.button == nil then nil else data.button,
			on_activated = if data == nil or data.on_activated == nil then nil else data.on_activated,
			tag = if data == nil or data.tag == nil then nil else data.tag,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
		}, _SubscriptionCellSchema_PropsImpl :: _SubscriptionCellSchema_PropsImpl)
	end

	function _SubscriptionCellSchema_PropsImpl.encode(self: SubscriptionCellSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.content_row ~= nil then
			local encoded = self.content_row:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.button ~= nil then
			local encoded = self.button:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_activated ~= nil then
			local encoded = self.on_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.tag ~= nil then
			local encoded = self.tag:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SubscriptionCellSchema_PropsImpl.decode(input: buffer): SubscriptionCellSchema_Props
		local self = _SubscriptionCellSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content_row =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.button = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SubscriptionCellSchema_PropsImpl.jsonEncode(self: SubscriptionCellSchema_Props): any
		local output = {}

		if self.content_row ~= nil then
			output.contentRow = self.content_row:jsonEncode()
		end

		if self.button ~= nil then
			output.button = self.button:jsonEncode()
		end

		if self.on_activated ~= nil then
			output.onActivated = self.on_activated:jsonEncode()
		end

		if self.tag ~= nil then
			output.tag = self.tag:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		return output
	end

	function _SubscriptionCellSchema_PropsImpl.jsonDecode(input: { [string]: any }): SubscriptionCellSchema_Props
		local self = _SubscriptionCellSchema_PropsImpl.new()

		if input.content_row ~= nil then
			self.content_row =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.content_row)
		end

		if input.contentRow ~= nil then
			self.content_row =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.contentRow)
		end

		if input.button ~= nil then
			self.button = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.button)
		end

		if input.on_activated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_activated)
		end

		if input.onActivated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onActivated)
		end

		if input.tag ~= nil then
			self.tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.tag)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		return self
	end

	_SubscriptionCellSchema_PropsImpl.descriptor = {
		name = "SubscriptionCellSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.SubscriptionCellSchema_Props = _SubscriptionCellSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SubscriptionCellSchema_Props)
end

return {
	SubscriptionCellSchema = messages.SubscriptionCellSchema,
	SubscriptionCellSchema_Props = messages.SubscriptionCellSchema_Props,
}
