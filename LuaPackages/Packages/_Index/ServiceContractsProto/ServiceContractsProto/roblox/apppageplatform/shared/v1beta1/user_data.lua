--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	UserData: _UserDataMessage,
}
local messages: _Messages = {} :: _Messages

type _UserDataImpl = {
	__index: _UserDataImpl,
	new: (fields: _UserDataPartialFields?) -> UserData,
	encode: (self: UserData) -> buffer,
	decode: (input: buffer) -> UserData,
	jsonEncode: (self: UserData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UserData,
	descriptor: proto.Descriptor,
}

type _UserDataFields = {
	user_id: number,
	username: string,
	display_name: string,
	has_verified_badge: boolean,
	previous_usernames: { string },
}

type _UserDataPartialFields = {
	user_id: number?,
	username: string?,
	display_name: string?,
	has_verified_badge: boolean?,
	previous_usernames: { string }?,
}

export type UserData = typeof(setmetatable({} :: _UserDataFields, {} :: _UserDataImpl))
type _UserDataMessage = proto.Message<UserData, _UserDataPartialFields>

do
	local _UserDataImpl = {}
	_UserDataImpl.__index = _UserDataImpl

	function _UserDataImpl.new(data: _UserDataPartialFields?): UserData
		return setmetatable({
			user_id = if data == nil or data.user_id == nil then 0 else data.user_id,
			username = if data == nil or data.username == nil then "" else data.username,
			display_name = if data == nil or data.display_name == nil then "" else data.display_name,
			has_verified_badge = if data == nil or data.has_verified_badge == nil
				then false
				else data.has_verified_badge,
			previous_usernames = if data == nil or data.previous_usernames == nil then {} else data.previous_usernames,
		}, _UserDataImpl :: _UserDataImpl)
	end

	function _UserDataImpl.encode(self: UserData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.user_id ~= nil and self.user_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.user_id)
		end

		if self.username ~= nil and self.username ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.username)
		end

		if self.display_name ~= nil and self.display_name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.display_name)
		end

		if self.has_verified_badge then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.has_verified_badge then 1 else 0)
		end

		if self.previous_usernames ~= nil and #self.previous_usernames > 0 then
			for _, value in self.previous_usernames do
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UserDataImpl.decode(input: buffer): UserData
		local self = _UserDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.user_id = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.has_verified_badge = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.username = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.display_name = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.previous_usernames, buffer.tostring(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UserDataImpl.jsonEncode(self: UserData): any
		local output = {}

		if self.user_id ~= nil and self.user_id ~= 0 then
			output.userId = self.user_id
		end

		if self.username ~= nil and self.username ~= "" then
			output.username = self.username
		end

		if self.display_name ~= nil and self.display_name ~= "" then
			output.displayName = self.display_name
		end

		if self.has_verified_badge then
			output.hasVerifiedBadge = self.has_verified_badge
		end

		if self.previous_usernames ~= nil and #self.previous_usernames > 0 then
			local newOutput = {}
			for _, value in self.previous_usernames do
				table.insert(newOutput, value)
			end
			output.previousUsernames = newOutput
		end

		return output
	end

	function _UserDataImpl.jsonDecode(input: { [string]: any }): UserData
		local self = _UserDataImpl.new()

		if input.user_id ~= nil then
			self.user_id = input.user_id
		end

		if input.userId ~= nil then
			self.user_id = input.userId
		end

		if input.username ~= nil then
			self.username = input.username
		end

		if input.display_name ~= nil then
			self.display_name = input.display_name
		end

		if input.displayName ~= nil then
			self.display_name = input.displayName
		end

		if input.has_verified_badge ~= nil then
			self.has_verified_badge = input.has_verified_badge
		end

		if input.hasVerifiedBadge ~= nil then
			self.has_verified_badge = input.hasVerifiedBadge
		end

		if input.previous_usernames ~= nil then
			local newOutput: { string } = {}
			for _, value in input.previous_usernames do
				table.insert(newOutput, value)
			end

			self.previous_usernames = newOutput
		end

		if input.previousUsernames ~= nil then
			local newOutput: { string } = {}
			for _, value in input.previousUsernames do
				table.insert(newOutput, value)
			end

			self.previous_usernames = newOutput
		end

		return self
	end

	_UserDataImpl.descriptor = {
		name = "UserData",
		fullName = "roblox.apppageplatform.shared.v1beta1.UserData",
	}

	messages.UserData = _UserDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UserData)
end

return {
	UserData = messages.UserData,
}
