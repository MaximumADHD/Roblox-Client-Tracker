--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	PlayButtonWithContextualMessageSchema: _PlayButtonWithContextualMessageSchemaMessage,
	PlayButtonWithContextualMessageSchema_Props: _PlayButtonWithContextualMessageSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _PlayButtonWithContextualMessageSchemaImpl = {
	__index: _PlayButtonWithContextualMessageSchemaImpl,
	new: (fields: _PlayButtonWithContextualMessageSchemaPartialFields?) -> PlayButtonWithContextualMessageSchema,
	encode: (self: PlayButtonWithContextualMessageSchema) -> buffer,
	decode: (input: buffer) -> PlayButtonWithContextualMessageSchema,
	jsonEncode: (self: PlayButtonWithContextualMessageSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PlayButtonWithContextualMessageSchema,
	descriptor: proto.Descriptor,
}

type _PlayButtonWithContextualMessageSchemaFields = {
	props: PlayButtonWithContextualMessageSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _PlayButtonWithContextualMessageSchemaPartialFields = {
	props: PlayButtonWithContextualMessageSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type PlayButtonWithContextualMessageSchema = typeof(setmetatable(
	{} :: _PlayButtonWithContextualMessageSchemaFields,
	{} :: _PlayButtonWithContextualMessageSchemaImpl
))
type _PlayButtonWithContextualMessageSchemaMessage = proto.Message<
	PlayButtonWithContextualMessageSchema,
	_PlayButtonWithContextualMessageSchemaPartialFields
>

type _PlayButtonWithContextualMessageSchema_PropsImpl = {
	__index: _PlayButtonWithContextualMessageSchema_PropsImpl,
	new: (
		fields: _PlayButtonWithContextualMessageSchema_PropsPartialFields?
	) -> PlayButtonWithContextualMessageSchema_Props,
	encode: (self: PlayButtonWithContextualMessageSchema_Props) -> buffer,
	decode: (input: buffer) -> PlayButtonWithContextualMessageSchema_Props,
	jsonEncode: (self: PlayButtonWithContextualMessageSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PlayButtonWithContextualMessageSchema_Props,
	descriptor: proto.Descriptor,
}

type _PlayButtonWithContextualMessageSchema_PropsFields = {
	play_button: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	universe_id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	fetch_delay_in_seconds: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

type _PlayButtonWithContextualMessageSchema_PropsPartialFields = {
	play_button: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	universe_id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	fetch_delay_in_seconds: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

export type PlayButtonWithContextualMessageSchema_Props = typeof(setmetatable(
	{} :: _PlayButtonWithContextualMessageSchema_PropsFields,
	{} :: _PlayButtonWithContextualMessageSchema_PropsImpl
))
type _PlayButtonWithContextualMessageSchema_PropsMessage = proto.Message<
	PlayButtonWithContextualMessageSchema_Props,
	_PlayButtonWithContextualMessageSchema_PropsPartialFields
>

do
	local _PlayButtonWithContextualMessageSchemaImpl = {}
	_PlayButtonWithContextualMessageSchemaImpl.__index = _PlayButtonWithContextualMessageSchemaImpl

	function _PlayButtonWithContextualMessageSchemaImpl.new(
		data: _PlayButtonWithContextualMessageSchemaPartialFields?
	): PlayButtonWithContextualMessageSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _PlayButtonWithContextualMessageSchemaImpl :: _PlayButtonWithContextualMessageSchemaImpl)
	end

	function _PlayButtonWithContextualMessageSchemaImpl.encode(self: PlayButtonWithContextualMessageSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PlayButtonWithContextualMessageSchemaImpl.decode(input: buffer): PlayButtonWithContextualMessageSchema
		local self = _PlayButtonWithContextualMessageSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.PlayButtonWithContextualMessageSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PlayButtonWithContextualMessageSchemaImpl.jsonEncode(self: PlayButtonWithContextualMessageSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _PlayButtonWithContextualMessageSchemaImpl.jsonDecode(
		input: { [string]: any }
	): PlayButtonWithContextualMessageSchema
		local self = _PlayButtonWithContextualMessageSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.PlayButtonWithContextualMessageSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_PlayButtonWithContextualMessageSchemaImpl.descriptor = {
		name = "PlayButtonWithContextualMessageSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.PlayButtonWithContextualMessageSchema",
	}

	messages.PlayButtonWithContextualMessageSchema = _PlayButtonWithContextualMessageSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PlayButtonWithContextualMessageSchema)
end

do
	local _PlayButtonWithContextualMessageSchema_PropsImpl = {}
	_PlayButtonWithContextualMessageSchema_PropsImpl.__index = _PlayButtonWithContextualMessageSchema_PropsImpl

	function _PlayButtonWithContextualMessageSchema_PropsImpl.new(
		data: _PlayButtonWithContextualMessageSchema_PropsPartialFields?
	): PlayButtonWithContextualMessageSchema_Props
		return setmetatable({
			play_button = if data == nil or data.play_button == nil then nil else data.play_button,
			universe_id = if data == nil or data.universe_id == nil then nil else data.universe_id,
			fetch_delay_in_seconds = if data == nil or data.fetch_delay_in_seconds == nil
				then nil
				else data.fetch_delay_in_seconds,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
		}, _PlayButtonWithContextualMessageSchema_PropsImpl :: _PlayButtonWithContextualMessageSchema_PropsImpl)
	end

	function _PlayButtonWithContextualMessageSchema_PropsImpl.encode(
		self: PlayButtonWithContextualMessageSchema_Props
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.play_button ~= nil then
			local encoded = self.play_button:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.universe_id ~= nil then
			local encoded = self.universe_id:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.fetch_delay_in_seconds ~= nil then
			local encoded = self.fetch_delay_in_seconds:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PlayButtonWithContextualMessageSchema_PropsImpl.decode(
		input: buffer
	): PlayButtonWithContextualMessageSchema_Props
		local self = _PlayButtonWithContextualMessageSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.play_button =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.universe_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.fetch_delay_in_seconds =
						_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PlayButtonWithContextualMessageSchema_PropsImpl.jsonEncode(
		self: PlayButtonWithContextualMessageSchema_Props
	): any
		local output = {}

		if self.play_button ~= nil then
			output.playButton = self.play_button:jsonEncode()
		end

		if self.universe_id ~= nil then
			output.universeId = self.universe_id:jsonEncode()
		end

		if self.fetch_delay_in_seconds ~= nil then
			output.fetchDelayInSeconds = self.fetch_delay_in_seconds:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		return output
	end

	function _PlayButtonWithContextualMessageSchema_PropsImpl.jsonDecode(
		input: { [string]: any }
	): PlayButtonWithContextualMessageSchema_Props
		local self = _PlayButtonWithContextualMessageSchema_PropsImpl.new()

		if input.play_button ~= nil then
			self.play_button =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.play_button)
		end

		if input.playButton ~= nil then
			self.play_button =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.playButton)
		end

		if input.universe_id ~= nil then
			self.universe_id =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.universe_id)
		end

		if input.universeId ~= nil then
			self.universe_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.universeId)
		end

		if input.fetch_delay_in_seconds ~= nil then
			self.fetch_delay_in_seconds =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.fetch_delay_in_seconds)
		end

		if input.fetchDelayInSeconds ~= nil then
			self.fetch_delay_in_seconds =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.fetchDelayInSeconds)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		return self
	end

	_PlayButtonWithContextualMessageSchema_PropsImpl.descriptor = {
		name = "PlayButtonWithContextualMessageSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.PlayButtonWithContextualMessageSchema_Props = _PlayButtonWithContextualMessageSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PlayButtonWithContextualMessageSchema_Props)
end

return {
	PlayButtonWithContextualMessageSchema = messages.PlayButtonWithContextualMessageSchema,
	PlayButtonWithContextualMessageSchema_Props = messages.PlayButtonWithContextualMessageSchema_Props,
}
