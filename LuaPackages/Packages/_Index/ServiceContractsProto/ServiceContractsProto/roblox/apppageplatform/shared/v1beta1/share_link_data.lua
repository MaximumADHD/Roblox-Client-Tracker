--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GenericShareLinkData: _GenericShareLinkDataMessage,
	GenericShareLinkData_GenericShareLinkLiteralData: _GenericShareLinkData_GenericShareLinkLiteralDataMessage,
	ShareLinkFetchData: _ShareLinkFetchDataMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)

type _GenericShareLinkDataImpl = {
	__index: _GenericShareLinkDataImpl,
	new: (fields: _GenericShareLinkDataPartialFields?) -> GenericShareLinkData,
	encode: (self: GenericShareLinkData) -> buffer,
	decode: (input: buffer) -> GenericShareLinkData,
	jsonEncode: (self: GenericShareLinkData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GenericShareLinkData,
	descriptor: proto.Descriptor,
}

type _GenericShareLinkDataFields = {
	kind: { type: "literal", value: GenericShareLinkData_GenericShareLinkLiteralData }?,
}

type _GenericShareLinkDataPartialFields = {
	kind: { type: "literal", value: GenericShareLinkData_GenericShareLinkLiteralData }?,
}

export type GenericShareLinkData = typeof(setmetatable(
	{} :: _GenericShareLinkDataFields,
	{} :: _GenericShareLinkDataImpl
))
type _GenericShareLinkDataMessage = proto.Message<GenericShareLinkData, _GenericShareLinkDataPartialFields>

type _GenericShareLinkData_GenericShareLinkLiteralDataImpl = {
	__index: _GenericShareLinkData_GenericShareLinkLiteralDataImpl,
	new: (
		fields: _GenericShareLinkData_GenericShareLinkLiteralDataPartialFields?
	) -> GenericShareLinkData_GenericShareLinkLiteralData,
	encode: (self: GenericShareLinkData_GenericShareLinkLiteralData) -> buffer,
	decode: (input: buffer) -> GenericShareLinkData_GenericShareLinkLiteralData,
	jsonEncode: (self: GenericShareLinkData_GenericShareLinkLiteralData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GenericShareLinkData_GenericShareLinkLiteralData,
	descriptor: proto.Descriptor,
}

type _GenericShareLinkData_GenericShareLinkLiteralDataFields = {
	url: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	share_url_context: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _GenericShareLinkData_GenericShareLinkLiteralDataPartialFields = {
	url: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	share_url_context: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type GenericShareLinkData_GenericShareLinkLiteralData = typeof(setmetatable(
	{} :: _GenericShareLinkData_GenericShareLinkLiteralDataFields,
	{} :: _GenericShareLinkData_GenericShareLinkLiteralDataImpl
))
type _GenericShareLinkData_GenericShareLinkLiteralDataMessage = proto.Message<
	GenericShareLinkData_GenericShareLinkLiteralData,
	_GenericShareLinkData_GenericShareLinkLiteralDataPartialFields
>

type _ShareLinkFetchDataImpl = {
	__index: _ShareLinkFetchDataImpl,
	new: (fields: _ShareLinkFetchDataPartialFields?) -> ShareLinkFetchData,
	encode: (self: ShareLinkFetchData) -> buffer,
	decode: (input: buffer) -> ShareLinkFetchData,
	jsonEncode: (self: ShareLinkFetchData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ShareLinkFetchData,
	descriptor: proto.Descriptor,
}

type _ShareLinkFetchDataFields = {
	share_url_context: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	link_type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	link_data: _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp?,
}

type _ShareLinkFetchDataPartialFields = {
	share_url_context: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	link_type: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	link_data: _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp?,
}

export type ShareLinkFetchData = typeof(setmetatable({} :: _ShareLinkFetchDataFields, {} :: _ShareLinkFetchDataImpl))
type _ShareLinkFetchDataMessage = proto.Message<ShareLinkFetchData, _ShareLinkFetchDataPartialFields>

do
	local _GenericShareLinkDataImpl = {}
	_GenericShareLinkDataImpl.__index = _GenericShareLinkDataImpl

	function _GenericShareLinkDataImpl.new(data: _GenericShareLinkDataPartialFields?): GenericShareLinkData
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _GenericShareLinkDataImpl :: _GenericShareLinkDataImpl)
	end

	function _GenericShareLinkDataImpl.encode(self: GenericShareLinkData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GenericShareLinkDataImpl.decode(input: buffer): GenericShareLinkData
		local self = _GenericShareLinkDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "literal",
						value = messages.GenericShareLinkData_GenericShareLinkLiteralData.decode(value),
					}
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GenericShareLinkDataImpl.jsonEncode(self: GenericShareLinkData): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _GenericShareLinkDataImpl.jsonDecode(input: { [string]: any }): GenericShareLinkData
		local self = _GenericShareLinkDataImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = messages.GenericShareLinkData_GenericShareLinkLiteralData.jsonDecode(input.literal),
			}
		end

		return self
	end

	_GenericShareLinkDataImpl.descriptor = {
		name = "GenericShareLinkData",
		fullName = "roblox.apppageplatform.shared.v1beta1.GenericShareLinkData",
	}

	messages.GenericShareLinkData = _GenericShareLinkDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GenericShareLinkData)
end

do
	local _GenericShareLinkData_GenericShareLinkLiteralDataImpl = {}
	_GenericShareLinkData_GenericShareLinkLiteralDataImpl.__index =
		_GenericShareLinkData_GenericShareLinkLiteralDataImpl

	function _GenericShareLinkData_GenericShareLinkLiteralDataImpl.new(
		data: _GenericShareLinkData_GenericShareLinkLiteralDataPartialFields?
	): GenericShareLinkData_GenericShareLinkLiteralData
		return setmetatable(
			{
				url = if data == nil or data.url == nil then nil else data.url,
				share_url_context = if data == nil or data.share_url_context == nil
					then nil
					else data.share_url_context,
			},
			_GenericShareLinkData_GenericShareLinkLiteralDataImpl :: _GenericShareLinkData_GenericShareLinkLiteralDataImpl
		)
	end

	function _GenericShareLinkData_GenericShareLinkLiteralDataImpl.encode(
		self: GenericShareLinkData_GenericShareLinkLiteralData
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.url ~= nil then
			local encoded = self.url:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.share_url_context ~= nil then
			local encoded = self.share_url_context:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GenericShareLinkData_GenericShareLinkLiteralDataImpl.decode(
		input: buffer
	): GenericShareLinkData_GenericShareLinkLiteralData
		local self = _GenericShareLinkData_GenericShareLinkLiteralDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.url = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.share_url_context = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GenericShareLinkData_GenericShareLinkLiteralDataImpl.jsonEncode(
		self: GenericShareLinkData_GenericShareLinkLiteralData
	): any
		local output = {}

		if self.url ~= nil then
			output.url = self.url:jsonEncode()
		end

		if self.share_url_context ~= nil then
			output.shareUrlContext = self.share_url_context:jsonEncode()
		end

		return output
	end

	function _GenericShareLinkData_GenericShareLinkLiteralDataImpl.jsonDecode(
		input: { [string]: any }
	): GenericShareLinkData_GenericShareLinkLiteralData
		local self = _GenericShareLinkData_GenericShareLinkLiteralDataImpl.new()

		if input.url ~= nil then
			self.url = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.url)
		end

		if input.share_url_context ~= nil then
			self.share_url_context =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.share_url_context)
		end

		if input.shareUrlContext ~= nil then
			self.share_url_context =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.shareUrlContext)
		end

		return self
	end

	_GenericShareLinkData_GenericShareLinkLiteralDataImpl.descriptor = {
		name = "GenericShareLinkData_GenericShareLinkLiteralData",
		fullName = "roblox.apppageplatform.shared.v1beta1.GenericShareLinkLiteralData",
	}

	messages.GenericShareLinkData_GenericShareLinkLiteralData =
		_GenericShareLinkData_GenericShareLinkLiteralDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GenericShareLinkData_GenericShareLinkLiteralData)
end

do
	local _ShareLinkFetchDataImpl = {}
	_ShareLinkFetchDataImpl.__index = _ShareLinkFetchDataImpl

	function _ShareLinkFetchDataImpl.new(data: _ShareLinkFetchDataPartialFields?): ShareLinkFetchData
		return setmetatable({
			share_url_context = if data == nil or data.share_url_context == nil then nil else data.share_url_context,
			link_type = if data == nil or data.link_type == nil then nil else data.link_type,
			link_data = if data == nil or data.link_data == nil then nil else data.link_data,
		}, _ShareLinkFetchDataImpl :: _ShareLinkFetchDataImpl)
	end

	function _ShareLinkFetchDataImpl.encode(self: ShareLinkFetchData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.share_url_context ~= nil then
			local encoded = self.share_url_context:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.link_type ~= nil then
			local encoded = self.link_type:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.link_data ~= nil then
			local encoded = self.link_data:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ShareLinkFetchDataImpl.decode(input: buffer): ShareLinkFetchData
		local self = _ShareLinkFetchDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.share_url_context = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.link_type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.link_data = _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ShareLinkFetchDataImpl.jsonEncode(self: ShareLinkFetchData): any
		local output = {}

		if self.share_url_context ~= nil then
			output.shareUrlContext = self.share_url_context:jsonEncode()
		end

		if self.link_type ~= nil then
			output.linkType = self.link_type:jsonEncode()
		end

		if self.link_data ~= nil then
			output.linkData = self.link_data:jsonEncode()
		end

		return output
	end

	function _ShareLinkFetchDataImpl.jsonDecode(input: { [string]: any }): ShareLinkFetchData
		local self = _ShareLinkFetchDataImpl.new()

		if input.share_url_context ~= nil then
			self.share_url_context =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.share_url_context)
		end

		if input.shareUrlContext ~= nil then
			self.share_url_context =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.shareUrlContext)
		end

		if input.link_type ~= nil then
			self.link_type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.link_type)
		end

		if input.linkType ~= nil then
			self.link_type = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.linkType)
		end

		if input.link_data ~= nil then
			self.link_data = _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp.jsonDecode(input.link_data)
		end

		if input.linkData ~= nil then
			self.link_data = _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp.jsonDecode(input.linkData)
		end

		return self
	end

	_ShareLinkFetchDataImpl.descriptor = {
		name = "ShareLinkFetchData",
		fullName = "roblox.apppageplatform.shared.v1beta1.ShareLinkFetchData",
	}

	messages.ShareLinkFetchData = _ShareLinkFetchDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ShareLinkFetchData)
end

return {
	GenericShareLinkData = messages.GenericShareLinkData,
	GenericShareLinkData_GenericShareLinkLiteralData = messages.GenericShareLinkData_GenericShareLinkLiteralData,
	ShareLinkFetchData = messages.ShareLinkFetchData,
}
