--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	AgeRecommendationData: _AgeRecommendationDataMessage,
}
local messages: _Messages = {} :: _Messages

type _AgeRecommendationDataImpl = {
	__index: _AgeRecommendationDataImpl,
	new: (fields: _AgeRecommendationDataPartialFields?) -> AgeRecommendationData,
	encode: (self: AgeRecommendationData) -> buffer,
	decode: (input: buffer) -> AgeRecommendationData,
	jsonEncode: (self: AgeRecommendationData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AgeRecommendationData,
	descriptor: proto.Descriptor,
}

type _AgeRecommendationDataFields = {
	display_name: string,
	display_name_with_header_short: string,
	igrs_rating: string,
	igrs_rating_display_message: string,
}

type _AgeRecommendationDataPartialFields = {
	display_name: string?,
	display_name_with_header_short: string?,
	igrs_rating: string?,
	igrs_rating_display_message: string?,
}

export type AgeRecommendationData = typeof(setmetatable(
	{} :: _AgeRecommendationDataFields,
	{} :: _AgeRecommendationDataImpl
))
type _AgeRecommendationDataMessage = proto.Message<AgeRecommendationData, _AgeRecommendationDataPartialFields>

do
	local _AgeRecommendationDataImpl = {}
	_AgeRecommendationDataImpl.__index = _AgeRecommendationDataImpl

	function _AgeRecommendationDataImpl.new(data: _AgeRecommendationDataPartialFields?): AgeRecommendationData
		return setmetatable({
			display_name = if data == nil or data.display_name == nil then "" else data.display_name,
			display_name_with_header_short = if data == nil or data.display_name_with_header_short == nil
				then ""
				else data.display_name_with_header_short,
			igrs_rating = if data == nil or data.igrs_rating == nil then "" else data.igrs_rating,
			igrs_rating_display_message = if data == nil or data.igrs_rating_display_message == nil
				then ""
				else data.igrs_rating_display_message,
		}, _AgeRecommendationDataImpl :: _AgeRecommendationDataImpl)
	end

	function _AgeRecommendationDataImpl.encode(self: AgeRecommendationData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.display_name ~= nil and self.display_name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.display_name)
		end

		if self.display_name_with_header_short ~= nil and self.display_name_with_header_short ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.display_name_with_header_short)
		end

		if self.igrs_rating ~= nil and self.igrs_rating ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.igrs_rating)
		end

		if self.igrs_rating_display_message ~= nil and self.igrs_rating_display_message ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.igrs_rating_display_message)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AgeRecommendationDataImpl.decode(input: buffer): AgeRecommendationData
		local self = _AgeRecommendationDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.display_name = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.display_name_with_header_short = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.igrs_rating = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.igrs_rating_display_message = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AgeRecommendationDataImpl.jsonEncode(self: AgeRecommendationData): any
		local output = {}

		if self.display_name ~= nil and self.display_name ~= "" then
			output.displayName = self.display_name
		end

		if self.display_name_with_header_short ~= nil and self.display_name_with_header_short ~= "" then
			output.displayNameWithHeaderShort = self.display_name_with_header_short
		end

		if self.igrs_rating ~= nil and self.igrs_rating ~= "" then
			output.igrsRating = self.igrs_rating
		end

		if self.igrs_rating_display_message ~= nil and self.igrs_rating_display_message ~= "" then
			output.igrsRatingDisplayMessage = self.igrs_rating_display_message
		end

		return output
	end

	function _AgeRecommendationDataImpl.jsonDecode(input: { [string]: any }): AgeRecommendationData
		local self = _AgeRecommendationDataImpl.new()

		if input.display_name ~= nil then
			self.display_name = input.display_name
		end

		if input.displayName ~= nil then
			self.display_name = input.displayName
		end

		if input.display_name_with_header_short ~= nil then
			self.display_name_with_header_short = input.display_name_with_header_short
		end

		if input.displayNameWithHeaderShort ~= nil then
			self.display_name_with_header_short = input.displayNameWithHeaderShort
		end

		if input.igrs_rating ~= nil then
			self.igrs_rating = input.igrs_rating
		end

		if input.igrsRating ~= nil then
			self.igrs_rating = input.igrsRating
		end

		if input.igrs_rating_display_message ~= nil then
			self.igrs_rating_display_message = input.igrs_rating_display_message
		end

		if input.igrsRatingDisplayMessage ~= nil then
			self.igrs_rating_display_message = input.igrsRatingDisplayMessage
		end

		return self
	end

	_AgeRecommendationDataImpl.descriptor = {
		name = "AgeRecommendationData",
		fullName = "roblox.apppageplatform.shared.v1beta1.AgeRecommendationData",
	}

	messages.AgeRecommendationData = _AgeRecommendationDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AgeRecommendationData)
end

return {
	AgeRecommendationData = messages.AgeRecommendationData,
}
