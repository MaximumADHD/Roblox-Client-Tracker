--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	MenuItemProp: _MenuItemPropMessage,
	MenuItemProp_ConditionalOption: _MenuItemProp_ConditionalOptionMessage,
	MenuItemProp_ConditionalOptions: _MenuItemProp_ConditionalOptionsMessage,
	MenuItemProp_MenuItem: _MenuItemProp_MenuItemMessage,
	ArrayOfMenuItemProp: _ArrayOfMenuItemPropMessage,
	ArrayOfMenuItemProp_ConditionalOption: _ArrayOfMenuItemProp_ConditionalOptionMessage,
	ArrayOfMenuItemProp_ConditionalOptions: _ArrayOfMenuItemProp_ConditionalOptionsMessage,
	ArrayOfMenuItemProp_ArrayOfMenuItems: _ArrayOfMenuItemProp_ArrayOfMenuItemsMessage,
	OverflowMenuSchema: _OverflowMenuSchemaMessage,
	OverflowMenuSchema_Props: _OverflowMenuSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_prop_condition = require(script.Parent.prop_condition)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)

type _MenuItemPropImpl = {
	__index: _MenuItemPropImpl,
	new: (fields: _MenuItemPropPartialFields?) -> MenuItemProp,
	encode: (self: MenuItemProp) -> buffer,
	decode: (input: buffer) -> MenuItemProp,
	jsonEncode: (self: MenuItemProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MenuItemProp,
	descriptor: proto.Descriptor,
}

type _MenuItemPropFields = {
	kind: (
		{ type: "literal", value: MenuItemProp_MenuItem }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: MenuItemProp_ConditionalOptions }
	)?,
}

type _MenuItemPropPartialFields = {
	kind: (
		{ type: "literal", value: MenuItemProp_MenuItem }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: MenuItemProp_ConditionalOptions }
	)?,
}

export type MenuItemProp = typeof(setmetatable({} :: _MenuItemPropFields, {} :: _MenuItemPropImpl))
type _MenuItemPropMessage = proto.Message<MenuItemProp, _MenuItemPropPartialFields>

type _MenuItemProp_ConditionalOptionImpl = {
	__index: _MenuItemProp_ConditionalOptionImpl,
	new: (fields: _MenuItemProp_ConditionalOptionPartialFields?) -> MenuItemProp_ConditionalOption,
	encode: (self: MenuItemProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> MenuItemProp_ConditionalOption,
	jsonEncode: (self: MenuItemProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MenuItemProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _MenuItemProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: MenuItemProp_MenuItem } | { type: "binding_path", value: string })?,
}

type _MenuItemProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: MenuItemProp_MenuItem } | { type: "binding_path", value: string })?,
}

export type MenuItemProp_ConditionalOption = typeof(setmetatable(
	{} :: _MenuItemProp_ConditionalOptionFields,
	{} :: _MenuItemProp_ConditionalOptionImpl
))
type _MenuItemProp_ConditionalOptionMessage = proto.Message<
	MenuItemProp_ConditionalOption,
	_MenuItemProp_ConditionalOptionPartialFields
>

type _MenuItemProp_ConditionalOptionsImpl = {
	__index: _MenuItemProp_ConditionalOptionsImpl,
	new: (fields: _MenuItemProp_ConditionalOptionsPartialFields?) -> MenuItemProp_ConditionalOptions,
	encode: (self: MenuItemProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> MenuItemProp_ConditionalOptions,
	jsonEncode: (self: MenuItemProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MenuItemProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _MenuItemProp_ConditionalOptionsFields = {
	options: { MenuItemProp_ConditionalOption },
}

type _MenuItemProp_ConditionalOptionsPartialFields = {
	options: { MenuItemProp_ConditionalOption }?,
}

export type MenuItemProp_ConditionalOptions = typeof(setmetatable(
	{} :: _MenuItemProp_ConditionalOptionsFields,
	{} :: _MenuItemProp_ConditionalOptionsImpl
))
type _MenuItemProp_ConditionalOptionsMessage = proto.Message<
	MenuItemProp_ConditionalOptions,
	_MenuItemProp_ConditionalOptionsPartialFields
>

type _MenuItemProp_MenuItemImpl = {
	__index: _MenuItemProp_MenuItemImpl,
	new: (fields: _MenuItemProp_MenuItemPartialFields?) -> MenuItemProp_MenuItem,
	encode: (self: MenuItemProp_MenuItem) -> buffer,
	decode: (input: buffer) -> MenuItemProp_MenuItem,
	jsonEncode: (self: MenuItemProp_MenuItem) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MenuItemProp_MenuItem,
	descriptor: proto.Descriptor,
}

type _MenuItemProp_MenuItemFields = {
	id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	icon: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	is_disabled: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	is_checked: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	input_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _MenuItemProp_MenuItemPartialFields = {
	id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	icon: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	on_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	is_disabled: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	is_checked: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	input_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type MenuItemProp_MenuItem = typeof(setmetatable(
	{} :: _MenuItemProp_MenuItemFields,
	{} :: _MenuItemProp_MenuItemImpl
))
type _MenuItemProp_MenuItemMessage = proto.Message<MenuItemProp_MenuItem, _MenuItemProp_MenuItemPartialFields>

type _ArrayOfMenuItemPropImpl = {
	__index: _ArrayOfMenuItemPropImpl,
	new: (fields: _ArrayOfMenuItemPropPartialFields?) -> ArrayOfMenuItemProp,
	encode: (self: ArrayOfMenuItemProp) -> buffer,
	decode: (input: buffer) -> ArrayOfMenuItemProp,
	jsonEncode: (self: ArrayOfMenuItemProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfMenuItemProp,
	descriptor: proto.Descriptor,
}

type _ArrayOfMenuItemPropFields = {
	kind: (
		{ type: "literal", value: ArrayOfMenuItemProp_ArrayOfMenuItems }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfMenuItemProp_ConditionalOptions }
	)?,
}

type _ArrayOfMenuItemPropPartialFields = {
	kind: (
		{ type: "literal", value: ArrayOfMenuItemProp_ArrayOfMenuItems }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ArrayOfMenuItemProp_ConditionalOptions }
	)?,
}

export type ArrayOfMenuItemProp = typeof(setmetatable({} :: _ArrayOfMenuItemPropFields, {} :: _ArrayOfMenuItemPropImpl))
type _ArrayOfMenuItemPropMessage = proto.Message<ArrayOfMenuItemProp, _ArrayOfMenuItemPropPartialFields>

type _ArrayOfMenuItemProp_ConditionalOptionImpl = {
	__index: _ArrayOfMenuItemProp_ConditionalOptionImpl,
	new: (fields: _ArrayOfMenuItemProp_ConditionalOptionPartialFields?) -> ArrayOfMenuItemProp_ConditionalOption,
	encode: (self: ArrayOfMenuItemProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> ArrayOfMenuItemProp_ConditionalOption,
	jsonEncode: (self: ArrayOfMenuItemProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfMenuItemProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _ArrayOfMenuItemProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: ArrayOfMenuItemProp_ArrayOfMenuItems } | { type: "binding_path", value: string })?,
}

type _ArrayOfMenuItemProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: ArrayOfMenuItemProp_ArrayOfMenuItems } | { type: "binding_path", value: string })?,
}

export type ArrayOfMenuItemProp_ConditionalOption = typeof(setmetatable(
	{} :: _ArrayOfMenuItemProp_ConditionalOptionFields,
	{} :: _ArrayOfMenuItemProp_ConditionalOptionImpl
))
type _ArrayOfMenuItemProp_ConditionalOptionMessage = proto.Message<
	ArrayOfMenuItemProp_ConditionalOption,
	_ArrayOfMenuItemProp_ConditionalOptionPartialFields
>

type _ArrayOfMenuItemProp_ConditionalOptionsImpl = {
	__index: _ArrayOfMenuItemProp_ConditionalOptionsImpl,
	new: (fields: _ArrayOfMenuItemProp_ConditionalOptionsPartialFields?) -> ArrayOfMenuItemProp_ConditionalOptions,
	encode: (self: ArrayOfMenuItemProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> ArrayOfMenuItemProp_ConditionalOptions,
	jsonEncode: (self: ArrayOfMenuItemProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfMenuItemProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _ArrayOfMenuItemProp_ConditionalOptionsFields = {
	options: { ArrayOfMenuItemProp_ConditionalOption },
}

type _ArrayOfMenuItemProp_ConditionalOptionsPartialFields = {
	options: { ArrayOfMenuItemProp_ConditionalOption }?,
}

export type ArrayOfMenuItemProp_ConditionalOptions = typeof(setmetatable(
	{} :: _ArrayOfMenuItemProp_ConditionalOptionsFields,
	{} :: _ArrayOfMenuItemProp_ConditionalOptionsImpl
))
type _ArrayOfMenuItemProp_ConditionalOptionsMessage = proto.Message<
	ArrayOfMenuItemProp_ConditionalOptions,
	_ArrayOfMenuItemProp_ConditionalOptionsPartialFields
>

type _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl = {
	__index: _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl,
	new: (fields: _ArrayOfMenuItemProp_ArrayOfMenuItemsPartialFields?) -> ArrayOfMenuItemProp_ArrayOfMenuItems,
	encode: (self: ArrayOfMenuItemProp_ArrayOfMenuItems) -> buffer,
	decode: (input: buffer) -> ArrayOfMenuItemProp_ArrayOfMenuItems,
	jsonEncode: (self: ArrayOfMenuItemProp_ArrayOfMenuItems) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ArrayOfMenuItemProp_ArrayOfMenuItems,
	descriptor: proto.Descriptor,
}

type _ArrayOfMenuItemProp_ArrayOfMenuItemsFields = {
	array: { MenuItemProp },
}

type _ArrayOfMenuItemProp_ArrayOfMenuItemsPartialFields = {
	array: { MenuItemProp }?,
}

export type ArrayOfMenuItemProp_ArrayOfMenuItems = typeof(setmetatable(
	{} :: _ArrayOfMenuItemProp_ArrayOfMenuItemsFields,
	{} :: _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl
))
type _ArrayOfMenuItemProp_ArrayOfMenuItemsMessage = proto.Message<
	ArrayOfMenuItemProp_ArrayOfMenuItems,
	_ArrayOfMenuItemProp_ArrayOfMenuItemsPartialFields
>

type _OverflowMenuSchemaImpl = {
	__index: _OverflowMenuSchemaImpl,
	new: (fields: _OverflowMenuSchemaPartialFields?) -> OverflowMenuSchema,
	encode: (self: OverflowMenuSchema) -> buffer,
	decode: (input: buffer) -> OverflowMenuSchema,
	jsonEncode: (self: OverflowMenuSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> OverflowMenuSchema,
	descriptor: proto.Descriptor,
}

type _OverflowMenuSchemaFields = {
	props: OverflowMenuSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _OverflowMenuSchemaPartialFields = {
	props: OverflowMenuSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type OverflowMenuSchema = typeof(setmetatable({} :: _OverflowMenuSchemaFields, {} :: _OverflowMenuSchemaImpl))
type _OverflowMenuSchemaMessage = proto.Message<OverflowMenuSchema, _OverflowMenuSchemaPartialFields>

type _OverflowMenuSchema_PropsImpl = {
	__index: _OverflowMenuSchema_PropsImpl,
	new: (fields: _OverflowMenuSchema_PropsPartialFields?) -> OverflowMenuSchema_Props,
	encode: (self: OverflowMenuSchema_Props) -> buffer,
	decode: (input: buffer) -> OverflowMenuSchema_Props,
	jsonEncode: (self: OverflowMenuSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> OverflowMenuSchema_Props,
	descriptor: proto.Descriptor,
}

type _OverflowMenuSchema_PropsFields = {
	items: ArrayOfMenuItemProp?,
	button_variant: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	button_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	button_icon: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	popover_align: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	popover_side: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	menu_width: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	button_icon_background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
}

type _OverflowMenuSchema_PropsPartialFields = {
	items: ArrayOfMenuItemProp?,
	button_variant: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	button_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	button_icon: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	popover_align: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	popover_side: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	menu_width: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	button_icon_background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
}

export type OverflowMenuSchema_Props = typeof(setmetatable(
	{} :: _OverflowMenuSchema_PropsFields,
	{} :: _OverflowMenuSchema_PropsImpl
))
type _OverflowMenuSchema_PropsMessage = proto.Message<OverflowMenuSchema_Props, _OverflowMenuSchema_PropsPartialFields>

do
	local _MenuItemPropImpl = {}
	_MenuItemPropImpl.__index = _MenuItemPropImpl

	function _MenuItemPropImpl.new(data: _MenuItemPropPartialFields?): MenuItemProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _MenuItemPropImpl :: _MenuItemPropImpl)
	end

	function _MenuItemPropImpl.encode(self: MenuItemProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MenuItemPropImpl.decode(input: buffer): MenuItemProp
		local self = _MenuItemPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "literal", value = messages.MenuItemProp_MenuItem.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "conditional", value = messages.MenuItemProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MenuItemPropImpl.jsonEncode(self: MenuItemProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _MenuItemPropImpl.jsonDecode(input: { [string]: any }): MenuItemProp
		local self = _MenuItemPropImpl.new()

		if input.literal ~= nil then
			self.kind = { type = "literal", value = messages.MenuItemProp_MenuItem.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind =
				{ type = "conditional", value = messages.MenuItemProp_ConditionalOptions.jsonDecode(input.conditional) }
		end

		return self
	end

	_MenuItemPropImpl.descriptor = {
		name = "MenuItemProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.MenuItemProp",
	}

	messages.MenuItemProp = _MenuItemPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MenuItemProp)
end

do
	local _MenuItemProp_ConditionalOptionImpl = {}
	_MenuItemProp_ConditionalOptionImpl.__index = _MenuItemProp_ConditionalOptionImpl

	function _MenuItemProp_ConditionalOptionImpl.new(
		data: _MenuItemProp_ConditionalOptionPartialFields?
	): MenuItemProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _MenuItemProp_ConditionalOptionImpl :: _MenuItemProp_ConditionalOptionImpl)
	end

	function _MenuItemProp_ConditionalOptionImpl.encode(self: MenuItemProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MenuItemProp_ConditionalOptionImpl.decode(input: buffer): MenuItemProp_ConditionalOption
		local self = _MenuItemProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "literal", value = messages.MenuItemProp_MenuItem.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MenuItemProp_ConditionalOptionImpl.jsonEncode(self: MenuItemProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _MenuItemProp_ConditionalOptionImpl.jsonDecode(input: { [string]: any }): MenuItemProp_ConditionalOption
		local self = _MenuItemProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = { type = "literal", value = messages.MenuItemProp_MenuItem.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_MenuItemProp_ConditionalOptionImpl.descriptor = {
		name = "MenuItemProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.MenuItemProp_ConditionalOption = _MenuItemProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MenuItemProp_ConditionalOption)
end

do
	local _MenuItemProp_ConditionalOptionsImpl = {}
	_MenuItemProp_ConditionalOptionsImpl.__index = _MenuItemProp_ConditionalOptionsImpl

	function _MenuItemProp_ConditionalOptionsImpl.new(
		data: _MenuItemProp_ConditionalOptionsPartialFields?
	): MenuItemProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _MenuItemProp_ConditionalOptionsImpl :: _MenuItemProp_ConditionalOptionsImpl)
	end

	function _MenuItemProp_ConditionalOptionsImpl.encode(self: MenuItemProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MenuItemProp_ConditionalOptionsImpl.decode(input: buffer): MenuItemProp_ConditionalOptions
		local self = _MenuItemProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.MenuItemProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MenuItemProp_ConditionalOptionsImpl.jsonEncode(self: MenuItemProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _MenuItemProp_ConditionalOptionsImpl.jsonDecode(input: { [string]: any }): MenuItemProp_ConditionalOptions
		local self = _MenuItemProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { MenuItemProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.MenuItemProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_MenuItemProp_ConditionalOptionsImpl.descriptor = {
		name = "MenuItemProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.MenuItemProp_ConditionalOptions = _MenuItemProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MenuItemProp_ConditionalOptions)
end

do
	local _MenuItemProp_MenuItemImpl = {}
	_MenuItemProp_MenuItemImpl.__index = _MenuItemProp_MenuItemImpl

	function _MenuItemProp_MenuItemImpl.new(data: _MenuItemProp_MenuItemPartialFields?): MenuItemProp_MenuItem
		return setmetatable({
			id = if data == nil or data.id == nil then nil else data.id,
			text = if data == nil or data.text == nil then nil else data.text,
			icon = if data == nil or data.icon == nil then nil else data.icon,
			on_activated = if data == nil or data.on_activated == nil then nil else data.on_activated,
			is_disabled = if data == nil or data.is_disabled == nil then nil else data.is_disabled,
			is_checked = if data == nil or data.is_checked == nil then nil else data.is_checked,
			input_size = if data == nil or data.input_size == nil then nil else data.input_size,
		}, _MenuItemProp_MenuItemImpl :: _MenuItemProp_MenuItemImpl)
	end

	function _MenuItemProp_MenuItemImpl.encode(self: MenuItemProp_MenuItem): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil then
			local encoded = self.id:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.text ~= nil then
			local encoded = self.text:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon ~= nil then
			local encoded = self.icon:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_activated ~= nil then
			local encoded = self.on_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.is_disabled ~= nil then
			local encoded = self.is_disabled:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.is_checked ~= nil then
			local encoded = self.is_checked:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.input_size ~= nil then
			local encoded = self.input_size:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MenuItemProp_MenuItemImpl.decode(input: buffer): MenuItemProp_MenuItem
		local self = _MenuItemProp_MenuItemImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.is_disabled = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.is_checked = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.input_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MenuItemProp_MenuItemImpl.jsonEncode(self: MenuItemProp_MenuItem): any
		local output = {}

		if self.id ~= nil then
			output.id = self.id:jsonEncode()
		end

		if self.text ~= nil then
			output.text = self.text:jsonEncode()
		end

		if self.icon ~= nil then
			output.icon = self.icon:jsonEncode()
		end

		if self.on_activated ~= nil then
			output.onActivated = self.on_activated:jsonEncode()
		end

		if self.is_disabled ~= nil then
			output.isDisabled = self.is_disabled:jsonEncode()
		end

		if self.is_checked ~= nil then
			output.isChecked = self.is_checked:jsonEncode()
		end

		if self.input_size ~= nil then
			output.inputSize = self.input_size:jsonEncode()
		end

		return output
	end

	function _MenuItemProp_MenuItemImpl.jsonDecode(input: { [string]: any }): MenuItemProp_MenuItem
		local self = _MenuItemProp_MenuItemImpl.new()

		if input.id ~= nil then
			self.id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.id)
		end

		if input.text ~= nil then
			self.text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.text)
		end

		if input.icon ~= nil then
			self.icon = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.icon)
		end

		if input.on_activated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_activated)
		end

		if input.onActivated ~= nil then
			self.on_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onActivated)
		end

		if input.is_disabled ~= nil then
			self.is_disabled = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.is_disabled)
		end

		if input.isDisabled ~= nil then
			self.is_disabled = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.isDisabled)
		end

		if input.is_checked ~= nil then
			self.is_checked = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.is_checked)
		end

		if input.isChecked ~= nil then
			self.is_checked = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.isChecked)
		end

		if input.input_size ~= nil then
			self.input_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.input_size)
		end

		if input.inputSize ~= nil then
			self.input_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.inputSize)
		end

		return self
	end

	_MenuItemProp_MenuItemImpl.descriptor = {
		name = "MenuItemProp_MenuItem",
		fullName = "roblox.apppageplatform.shared.v1beta1.MenuItem",
	}

	messages.MenuItemProp_MenuItem = _MenuItemProp_MenuItemImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MenuItemProp_MenuItem)
end

do
	local _ArrayOfMenuItemPropImpl = {}
	_ArrayOfMenuItemPropImpl.__index = _ArrayOfMenuItemPropImpl

	function _ArrayOfMenuItemPropImpl.new(data: _ArrayOfMenuItemPropPartialFields?): ArrayOfMenuItemProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfMenuItemPropImpl :: _ArrayOfMenuItemPropImpl)
	end

	function _ArrayOfMenuItemPropImpl.encode(self: ArrayOfMenuItemProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfMenuItemPropImpl.decode(input: buffer): ArrayOfMenuItemProp
		local self = _ArrayOfMenuItemPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "literal", value = messages.ArrayOfMenuItemProp_ArrayOfMenuItems.decode(value) }
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.ArrayOfMenuItemProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfMenuItemPropImpl.jsonEncode(self: ArrayOfMenuItemProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _ArrayOfMenuItemPropImpl.jsonDecode(input: { [string]: any }): ArrayOfMenuItemProp
		local self = _ArrayOfMenuItemPropImpl.new()

		if input.literal ~= nil then
			self.kind =
				{ type = "literal", value = messages.ArrayOfMenuItemProp_ArrayOfMenuItems.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.ArrayOfMenuItemProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_ArrayOfMenuItemPropImpl.descriptor = {
		name = "ArrayOfMenuItemProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfMenuItemProp",
	}

	messages.ArrayOfMenuItemProp = _ArrayOfMenuItemPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfMenuItemProp)
end

do
	local _ArrayOfMenuItemProp_ConditionalOptionImpl = {}
	_ArrayOfMenuItemProp_ConditionalOptionImpl.__index = _ArrayOfMenuItemProp_ConditionalOptionImpl

	function _ArrayOfMenuItemProp_ConditionalOptionImpl.new(
		data: _ArrayOfMenuItemProp_ConditionalOptionPartialFields?
	): ArrayOfMenuItemProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ArrayOfMenuItemProp_ConditionalOptionImpl :: _ArrayOfMenuItemProp_ConditionalOptionImpl)
	end

	function _ArrayOfMenuItemProp_ConditionalOptionImpl.encode(self: ArrayOfMenuItemProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfMenuItemProp_ConditionalOptionImpl.decode(input: buffer): ArrayOfMenuItemProp_ConditionalOption
		local self = _ArrayOfMenuItemProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "literal", value = messages.ArrayOfMenuItemProp_ArrayOfMenuItems.decode(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfMenuItemProp_ConditionalOptionImpl.jsonEncode(self: ArrayOfMenuItemProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = self.kind.value:jsonEncode()
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _ArrayOfMenuItemProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfMenuItemProp_ConditionalOption
		local self = _ArrayOfMenuItemProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind =
				{ type = "literal", value = messages.ArrayOfMenuItemProp_ArrayOfMenuItems.jsonDecode(input.literal) }
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_ArrayOfMenuItemProp_ConditionalOptionImpl.descriptor = {
		name = "ArrayOfMenuItemProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.ArrayOfMenuItemProp_ConditionalOption = _ArrayOfMenuItemProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfMenuItemProp_ConditionalOption)
end

do
	local _ArrayOfMenuItemProp_ConditionalOptionsImpl = {}
	_ArrayOfMenuItemProp_ConditionalOptionsImpl.__index = _ArrayOfMenuItemProp_ConditionalOptionsImpl

	function _ArrayOfMenuItemProp_ConditionalOptionsImpl.new(
		data: _ArrayOfMenuItemProp_ConditionalOptionsPartialFields?
	): ArrayOfMenuItemProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _ArrayOfMenuItemProp_ConditionalOptionsImpl :: _ArrayOfMenuItemProp_ConditionalOptionsImpl)
	end

	function _ArrayOfMenuItemProp_ConditionalOptionsImpl.encode(self: ArrayOfMenuItemProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfMenuItemProp_ConditionalOptionsImpl.decode(input: buffer): ArrayOfMenuItemProp_ConditionalOptions
		local self = _ArrayOfMenuItemProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.ArrayOfMenuItemProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfMenuItemProp_ConditionalOptionsImpl.jsonEncode(self: ArrayOfMenuItemProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _ArrayOfMenuItemProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfMenuItemProp_ConditionalOptions
		local self = _ArrayOfMenuItemProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { ArrayOfMenuItemProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.ArrayOfMenuItemProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_ArrayOfMenuItemProp_ConditionalOptionsImpl.descriptor = {
		name = "ArrayOfMenuItemProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.ArrayOfMenuItemProp_ConditionalOptions = _ArrayOfMenuItemProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfMenuItemProp_ConditionalOptions)
end

do
	local _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl = {}
	_ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.__index = _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl

	function _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.new(
		data: _ArrayOfMenuItemProp_ArrayOfMenuItemsPartialFields?
	): ArrayOfMenuItemProp_ArrayOfMenuItems
		return setmetatable({
			array = if data == nil or data.array == nil then {} else data.array,
		}, _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl :: _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl)
	end

	function _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.encode(self: ArrayOfMenuItemProp_ArrayOfMenuItems): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.array ~= nil and #self.array > 0 then
			for _, value in self.array do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.decode(input: buffer): ArrayOfMenuItemProp_ArrayOfMenuItems
		local self = _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.array, messages.MenuItemProp.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.jsonEncode(self: ArrayOfMenuItemProp_ArrayOfMenuItems): any
		local output = {}

		if self.array ~= nil and #self.array > 0 then
			local newOutput = {}
			for _, value in self.array do
				table.insert(newOutput, value:jsonEncode())
			end
			output.array = newOutput
		end

		return output
	end

	function _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.jsonDecode(
		input: { [string]: any }
	): ArrayOfMenuItemProp_ArrayOfMenuItems
		local self = _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.new()

		if input.array ~= nil then
			local newOutput: { MenuItemProp } = {}
			for _, value in input.array do
				table.insert(newOutput, messages.MenuItemProp.jsonDecode(value))
			end

			self.array = newOutput
		end

		return self
	end

	_ArrayOfMenuItemProp_ArrayOfMenuItemsImpl.descriptor = {
		name = "ArrayOfMenuItemProp_ArrayOfMenuItems",
		fullName = "roblox.apppageplatform.shared.v1beta1.ArrayOfMenuItems",
	}

	messages.ArrayOfMenuItemProp_ArrayOfMenuItems = _ArrayOfMenuItemProp_ArrayOfMenuItemsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ArrayOfMenuItemProp_ArrayOfMenuItems)
end

do
	local _OverflowMenuSchemaImpl = {}
	_OverflowMenuSchemaImpl.__index = _OverflowMenuSchemaImpl

	function _OverflowMenuSchemaImpl.new(data: _OverflowMenuSchemaPartialFields?): OverflowMenuSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _OverflowMenuSchemaImpl :: _OverflowMenuSchemaImpl)
	end

	function _OverflowMenuSchemaImpl.encode(self: OverflowMenuSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _OverflowMenuSchemaImpl.decode(input: buffer): OverflowMenuSchema
		local self = _OverflowMenuSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.OverflowMenuSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _OverflowMenuSchemaImpl.jsonEncode(self: OverflowMenuSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _OverflowMenuSchemaImpl.jsonDecode(input: { [string]: any }): OverflowMenuSchema
		local self = _OverflowMenuSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.OverflowMenuSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_OverflowMenuSchemaImpl.descriptor = {
		name = "OverflowMenuSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.OverflowMenuSchema",
	}

	messages.OverflowMenuSchema = _OverflowMenuSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.OverflowMenuSchema)
end

do
	local _OverflowMenuSchema_PropsImpl = {}
	_OverflowMenuSchema_PropsImpl.__index = _OverflowMenuSchema_PropsImpl

	function _OverflowMenuSchema_PropsImpl.new(data: _OverflowMenuSchema_PropsPartialFields?): OverflowMenuSchema_Props
		return setmetatable({
			items = if data == nil or data.items == nil then nil else data.items,
			button_variant = if data == nil or data.button_variant == nil then nil else data.button_variant,
			button_size = if data == nil or data.button_size == nil then nil else data.button_size,
			button_icon = if data == nil or data.button_icon == nil then nil else data.button_icon,
			popover_align = if data == nil or data.popover_align == nil then nil else data.popover_align,
			popover_side = if data == nil or data.popover_side == nil then nil else data.popover_side,
			menu_width = if data == nil or data.menu_width == nil then nil else data.menu_width,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			position = if data == nil or data.position == nil then nil else data.position,
			z_index = if data == nil or data.z_index == nil then nil else data.z_index,
			button_icon_background_style = if data == nil or data.button_icon_background_style == nil
				then nil
				else data.button_icon_background_style,
		}, _OverflowMenuSchema_PropsImpl :: _OverflowMenuSchema_PropsImpl)
	end

	function _OverflowMenuSchema_PropsImpl.encode(self: OverflowMenuSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.items ~= nil then
			local encoded = self.items:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.button_variant ~= nil then
			local encoded = self.button_variant:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.button_size ~= nil then
			local encoded = self.button_size:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.button_icon ~= nil then
			local encoded = self.button_icon:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.popover_align ~= nil then
			local encoded = self.popover_align:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.popover_side ~= nil then
			local encoded = self.popover_side:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.menu_width ~= nil then
			local encoded = self.menu_width:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.z_index ~= nil then
			local encoded = self.z_index:encode()
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.button_icon_background_style ~= nil then
			local encoded = self.button_icon_background_style:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _OverflowMenuSchema_PropsImpl.decode(input: buffer): OverflowMenuSchema_Props
		local self = _OverflowMenuSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.items = messages.ArrayOfMenuItemProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.button_variant = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.button_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.button_icon = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.popover_align = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.popover_side = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.menu_width = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.button_icon_background_style =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _OverflowMenuSchema_PropsImpl.jsonEncode(self: OverflowMenuSchema_Props): any
		local output = {}

		if self.items ~= nil then
			output.items = self.items:jsonEncode()
		end

		if self.button_variant ~= nil then
			output.buttonVariant = self.button_variant:jsonEncode()
		end

		if self.button_size ~= nil then
			output.buttonSize = self.button_size:jsonEncode()
		end

		if self.button_icon ~= nil then
			output.buttonIcon = self.button_icon:jsonEncode()
		end

		if self.popover_align ~= nil then
			output.popoverAlign = self.popover_align:jsonEncode()
		end

		if self.popover_side ~= nil then
			output.popoverSide = self.popover_side:jsonEncode()
		end

		if self.menu_width ~= nil then
			output.menuWidth = self.menu_width:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.z_index ~= nil then
			output.zIndex = self.z_index:jsonEncode()
		end

		if self.button_icon_background_style ~= nil then
			output.buttonIconBackgroundStyle = self.button_icon_background_style:jsonEncode()
		end

		return output
	end

	function _OverflowMenuSchema_PropsImpl.jsonDecode(input: { [string]: any }): OverflowMenuSchema_Props
		local self = _OverflowMenuSchema_PropsImpl.new()

		if input.items ~= nil then
			self.items = messages.ArrayOfMenuItemProp.jsonDecode(input.items)
		end

		if input.button_variant ~= nil then
			self.button_variant =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.button_variant)
		end

		if input.buttonVariant ~= nil then
			self.button_variant =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.buttonVariant)
		end

		if input.button_size ~= nil then
			self.button_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.button_size)
		end

		if input.buttonSize ~= nil then
			self.button_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.buttonSize)
		end

		if input.button_icon ~= nil then
			self.button_icon =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.button_icon)
		end

		if input.buttonIcon ~= nil then
			self.button_icon = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.buttonIcon)
		end

		if input.popover_align ~= nil then
			self.popover_align =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.popover_align)
		end

		if input.popoverAlign ~= nil then
			self.popover_align =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.popoverAlign)
		end

		if input.popover_side ~= nil then
			self.popover_side =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.popover_side)
		end

		if input.popoverSide ~= nil then
			self.popover_side =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.popoverSide)
		end

		if input.menu_width ~= nil then
			self.menu_width = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.menu_width)
		end

		if input.menuWidth ~= nil then
			self.menu_width = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.menuWidth)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.z_index ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.z_index)
		end

		if input.zIndex ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.zIndex)
		end

		if input.button_icon_background_style ~= nil then
			self.button_icon_background_style = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(
				input.button_icon_background_style
			)
		end

		if input.buttonIconBackgroundStyle ~= nil then
			self.button_icon_background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.buttonIconBackgroundStyle)
		end

		return self
	end

	_OverflowMenuSchema_PropsImpl.descriptor = {
		name = "OverflowMenuSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.OverflowMenuSchema_Props = _OverflowMenuSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.OverflowMenuSchema_Props)
end

return {
	MenuItemProp = messages.MenuItemProp,
	MenuItemProp_ConditionalOption = messages.MenuItemProp_ConditionalOption,
	MenuItemProp_ConditionalOptions = messages.MenuItemProp_ConditionalOptions,
	MenuItemProp_MenuItem = messages.MenuItemProp_MenuItem,
	ArrayOfMenuItemProp = messages.ArrayOfMenuItemProp,
	ArrayOfMenuItemProp_ConditionalOption = messages.ArrayOfMenuItemProp_ConditionalOption,
	ArrayOfMenuItemProp_ConditionalOptions = messages.ArrayOfMenuItemProp_ConditionalOptions,
	ArrayOfMenuItemProp_ArrayOfMenuItems = messages.ArrayOfMenuItemProp_ArrayOfMenuItems,
	OverflowMenuSchema = messages.OverflowMenuSchema,
	OverflowMenuSchema_Props = messages.OverflowMenuSchema_Props,
}
