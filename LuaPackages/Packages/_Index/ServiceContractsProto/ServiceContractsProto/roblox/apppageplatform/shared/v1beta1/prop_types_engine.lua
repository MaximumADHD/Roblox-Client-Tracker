--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	AutomaticSizeProp: _AutomaticSizePropMessage,
	AutomaticSizeProp_ConditionalOption: _AutomaticSizeProp_ConditionalOptionMessage,
	AutomaticSizeProp_ConditionalOptions: _AutomaticSizeProp_ConditionalOptionsMessage,
	TextTruncateProp: _TextTruncatePropMessage,
	TextTruncateProp_ConditionalOption: _TextTruncateProp_ConditionalOptionMessage,
	TextTruncateProp_ConditionalOptions: _TextTruncateProp_ConditionalOptionsMessage,
	TextXAlignmentProp: _TextXAlignmentPropMessage,
	TextXAlignmentProp_ConditionalOption: _TextXAlignmentProp_ConditionalOptionMessage,
	TextXAlignmentProp_ConditionalOptions: _TextXAlignmentProp_ConditionalOptionsMessage,
	TextYAlignmentProp: _TextYAlignmentPropMessage,
	TextYAlignmentProp_ConditionalOption: _TextYAlignmentProp_ConditionalOptionMessage,
	TextYAlignmentProp_ConditionalOptions: _TextYAlignmentProp_ConditionalOptionsMessage,
	UIFlexModeProp: _UIFlexModePropMessage,
	UIFlexModeProp_ConditionalOption: _UIFlexModeProp_ConditionalOptionMessage,
	UIFlexModeProp_ConditionalOptions: _UIFlexModeProp_ConditionalOptionsMessage,
	ScaleTypeProp: _ScaleTypePropMessage,
	ScaleTypeProp_ConditionalOption: _ScaleTypeProp_ConditionalOptionMessage,
	ScaleTypeProp_ConditionalOptions: _ScaleTypeProp_ConditionalOptionsMessage,
	ResamplerModeProp: _ResamplerModePropMessage,
	ResamplerModeProp_ConditionalOption: _ResamplerModeProp_ConditionalOptionMessage,
	ResamplerModeProp_ConditionalOptions: _ResamplerModeProp_ConditionalOptionsMessage,
	SizeConstraintProp: _SizeConstraintPropMessage,
	SizeConstraintProp_ConditionalOption: _SizeConstraintProp_ConditionalOptionMessage,
	SizeConstraintProp_ConditionalOptions: _SizeConstraintProp_ConditionalOptionsMessage,
	HorizontalAlignmentProp: _HorizontalAlignmentPropMessage,
	HorizontalAlignmentProp_ConditionalOption: _HorizontalAlignmentProp_ConditionalOptionMessage,
	HorizontalAlignmentProp_ConditionalOptions: _HorizontalAlignmentProp_ConditionalOptionsMessage,
	SelectionBehaviorProp: _SelectionBehaviorPropMessage,
	SelectionBehaviorProp_ConditionalOption: _SelectionBehaviorProp_ConditionalOptionMessage,
	SelectionBehaviorProp_ConditionalOptions: _SelectionBehaviorProp_ConditionalOptionsMessage,
	KeyCodeProp: _KeyCodePropMessage,
	KeyCodeProp_ConditionalOption: _KeyCodeProp_ConditionalOptionMessage,
	KeyCodeProp_ConditionalOptions: _KeyCodeProp_ConditionalOptionsMessage,
	VerticalAlignmentProp: _VerticalAlignmentPropMessage,
	VerticalAlignmentProp_ConditionalOption: _VerticalAlignmentProp_ConditionalOptionMessage,
	VerticalAlignmentProp_ConditionalOptions: _VerticalAlignmentProp_ConditionalOptionsMessage,
	AutomaticSize: _AutomaticSizeMessage,
	TextTruncate: _TextTruncateMessage,
	TextXAlignment: _TextXAlignmentMessage,
	TextYAlignment: _TextYAlignmentMessage,
	UIFlexMode: _UIFlexModeMessage,
	ScaleType: _ScaleTypeMessage,
	ResamplerMode: _ResamplerModeMessage,
	SizeConstraint: _SizeConstraintMessage,
	HorizontalAlignment: _HorizontalAlignmentMessage,
	VerticalAlignment: _VerticalAlignmentMessage,
	SelectionBehavior: _SelectionBehaviorMessage,
	KeyCode: _KeyCodeMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_condition = require(script.Parent.prop_condition)

type _AutomaticSizePropImpl = {
	__index: _AutomaticSizePropImpl,
	new: (fields: _AutomaticSizePropPartialFields?) -> AutomaticSizeProp,
	encode: (self: AutomaticSizeProp) -> buffer,
	decode: (input: buffer) -> AutomaticSizeProp,
	jsonEncode: (self: AutomaticSizeProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AutomaticSizeProp,
	descriptor: proto.Descriptor,
}

type _AutomaticSizePropFields = {
	kind: (
		{ type: "literal", value: AutomaticSize }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: AutomaticSizeProp_ConditionalOptions }
	)?,
}

type _AutomaticSizePropPartialFields = {
	kind: (
		{ type: "literal", value: AutomaticSize }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: AutomaticSizeProp_ConditionalOptions }
	)?,
}

export type AutomaticSizeProp = typeof(setmetatable({} :: _AutomaticSizePropFields, {} :: _AutomaticSizePropImpl))
type _AutomaticSizePropMessage = proto.Message<AutomaticSizeProp, _AutomaticSizePropPartialFields>

type _AutomaticSizeProp_ConditionalOptionImpl = {
	__index: _AutomaticSizeProp_ConditionalOptionImpl,
	new: (fields: _AutomaticSizeProp_ConditionalOptionPartialFields?) -> AutomaticSizeProp_ConditionalOption,
	encode: (self: AutomaticSizeProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> AutomaticSizeProp_ConditionalOption,
	jsonEncode: (self: AutomaticSizeProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AutomaticSizeProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _AutomaticSizeProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: AutomaticSize } | { type: "binding_path", value: string })?,
}

type _AutomaticSizeProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: AutomaticSize } | { type: "binding_path", value: string })?,
}

export type AutomaticSizeProp_ConditionalOption = typeof(setmetatable(
	{} :: _AutomaticSizeProp_ConditionalOptionFields,
	{} :: _AutomaticSizeProp_ConditionalOptionImpl
))
type _AutomaticSizeProp_ConditionalOptionMessage = proto.Message<
	AutomaticSizeProp_ConditionalOption,
	_AutomaticSizeProp_ConditionalOptionPartialFields
>

type _AutomaticSizeProp_ConditionalOptionsImpl = {
	__index: _AutomaticSizeProp_ConditionalOptionsImpl,
	new: (fields: _AutomaticSizeProp_ConditionalOptionsPartialFields?) -> AutomaticSizeProp_ConditionalOptions,
	encode: (self: AutomaticSizeProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> AutomaticSizeProp_ConditionalOptions,
	jsonEncode: (self: AutomaticSizeProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AutomaticSizeProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _AutomaticSizeProp_ConditionalOptionsFields = {
	options: { AutomaticSizeProp_ConditionalOption },
}

type _AutomaticSizeProp_ConditionalOptionsPartialFields = {
	options: { AutomaticSizeProp_ConditionalOption }?,
}

export type AutomaticSizeProp_ConditionalOptions = typeof(setmetatable(
	{} :: _AutomaticSizeProp_ConditionalOptionsFields,
	{} :: _AutomaticSizeProp_ConditionalOptionsImpl
))
type _AutomaticSizeProp_ConditionalOptionsMessage = proto.Message<
	AutomaticSizeProp_ConditionalOptions,
	_AutomaticSizeProp_ConditionalOptionsPartialFields
>

type _TextTruncatePropImpl = {
	__index: _TextTruncatePropImpl,
	new: (fields: _TextTruncatePropPartialFields?) -> TextTruncateProp,
	encode: (self: TextTruncateProp) -> buffer,
	decode: (input: buffer) -> TextTruncateProp,
	jsonEncode: (self: TextTruncateProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextTruncateProp,
	descriptor: proto.Descriptor,
}

type _TextTruncatePropFields = {
	kind: (
		{ type: "literal", value: TextTruncate }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: TextTruncateProp_ConditionalOptions }
	)?,
}

type _TextTruncatePropPartialFields = {
	kind: (
		{ type: "literal", value: TextTruncate }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: TextTruncateProp_ConditionalOptions }
	)?,
}

export type TextTruncateProp = typeof(setmetatable({} :: _TextTruncatePropFields, {} :: _TextTruncatePropImpl))
type _TextTruncatePropMessage = proto.Message<TextTruncateProp, _TextTruncatePropPartialFields>

type _TextTruncateProp_ConditionalOptionImpl = {
	__index: _TextTruncateProp_ConditionalOptionImpl,
	new: (fields: _TextTruncateProp_ConditionalOptionPartialFields?) -> TextTruncateProp_ConditionalOption,
	encode: (self: TextTruncateProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> TextTruncateProp_ConditionalOption,
	jsonEncode: (self: TextTruncateProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextTruncateProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _TextTruncateProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: TextTruncate } | { type: "binding_path", value: string })?,
}

type _TextTruncateProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: TextTruncate } | { type: "binding_path", value: string })?,
}

export type TextTruncateProp_ConditionalOption = typeof(setmetatable(
	{} :: _TextTruncateProp_ConditionalOptionFields,
	{} :: _TextTruncateProp_ConditionalOptionImpl
))
type _TextTruncateProp_ConditionalOptionMessage = proto.Message<
	TextTruncateProp_ConditionalOption,
	_TextTruncateProp_ConditionalOptionPartialFields
>

type _TextTruncateProp_ConditionalOptionsImpl = {
	__index: _TextTruncateProp_ConditionalOptionsImpl,
	new: (fields: _TextTruncateProp_ConditionalOptionsPartialFields?) -> TextTruncateProp_ConditionalOptions,
	encode: (self: TextTruncateProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> TextTruncateProp_ConditionalOptions,
	jsonEncode: (self: TextTruncateProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextTruncateProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _TextTruncateProp_ConditionalOptionsFields = {
	options: { TextTruncateProp_ConditionalOption },
}

type _TextTruncateProp_ConditionalOptionsPartialFields = {
	options: { TextTruncateProp_ConditionalOption }?,
}

export type TextTruncateProp_ConditionalOptions = typeof(setmetatable(
	{} :: _TextTruncateProp_ConditionalOptionsFields,
	{} :: _TextTruncateProp_ConditionalOptionsImpl
))
type _TextTruncateProp_ConditionalOptionsMessage = proto.Message<
	TextTruncateProp_ConditionalOptions,
	_TextTruncateProp_ConditionalOptionsPartialFields
>

type _TextXAlignmentPropImpl = {
	__index: _TextXAlignmentPropImpl,
	new: (fields: _TextXAlignmentPropPartialFields?) -> TextXAlignmentProp,
	encode: (self: TextXAlignmentProp) -> buffer,
	decode: (input: buffer) -> TextXAlignmentProp,
	jsonEncode: (self: TextXAlignmentProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextXAlignmentProp,
	descriptor: proto.Descriptor,
}

type _TextXAlignmentPropFields = {
	kind: (
		{ type: "literal", value: TextXAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: TextXAlignmentProp_ConditionalOptions }
	)?,
}

type _TextXAlignmentPropPartialFields = {
	kind: (
		{ type: "literal", value: TextXAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: TextXAlignmentProp_ConditionalOptions }
	)?,
}

export type TextXAlignmentProp = typeof(setmetatable({} :: _TextXAlignmentPropFields, {} :: _TextXAlignmentPropImpl))
type _TextXAlignmentPropMessage = proto.Message<TextXAlignmentProp, _TextXAlignmentPropPartialFields>

type _TextXAlignmentProp_ConditionalOptionImpl = {
	__index: _TextXAlignmentProp_ConditionalOptionImpl,
	new: (fields: _TextXAlignmentProp_ConditionalOptionPartialFields?) -> TextXAlignmentProp_ConditionalOption,
	encode: (self: TextXAlignmentProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> TextXAlignmentProp_ConditionalOption,
	jsonEncode: (self: TextXAlignmentProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextXAlignmentProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _TextXAlignmentProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: TextXAlignment } | { type: "binding_path", value: string })?,
}

type _TextXAlignmentProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: TextXAlignment } | { type: "binding_path", value: string })?,
}

export type TextXAlignmentProp_ConditionalOption = typeof(setmetatable(
	{} :: _TextXAlignmentProp_ConditionalOptionFields,
	{} :: _TextXAlignmentProp_ConditionalOptionImpl
))
type _TextXAlignmentProp_ConditionalOptionMessage = proto.Message<
	TextXAlignmentProp_ConditionalOption,
	_TextXAlignmentProp_ConditionalOptionPartialFields
>

type _TextXAlignmentProp_ConditionalOptionsImpl = {
	__index: _TextXAlignmentProp_ConditionalOptionsImpl,
	new: (fields: _TextXAlignmentProp_ConditionalOptionsPartialFields?) -> TextXAlignmentProp_ConditionalOptions,
	encode: (self: TextXAlignmentProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> TextXAlignmentProp_ConditionalOptions,
	jsonEncode: (self: TextXAlignmentProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextXAlignmentProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _TextXAlignmentProp_ConditionalOptionsFields = {
	options: { TextXAlignmentProp_ConditionalOption },
}

type _TextXAlignmentProp_ConditionalOptionsPartialFields = {
	options: { TextXAlignmentProp_ConditionalOption }?,
}

export type TextXAlignmentProp_ConditionalOptions = typeof(setmetatable(
	{} :: _TextXAlignmentProp_ConditionalOptionsFields,
	{} :: _TextXAlignmentProp_ConditionalOptionsImpl
))
type _TextXAlignmentProp_ConditionalOptionsMessage = proto.Message<
	TextXAlignmentProp_ConditionalOptions,
	_TextXAlignmentProp_ConditionalOptionsPartialFields
>

type _TextYAlignmentPropImpl = {
	__index: _TextYAlignmentPropImpl,
	new: (fields: _TextYAlignmentPropPartialFields?) -> TextYAlignmentProp,
	encode: (self: TextYAlignmentProp) -> buffer,
	decode: (input: buffer) -> TextYAlignmentProp,
	jsonEncode: (self: TextYAlignmentProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextYAlignmentProp,
	descriptor: proto.Descriptor,
}

type _TextYAlignmentPropFields = {
	kind: (
		{ type: "literal", value: TextYAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: TextYAlignmentProp_ConditionalOptions }
	)?,
}

type _TextYAlignmentPropPartialFields = {
	kind: (
		{ type: "literal", value: TextYAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: TextYAlignmentProp_ConditionalOptions }
	)?,
}

export type TextYAlignmentProp = typeof(setmetatable({} :: _TextYAlignmentPropFields, {} :: _TextYAlignmentPropImpl))
type _TextYAlignmentPropMessage = proto.Message<TextYAlignmentProp, _TextYAlignmentPropPartialFields>

type _TextYAlignmentProp_ConditionalOptionImpl = {
	__index: _TextYAlignmentProp_ConditionalOptionImpl,
	new: (fields: _TextYAlignmentProp_ConditionalOptionPartialFields?) -> TextYAlignmentProp_ConditionalOption,
	encode: (self: TextYAlignmentProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> TextYAlignmentProp_ConditionalOption,
	jsonEncode: (self: TextYAlignmentProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextYAlignmentProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _TextYAlignmentProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: TextYAlignment } | { type: "binding_path", value: string })?,
}

type _TextYAlignmentProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: TextYAlignment } | { type: "binding_path", value: string })?,
}

export type TextYAlignmentProp_ConditionalOption = typeof(setmetatable(
	{} :: _TextYAlignmentProp_ConditionalOptionFields,
	{} :: _TextYAlignmentProp_ConditionalOptionImpl
))
type _TextYAlignmentProp_ConditionalOptionMessage = proto.Message<
	TextYAlignmentProp_ConditionalOption,
	_TextYAlignmentProp_ConditionalOptionPartialFields
>

type _TextYAlignmentProp_ConditionalOptionsImpl = {
	__index: _TextYAlignmentProp_ConditionalOptionsImpl,
	new: (fields: _TextYAlignmentProp_ConditionalOptionsPartialFields?) -> TextYAlignmentProp_ConditionalOptions,
	encode: (self: TextYAlignmentProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> TextYAlignmentProp_ConditionalOptions,
	jsonEncode: (self: TextYAlignmentProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextYAlignmentProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _TextYAlignmentProp_ConditionalOptionsFields = {
	options: { TextYAlignmentProp_ConditionalOption },
}

type _TextYAlignmentProp_ConditionalOptionsPartialFields = {
	options: { TextYAlignmentProp_ConditionalOption }?,
}

export type TextYAlignmentProp_ConditionalOptions = typeof(setmetatable(
	{} :: _TextYAlignmentProp_ConditionalOptionsFields,
	{} :: _TextYAlignmentProp_ConditionalOptionsImpl
))
type _TextYAlignmentProp_ConditionalOptionsMessage = proto.Message<
	TextYAlignmentProp_ConditionalOptions,
	_TextYAlignmentProp_ConditionalOptionsPartialFields
>

type _UIFlexModePropImpl = {
	__index: _UIFlexModePropImpl,
	new: (fields: _UIFlexModePropPartialFields?) -> UIFlexModeProp,
	encode: (self: UIFlexModeProp) -> buffer,
	decode: (input: buffer) -> UIFlexModeProp,
	jsonEncode: (self: UIFlexModeProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UIFlexModeProp,
	descriptor: proto.Descriptor,
}

type _UIFlexModePropFields = {
	kind: (
		{ type: "literal", value: UIFlexMode }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: UIFlexModeProp_ConditionalOptions }
	)?,
}

type _UIFlexModePropPartialFields = {
	kind: (
		{ type: "literal", value: UIFlexMode }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: UIFlexModeProp_ConditionalOptions }
	)?,
}

export type UIFlexModeProp = typeof(setmetatable({} :: _UIFlexModePropFields, {} :: _UIFlexModePropImpl))
type _UIFlexModePropMessage = proto.Message<UIFlexModeProp, _UIFlexModePropPartialFields>

type _UIFlexModeProp_ConditionalOptionImpl = {
	__index: _UIFlexModeProp_ConditionalOptionImpl,
	new: (fields: _UIFlexModeProp_ConditionalOptionPartialFields?) -> UIFlexModeProp_ConditionalOption,
	encode: (self: UIFlexModeProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> UIFlexModeProp_ConditionalOption,
	jsonEncode: (self: UIFlexModeProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UIFlexModeProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _UIFlexModeProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: UIFlexMode } | { type: "binding_path", value: string })?,
}

type _UIFlexModeProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: UIFlexMode } | { type: "binding_path", value: string })?,
}

export type UIFlexModeProp_ConditionalOption = typeof(setmetatable(
	{} :: _UIFlexModeProp_ConditionalOptionFields,
	{} :: _UIFlexModeProp_ConditionalOptionImpl
))
type _UIFlexModeProp_ConditionalOptionMessage = proto.Message<
	UIFlexModeProp_ConditionalOption,
	_UIFlexModeProp_ConditionalOptionPartialFields
>

type _UIFlexModeProp_ConditionalOptionsImpl = {
	__index: _UIFlexModeProp_ConditionalOptionsImpl,
	new: (fields: _UIFlexModeProp_ConditionalOptionsPartialFields?) -> UIFlexModeProp_ConditionalOptions,
	encode: (self: UIFlexModeProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> UIFlexModeProp_ConditionalOptions,
	jsonEncode: (self: UIFlexModeProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UIFlexModeProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _UIFlexModeProp_ConditionalOptionsFields = {
	options: { UIFlexModeProp_ConditionalOption },
}

type _UIFlexModeProp_ConditionalOptionsPartialFields = {
	options: { UIFlexModeProp_ConditionalOption }?,
}

export type UIFlexModeProp_ConditionalOptions = typeof(setmetatable(
	{} :: _UIFlexModeProp_ConditionalOptionsFields,
	{} :: _UIFlexModeProp_ConditionalOptionsImpl
))
type _UIFlexModeProp_ConditionalOptionsMessage = proto.Message<
	UIFlexModeProp_ConditionalOptions,
	_UIFlexModeProp_ConditionalOptionsPartialFields
>

type _ScaleTypePropImpl = {
	__index: _ScaleTypePropImpl,
	new: (fields: _ScaleTypePropPartialFields?) -> ScaleTypeProp,
	encode: (self: ScaleTypeProp) -> buffer,
	decode: (input: buffer) -> ScaleTypeProp,
	jsonEncode: (self: ScaleTypeProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ScaleTypeProp,
	descriptor: proto.Descriptor,
}

type _ScaleTypePropFields = {
	kind: (
		{ type: "literal", value: ScaleType }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ScaleTypeProp_ConditionalOptions }
	)?,
}

type _ScaleTypePropPartialFields = {
	kind: (
		{ type: "literal", value: ScaleType }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ScaleTypeProp_ConditionalOptions }
	)?,
}

export type ScaleTypeProp = typeof(setmetatable({} :: _ScaleTypePropFields, {} :: _ScaleTypePropImpl))
type _ScaleTypePropMessage = proto.Message<ScaleTypeProp, _ScaleTypePropPartialFields>

type _ScaleTypeProp_ConditionalOptionImpl = {
	__index: _ScaleTypeProp_ConditionalOptionImpl,
	new: (fields: _ScaleTypeProp_ConditionalOptionPartialFields?) -> ScaleTypeProp_ConditionalOption,
	encode: (self: ScaleTypeProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> ScaleTypeProp_ConditionalOption,
	jsonEncode: (self: ScaleTypeProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ScaleTypeProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _ScaleTypeProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: ScaleType } | { type: "binding_path", value: string })?,
}

type _ScaleTypeProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: ScaleType } | { type: "binding_path", value: string })?,
}

export type ScaleTypeProp_ConditionalOption = typeof(setmetatable(
	{} :: _ScaleTypeProp_ConditionalOptionFields,
	{} :: _ScaleTypeProp_ConditionalOptionImpl
))
type _ScaleTypeProp_ConditionalOptionMessage = proto.Message<
	ScaleTypeProp_ConditionalOption,
	_ScaleTypeProp_ConditionalOptionPartialFields
>

type _ScaleTypeProp_ConditionalOptionsImpl = {
	__index: _ScaleTypeProp_ConditionalOptionsImpl,
	new: (fields: _ScaleTypeProp_ConditionalOptionsPartialFields?) -> ScaleTypeProp_ConditionalOptions,
	encode: (self: ScaleTypeProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> ScaleTypeProp_ConditionalOptions,
	jsonEncode: (self: ScaleTypeProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ScaleTypeProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _ScaleTypeProp_ConditionalOptionsFields = {
	options: { ScaleTypeProp_ConditionalOption },
}

type _ScaleTypeProp_ConditionalOptionsPartialFields = {
	options: { ScaleTypeProp_ConditionalOption }?,
}

export type ScaleTypeProp_ConditionalOptions = typeof(setmetatable(
	{} :: _ScaleTypeProp_ConditionalOptionsFields,
	{} :: _ScaleTypeProp_ConditionalOptionsImpl
))
type _ScaleTypeProp_ConditionalOptionsMessage = proto.Message<
	ScaleTypeProp_ConditionalOptions,
	_ScaleTypeProp_ConditionalOptionsPartialFields
>

type _ResamplerModePropImpl = {
	__index: _ResamplerModePropImpl,
	new: (fields: _ResamplerModePropPartialFields?) -> ResamplerModeProp,
	encode: (self: ResamplerModeProp) -> buffer,
	decode: (input: buffer) -> ResamplerModeProp,
	jsonEncode: (self: ResamplerModeProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ResamplerModeProp,
	descriptor: proto.Descriptor,
}

type _ResamplerModePropFields = {
	kind: (
		{ type: "literal", value: ResamplerMode }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ResamplerModeProp_ConditionalOptions }
	)?,
}

type _ResamplerModePropPartialFields = {
	kind: (
		{ type: "literal", value: ResamplerMode }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: ResamplerModeProp_ConditionalOptions }
	)?,
}

export type ResamplerModeProp = typeof(setmetatable({} :: _ResamplerModePropFields, {} :: _ResamplerModePropImpl))
type _ResamplerModePropMessage = proto.Message<ResamplerModeProp, _ResamplerModePropPartialFields>

type _ResamplerModeProp_ConditionalOptionImpl = {
	__index: _ResamplerModeProp_ConditionalOptionImpl,
	new: (fields: _ResamplerModeProp_ConditionalOptionPartialFields?) -> ResamplerModeProp_ConditionalOption,
	encode: (self: ResamplerModeProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> ResamplerModeProp_ConditionalOption,
	jsonEncode: (self: ResamplerModeProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ResamplerModeProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _ResamplerModeProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: ResamplerMode } | { type: "binding_path", value: string })?,
}

type _ResamplerModeProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: ResamplerMode } | { type: "binding_path", value: string })?,
}

export type ResamplerModeProp_ConditionalOption = typeof(setmetatable(
	{} :: _ResamplerModeProp_ConditionalOptionFields,
	{} :: _ResamplerModeProp_ConditionalOptionImpl
))
type _ResamplerModeProp_ConditionalOptionMessage = proto.Message<
	ResamplerModeProp_ConditionalOption,
	_ResamplerModeProp_ConditionalOptionPartialFields
>

type _ResamplerModeProp_ConditionalOptionsImpl = {
	__index: _ResamplerModeProp_ConditionalOptionsImpl,
	new: (fields: _ResamplerModeProp_ConditionalOptionsPartialFields?) -> ResamplerModeProp_ConditionalOptions,
	encode: (self: ResamplerModeProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> ResamplerModeProp_ConditionalOptions,
	jsonEncode: (self: ResamplerModeProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ResamplerModeProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _ResamplerModeProp_ConditionalOptionsFields = {
	options: { ResamplerModeProp_ConditionalOption },
}

type _ResamplerModeProp_ConditionalOptionsPartialFields = {
	options: { ResamplerModeProp_ConditionalOption }?,
}

export type ResamplerModeProp_ConditionalOptions = typeof(setmetatable(
	{} :: _ResamplerModeProp_ConditionalOptionsFields,
	{} :: _ResamplerModeProp_ConditionalOptionsImpl
))
type _ResamplerModeProp_ConditionalOptionsMessage = proto.Message<
	ResamplerModeProp_ConditionalOptions,
	_ResamplerModeProp_ConditionalOptionsPartialFields
>

type _SizeConstraintPropImpl = {
	__index: _SizeConstraintPropImpl,
	new: (fields: _SizeConstraintPropPartialFields?) -> SizeConstraintProp,
	encode: (self: SizeConstraintProp) -> buffer,
	decode: (input: buffer) -> SizeConstraintProp,
	jsonEncode: (self: SizeConstraintProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SizeConstraintProp,
	descriptor: proto.Descriptor,
}

type _SizeConstraintPropFields = {
	kind: (
		{ type: "literal", value: SizeConstraint }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: SizeConstraintProp_ConditionalOptions }
	)?,
}

type _SizeConstraintPropPartialFields = {
	kind: (
		{ type: "literal", value: SizeConstraint }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: SizeConstraintProp_ConditionalOptions }
	)?,
}

export type SizeConstraintProp = typeof(setmetatable({} :: _SizeConstraintPropFields, {} :: _SizeConstraintPropImpl))
type _SizeConstraintPropMessage = proto.Message<SizeConstraintProp, _SizeConstraintPropPartialFields>

type _SizeConstraintProp_ConditionalOptionImpl = {
	__index: _SizeConstraintProp_ConditionalOptionImpl,
	new: (fields: _SizeConstraintProp_ConditionalOptionPartialFields?) -> SizeConstraintProp_ConditionalOption,
	encode: (self: SizeConstraintProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> SizeConstraintProp_ConditionalOption,
	jsonEncode: (self: SizeConstraintProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SizeConstraintProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _SizeConstraintProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: SizeConstraint } | { type: "binding_path", value: string })?,
}

type _SizeConstraintProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: SizeConstraint } | { type: "binding_path", value: string })?,
}

export type SizeConstraintProp_ConditionalOption = typeof(setmetatable(
	{} :: _SizeConstraintProp_ConditionalOptionFields,
	{} :: _SizeConstraintProp_ConditionalOptionImpl
))
type _SizeConstraintProp_ConditionalOptionMessage = proto.Message<
	SizeConstraintProp_ConditionalOption,
	_SizeConstraintProp_ConditionalOptionPartialFields
>

type _SizeConstraintProp_ConditionalOptionsImpl = {
	__index: _SizeConstraintProp_ConditionalOptionsImpl,
	new: (fields: _SizeConstraintProp_ConditionalOptionsPartialFields?) -> SizeConstraintProp_ConditionalOptions,
	encode: (self: SizeConstraintProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> SizeConstraintProp_ConditionalOptions,
	jsonEncode: (self: SizeConstraintProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SizeConstraintProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _SizeConstraintProp_ConditionalOptionsFields = {
	options: { SizeConstraintProp_ConditionalOption },
}

type _SizeConstraintProp_ConditionalOptionsPartialFields = {
	options: { SizeConstraintProp_ConditionalOption }?,
}

export type SizeConstraintProp_ConditionalOptions = typeof(setmetatable(
	{} :: _SizeConstraintProp_ConditionalOptionsFields,
	{} :: _SizeConstraintProp_ConditionalOptionsImpl
))
type _SizeConstraintProp_ConditionalOptionsMessage = proto.Message<
	SizeConstraintProp_ConditionalOptions,
	_SizeConstraintProp_ConditionalOptionsPartialFields
>

type _HorizontalAlignmentPropImpl = {
	__index: _HorizontalAlignmentPropImpl,
	new: (fields: _HorizontalAlignmentPropPartialFields?) -> HorizontalAlignmentProp,
	encode: (self: HorizontalAlignmentProp) -> buffer,
	decode: (input: buffer) -> HorizontalAlignmentProp,
	jsonEncode: (self: HorizontalAlignmentProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HorizontalAlignmentProp,
	descriptor: proto.Descriptor,
}

type _HorizontalAlignmentPropFields = {
	kind: (
		{ type: "literal", value: HorizontalAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: HorizontalAlignmentProp_ConditionalOptions }
	)?,
}

type _HorizontalAlignmentPropPartialFields = {
	kind: (
		{ type: "literal", value: HorizontalAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: HorizontalAlignmentProp_ConditionalOptions }
	)?,
}

export type HorizontalAlignmentProp = typeof(setmetatable(
	{} :: _HorizontalAlignmentPropFields,
	{} :: _HorizontalAlignmentPropImpl
))
type _HorizontalAlignmentPropMessage = proto.Message<HorizontalAlignmentProp, _HorizontalAlignmentPropPartialFields>

type _HorizontalAlignmentProp_ConditionalOptionImpl = {
	__index: _HorizontalAlignmentProp_ConditionalOptionImpl,
	new: (
		fields: _HorizontalAlignmentProp_ConditionalOptionPartialFields?
	) -> HorizontalAlignmentProp_ConditionalOption,
	encode: (self: HorizontalAlignmentProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> HorizontalAlignmentProp_ConditionalOption,
	jsonEncode: (self: HorizontalAlignmentProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HorizontalAlignmentProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _HorizontalAlignmentProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: HorizontalAlignment } | { type: "binding_path", value: string })?,
}

type _HorizontalAlignmentProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: HorizontalAlignment } | { type: "binding_path", value: string })?,
}

export type HorizontalAlignmentProp_ConditionalOption = typeof(setmetatable(
	{} :: _HorizontalAlignmentProp_ConditionalOptionFields,
	{} :: _HorizontalAlignmentProp_ConditionalOptionImpl
))
type _HorizontalAlignmentProp_ConditionalOptionMessage = proto.Message<
	HorizontalAlignmentProp_ConditionalOption,
	_HorizontalAlignmentProp_ConditionalOptionPartialFields
>

type _HorizontalAlignmentProp_ConditionalOptionsImpl = {
	__index: _HorizontalAlignmentProp_ConditionalOptionsImpl,
	new: (
		fields: _HorizontalAlignmentProp_ConditionalOptionsPartialFields?
	) -> HorizontalAlignmentProp_ConditionalOptions,
	encode: (self: HorizontalAlignmentProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> HorizontalAlignmentProp_ConditionalOptions,
	jsonEncode: (self: HorizontalAlignmentProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HorizontalAlignmentProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _HorizontalAlignmentProp_ConditionalOptionsFields = {
	option: { HorizontalAlignmentProp_ConditionalOption },
}

type _HorizontalAlignmentProp_ConditionalOptionsPartialFields = {
	option: { HorizontalAlignmentProp_ConditionalOption }?,
}

export type HorizontalAlignmentProp_ConditionalOptions = typeof(setmetatable(
	{} :: _HorizontalAlignmentProp_ConditionalOptionsFields,
	{} :: _HorizontalAlignmentProp_ConditionalOptionsImpl
))
type _HorizontalAlignmentProp_ConditionalOptionsMessage = proto.Message<
	HorizontalAlignmentProp_ConditionalOptions,
	_HorizontalAlignmentProp_ConditionalOptionsPartialFields
>

type _SelectionBehaviorPropImpl = {
	__index: _SelectionBehaviorPropImpl,
	new: (fields: _SelectionBehaviorPropPartialFields?) -> SelectionBehaviorProp,
	encode: (self: SelectionBehaviorProp) -> buffer,
	decode: (input: buffer) -> SelectionBehaviorProp,
	jsonEncode: (self: SelectionBehaviorProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SelectionBehaviorProp,
	descriptor: proto.Descriptor,
}

type _SelectionBehaviorPropFields = {
	kind: (
		{ type: "literal", value: SelectionBehavior }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: SelectionBehaviorProp_ConditionalOptions }
	)?,
}

type _SelectionBehaviorPropPartialFields = {
	kind: (
		{ type: "literal", value: SelectionBehavior }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: SelectionBehaviorProp_ConditionalOptions }
	)?,
}

export type SelectionBehaviorProp = typeof(setmetatable(
	{} :: _SelectionBehaviorPropFields,
	{} :: _SelectionBehaviorPropImpl
))
type _SelectionBehaviorPropMessage = proto.Message<SelectionBehaviorProp, _SelectionBehaviorPropPartialFields>

type _SelectionBehaviorProp_ConditionalOptionImpl = {
	__index: _SelectionBehaviorProp_ConditionalOptionImpl,
	new: (fields: _SelectionBehaviorProp_ConditionalOptionPartialFields?) -> SelectionBehaviorProp_ConditionalOption,
	encode: (self: SelectionBehaviorProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> SelectionBehaviorProp_ConditionalOption,
	jsonEncode: (self: SelectionBehaviorProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SelectionBehaviorProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _SelectionBehaviorProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: SelectionBehavior } | { type: "binding_path", value: string })?,
}

type _SelectionBehaviorProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: SelectionBehavior } | { type: "binding_path", value: string })?,
}

export type SelectionBehaviorProp_ConditionalOption = typeof(setmetatable(
	{} :: _SelectionBehaviorProp_ConditionalOptionFields,
	{} :: _SelectionBehaviorProp_ConditionalOptionImpl
))
type _SelectionBehaviorProp_ConditionalOptionMessage = proto.Message<
	SelectionBehaviorProp_ConditionalOption,
	_SelectionBehaviorProp_ConditionalOptionPartialFields
>

type _SelectionBehaviorProp_ConditionalOptionsImpl = {
	__index: _SelectionBehaviorProp_ConditionalOptionsImpl,
	new: (fields: _SelectionBehaviorProp_ConditionalOptionsPartialFields?) -> SelectionBehaviorProp_ConditionalOptions,
	encode: (self: SelectionBehaviorProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> SelectionBehaviorProp_ConditionalOptions,
	jsonEncode: (self: SelectionBehaviorProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SelectionBehaviorProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _SelectionBehaviorProp_ConditionalOptionsFields = {
	options: { SelectionBehaviorProp_ConditionalOption },
}

type _SelectionBehaviorProp_ConditionalOptionsPartialFields = {
	options: { SelectionBehaviorProp_ConditionalOption }?,
}

export type SelectionBehaviorProp_ConditionalOptions = typeof(setmetatable(
	{} :: _SelectionBehaviorProp_ConditionalOptionsFields,
	{} :: _SelectionBehaviorProp_ConditionalOptionsImpl
))
type _SelectionBehaviorProp_ConditionalOptionsMessage = proto.Message<
	SelectionBehaviorProp_ConditionalOptions,
	_SelectionBehaviorProp_ConditionalOptionsPartialFields
>

type _KeyCodePropImpl = {
	__index: _KeyCodePropImpl,
	new: (fields: _KeyCodePropPartialFields?) -> KeyCodeProp,
	encode: (self: KeyCodeProp) -> buffer,
	decode: (input: buffer) -> KeyCodeProp,
	jsonEncode: (self: KeyCodeProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> KeyCodeProp,
	descriptor: proto.Descriptor,
}

type _KeyCodePropFields = {
	kind: (
		{ type: "literal", value: KeyCode }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: KeyCodeProp_ConditionalOptions }
	)?,
}

type _KeyCodePropPartialFields = {
	kind: (
		{ type: "literal", value: KeyCode }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: KeyCodeProp_ConditionalOptions }
	)?,
}

export type KeyCodeProp = typeof(setmetatable({} :: _KeyCodePropFields, {} :: _KeyCodePropImpl))
type _KeyCodePropMessage = proto.Message<KeyCodeProp, _KeyCodePropPartialFields>

type _KeyCodeProp_ConditionalOptionImpl = {
	__index: _KeyCodeProp_ConditionalOptionImpl,
	new: (fields: _KeyCodeProp_ConditionalOptionPartialFields?) -> KeyCodeProp_ConditionalOption,
	encode: (self: KeyCodeProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> KeyCodeProp_ConditionalOption,
	jsonEncode: (self: KeyCodeProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> KeyCodeProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _KeyCodeProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: KeyCode } | { type: "binding_path", value: string })?,
}

type _KeyCodeProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: KeyCode } | { type: "binding_path", value: string })?,
}

export type KeyCodeProp_ConditionalOption = typeof(setmetatable(
	{} :: _KeyCodeProp_ConditionalOptionFields,
	{} :: _KeyCodeProp_ConditionalOptionImpl
))
type _KeyCodeProp_ConditionalOptionMessage = proto.Message<
	KeyCodeProp_ConditionalOption,
	_KeyCodeProp_ConditionalOptionPartialFields
>

type _KeyCodeProp_ConditionalOptionsImpl = {
	__index: _KeyCodeProp_ConditionalOptionsImpl,
	new: (fields: _KeyCodeProp_ConditionalOptionsPartialFields?) -> KeyCodeProp_ConditionalOptions,
	encode: (self: KeyCodeProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> KeyCodeProp_ConditionalOptions,
	jsonEncode: (self: KeyCodeProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> KeyCodeProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _KeyCodeProp_ConditionalOptionsFields = {
	options: { KeyCodeProp_ConditionalOption },
}

type _KeyCodeProp_ConditionalOptionsPartialFields = {
	options: { KeyCodeProp_ConditionalOption }?,
}

export type KeyCodeProp_ConditionalOptions = typeof(setmetatable(
	{} :: _KeyCodeProp_ConditionalOptionsFields,
	{} :: _KeyCodeProp_ConditionalOptionsImpl
))
type _KeyCodeProp_ConditionalOptionsMessage = proto.Message<
	KeyCodeProp_ConditionalOptions,
	_KeyCodeProp_ConditionalOptionsPartialFields
>

type _VerticalAlignmentPropImpl = {
	__index: _VerticalAlignmentPropImpl,
	new: (fields: _VerticalAlignmentPropPartialFields?) -> VerticalAlignmentProp,
	encode: (self: VerticalAlignmentProp) -> buffer,
	decode: (input: buffer) -> VerticalAlignmentProp,
	jsonEncode: (self: VerticalAlignmentProp) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VerticalAlignmentProp,
	descriptor: proto.Descriptor,
}

type _VerticalAlignmentPropFields = {
	kind: (
		{ type: "literal", value: VerticalAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: VerticalAlignmentProp_ConditionalOptions }
	)?,
}

type _VerticalAlignmentPropPartialFields = {
	kind: (
		{ type: "literal", value: VerticalAlignment }
		| { type: "binding_path", value: string }
		| { type: "conditional", value: VerticalAlignmentProp_ConditionalOptions }
	)?,
}

export type VerticalAlignmentProp = typeof(setmetatable(
	{} :: _VerticalAlignmentPropFields,
	{} :: _VerticalAlignmentPropImpl
))
type _VerticalAlignmentPropMessage = proto.Message<VerticalAlignmentProp, _VerticalAlignmentPropPartialFields>

type _VerticalAlignmentProp_ConditionalOptionImpl = {
	__index: _VerticalAlignmentProp_ConditionalOptionImpl,
	new: (fields: _VerticalAlignmentProp_ConditionalOptionPartialFields?) -> VerticalAlignmentProp_ConditionalOption,
	encode: (self: VerticalAlignmentProp_ConditionalOption) -> buffer,
	decode: (input: buffer) -> VerticalAlignmentProp_ConditionalOption,
	jsonEncode: (self: VerticalAlignmentProp_ConditionalOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VerticalAlignmentProp_ConditionalOption,
	descriptor: proto.Descriptor,
}

type _VerticalAlignmentProp_ConditionalOptionFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: VerticalAlignment } | { type: "binding_path", value: string })?,
}

type _VerticalAlignmentProp_ConditionalOptionPartialFields = {
	condition: _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition?,
	kind: ({ type: "literal", value: VerticalAlignment } | { type: "binding_path", value: string })?,
}

export type VerticalAlignmentProp_ConditionalOption = typeof(setmetatable(
	{} :: _VerticalAlignmentProp_ConditionalOptionFields,
	{} :: _VerticalAlignmentProp_ConditionalOptionImpl
))
type _VerticalAlignmentProp_ConditionalOptionMessage = proto.Message<
	VerticalAlignmentProp_ConditionalOption,
	_VerticalAlignmentProp_ConditionalOptionPartialFields
>

type _VerticalAlignmentProp_ConditionalOptionsImpl = {
	__index: _VerticalAlignmentProp_ConditionalOptionsImpl,
	new: (fields: _VerticalAlignmentProp_ConditionalOptionsPartialFields?) -> VerticalAlignmentProp_ConditionalOptions,
	encode: (self: VerticalAlignmentProp_ConditionalOptions) -> buffer,
	decode: (input: buffer) -> VerticalAlignmentProp_ConditionalOptions,
	jsonEncode: (self: VerticalAlignmentProp_ConditionalOptions) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VerticalAlignmentProp_ConditionalOptions,
	descriptor: proto.Descriptor,
}

type _VerticalAlignmentProp_ConditionalOptionsFields = {
	option: { VerticalAlignmentProp_ConditionalOption },
}

type _VerticalAlignmentProp_ConditionalOptionsPartialFields = {
	option: { VerticalAlignmentProp_ConditionalOption }?,
}

export type VerticalAlignmentProp_ConditionalOptions = typeof(setmetatable(
	{} :: _VerticalAlignmentProp_ConditionalOptionsFields,
	{} :: _VerticalAlignmentProp_ConditionalOptionsImpl
))
type _VerticalAlignmentProp_ConditionalOptionsMessage = proto.Message<
	VerticalAlignmentProp_ConditionalOptions,
	_VerticalAlignmentProp_ConditionalOptionsPartialFields
>

type _AutomaticSizeMessage = proto.Enum<AutomaticSize>
export type AutomaticSize =
	"AUTOMATIC_SIZE_INVALID"
	| "AUTOMATIC_SIZE_NONE"
	| "AUTOMATIC_SIZE_X"
	| "AUTOMATIC_SIZE_Y"
	| "AUTOMATIC_SIZE_XY"
	| number -- Unknown

type _TextTruncateMessage = proto.Enum<TextTruncate>
export type TextTruncate =
	"TEXT_TRUNCATE_INVALID"
	| "TEXT_TRUNCATE_NONE"
	| "TEXT_TRUNCATE_AT_END"
	| "TEXT_TRUNCATE_SPLIT_WORD"
	| number -- Unknown

type _TextXAlignmentMessage = proto.Enum<TextXAlignment>
export type TextXAlignment =
	"TEXT_X_ALIGNMENT_INVALID"
	| "TEXT_X_ALIGNMENT_LEFT"
	| "TEXT_X_ALIGNMENT_CENTER"
	| "TEXT_X_ALIGNMENT_RIGHT"
	| number -- Unknown

type _TextYAlignmentMessage = proto.Enum<TextYAlignment>
export type TextYAlignment =
	"TEXT_Y_ALIGNMENT_INVALID"
	| "TEXT_Y_ALIGNMENT_TOP"
	| "TEXT_Y_ALIGNMENT_CENTER"
	| "TEXT_Y_ALIGNMENT_BOTTOM"
	| number -- Unknown

type _UIFlexModeMessage = proto.Enum<UIFlexMode>
export type UIFlexMode =
	"UI_FLEX_MODE_INVALID"
	| "UI_FLEX_MODE_NONE"
	| "UI_FLEX_MODE_GROW"
	| "UI_FLEX_MODE_SHRINK"
	| "UI_FLEX_MODE_FILL"
	| "UI_FLEX_MODE_CUSTOM"
	| number -- Unknown

type _ScaleTypeMessage = proto.Enum<ScaleType>
export type ScaleType =
	"SCALE_TYPE_INVALID"
	| "SCALE_TYPE_STRETCH"
	| "SCALE_TYPE_SLICE"
	| "SCALE_TYPE_TILE"
	| "SCALE_TYPE_FIT"
	| "SCALE_TYPE_CROP"
	| number -- Unknown

type _ResamplerModeMessage = proto.Enum<ResamplerMode>
export type ResamplerMode = "RESAMPLER_MODE_INVALID" | "RESAMPLER_MODE_DEFAULT" | "RESAMPLER_MODE_PIXELATED" | number -- Unknown

type _SizeConstraintMessage = proto.Enum<SizeConstraint>
export type SizeConstraint =
	"SIZE_CONSTRAINT_INVALID"
	| "SIZE_CONSTRAINT_RELATIVE_XY"
	| "SIZE_CONSTRAINT_RELATIVE_XX"
	| "SIZE_CONSTRAINT_RELATIVE_YY"
	| number -- Unknown

type _HorizontalAlignmentMessage = proto.Enum<HorizontalAlignment>
export type HorizontalAlignment =
	"HORIZONTAL_ALIGNMENT_INVALID"
	| "HORIZONTAL_ALIGNMENT_LEFT"
	| "HORIZONTAL_ALIGNMENT_CENTER"
	| "HORIZONTAL_ALIGNMENT_RIGHT"
	| number -- Unknown

type _VerticalAlignmentMessage = proto.Enum<VerticalAlignment>
export type VerticalAlignment =
	"VERTICAL_ALIGNMENT_INVALID"
	| "VERTICAL_ALIGNMENT_TOP"
	| "VERTICAL_ALIGNMENT_CENTER"
	| "VERTICAL_ALIGNMENT_BOTTOM"
	| number -- Unknown

type _SelectionBehaviorMessage = proto.Enum<SelectionBehavior>
export type SelectionBehavior =
	"SELECTION_BEHAVIOR_INVALID"
	| "SELECTION_BEHAVIOR_ESCAPE"
	| "SELECTION_BEHAVIOR_STOP"
	| number -- Unknown

type _KeyCodeMessage = proto.Enum<KeyCode>
export type KeyCode =
	"KEY_CODE_INVALID"
	| "KEY_CODE_BUTTON_A"
	| "KEY_CODE_BUTTON_B"
	| "KEY_CODE_BUTTON_X"
	| "KEY_CODE_BUTTON_Y"
	| "KEY_CODE_BUTTON_L1"
	| "KEY_CODE_BUTTON_R1"
	| "KEY_CODE_BUTTON_L2"
	| "KEY_CODE_BUTTON_R2"
	| "KEY_CODE_BUTTON_L3"
	| "KEY_CODE_BUTTON_R3"
	| number -- Unknown

do
	local _AutomaticSizePropImpl = {}
	_AutomaticSizePropImpl.__index = _AutomaticSizePropImpl

	function _AutomaticSizePropImpl.new(data: _AutomaticSizePropPartialFields?): AutomaticSizeProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _AutomaticSizePropImpl :: _AutomaticSizePropImpl)
	end

	function _AutomaticSizePropImpl.encode(self: AutomaticSizeProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.AutomaticSize.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AutomaticSizePropImpl.decode(input: buffer): AutomaticSizeProp
		local self = _AutomaticSizePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.AutomaticSize.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.AutomaticSizeProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AutomaticSizePropImpl.jsonEncode(self: AutomaticSizeProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.AutomaticSize.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _AutomaticSizePropImpl.jsonDecode(input: { [string]: any }): AutomaticSizeProp
		local self = _AutomaticSizePropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.AutomaticSize.fromNumber(input.literal) or input.literal)
					else (messages.AutomaticSize.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.AutomaticSizeProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_AutomaticSizePropImpl.descriptor = {
		name = "AutomaticSizeProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.AutomaticSizeProp",
	}

	messages.AutomaticSizeProp = _AutomaticSizePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AutomaticSizeProp)
end

do
	local _AutomaticSizeProp_ConditionalOptionImpl = {}
	_AutomaticSizeProp_ConditionalOptionImpl.__index = _AutomaticSizeProp_ConditionalOptionImpl

	function _AutomaticSizeProp_ConditionalOptionImpl.new(
		data: _AutomaticSizeProp_ConditionalOptionPartialFields?
	): AutomaticSizeProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _AutomaticSizeProp_ConditionalOptionImpl :: _AutomaticSizeProp_ConditionalOptionImpl)
	end

	function _AutomaticSizeProp_ConditionalOptionImpl.encode(self: AutomaticSizeProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.AutomaticSize.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AutomaticSizeProp_ConditionalOptionImpl.decode(input: buffer): AutomaticSizeProp_ConditionalOption
		local self = _AutomaticSizeProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.AutomaticSize.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AutomaticSizeProp_ConditionalOptionImpl.jsonEncode(self: AutomaticSizeProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.AutomaticSize.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _AutomaticSizeProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): AutomaticSizeProp_ConditionalOption
		local self = _AutomaticSizeProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.AutomaticSize.fromNumber(input.literal) or input.literal)
					else (messages.AutomaticSize.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_AutomaticSizeProp_ConditionalOptionImpl.descriptor = {
		name = "AutomaticSizeProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.AutomaticSizeProp_ConditionalOption = _AutomaticSizeProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AutomaticSizeProp_ConditionalOption)
end

do
	local _AutomaticSizeProp_ConditionalOptionsImpl = {}
	_AutomaticSizeProp_ConditionalOptionsImpl.__index = _AutomaticSizeProp_ConditionalOptionsImpl

	function _AutomaticSizeProp_ConditionalOptionsImpl.new(
		data: _AutomaticSizeProp_ConditionalOptionsPartialFields?
	): AutomaticSizeProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _AutomaticSizeProp_ConditionalOptionsImpl :: _AutomaticSizeProp_ConditionalOptionsImpl)
	end

	function _AutomaticSizeProp_ConditionalOptionsImpl.encode(self: AutomaticSizeProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AutomaticSizeProp_ConditionalOptionsImpl.decode(input: buffer): AutomaticSizeProp_ConditionalOptions
		local self = _AutomaticSizeProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.AutomaticSizeProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AutomaticSizeProp_ConditionalOptionsImpl.jsonEncode(self: AutomaticSizeProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _AutomaticSizeProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): AutomaticSizeProp_ConditionalOptions
		local self = _AutomaticSizeProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { AutomaticSizeProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.AutomaticSizeProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_AutomaticSizeProp_ConditionalOptionsImpl.descriptor = {
		name = "AutomaticSizeProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.AutomaticSizeProp_ConditionalOptions = _AutomaticSizeProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AutomaticSizeProp_ConditionalOptions)
end

do
	local _TextTruncatePropImpl = {}
	_TextTruncatePropImpl.__index = _TextTruncatePropImpl

	function _TextTruncatePropImpl.new(data: _TextTruncatePropPartialFields?): TextTruncateProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _TextTruncatePropImpl :: _TextTruncatePropImpl)
	end

	function _TextTruncatePropImpl.encode(self: TextTruncateProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.TextTruncate.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextTruncatePropImpl.decode(input: buffer): TextTruncateProp
		local self = _TextTruncatePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.TextTruncate.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.TextTruncateProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextTruncatePropImpl.jsonEncode(self: TextTruncateProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.TextTruncate.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _TextTruncatePropImpl.jsonDecode(input: { [string]: any }): TextTruncateProp
		local self = _TextTruncatePropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.TextTruncate.fromNumber(input.literal) or input.literal)
					else (messages.TextTruncate.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.TextTruncateProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_TextTruncatePropImpl.descriptor = {
		name = "TextTruncateProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.TextTruncateProp",
	}

	messages.TextTruncateProp = _TextTruncatePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextTruncateProp)
end

do
	local _TextTruncateProp_ConditionalOptionImpl = {}
	_TextTruncateProp_ConditionalOptionImpl.__index = _TextTruncateProp_ConditionalOptionImpl

	function _TextTruncateProp_ConditionalOptionImpl.new(
		data: _TextTruncateProp_ConditionalOptionPartialFields?
	): TextTruncateProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _TextTruncateProp_ConditionalOptionImpl :: _TextTruncateProp_ConditionalOptionImpl)
	end

	function _TextTruncateProp_ConditionalOptionImpl.encode(self: TextTruncateProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.TextTruncate.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextTruncateProp_ConditionalOptionImpl.decode(input: buffer): TextTruncateProp_ConditionalOption
		local self = _TextTruncateProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.TextTruncate.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextTruncateProp_ConditionalOptionImpl.jsonEncode(self: TextTruncateProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.TextTruncate.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _TextTruncateProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): TextTruncateProp_ConditionalOption
		local self = _TextTruncateProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.TextTruncate.fromNumber(input.literal) or input.literal)
					else (messages.TextTruncate.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_TextTruncateProp_ConditionalOptionImpl.descriptor = {
		name = "TextTruncateProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.TextTruncateProp_ConditionalOption = _TextTruncateProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextTruncateProp_ConditionalOption)
end

do
	local _TextTruncateProp_ConditionalOptionsImpl = {}
	_TextTruncateProp_ConditionalOptionsImpl.__index = _TextTruncateProp_ConditionalOptionsImpl

	function _TextTruncateProp_ConditionalOptionsImpl.new(
		data: _TextTruncateProp_ConditionalOptionsPartialFields?
	): TextTruncateProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _TextTruncateProp_ConditionalOptionsImpl :: _TextTruncateProp_ConditionalOptionsImpl)
	end

	function _TextTruncateProp_ConditionalOptionsImpl.encode(self: TextTruncateProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextTruncateProp_ConditionalOptionsImpl.decode(input: buffer): TextTruncateProp_ConditionalOptions
		local self = _TextTruncateProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.TextTruncateProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextTruncateProp_ConditionalOptionsImpl.jsonEncode(self: TextTruncateProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _TextTruncateProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): TextTruncateProp_ConditionalOptions
		local self = _TextTruncateProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { TextTruncateProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.TextTruncateProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_TextTruncateProp_ConditionalOptionsImpl.descriptor = {
		name = "TextTruncateProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.TextTruncateProp_ConditionalOptions = _TextTruncateProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextTruncateProp_ConditionalOptions)
end

do
	local _TextXAlignmentPropImpl = {}
	_TextXAlignmentPropImpl.__index = _TextXAlignmentPropImpl

	function _TextXAlignmentPropImpl.new(data: _TextXAlignmentPropPartialFields?): TextXAlignmentProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _TextXAlignmentPropImpl :: _TextXAlignmentPropImpl)
	end

	function _TextXAlignmentPropImpl.encode(self: TextXAlignmentProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.TextXAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextXAlignmentPropImpl.decode(input: buffer): TextXAlignmentProp
		local self = _TextXAlignmentPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.TextXAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.TextXAlignmentProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextXAlignmentPropImpl.jsonEncode(self: TextXAlignmentProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.TextXAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _TextXAlignmentPropImpl.jsonDecode(input: { [string]: any }): TextXAlignmentProp
		local self = _TextXAlignmentPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.TextXAlignment.fromNumber(input.literal) or input.literal)
					else (messages.TextXAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.TextXAlignmentProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_TextXAlignmentPropImpl.descriptor = {
		name = "TextXAlignmentProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.TextXAlignmentProp",
	}

	messages.TextXAlignmentProp = _TextXAlignmentPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextXAlignmentProp)
end

do
	local _TextXAlignmentProp_ConditionalOptionImpl = {}
	_TextXAlignmentProp_ConditionalOptionImpl.__index = _TextXAlignmentProp_ConditionalOptionImpl

	function _TextXAlignmentProp_ConditionalOptionImpl.new(
		data: _TextXAlignmentProp_ConditionalOptionPartialFields?
	): TextXAlignmentProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _TextXAlignmentProp_ConditionalOptionImpl :: _TextXAlignmentProp_ConditionalOptionImpl)
	end

	function _TextXAlignmentProp_ConditionalOptionImpl.encode(self: TextXAlignmentProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.TextXAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextXAlignmentProp_ConditionalOptionImpl.decode(input: buffer): TextXAlignmentProp_ConditionalOption
		local self = _TextXAlignmentProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.TextXAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextXAlignmentProp_ConditionalOptionImpl.jsonEncode(self: TextXAlignmentProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.TextXAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _TextXAlignmentProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): TextXAlignmentProp_ConditionalOption
		local self = _TextXAlignmentProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.TextXAlignment.fromNumber(input.literal) or input.literal)
					else (messages.TextXAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_TextXAlignmentProp_ConditionalOptionImpl.descriptor = {
		name = "TextXAlignmentProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.TextXAlignmentProp_ConditionalOption = _TextXAlignmentProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextXAlignmentProp_ConditionalOption)
end

do
	local _TextXAlignmentProp_ConditionalOptionsImpl = {}
	_TextXAlignmentProp_ConditionalOptionsImpl.__index = _TextXAlignmentProp_ConditionalOptionsImpl

	function _TextXAlignmentProp_ConditionalOptionsImpl.new(
		data: _TextXAlignmentProp_ConditionalOptionsPartialFields?
	): TextXAlignmentProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _TextXAlignmentProp_ConditionalOptionsImpl :: _TextXAlignmentProp_ConditionalOptionsImpl)
	end

	function _TextXAlignmentProp_ConditionalOptionsImpl.encode(self: TextXAlignmentProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextXAlignmentProp_ConditionalOptionsImpl.decode(input: buffer): TextXAlignmentProp_ConditionalOptions
		local self = _TextXAlignmentProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.TextXAlignmentProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextXAlignmentProp_ConditionalOptionsImpl.jsonEncode(self: TextXAlignmentProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _TextXAlignmentProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): TextXAlignmentProp_ConditionalOptions
		local self = _TextXAlignmentProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { TextXAlignmentProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.TextXAlignmentProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_TextXAlignmentProp_ConditionalOptionsImpl.descriptor = {
		name = "TextXAlignmentProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.TextXAlignmentProp_ConditionalOptions = _TextXAlignmentProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextXAlignmentProp_ConditionalOptions)
end

do
	local _TextYAlignmentPropImpl = {}
	_TextYAlignmentPropImpl.__index = _TextYAlignmentPropImpl

	function _TextYAlignmentPropImpl.new(data: _TextYAlignmentPropPartialFields?): TextYAlignmentProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _TextYAlignmentPropImpl :: _TextYAlignmentPropImpl)
	end

	function _TextYAlignmentPropImpl.encode(self: TextYAlignmentProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.TextYAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextYAlignmentPropImpl.decode(input: buffer): TextYAlignmentProp
		local self = _TextYAlignmentPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.TextYAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.TextYAlignmentProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextYAlignmentPropImpl.jsonEncode(self: TextYAlignmentProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.TextYAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _TextYAlignmentPropImpl.jsonDecode(input: { [string]: any }): TextYAlignmentProp
		local self = _TextYAlignmentPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.TextYAlignment.fromNumber(input.literal) or input.literal)
					else (messages.TextYAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.TextYAlignmentProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_TextYAlignmentPropImpl.descriptor = {
		name = "TextYAlignmentProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.TextYAlignmentProp",
	}

	messages.TextYAlignmentProp = _TextYAlignmentPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextYAlignmentProp)
end

do
	local _TextYAlignmentProp_ConditionalOptionImpl = {}
	_TextYAlignmentProp_ConditionalOptionImpl.__index = _TextYAlignmentProp_ConditionalOptionImpl

	function _TextYAlignmentProp_ConditionalOptionImpl.new(
		data: _TextYAlignmentProp_ConditionalOptionPartialFields?
	): TextYAlignmentProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _TextYAlignmentProp_ConditionalOptionImpl :: _TextYAlignmentProp_ConditionalOptionImpl)
	end

	function _TextYAlignmentProp_ConditionalOptionImpl.encode(self: TextYAlignmentProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.TextYAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextYAlignmentProp_ConditionalOptionImpl.decode(input: buffer): TextYAlignmentProp_ConditionalOption
		local self = _TextYAlignmentProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.TextYAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextYAlignmentProp_ConditionalOptionImpl.jsonEncode(self: TextYAlignmentProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.TextYAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _TextYAlignmentProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): TextYAlignmentProp_ConditionalOption
		local self = _TextYAlignmentProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.TextYAlignment.fromNumber(input.literal) or input.literal)
					else (messages.TextYAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_TextYAlignmentProp_ConditionalOptionImpl.descriptor = {
		name = "TextYAlignmentProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.TextYAlignmentProp_ConditionalOption = _TextYAlignmentProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextYAlignmentProp_ConditionalOption)
end

do
	local _TextYAlignmentProp_ConditionalOptionsImpl = {}
	_TextYAlignmentProp_ConditionalOptionsImpl.__index = _TextYAlignmentProp_ConditionalOptionsImpl

	function _TextYAlignmentProp_ConditionalOptionsImpl.new(
		data: _TextYAlignmentProp_ConditionalOptionsPartialFields?
	): TextYAlignmentProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _TextYAlignmentProp_ConditionalOptionsImpl :: _TextYAlignmentProp_ConditionalOptionsImpl)
	end

	function _TextYAlignmentProp_ConditionalOptionsImpl.encode(self: TextYAlignmentProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextYAlignmentProp_ConditionalOptionsImpl.decode(input: buffer): TextYAlignmentProp_ConditionalOptions
		local self = _TextYAlignmentProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.TextYAlignmentProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextYAlignmentProp_ConditionalOptionsImpl.jsonEncode(self: TextYAlignmentProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _TextYAlignmentProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): TextYAlignmentProp_ConditionalOptions
		local self = _TextYAlignmentProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { TextYAlignmentProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.TextYAlignmentProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_TextYAlignmentProp_ConditionalOptionsImpl.descriptor = {
		name = "TextYAlignmentProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.TextYAlignmentProp_ConditionalOptions = _TextYAlignmentProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextYAlignmentProp_ConditionalOptions)
end

do
	local _UIFlexModePropImpl = {}
	_UIFlexModePropImpl.__index = _UIFlexModePropImpl

	function _UIFlexModePropImpl.new(data: _UIFlexModePropPartialFields?): UIFlexModeProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _UIFlexModePropImpl :: _UIFlexModePropImpl)
	end

	function _UIFlexModePropImpl.encode(self: UIFlexModeProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, messages.UIFlexMode.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UIFlexModePropImpl.decode(input: buffer): UIFlexModeProp
		local self = _UIFlexModePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.UIFlexMode.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.UIFlexModeProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UIFlexModePropImpl.jsonEncode(self: UIFlexModeProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.UIFlexMode.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _UIFlexModePropImpl.jsonDecode(input: { [string]: any }): UIFlexModeProp
		local self = _UIFlexModePropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.UIFlexMode.fromNumber(input.literal) or input.literal)
					else (messages.UIFlexMode.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.UIFlexModeProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_UIFlexModePropImpl.descriptor = {
		name = "UIFlexModeProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.UIFlexModeProp",
	}

	messages.UIFlexModeProp = _UIFlexModePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UIFlexModeProp)
end

do
	local _UIFlexModeProp_ConditionalOptionImpl = {}
	_UIFlexModeProp_ConditionalOptionImpl.__index = _UIFlexModeProp_ConditionalOptionImpl

	function _UIFlexModeProp_ConditionalOptionImpl.new(
		data: _UIFlexModeProp_ConditionalOptionPartialFields?
	): UIFlexModeProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _UIFlexModeProp_ConditionalOptionImpl :: _UIFlexModeProp_ConditionalOptionImpl)
	end

	function _UIFlexModeProp_ConditionalOptionImpl.encode(self: UIFlexModeProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, messages.UIFlexMode.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UIFlexModeProp_ConditionalOptionImpl.decode(input: buffer): UIFlexModeProp_ConditionalOption
		local self = _UIFlexModeProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.UIFlexMode.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UIFlexModeProp_ConditionalOptionImpl.jsonEncode(self: UIFlexModeProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.UIFlexMode.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _UIFlexModeProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): UIFlexModeProp_ConditionalOption
		local self = _UIFlexModeProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.UIFlexMode.fromNumber(input.literal) or input.literal)
					else (messages.UIFlexMode.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_UIFlexModeProp_ConditionalOptionImpl.descriptor = {
		name = "UIFlexModeProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.UIFlexModeProp_ConditionalOption = _UIFlexModeProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UIFlexModeProp_ConditionalOption)
end

do
	local _UIFlexModeProp_ConditionalOptionsImpl = {}
	_UIFlexModeProp_ConditionalOptionsImpl.__index = _UIFlexModeProp_ConditionalOptionsImpl

	function _UIFlexModeProp_ConditionalOptionsImpl.new(
		data: _UIFlexModeProp_ConditionalOptionsPartialFields?
	): UIFlexModeProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _UIFlexModeProp_ConditionalOptionsImpl :: _UIFlexModeProp_ConditionalOptionsImpl)
	end

	function _UIFlexModeProp_ConditionalOptionsImpl.encode(self: UIFlexModeProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UIFlexModeProp_ConditionalOptionsImpl.decode(input: buffer): UIFlexModeProp_ConditionalOptions
		local self = _UIFlexModeProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.UIFlexModeProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UIFlexModeProp_ConditionalOptionsImpl.jsonEncode(self: UIFlexModeProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _UIFlexModeProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): UIFlexModeProp_ConditionalOptions
		local self = _UIFlexModeProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { UIFlexModeProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.UIFlexModeProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_UIFlexModeProp_ConditionalOptionsImpl.descriptor = {
		name = "UIFlexModeProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.UIFlexModeProp_ConditionalOptions = _UIFlexModeProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UIFlexModeProp_ConditionalOptions)
end

do
	local _ScaleTypePropImpl = {}
	_ScaleTypePropImpl.__index = _ScaleTypePropImpl

	function _ScaleTypePropImpl.new(data: _ScaleTypePropPartialFields?): ScaleTypeProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ScaleTypePropImpl :: _ScaleTypePropImpl)
	end

	function _ScaleTypePropImpl.encode(self: ScaleTypeProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, messages.ScaleType.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ScaleTypePropImpl.decode(input: buffer): ScaleTypeProp
		local self = _ScaleTypePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.ScaleType.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.ScaleTypeProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ScaleTypePropImpl.jsonEncode(self: ScaleTypeProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.ScaleType.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _ScaleTypePropImpl.jsonDecode(input: { [string]: any }): ScaleTypeProp
		local self = _ScaleTypePropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.ScaleType.fromNumber(input.literal) or input.literal)
					else (messages.ScaleType.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.ScaleTypeProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_ScaleTypePropImpl.descriptor = {
		name = "ScaleTypeProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.ScaleTypeProp",
	}

	messages.ScaleTypeProp = _ScaleTypePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ScaleTypeProp)
end

do
	local _ScaleTypeProp_ConditionalOptionImpl = {}
	_ScaleTypeProp_ConditionalOptionImpl.__index = _ScaleTypeProp_ConditionalOptionImpl

	function _ScaleTypeProp_ConditionalOptionImpl.new(
		data: _ScaleTypeProp_ConditionalOptionPartialFields?
	): ScaleTypeProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ScaleTypeProp_ConditionalOptionImpl :: _ScaleTypeProp_ConditionalOptionImpl)
	end

	function _ScaleTypeProp_ConditionalOptionImpl.encode(self: ScaleTypeProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, messages.ScaleType.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ScaleTypeProp_ConditionalOptionImpl.decode(input: buffer): ScaleTypeProp_ConditionalOption
		local self = _ScaleTypeProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.ScaleType.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ScaleTypeProp_ConditionalOptionImpl.jsonEncode(self: ScaleTypeProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.ScaleType.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _ScaleTypeProp_ConditionalOptionImpl.jsonDecode(input: { [string]: any }): ScaleTypeProp_ConditionalOption
		local self = _ScaleTypeProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.ScaleType.fromNumber(input.literal) or input.literal)
					else (messages.ScaleType.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_ScaleTypeProp_ConditionalOptionImpl.descriptor = {
		name = "ScaleTypeProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.ScaleTypeProp_ConditionalOption = _ScaleTypeProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ScaleTypeProp_ConditionalOption)
end

do
	local _ScaleTypeProp_ConditionalOptionsImpl = {}
	_ScaleTypeProp_ConditionalOptionsImpl.__index = _ScaleTypeProp_ConditionalOptionsImpl

	function _ScaleTypeProp_ConditionalOptionsImpl.new(
		data: _ScaleTypeProp_ConditionalOptionsPartialFields?
	): ScaleTypeProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _ScaleTypeProp_ConditionalOptionsImpl :: _ScaleTypeProp_ConditionalOptionsImpl)
	end

	function _ScaleTypeProp_ConditionalOptionsImpl.encode(self: ScaleTypeProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ScaleTypeProp_ConditionalOptionsImpl.decode(input: buffer): ScaleTypeProp_ConditionalOptions
		local self = _ScaleTypeProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.ScaleTypeProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ScaleTypeProp_ConditionalOptionsImpl.jsonEncode(self: ScaleTypeProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _ScaleTypeProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): ScaleTypeProp_ConditionalOptions
		local self = _ScaleTypeProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { ScaleTypeProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.ScaleTypeProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_ScaleTypeProp_ConditionalOptionsImpl.descriptor = {
		name = "ScaleTypeProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.ScaleTypeProp_ConditionalOptions = _ScaleTypeProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ScaleTypeProp_ConditionalOptions)
end

do
	local _ResamplerModePropImpl = {}
	_ResamplerModePropImpl.__index = _ResamplerModePropImpl

	function _ResamplerModePropImpl.new(data: _ResamplerModePropPartialFields?): ResamplerModeProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ResamplerModePropImpl :: _ResamplerModePropImpl)
	end

	function _ResamplerModePropImpl.encode(self: ResamplerModeProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.ResamplerMode.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ResamplerModePropImpl.decode(input: buffer): ResamplerModeProp
		local self = _ResamplerModePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.ResamplerMode.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.ResamplerModeProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ResamplerModePropImpl.jsonEncode(self: ResamplerModeProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.ResamplerMode.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _ResamplerModePropImpl.jsonDecode(input: { [string]: any }): ResamplerModeProp
		local self = _ResamplerModePropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.ResamplerMode.fromNumber(input.literal) or input.literal)
					else (messages.ResamplerMode.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.ResamplerModeProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_ResamplerModePropImpl.descriptor = {
		name = "ResamplerModeProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.ResamplerModeProp",
	}

	messages.ResamplerModeProp = _ResamplerModePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ResamplerModeProp)
end

do
	local _ResamplerModeProp_ConditionalOptionImpl = {}
	_ResamplerModeProp_ConditionalOptionImpl.__index = _ResamplerModeProp_ConditionalOptionImpl

	function _ResamplerModeProp_ConditionalOptionImpl.new(
		data: _ResamplerModeProp_ConditionalOptionPartialFields?
	): ResamplerModeProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _ResamplerModeProp_ConditionalOptionImpl :: _ResamplerModeProp_ConditionalOptionImpl)
	end

	function _ResamplerModeProp_ConditionalOptionImpl.encode(self: ResamplerModeProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.ResamplerMode.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ResamplerModeProp_ConditionalOptionImpl.decode(input: buffer): ResamplerModeProp_ConditionalOption
		local self = _ResamplerModeProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.ResamplerMode.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ResamplerModeProp_ConditionalOptionImpl.jsonEncode(self: ResamplerModeProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.ResamplerMode.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _ResamplerModeProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): ResamplerModeProp_ConditionalOption
		local self = _ResamplerModeProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.ResamplerMode.fromNumber(input.literal) or input.literal)
					else (messages.ResamplerMode.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_ResamplerModeProp_ConditionalOptionImpl.descriptor = {
		name = "ResamplerModeProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.ResamplerModeProp_ConditionalOption = _ResamplerModeProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ResamplerModeProp_ConditionalOption)
end

do
	local _ResamplerModeProp_ConditionalOptionsImpl = {}
	_ResamplerModeProp_ConditionalOptionsImpl.__index = _ResamplerModeProp_ConditionalOptionsImpl

	function _ResamplerModeProp_ConditionalOptionsImpl.new(
		data: _ResamplerModeProp_ConditionalOptionsPartialFields?
	): ResamplerModeProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _ResamplerModeProp_ConditionalOptionsImpl :: _ResamplerModeProp_ConditionalOptionsImpl)
	end

	function _ResamplerModeProp_ConditionalOptionsImpl.encode(self: ResamplerModeProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ResamplerModeProp_ConditionalOptionsImpl.decode(input: buffer): ResamplerModeProp_ConditionalOptions
		local self = _ResamplerModeProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.ResamplerModeProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ResamplerModeProp_ConditionalOptionsImpl.jsonEncode(self: ResamplerModeProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _ResamplerModeProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): ResamplerModeProp_ConditionalOptions
		local self = _ResamplerModeProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { ResamplerModeProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.ResamplerModeProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_ResamplerModeProp_ConditionalOptionsImpl.descriptor = {
		name = "ResamplerModeProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.ResamplerModeProp_ConditionalOptions = _ResamplerModeProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ResamplerModeProp_ConditionalOptions)
end

do
	local _SizeConstraintPropImpl = {}
	_SizeConstraintPropImpl.__index = _SizeConstraintPropImpl

	function _SizeConstraintPropImpl.new(data: _SizeConstraintPropPartialFields?): SizeConstraintProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _SizeConstraintPropImpl :: _SizeConstraintPropImpl)
	end

	function _SizeConstraintPropImpl.encode(self: SizeConstraintProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.SizeConstraint.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SizeConstraintPropImpl.decode(input: buffer): SizeConstraintProp
		local self = _SizeConstraintPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.SizeConstraint.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind =
						{ type = "conditional", value = messages.SizeConstraintProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SizeConstraintPropImpl.jsonEncode(self: SizeConstraintProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.SizeConstraint.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _SizeConstraintPropImpl.jsonDecode(input: { [string]: any }): SizeConstraintProp
		local self = _SizeConstraintPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.SizeConstraint.fromNumber(input.literal) or input.literal)
					else (messages.SizeConstraint.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.SizeConstraintProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_SizeConstraintPropImpl.descriptor = {
		name = "SizeConstraintProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.SizeConstraintProp",
	}

	messages.SizeConstraintProp = _SizeConstraintPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SizeConstraintProp)
end

do
	local _SizeConstraintProp_ConditionalOptionImpl = {}
	_SizeConstraintProp_ConditionalOptionImpl.__index = _SizeConstraintProp_ConditionalOptionImpl

	function _SizeConstraintProp_ConditionalOptionImpl.new(
		data: _SizeConstraintProp_ConditionalOptionPartialFields?
	): SizeConstraintProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _SizeConstraintProp_ConditionalOptionImpl :: _SizeConstraintProp_ConditionalOptionImpl)
	end

	function _SizeConstraintProp_ConditionalOptionImpl.encode(self: SizeConstraintProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.SizeConstraint.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SizeConstraintProp_ConditionalOptionImpl.decode(input: buffer): SizeConstraintProp_ConditionalOption
		local self = _SizeConstraintProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.SizeConstraint.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SizeConstraintProp_ConditionalOptionImpl.jsonEncode(self: SizeConstraintProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.SizeConstraint.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _SizeConstraintProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): SizeConstraintProp_ConditionalOption
		local self = _SizeConstraintProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.SizeConstraint.fromNumber(input.literal) or input.literal)
					else (messages.SizeConstraint.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_SizeConstraintProp_ConditionalOptionImpl.descriptor = {
		name = "SizeConstraintProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.SizeConstraintProp_ConditionalOption = _SizeConstraintProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SizeConstraintProp_ConditionalOption)
end

do
	local _SizeConstraintProp_ConditionalOptionsImpl = {}
	_SizeConstraintProp_ConditionalOptionsImpl.__index = _SizeConstraintProp_ConditionalOptionsImpl

	function _SizeConstraintProp_ConditionalOptionsImpl.new(
		data: _SizeConstraintProp_ConditionalOptionsPartialFields?
	): SizeConstraintProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _SizeConstraintProp_ConditionalOptionsImpl :: _SizeConstraintProp_ConditionalOptionsImpl)
	end

	function _SizeConstraintProp_ConditionalOptionsImpl.encode(self: SizeConstraintProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SizeConstraintProp_ConditionalOptionsImpl.decode(input: buffer): SizeConstraintProp_ConditionalOptions
		local self = _SizeConstraintProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.SizeConstraintProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SizeConstraintProp_ConditionalOptionsImpl.jsonEncode(self: SizeConstraintProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _SizeConstraintProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): SizeConstraintProp_ConditionalOptions
		local self = _SizeConstraintProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { SizeConstraintProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.SizeConstraintProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_SizeConstraintProp_ConditionalOptionsImpl.descriptor = {
		name = "SizeConstraintProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.SizeConstraintProp_ConditionalOptions = _SizeConstraintProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SizeConstraintProp_ConditionalOptions)
end

do
	local _HorizontalAlignmentPropImpl = {}
	_HorizontalAlignmentPropImpl.__index = _HorizontalAlignmentPropImpl

	function _HorizontalAlignmentPropImpl.new(data: _HorizontalAlignmentPropPartialFields?): HorizontalAlignmentProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _HorizontalAlignmentPropImpl :: _HorizontalAlignmentPropImpl)
	end

	function _HorizontalAlignmentPropImpl.encode(self: HorizontalAlignmentProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.HorizontalAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HorizontalAlignmentPropImpl.decode(input: buffer): HorizontalAlignmentProp
		local self = _HorizontalAlignmentPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.HorizontalAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "conditional",
						value = messages.HorizontalAlignmentProp_ConditionalOptions.decode(value),
					}
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HorizontalAlignmentPropImpl.jsonEncode(self: HorizontalAlignmentProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.HorizontalAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _HorizontalAlignmentPropImpl.jsonDecode(input: { [string]: any }): HorizontalAlignmentProp
		local self = _HorizontalAlignmentPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.HorizontalAlignment.fromNumber(input.literal) or input.literal)
					else (messages.HorizontalAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.HorizontalAlignmentProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_HorizontalAlignmentPropImpl.descriptor = {
		name = "HorizontalAlignmentProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.HorizontalAlignmentProp",
	}

	messages.HorizontalAlignmentProp = _HorizontalAlignmentPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HorizontalAlignmentProp)
end

do
	local _HorizontalAlignmentProp_ConditionalOptionImpl = {}
	_HorizontalAlignmentProp_ConditionalOptionImpl.__index = _HorizontalAlignmentProp_ConditionalOptionImpl

	function _HorizontalAlignmentProp_ConditionalOptionImpl.new(
		data: _HorizontalAlignmentProp_ConditionalOptionPartialFields?
	): HorizontalAlignmentProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _HorizontalAlignmentProp_ConditionalOptionImpl :: _HorizontalAlignmentProp_ConditionalOptionImpl)
	end

	function _HorizontalAlignmentProp_ConditionalOptionImpl.encode(
		self: HorizontalAlignmentProp_ConditionalOption
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.HorizontalAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HorizontalAlignmentProp_ConditionalOptionImpl.decode(
		input: buffer
	): HorizontalAlignmentProp_ConditionalOption
		local self = _HorizontalAlignmentProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.HorizontalAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HorizontalAlignmentProp_ConditionalOptionImpl.jsonEncode(
		self: HorizontalAlignmentProp_ConditionalOption
	): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.HorizontalAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _HorizontalAlignmentProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): HorizontalAlignmentProp_ConditionalOption
		local self = _HorizontalAlignmentProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.HorizontalAlignment.fromNumber(input.literal) or input.literal)
					else (messages.HorizontalAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_HorizontalAlignmentProp_ConditionalOptionImpl.descriptor = {
		name = "HorizontalAlignmentProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.HorizontalAlignmentProp_ConditionalOption = _HorizontalAlignmentProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HorizontalAlignmentProp_ConditionalOption)
end

do
	local _HorizontalAlignmentProp_ConditionalOptionsImpl = {}
	_HorizontalAlignmentProp_ConditionalOptionsImpl.__index = _HorizontalAlignmentProp_ConditionalOptionsImpl

	function _HorizontalAlignmentProp_ConditionalOptionsImpl.new(
		data: _HorizontalAlignmentProp_ConditionalOptionsPartialFields?
	): HorizontalAlignmentProp_ConditionalOptions
		return setmetatable({
			option = if data == nil or data.option == nil then {} else data.option,
		}, _HorizontalAlignmentProp_ConditionalOptionsImpl :: _HorizontalAlignmentProp_ConditionalOptionsImpl)
	end

	function _HorizontalAlignmentProp_ConditionalOptionsImpl.encode(
		self: HorizontalAlignmentProp_ConditionalOptions
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.option ~= nil and #self.option > 0 then
			for _, value in self.option do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HorizontalAlignmentProp_ConditionalOptionsImpl.decode(
		input: buffer
	): HorizontalAlignmentProp_ConditionalOptions
		local self = _HorizontalAlignmentProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.option, messages.HorizontalAlignmentProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HorizontalAlignmentProp_ConditionalOptionsImpl.jsonEncode(
		self: HorizontalAlignmentProp_ConditionalOptions
	): any
		local output = {}

		if self.option ~= nil and #self.option > 0 then
			local newOutput = {}
			for _, value in self.option do
				table.insert(newOutput, value:jsonEncode())
			end
			output.option = newOutput
		end

		return output
	end

	function _HorizontalAlignmentProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): HorizontalAlignmentProp_ConditionalOptions
		local self = _HorizontalAlignmentProp_ConditionalOptionsImpl.new()

		if input.option ~= nil then
			local newOutput: { HorizontalAlignmentProp_ConditionalOption } = {}
			for _, value in input.option do
				table.insert(newOutput, messages.HorizontalAlignmentProp_ConditionalOption.jsonDecode(value))
			end

			self.option = newOutput
		end

		return self
	end

	_HorizontalAlignmentProp_ConditionalOptionsImpl.descriptor = {
		name = "HorizontalAlignmentProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.HorizontalAlignmentProp_ConditionalOptions = _HorizontalAlignmentProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HorizontalAlignmentProp_ConditionalOptions)
end

do
	local _SelectionBehaviorPropImpl = {}
	_SelectionBehaviorPropImpl.__index = _SelectionBehaviorPropImpl

	function _SelectionBehaviorPropImpl.new(data: _SelectionBehaviorPropPartialFields?): SelectionBehaviorProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _SelectionBehaviorPropImpl :: _SelectionBehaviorPropImpl)
	end

	function _SelectionBehaviorPropImpl.encode(self: SelectionBehaviorProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.SelectionBehavior.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SelectionBehaviorPropImpl.decode(input: buffer): SelectionBehaviorProp
		local self = _SelectionBehaviorPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.SelectionBehavior.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "conditional",
						value = messages.SelectionBehaviorProp_ConditionalOptions.decode(value),
					}
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SelectionBehaviorPropImpl.jsonEncode(self: SelectionBehaviorProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.SelectionBehavior.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _SelectionBehaviorPropImpl.jsonDecode(input: { [string]: any }): SelectionBehaviorProp
		local self = _SelectionBehaviorPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.SelectionBehavior.fromNumber(input.literal) or input.literal)
					else (messages.SelectionBehavior.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.SelectionBehaviorProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_SelectionBehaviorPropImpl.descriptor = {
		name = "SelectionBehaviorProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.SelectionBehaviorProp",
	}

	messages.SelectionBehaviorProp = _SelectionBehaviorPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SelectionBehaviorProp)
end

do
	local _SelectionBehaviorProp_ConditionalOptionImpl = {}
	_SelectionBehaviorProp_ConditionalOptionImpl.__index = _SelectionBehaviorProp_ConditionalOptionImpl

	function _SelectionBehaviorProp_ConditionalOptionImpl.new(
		data: _SelectionBehaviorProp_ConditionalOptionPartialFields?
	): SelectionBehaviorProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _SelectionBehaviorProp_ConditionalOptionImpl :: _SelectionBehaviorProp_ConditionalOptionImpl)
	end

	function _SelectionBehaviorProp_ConditionalOptionImpl.encode(self: SelectionBehaviorProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.SelectionBehavior.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SelectionBehaviorProp_ConditionalOptionImpl.decode(input: buffer): SelectionBehaviorProp_ConditionalOption
		local self = _SelectionBehaviorProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.SelectionBehavior.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SelectionBehaviorProp_ConditionalOptionImpl.jsonEncode(self: SelectionBehaviorProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.SelectionBehavior.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _SelectionBehaviorProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): SelectionBehaviorProp_ConditionalOption
		local self = _SelectionBehaviorProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.SelectionBehavior.fromNumber(input.literal) or input.literal)
					else (messages.SelectionBehavior.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_SelectionBehaviorProp_ConditionalOptionImpl.descriptor = {
		name = "SelectionBehaviorProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.SelectionBehaviorProp_ConditionalOption = _SelectionBehaviorProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SelectionBehaviorProp_ConditionalOption)
end

do
	local _SelectionBehaviorProp_ConditionalOptionsImpl = {}
	_SelectionBehaviorProp_ConditionalOptionsImpl.__index = _SelectionBehaviorProp_ConditionalOptionsImpl

	function _SelectionBehaviorProp_ConditionalOptionsImpl.new(
		data: _SelectionBehaviorProp_ConditionalOptionsPartialFields?
	): SelectionBehaviorProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _SelectionBehaviorProp_ConditionalOptionsImpl :: _SelectionBehaviorProp_ConditionalOptionsImpl)
	end

	function _SelectionBehaviorProp_ConditionalOptionsImpl.encode(
		self: SelectionBehaviorProp_ConditionalOptions
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SelectionBehaviorProp_ConditionalOptionsImpl.decode(
		input: buffer
	): SelectionBehaviorProp_ConditionalOptions
		local self = _SelectionBehaviorProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.SelectionBehaviorProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SelectionBehaviorProp_ConditionalOptionsImpl.jsonEncode(
		self: SelectionBehaviorProp_ConditionalOptions
	): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _SelectionBehaviorProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): SelectionBehaviorProp_ConditionalOptions
		local self = _SelectionBehaviorProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { SelectionBehaviorProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.SelectionBehaviorProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_SelectionBehaviorProp_ConditionalOptionsImpl.descriptor = {
		name = "SelectionBehaviorProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.SelectionBehaviorProp_ConditionalOptions = _SelectionBehaviorProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SelectionBehaviorProp_ConditionalOptions)
end

do
	local _KeyCodePropImpl = {}
	_KeyCodePropImpl.__index = _KeyCodePropImpl

	function _KeyCodePropImpl.new(data: _KeyCodePropPartialFields?): KeyCodeProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _KeyCodePropImpl :: _KeyCodePropImpl)
	end

	function _KeyCodePropImpl.encode(self: KeyCodeProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, messages.KeyCode.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _KeyCodePropImpl.decode(input: buffer): KeyCodeProp
		local self = _KeyCodePropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.KeyCode.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "conditional", value = messages.KeyCodeProp_ConditionalOptions.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _KeyCodePropImpl.jsonEncode(self: KeyCodeProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.KeyCode.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _KeyCodePropImpl.jsonDecode(input: { [string]: any }): KeyCodeProp
		local self = _KeyCodePropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.KeyCode.fromNumber(input.literal) or input.literal)
					else (messages.KeyCode.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind =
				{ type = "conditional", value = messages.KeyCodeProp_ConditionalOptions.jsonDecode(input.conditional) }
		end

		return self
	end

	_KeyCodePropImpl.descriptor = {
		name = "KeyCodeProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.KeyCodeProp",
	}

	messages.KeyCodeProp = _KeyCodePropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.KeyCodeProp)
end

do
	local _KeyCodeProp_ConditionalOptionImpl = {}
	_KeyCodeProp_ConditionalOptionImpl.__index = _KeyCodeProp_ConditionalOptionImpl

	function _KeyCodeProp_ConditionalOptionImpl.new(
		data: _KeyCodeProp_ConditionalOptionPartialFields?
	): KeyCodeProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _KeyCodeProp_ConditionalOptionImpl :: _KeyCodeProp_ConditionalOptionImpl)
	end

	function _KeyCodeProp_ConditionalOptionImpl.encode(self: KeyCodeProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, messages.KeyCode.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _KeyCodeProp_ConditionalOptionImpl.decode(input: buffer): KeyCodeProp_ConditionalOption
		local self = _KeyCodeProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.KeyCode.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _KeyCodeProp_ConditionalOptionImpl.jsonEncode(self: KeyCodeProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.KeyCode.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _KeyCodeProp_ConditionalOptionImpl.jsonDecode(input: { [string]: any }): KeyCodeProp_ConditionalOption
		local self = _KeyCodeProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.KeyCode.fromNumber(input.literal) or input.literal)
					else (messages.KeyCode.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_KeyCodeProp_ConditionalOptionImpl.descriptor = {
		name = "KeyCodeProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.KeyCodeProp_ConditionalOption = _KeyCodeProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.KeyCodeProp_ConditionalOption)
end

do
	local _KeyCodeProp_ConditionalOptionsImpl = {}
	_KeyCodeProp_ConditionalOptionsImpl.__index = _KeyCodeProp_ConditionalOptionsImpl

	function _KeyCodeProp_ConditionalOptionsImpl.new(
		data: _KeyCodeProp_ConditionalOptionsPartialFields?
	): KeyCodeProp_ConditionalOptions
		return setmetatable({
			options = if data == nil or data.options == nil then {} else data.options,
		}, _KeyCodeProp_ConditionalOptionsImpl :: _KeyCodeProp_ConditionalOptionsImpl)
	end

	function _KeyCodeProp_ConditionalOptionsImpl.encode(self: KeyCodeProp_ConditionalOptions): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.options ~= nil and #self.options > 0 then
			for _, value in self.options do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _KeyCodeProp_ConditionalOptionsImpl.decode(input: buffer): KeyCodeProp_ConditionalOptions
		local self = _KeyCodeProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.options, messages.KeyCodeProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _KeyCodeProp_ConditionalOptionsImpl.jsonEncode(self: KeyCodeProp_ConditionalOptions): any
		local output = {}

		if self.options ~= nil and #self.options > 0 then
			local newOutput = {}
			for _, value in self.options do
				table.insert(newOutput, value:jsonEncode())
			end
			output.options = newOutput
		end

		return output
	end

	function _KeyCodeProp_ConditionalOptionsImpl.jsonDecode(input: { [string]: any }): KeyCodeProp_ConditionalOptions
		local self = _KeyCodeProp_ConditionalOptionsImpl.new()

		if input.options ~= nil then
			local newOutput: { KeyCodeProp_ConditionalOption } = {}
			for _, value in input.options do
				table.insert(newOutput, messages.KeyCodeProp_ConditionalOption.jsonDecode(value))
			end

			self.options = newOutput
		end

		return self
	end

	_KeyCodeProp_ConditionalOptionsImpl.descriptor = {
		name = "KeyCodeProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.KeyCodeProp_ConditionalOptions = _KeyCodeProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.KeyCodeProp_ConditionalOptions)
end

do
	local _VerticalAlignmentPropImpl = {}
	_VerticalAlignmentPropImpl.__index = _VerticalAlignmentPropImpl

	function _VerticalAlignmentPropImpl.new(data: _VerticalAlignmentPropPartialFields?): VerticalAlignmentProp
		return setmetatable({
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _VerticalAlignmentPropImpl :: _VerticalAlignmentPropImpl)
	end

	function _VerticalAlignmentPropImpl.encode(self: VerticalAlignmentProp): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.VerticalAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			elseif self.kind.type == "conditional" then
				local encoded = self.kind.value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VerticalAlignmentPropImpl.decode(input: buffer): VerticalAlignmentProp
		local self = _VerticalAlignmentPropImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.VerticalAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = {
						type = "conditional",
						value = messages.VerticalAlignmentProp_ConditionalOptions.decode(value),
					}
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VerticalAlignmentPropImpl.jsonEncode(self: VerticalAlignmentProp): any
		local output = {}

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.VerticalAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			elseif self.kind.type == "conditional" then
				output.conditional = self.kind.value:jsonEncode()
			end
		end

		return output
	end

	function _VerticalAlignmentPropImpl.jsonDecode(input: { [string]: any }): VerticalAlignmentProp
		local self = _VerticalAlignmentPropImpl.new()

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.VerticalAlignment.fromNumber(input.literal) or input.literal)
					else (messages.VerticalAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		if input.conditional ~= nil then
			self.kind = {
				type = "conditional",
				value = messages.VerticalAlignmentProp_ConditionalOptions.jsonDecode(input.conditional),
			}
		end

		return self
	end

	_VerticalAlignmentPropImpl.descriptor = {
		name = "VerticalAlignmentProp",
		fullName = "roblox.apppageplatform.shared.v1beta1.VerticalAlignmentProp",
	}

	messages.VerticalAlignmentProp = _VerticalAlignmentPropImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VerticalAlignmentProp)
end

do
	local _VerticalAlignmentProp_ConditionalOptionImpl = {}
	_VerticalAlignmentProp_ConditionalOptionImpl.__index = _VerticalAlignmentProp_ConditionalOptionImpl

	function _VerticalAlignmentProp_ConditionalOptionImpl.new(
		data: _VerticalAlignmentProp_ConditionalOptionPartialFields?
	): VerticalAlignmentProp_ConditionalOption
		return setmetatable({
			condition = if data == nil or data.condition == nil then nil else data.condition,
			kind = if data == nil or data.kind == nil then nil else data.kind,
		}, _VerticalAlignmentProp_ConditionalOptionImpl :: _VerticalAlignmentProp_ConditionalOptionImpl)
	end

	function _VerticalAlignmentProp_ConditionalOptionImpl.encode(self: VerticalAlignmentProp_ConditionalOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.condition ~= nil then
			local encoded = self.condition:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.VerticalAlignment.toNumber(self.kind.value :: any))
			elseif self.kind.type == "binding_path" then
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, self.kind.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VerticalAlignmentProp_ConditionalOptionImpl.decode(input: buffer): VerticalAlignmentProp_ConditionalOption
		local self = _VerticalAlignmentProp_ConditionalOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.kind = {
						type = "literal",
						value = (messages.VerticalAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.condition = _roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.kind = { type = "binding_path", value = buffer.tostring(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VerticalAlignmentProp_ConditionalOptionImpl.jsonEncode(self: VerticalAlignmentProp_ConditionalOption): any
		local output = {}

		if self.condition ~= nil then
			output.condition = self.condition:jsonEncode()
		end

		if self.kind ~= nil then
			if self.kind.type == "literal" then
				output.literal = if typeof(self.kind.value) == "number"
					then self.kind.value
					else messages.VerticalAlignment.toNumber(self.kind.value :: any)
			elseif self.kind.type == "binding_path" then
				output.bindingPath = self.kind.value
			end
		end

		return output
	end

	function _VerticalAlignmentProp_ConditionalOptionImpl.jsonDecode(
		input: { [string]: any }
	): VerticalAlignmentProp_ConditionalOption
		local self = _VerticalAlignmentProp_ConditionalOptionImpl.new()

		if input.condition ~= nil then
			self.condition =
				_roblox_apppageplatform_shared_v1beta1_prop_condition.PropCondition.jsonDecode(input.condition)
		end

		if input.literal ~= nil then
			self.kind = {
				type = "literal",
				value = if typeof(input.literal) == "number"
					then (messages.VerticalAlignment.fromNumber(input.literal) or input.literal)
					else (messages.VerticalAlignment.fromName(input.literal) or input.literal),
			}
		end

		if input.binding_path ~= nil then
			self.kind = { type = "binding_path", value = input.binding_path }
		end

		if input.bindingPath ~= nil then
			self.kind = { type = "binding_path", value = input.bindingPath }
		end

		return self
	end

	_VerticalAlignmentProp_ConditionalOptionImpl.descriptor = {
		name = "VerticalAlignmentProp_ConditionalOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOption",
	}

	messages.VerticalAlignmentProp_ConditionalOption = _VerticalAlignmentProp_ConditionalOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VerticalAlignmentProp_ConditionalOption)
end

do
	local _VerticalAlignmentProp_ConditionalOptionsImpl = {}
	_VerticalAlignmentProp_ConditionalOptionsImpl.__index = _VerticalAlignmentProp_ConditionalOptionsImpl

	function _VerticalAlignmentProp_ConditionalOptionsImpl.new(
		data: _VerticalAlignmentProp_ConditionalOptionsPartialFields?
	): VerticalAlignmentProp_ConditionalOptions
		return setmetatable({
			option = if data == nil or data.option == nil then {} else data.option,
		}, _VerticalAlignmentProp_ConditionalOptionsImpl :: _VerticalAlignmentProp_ConditionalOptionsImpl)
	end

	function _VerticalAlignmentProp_ConditionalOptionsImpl.encode(
		self: VerticalAlignmentProp_ConditionalOptions
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.option ~= nil and #self.option > 0 then
			for _, value in self.option do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VerticalAlignmentProp_ConditionalOptionsImpl.decode(
		input: buffer
	): VerticalAlignmentProp_ConditionalOptions
		local self = _VerticalAlignmentProp_ConditionalOptionsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.option, messages.VerticalAlignmentProp_ConditionalOption.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VerticalAlignmentProp_ConditionalOptionsImpl.jsonEncode(
		self: VerticalAlignmentProp_ConditionalOptions
	): any
		local output = {}

		if self.option ~= nil and #self.option > 0 then
			local newOutput = {}
			for _, value in self.option do
				table.insert(newOutput, value:jsonEncode())
			end
			output.option = newOutput
		end

		return output
	end

	function _VerticalAlignmentProp_ConditionalOptionsImpl.jsonDecode(
		input: { [string]: any }
	): VerticalAlignmentProp_ConditionalOptions
		local self = _VerticalAlignmentProp_ConditionalOptionsImpl.new()

		if input.option ~= nil then
			local newOutput: { VerticalAlignmentProp_ConditionalOption } = {}
			for _, value in input.option do
				table.insert(newOutput, messages.VerticalAlignmentProp_ConditionalOption.jsonDecode(value))
			end

			self.option = newOutput
		end

		return self
	end

	_VerticalAlignmentProp_ConditionalOptionsImpl.descriptor = {
		name = "VerticalAlignmentProp_ConditionalOptions",
		fullName = "roblox.apppageplatform.shared.v1beta1.ConditionalOptions",
	}

	messages.VerticalAlignmentProp_ConditionalOptions = _VerticalAlignmentProp_ConditionalOptionsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VerticalAlignmentProp_ConditionalOptions)
end

messages.AutomaticSize = {
	fromNumber = function(value: number): AutomaticSize?
		if value == 0 then
			return "AUTOMATIC_SIZE_INVALID"
		elseif value == 1 then
			return "AUTOMATIC_SIZE_NONE"
		elseif value == 2 then
			return "AUTOMATIC_SIZE_X"
		elseif value == 3 then
			return "AUTOMATIC_SIZE_Y"
		elseif value == 4 then
			return "AUTOMATIC_SIZE_XY"
		else
			return nil
		end
	end,

	toNumber = function(self: AutomaticSize): number
		if self == "AUTOMATIC_SIZE_INVALID" then
			return 0
		elseif self == "AUTOMATIC_SIZE_NONE" then
			return 1
		elseif self == "AUTOMATIC_SIZE_X" then
			return 2
		elseif self == "AUTOMATIC_SIZE_Y" then
			return 3
		elseif self == "AUTOMATIC_SIZE_XY" then
			return 4
		else
			return self
		end
	end,

	fromName = function(name: string): AutomaticSize?
		if name == "AUTOMATIC_SIZE_INVALID" then
			return "AUTOMATIC_SIZE_INVALID"
		elseif name == "AUTOMATIC_SIZE_NONE" then
			return "AUTOMATIC_SIZE_NONE"
		elseif name == "AUTOMATIC_SIZE_X" then
			return "AUTOMATIC_SIZE_X"
		elseif name == "AUTOMATIC_SIZE_Y" then
			return "AUTOMATIC_SIZE_Y"
		elseif name == "AUTOMATIC_SIZE_XY" then
			return "AUTOMATIC_SIZE_XY"
		else
			return nil
		end
	end,
}

messages.TextTruncate = {
	fromNumber = function(value: number): TextTruncate?
		if value == 0 then
			return "TEXT_TRUNCATE_INVALID"
		elseif value == 1 then
			return "TEXT_TRUNCATE_NONE"
		elseif value == 2 then
			return "TEXT_TRUNCATE_AT_END"
		elseif value == 3 then
			return "TEXT_TRUNCATE_SPLIT_WORD"
		else
			return nil
		end
	end,

	toNumber = function(self: TextTruncate): number
		if self == "TEXT_TRUNCATE_INVALID" then
			return 0
		elseif self == "TEXT_TRUNCATE_NONE" then
			return 1
		elseif self == "TEXT_TRUNCATE_AT_END" then
			return 2
		elseif self == "TEXT_TRUNCATE_SPLIT_WORD" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): TextTruncate?
		if name == "TEXT_TRUNCATE_INVALID" then
			return "TEXT_TRUNCATE_INVALID"
		elseif name == "TEXT_TRUNCATE_NONE" then
			return "TEXT_TRUNCATE_NONE"
		elseif name == "TEXT_TRUNCATE_AT_END" then
			return "TEXT_TRUNCATE_AT_END"
		elseif name == "TEXT_TRUNCATE_SPLIT_WORD" then
			return "TEXT_TRUNCATE_SPLIT_WORD"
		else
			return nil
		end
	end,
}

messages.TextXAlignment = {
	fromNumber = function(value: number): TextXAlignment?
		if value == 0 then
			return "TEXT_X_ALIGNMENT_INVALID"
		elseif value == 1 then
			return "TEXT_X_ALIGNMENT_LEFT"
		elseif value == 2 then
			return "TEXT_X_ALIGNMENT_CENTER"
		elseif value == 3 then
			return "TEXT_X_ALIGNMENT_RIGHT"
		else
			return nil
		end
	end,

	toNumber = function(self: TextXAlignment): number
		if self == "TEXT_X_ALIGNMENT_INVALID" then
			return 0
		elseif self == "TEXT_X_ALIGNMENT_LEFT" then
			return 1
		elseif self == "TEXT_X_ALIGNMENT_CENTER" then
			return 2
		elseif self == "TEXT_X_ALIGNMENT_RIGHT" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): TextXAlignment?
		if name == "TEXT_X_ALIGNMENT_INVALID" then
			return "TEXT_X_ALIGNMENT_INVALID"
		elseif name == "TEXT_X_ALIGNMENT_LEFT" then
			return "TEXT_X_ALIGNMENT_LEFT"
		elseif name == "TEXT_X_ALIGNMENT_CENTER" then
			return "TEXT_X_ALIGNMENT_CENTER"
		elseif name == "TEXT_X_ALIGNMENT_RIGHT" then
			return "TEXT_X_ALIGNMENT_RIGHT"
		else
			return nil
		end
	end,
}

messages.TextYAlignment = {
	fromNumber = function(value: number): TextYAlignment?
		if value == 0 then
			return "TEXT_Y_ALIGNMENT_INVALID"
		elseif value == 1 then
			return "TEXT_Y_ALIGNMENT_TOP"
		elseif value == 2 then
			return "TEXT_Y_ALIGNMENT_CENTER"
		elseif value == 3 then
			return "TEXT_Y_ALIGNMENT_BOTTOM"
		else
			return nil
		end
	end,

	toNumber = function(self: TextYAlignment): number
		if self == "TEXT_Y_ALIGNMENT_INVALID" then
			return 0
		elseif self == "TEXT_Y_ALIGNMENT_TOP" then
			return 1
		elseif self == "TEXT_Y_ALIGNMENT_CENTER" then
			return 2
		elseif self == "TEXT_Y_ALIGNMENT_BOTTOM" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): TextYAlignment?
		if name == "TEXT_Y_ALIGNMENT_INVALID" then
			return "TEXT_Y_ALIGNMENT_INVALID"
		elseif name == "TEXT_Y_ALIGNMENT_TOP" then
			return "TEXT_Y_ALIGNMENT_TOP"
		elseif name == "TEXT_Y_ALIGNMENT_CENTER" then
			return "TEXT_Y_ALIGNMENT_CENTER"
		elseif name == "TEXT_Y_ALIGNMENT_BOTTOM" then
			return "TEXT_Y_ALIGNMENT_BOTTOM"
		else
			return nil
		end
	end,
}

messages.UIFlexMode = {
	fromNumber = function(value: number): UIFlexMode?
		if value == 0 then
			return "UI_FLEX_MODE_INVALID"
		elseif value == 1 then
			return "UI_FLEX_MODE_NONE"
		elseif value == 2 then
			return "UI_FLEX_MODE_GROW"
		elseif value == 3 then
			return "UI_FLEX_MODE_SHRINK"
		elseif value == 4 then
			return "UI_FLEX_MODE_FILL"
		elseif value == 5 then
			return "UI_FLEX_MODE_CUSTOM"
		else
			return nil
		end
	end,

	toNumber = function(self: UIFlexMode): number
		if self == "UI_FLEX_MODE_INVALID" then
			return 0
		elseif self == "UI_FLEX_MODE_NONE" then
			return 1
		elseif self == "UI_FLEX_MODE_GROW" then
			return 2
		elseif self == "UI_FLEX_MODE_SHRINK" then
			return 3
		elseif self == "UI_FLEX_MODE_FILL" then
			return 4
		elseif self == "UI_FLEX_MODE_CUSTOM" then
			return 5
		else
			return self
		end
	end,

	fromName = function(name: string): UIFlexMode?
		if name == "UI_FLEX_MODE_INVALID" then
			return "UI_FLEX_MODE_INVALID"
		elseif name == "UI_FLEX_MODE_NONE" then
			return "UI_FLEX_MODE_NONE"
		elseif name == "UI_FLEX_MODE_GROW" then
			return "UI_FLEX_MODE_GROW"
		elseif name == "UI_FLEX_MODE_SHRINK" then
			return "UI_FLEX_MODE_SHRINK"
		elseif name == "UI_FLEX_MODE_FILL" then
			return "UI_FLEX_MODE_FILL"
		elseif name == "UI_FLEX_MODE_CUSTOM" then
			return "UI_FLEX_MODE_CUSTOM"
		else
			return nil
		end
	end,
}

messages.ScaleType = {
	fromNumber = function(value: number): ScaleType?
		if value == 0 then
			return "SCALE_TYPE_INVALID"
		elseif value == 1 then
			return "SCALE_TYPE_STRETCH"
		elseif value == 2 then
			return "SCALE_TYPE_SLICE"
		elseif value == 3 then
			return "SCALE_TYPE_TILE"
		elseif value == 4 then
			return "SCALE_TYPE_FIT"
		elseif value == 5 then
			return "SCALE_TYPE_CROP"
		else
			return nil
		end
	end,

	toNumber = function(self: ScaleType): number
		if self == "SCALE_TYPE_INVALID" then
			return 0
		elseif self == "SCALE_TYPE_STRETCH" then
			return 1
		elseif self == "SCALE_TYPE_SLICE" then
			return 2
		elseif self == "SCALE_TYPE_TILE" then
			return 3
		elseif self == "SCALE_TYPE_FIT" then
			return 4
		elseif self == "SCALE_TYPE_CROP" then
			return 5
		else
			return self
		end
	end,

	fromName = function(name: string): ScaleType?
		if name == "SCALE_TYPE_INVALID" then
			return "SCALE_TYPE_INVALID"
		elseif name == "SCALE_TYPE_STRETCH" then
			return "SCALE_TYPE_STRETCH"
		elseif name == "SCALE_TYPE_SLICE" then
			return "SCALE_TYPE_SLICE"
		elseif name == "SCALE_TYPE_TILE" then
			return "SCALE_TYPE_TILE"
		elseif name == "SCALE_TYPE_FIT" then
			return "SCALE_TYPE_FIT"
		elseif name == "SCALE_TYPE_CROP" then
			return "SCALE_TYPE_CROP"
		else
			return nil
		end
	end,
}

messages.ResamplerMode = {
	fromNumber = function(value: number): ResamplerMode?
		if value == 0 then
			return "RESAMPLER_MODE_INVALID"
		elseif value == 1 then
			return "RESAMPLER_MODE_DEFAULT"
		elseif value == 2 then
			return "RESAMPLER_MODE_PIXELATED"
		else
			return nil
		end
	end,

	toNumber = function(self: ResamplerMode): number
		if self == "RESAMPLER_MODE_INVALID" then
			return 0
		elseif self == "RESAMPLER_MODE_DEFAULT" then
			return 1
		elseif self == "RESAMPLER_MODE_PIXELATED" then
			return 2
		else
			return self
		end
	end,

	fromName = function(name: string): ResamplerMode?
		if name == "RESAMPLER_MODE_INVALID" then
			return "RESAMPLER_MODE_INVALID"
		elseif name == "RESAMPLER_MODE_DEFAULT" then
			return "RESAMPLER_MODE_DEFAULT"
		elseif name == "RESAMPLER_MODE_PIXELATED" then
			return "RESAMPLER_MODE_PIXELATED"
		else
			return nil
		end
	end,
}

messages.SizeConstraint = {
	fromNumber = function(value: number): SizeConstraint?
		if value == 0 then
			return "SIZE_CONSTRAINT_INVALID"
		elseif value == 1 then
			return "SIZE_CONSTRAINT_RELATIVE_XY"
		elseif value == 2 then
			return "SIZE_CONSTRAINT_RELATIVE_XX"
		elseif value == 3 then
			return "SIZE_CONSTRAINT_RELATIVE_YY"
		else
			return nil
		end
	end,

	toNumber = function(self: SizeConstraint): number
		if self == "SIZE_CONSTRAINT_INVALID" then
			return 0
		elseif self == "SIZE_CONSTRAINT_RELATIVE_XY" then
			return 1
		elseif self == "SIZE_CONSTRAINT_RELATIVE_XX" then
			return 2
		elseif self == "SIZE_CONSTRAINT_RELATIVE_YY" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): SizeConstraint?
		if name == "SIZE_CONSTRAINT_INVALID" then
			return "SIZE_CONSTRAINT_INVALID"
		elseif name == "SIZE_CONSTRAINT_RELATIVE_XY" then
			return "SIZE_CONSTRAINT_RELATIVE_XY"
		elseif name == "SIZE_CONSTRAINT_RELATIVE_XX" then
			return "SIZE_CONSTRAINT_RELATIVE_XX"
		elseif name == "SIZE_CONSTRAINT_RELATIVE_YY" then
			return "SIZE_CONSTRAINT_RELATIVE_YY"
		else
			return nil
		end
	end,
}

messages.HorizontalAlignment = {
	fromNumber = function(value: number): HorizontalAlignment?
		if value == 0 then
			return "HORIZONTAL_ALIGNMENT_INVALID"
		elseif value == 1 then
			return "HORIZONTAL_ALIGNMENT_LEFT"
		elseif value == 2 then
			return "HORIZONTAL_ALIGNMENT_CENTER"
		elseif value == 3 then
			return "HORIZONTAL_ALIGNMENT_RIGHT"
		else
			return nil
		end
	end,

	toNumber = function(self: HorizontalAlignment): number
		if self == "HORIZONTAL_ALIGNMENT_INVALID" then
			return 0
		elseif self == "HORIZONTAL_ALIGNMENT_LEFT" then
			return 1
		elseif self == "HORIZONTAL_ALIGNMENT_CENTER" then
			return 2
		elseif self == "HORIZONTAL_ALIGNMENT_RIGHT" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): HorizontalAlignment?
		if name == "HORIZONTAL_ALIGNMENT_INVALID" then
			return "HORIZONTAL_ALIGNMENT_INVALID"
		elseif name == "HORIZONTAL_ALIGNMENT_LEFT" then
			return "HORIZONTAL_ALIGNMENT_LEFT"
		elseif name == "HORIZONTAL_ALIGNMENT_CENTER" then
			return "HORIZONTAL_ALIGNMENT_CENTER"
		elseif name == "HORIZONTAL_ALIGNMENT_RIGHT" then
			return "HORIZONTAL_ALIGNMENT_RIGHT"
		else
			return nil
		end
	end,
}

messages.VerticalAlignment = {
	fromNumber = function(value: number): VerticalAlignment?
		if value == 0 then
			return "VERTICAL_ALIGNMENT_INVALID"
		elseif value == 1 then
			return "VERTICAL_ALIGNMENT_TOP"
		elseif value == 2 then
			return "VERTICAL_ALIGNMENT_CENTER"
		elseif value == 3 then
			return "VERTICAL_ALIGNMENT_BOTTOM"
		else
			return nil
		end
	end,

	toNumber = function(self: VerticalAlignment): number
		if self == "VERTICAL_ALIGNMENT_INVALID" then
			return 0
		elseif self == "VERTICAL_ALIGNMENT_TOP" then
			return 1
		elseif self == "VERTICAL_ALIGNMENT_CENTER" then
			return 2
		elseif self == "VERTICAL_ALIGNMENT_BOTTOM" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): VerticalAlignment?
		if name == "VERTICAL_ALIGNMENT_INVALID" then
			return "VERTICAL_ALIGNMENT_INVALID"
		elseif name == "VERTICAL_ALIGNMENT_TOP" then
			return "VERTICAL_ALIGNMENT_TOP"
		elseif name == "VERTICAL_ALIGNMENT_CENTER" then
			return "VERTICAL_ALIGNMENT_CENTER"
		elseif name == "VERTICAL_ALIGNMENT_BOTTOM" then
			return "VERTICAL_ALIGNMENT_BOTTOM"
		else
			return nil
		end
	end,
}

messages.SelectionBehavior = {
	fromNumber = function(value: number): SelectionBehavior?
		if value == 0 then
			return "SELECTION_BEHAVIOR_INVALID"
		elseif value == 1 then
			return "SELECTION_BEHAVIOR_ESCAPE"
		elseif value == 2 then
			return "SELECTION_BEHAVIOR_STOP"
		else
			return nil
		end
	end,

	toNumber = function(self: SelectionBehavior): number
		if self == "SELECTION_BEHAVIOR_INVALID" then
			return 0
		elseif self == "SELECTION_BEHAVIOR_ESCAPE" then
			return 1
		elseif self == "SELECTION_BEHAVIOR_STOP" then
			return 2
		else
			return self
		end
	end,

	fromName = function(name: string): SelectionBehavior?
		if name == "SELECTION_BEHAVIOR_INVALID" then
			return "SELECTION_BEHAVIOR_INVALID"
		elseif name == "SELECTION_BEHAVIOR_ESCAPE" then
			return "SELECTION_BEHAVIOR_ESCAPE"
		elseif name == "SELECTION_BEHAVIOR_STOP" then
			return "SELECTION_BEHAVIOR_STOP"
		else
			return nil
		end
	end,
}

messages.KeyCode = {
	fromNumber = function(value: number): KeyCode?
		if value == 0 then
			return "KEY_CODE_INVALID"
		elseif value == 1 then
			return "KEY_CODE_BUTTON_A"
		elseif value == 2 then
			return "KEY_CODE_BUTTON_B"
		elseif value == 3 then
			return "KEY_CODE_BUTTON_X"
		elseif value == 4 then
			return "KEY_CODE_BUTTON_Y"
		elseif value == 5 then
			return "KEY_CODE_BUTTON_L1"
		elseif value == 6 then
			return "KEY_CODE_BUTTON_R1"
		elseif value == 7 then
			return "KEY_CODE_BUTTON_L2"
		elseif value == 8 then
			return "KEY_CODE_BUTTON_R2"
		elseif value == 9 then
			return "KEY_CODE_BUTTON_L3"
		elseif value == 10 then
			return "KEY_CODE_BUTTON_R3"
		else
			return nil
		end
	end,

	toNumber = function(self: KeyCode): number
		if self == "KEY_CODE_INVALID" then
			return 0
		elseif self == "KEY_CODE_BUTTON_A" then
			return 1
		elseif self == "KEY_CODE_BUTTON_B" then
			return 2
		elseif self == "KEY_CODE_BUTTON_X" then
			return 3
		elseif self == "KEY_CODE_BUTTON_Y" then
			return 4
		elseif self == "KEY_CODE_BUTTON_L1" then
			return 5
		elseif self == "KEY_CODE_BUTTON_R1" then
			return 6
		elseif self == "KEY_CODE_BUTTON_L2" then
			return 7
		elseif self == "KEY_CODE_BUTTON_R2" then
			return 8
		elseif self == "KEY_CODE_BUTTON_L3" then
			return 9
		elseif self == "KEY_CODE_BUTTON_R3" then
			return 10
		else
			return self
		end
	end,

	fromName = function(name: string): KeyCode?
		if name == "KEY_CODE_INVALID" then
			return "KEY_CODE_INVALID"
		elseif name == "KEY_CODE_BUTTON_A" then
			return "KEY_CODE_BUTTON_A"
		elseif name == "KEY_CODE_BUTTON_B" then
			return "KEY_CODE_BUTTON_B"
		elseif name == "KEY_CODE_BUTTON_X" then
			return "KEY_CODE_BUTTON_X"
		elseif name == "KEY_CODE_BUTTON_Y" then
			return "KEY_CODE_BUTTON_Y"
		elseif name == "KEY_CODE_BUTTON_L1" then
			return "KEY_CODE_BUTTON_L1"
		elseif name == "KEY_CODE_BUTTON_R1" then
			return "KEY_CODE_BUTTON_R1"
		elseif name == "KEY_CODE_BUTTON_L2" then
			return "KEY_CODE_BUTTON_L2"
		elseif name == "KEY_CODE_BUTTON_R2" then
			return "KEY_CODE_BUTTON_R2"
		elseif name == "KEY_CODE_BUTTON_L3" then
			return "KEY_CODE_BUTTON_L3"
		elseif name == "KEY_CODE_BUTTON_R3" then
			return "KEY_CODE_BUTTON_R3"
		else
			return nil
		end
	end,
}

return {
	AutomaticSizeProp = messages.AutomaticSizeProp,
	AutomaticSizeProp_ConditionalOption = messages.AutomaticSizeProp_ConditionalOption,
	AutomaticSizeProp_ConditionalOptions = messages.AutomaticSizeProp_ConditionalOptions,
	TextTruncateProp = messages.TextTruncateProp,
	TextTruncateProp_ConditionalOption = messages.TextTruncateProp_ConditionalOption,
	TextTruncateProp_ConditionalOptions = messages.TextTruncateProp_ConditionalOptions,
	TextXAlignmentProp = messages.TextXAlignmentProp,
	TextXAlignmentProp_ConditionalOption = messages.TextXAlignmentProp_ConditionalOption,
	TextXAlignmentProp_ConditionalOptions = messages.TextXAlignmentProp_ConditionalOptions,
	TextYAlignmentProp = messages.TextYAlignmentProp,
	TextYAlignmentProp_ConditionalOption = messages.TextYAlignmentProp_ConditionalOption,
	TextYAlignmentProp_ConditionalOptions = messages.TextYAlignmentProp_ConditionalOptions,
	UIFlexModeProp = messages.UIFlexModeProp,
	UIFlexModeProp_ConditionalOption = messages.UIFlexModeProp_ConditionalOption,
	UIFlexModeProp_ConditionalOptions = messages.UIFlexModeProp_ConditionalOptions,
	ScaleTypeProp = messages.ScaleTypeProp,
	ScaleTypeProp_ConditionalOption = messages.ScaleTypeProp_ConditionalOption,
	ScaleTypeProp_ConditionalOptions = messages.ScaleTypeProp_ConditionalOptions,
	ResamplerModeProp = messages.ResamplerModeProp,
	ResamplerModeProp_ConditionalOption = messages.ResamplerModeProp_ConditionalOption,
	ResamplerModeProp_ConditionalOptions = messages.ResamplerModeProp_ConditionalOptions,
	SizeConstraintProp = messages.SizeConstraintProp,
	SizeConstraintProp_ConditionalOption = messages.SizeConstraintProp_ConditionalOption,
	SizeConstraintProp_ConditionalOptions = messages.SizeConstraintProp_ConditionalOptions,
	HorizontalAlignmentProp = messages.HorizontalAlignmentProp,
	HorizontalAlignmentProp_ConditionalOption = messages.HorizontalAlignmentProp_ConditionalOption,
	HorizontalAlignmentProp_ConditionalOptions = messages.HorizontalAlignmentProp_ConditionalOptions,
	SelectionBehaviorProp = messages.SelectionBehaviorProp,
	SelectionBehaviorProp_ConditionalOption = messages.SelectionBehaviorProp_ConditionalOption,
	SelectionBehaviorProp_ConditionalOptions = messages.SelectionBehaviorProp_ConditionalOptions,
	KeyCodeProp = messages.KeyCodeProp,
	KeyCodeProp_ConditionalOption = messages.KeyCodeProp_ConditionalOption,
	KeyCodeProp_ConditionalOptions = messages.KeyCodeProp_ConditionalOptions,
	VerticalAlignmentProp = messages.VerticalAlignmentProp,
	VerticalAlignmentProp_ConditionalOption = messages.VerticalAlignmentProp_ConditionalOption,
	VerticalAlignmentProp_ConditionalOptions = messages.VerticalAlignmentProp_ConditionalOptions,
	AutomaticSize = messages.AutomaticSize,
	TextTruncate = messages.TextTruncate,
	TextXAlignment = messages.TextXAlignment,
	TextYAlignment = messages.TextYAlignment,
	UIFlexMode = messages.UIFlexMode,
	ScaleType = messages.ScaleType,
	ResamplerMode = messages.ResamplerMode,
	SizeConstraint = messages.SizeConstraint,
	HorizontalAlignment = messages.HorizontalAlignment,
	VerticalAlignment = messages.VerticalAlignment,
	SelectionBehavior = messages.SelectionBehavior,
	KeyCode = messages.KeyCode,
}
