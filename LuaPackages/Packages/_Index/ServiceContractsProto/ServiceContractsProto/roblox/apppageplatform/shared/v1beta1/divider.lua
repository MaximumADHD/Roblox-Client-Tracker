--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	DividerSchema: _DividerSchemaMessage,
	DividerSchema_Props: _DividerSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _DividerSchemaImpl = {
	__index: _DividerSchemaImpl,
	new: (fields: _DividerSchemaPartialFields?) -> DividerSchema,
	encode: (self: DividerSchema) -> buffer,
	decode: (input: buffer) -> DividerSchema,
	jsonEncode: (self: DividerSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DividerSchema,
	descriptor: proto.Descriptor,
}

type _DividerSchemaFields = {
	props: DividerSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _DividerSchemaPartialFields = {
	props: DividerSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type DividerSchema = typeof(setmetatable({} :: _DividerSchemaFields, {} :: _DividerSchemaImpl))
type _DividerSchemaMessage = proto.Message<DividerSchema, _DividerSchemaPartialFields>

type _DividerSchema_PropsImpl = {
	__index: _DividerSchema_PropsImpl,
	new: (fields: _DividerSchema_PropsPartialFields?) -> DividerSchema_Props,
	encode: (self: DividerSchema_Props) -> buffer,
	decode: (input: buffer) -> DividerSchema_Props,
	jsonEncode: (self: DividerSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DividerSchema_Props,
	descriptor: proto.Descriptor,
}

type _DividerSchema_PropsFields = {
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	background_color3: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
}

type _DividerSchema_PropsPartialFields = {
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	background_color3: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
}

export type DividerSchema_Props = typeof(setmetatable({} :: _DividerSchema_PropsFields, {} :: _DividerSchema_PropsImpl))
type _DividerSchema_PropsMessage = proto.Message<DividerSchema_Props, _DividerSchema_PropsPartialFields>

do
	local _DividerSchemaImpl = {}
	_DividerSchemaImpl.__index = _DividerSchemaImpl

	function _DividerSchemaImpl.new(data: _DividerSchemaPartialFields?): DividerSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _DividerSchemaImpl :: _DividerSchemaImpl)
	end

	function _DividerSchemaImpl.encode(self: DividerSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DividerSchemaImpl.decode(input: buffer): DividerSchema
		local self = _DividerSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.DividerSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DividerSchemaImpl.jsonEncode(self: DividerSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _DividerSchemaImpl.jsonDecode(input: { [string]: any }): DividerSchema
		local self = _DividerSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.DividerSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_DividerSchemaImpl.descriptor = {
		name = "DividerSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.DividerSchema",
	}

	messages.DividerSchema = _DividerSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DividerSchema)
end

do
	local _DividerSchema_PropsImpl = {}
	_DividerSchema_PropsImpl.__index = _DividerSchema_PropsImpl

	function _DividerSchema_PropsImpl.new(data: _DividerSchema_PropsPartialFields?): DividerSchema_Props
		return setmetatable({
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			background_color3 = if data == nil or data.background_color3 == nil then nil else data.background_color3,
			position = if data == nil or data.position == nil then nil else data.position,
			size = if data == nil or data.size == nil then nil else data.size,
		}, _DividerSchema_PropsImpl :: _DividerSchema_PropsImpl)
	end

	function _DividerSchema_PropsImpl.encode(self: DividerSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_color3 ~= nil then
			local encoded = self.background_color3:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DividerSchema_PropsImpl.decode(input: buffer): DividerSchema_Props
		local self = _DividerSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_color3 = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DividerSchema_PropsImpl.jsonEncode(self: DividerSchema_Props): any
		local output = {}

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.background_color3 ~= nil then
			output.backgroundColor3 = self.background_color3:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		return output
	end

	function _DividerSchema_PropsImpl.jsonDecode(input: { [string]: any }): DividerSchema_Props
		local self = _DividerSchema_PropsImpl.new()

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.background_color3 ~= nil then
			self.background_color3 =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.background_color3)
		end

		if input.backgroundColor3 ~= nil then
			self.background_color3 =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.backgroundColor3)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		return self
	end

	_DividerSchema_PropsImpl.descriptor = {
		name = "DividerSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.DividerSchema_Props = _DividerSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DividerSchema_Props)
end

return {
	DividerSchema = messages.DividerSchema,
	DividerSchema_Props = messages.DividerSchema_Props,
}
