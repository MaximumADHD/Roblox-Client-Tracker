--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CommunityData: _CommunityDataMessage,
}
local messages: _Messages = {} :: _Messages

type _CommunityDataImpl = {
	__index: _CommunityDataImpl,
	new: (fields: _CommunityDataPartialFields?) -> CommunityData,
	encode: (self: CommunityData) -> buffer,
	decode: (input: buffer) -> CommunityData,
	jsonEncode: (self: CommunityData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CommunityData,
	descriptor: proto.Descriptor,
}

type _CommunityDataFields = {
	id: string,
	name: string,
	member_count: number,
	description: string,
	is_verified: boolean,
}

type _CommunityDataPartialFields = {
	id: string?,
	name: string?,
	member_count: number?,
	description: string?,
	is_verified: boolean?,
}

export type CommunityData = typeof(setmetatable({} :: _CommunityDataFields, {} :: _CommunityDataImpl))
type _CommunityDataMessage = proto.Message<CommunityData, _CommunityDataPartialFields>

do
	local _CommunityDataImpl = {}
	_CommunityDataImpl.__index = _CommunityDataImpl

	function _CommunityDataImpl.new(data: _CommunityDataPartialFields?): CommunityData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			name = if data == nil or data.name == nil then "" else data.name,
			member_count = if data == nil or data.member_count == nil then 0 else data.member_count,
			description = if data == nil or data.description == nil then "" else data.description,
			is_verified = if data == nil or data.is_verified == nil then false else data.is_verified,
		}, _CommunityDataImpl :: _CommunityDataImpl)
	end

	function _CommunityDataImpl.encode(self: CommunityData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.member_count ~= nil and self.member_count ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.member_count)
		end

		if self.description ~= nil and self.description ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		if self.is_verified then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_verified then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CommunityDataImpl.decode(input: buffer): CommunityData
		local self = _CommunityDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.member_count = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_verified = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CommunityDataImpl.jsonEncode(self: CommunityData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.member_count ~= nil and self.member_count ~= 0 then
			output.memberCount = self.member_count
		end

		if self.description ~= nil and self.description ~= "" then
			output.description = self.description
		end

		if self.is_verified then
			output.isVerified = self.is_verified
		end

		return output
	end

	function _CommunityDataImpl.jsonDecode(input: { [string]: any }): CommunityData
		local self = _CommunityDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.member_count ~= nil then
			self.member_count = input.member_count
		end

		if input.memberCount ~= nil then
			self.member_count = input.memberCount
		end

		if input.description ~= nil then
			self.description = input.description
		end

		if input.is_verified ~= nil then
			self.is_verified = input.is_verified
		end

		if input.isVerified ~= nil then
			self.is_verified = input.isVerified
		end

		return self
	end

	_CommunityDataImpl.descriptor = {
		name = "CommunityData",
		fullName = "roblox.apppageplatform.shared.v1beta1.CommunityData",
	}

	messages.CommunityData = _CommunityDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CommunityData)
end

return {
	CommunityData = messages.CommunityData,
}
