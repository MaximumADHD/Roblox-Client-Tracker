--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	SocialLinkData: _SocialLinkDataMessage,
}
local messages: _Messages = {} :: _Messages

type _SocialLinkDataImpl = {
	__index: _SocialLinkDataImpl,
	new: (fields: _SocialLinkDataPartialFields?) -> SocialLinkData,
	encode: (self: SocialLinkData) -> buffer,
	decode: (input: buffer) -> SocialLinkData,
	jsonEncode: (self: SocialLinkData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SocialLinkData,
	descriptor: proto.Descriptor,
}

type _SocialLinkDataFields = {
	id: number,
	title: string,
	url: string,
	type: string,
	community_id: number,
}

type _SocialLinkDataPartialFields = {
	id: number?,
	title: string?,
	url: string?,
	type: string?,
	community_id: number?,
}

export type SocialLinkData = typeof(setmetatable({} :: _SocialLinkDataFields, {} :: _SocialLinkDataImpl))
type _SocialLinkDataMessage = proto.Message<SocialLinkData, _SocialLinkDataPartialFields>

do
	local _SocialLinkDataImpl = {}
	_SocialLinkDataImpl.__index = _SocialLinkDataImpl

	function _SocialLinkDataImpl.new(data: _SocialLinkDataPartialFields?): SocialLinkData
		return setmetatable({
			id = if data == nil or data.id == nil then 0 else data.id,
			title = if data == nil or data.title == nil then "" else data.title,
			url = if data == nil or data.url == nil then "" else data.url,
			type = if data == nil or data.type == nil then "" else data.type,
			community_id = if data == nil or data.community_id == nil then 0 else data.community_id,
		}, _SocialLinkDataImpl :: _SocialLinkDataImpl)
	end

	function _SocialLinkDataImpl.encode(self: SocialLinkData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.id)
		end

		if self.title ~= nil and self.title ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.title)
		end

		if self.url ~= nil and self.url ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.url)
		end

		if self.type ~= nil and self.type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		if self.community_id ~= nil and self.community_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.community_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SocialLinkDataImpl.decode(input: buffer): SocialLinkData
		local self = _SocialLinkDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.id = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.community_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.url = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SocialLinkDataImpl.jsonEncode(self: SocialLinkData): any
		local output = {}

		if self.id ~= nil and self.id ~= 0 then
			output.id = self.id
		end

		if self.title ~= nil and self.title ~= "" then
			output.title = self.title
		end

		if self.url ~= nil and self.url ~= "" then
			output.url = self.url
		end

		if self.type ~= nil and self.type ~= "" then
			output.type = self.type
		end

		if self.community_id ~= nil and self.community_id ~= 0 then
			output.communityId = self.community_id
		end

		return output
	end

	function _SocialLinkDataImpl.jsonDecode(input: { [string]: any }): SocialLinkData
		local self = _SocialLinkDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.title ~= nil then
			self.title = input.title
		end

		if input.url ~= nil then
			self.url = input.url
		end

		if input.type ~= nil then
			self.type = input.type
		end

		if input.community_id ~= nil then
			self.community_id = input.community_id
		end

		if input.communityId ~= nil then
			self.community_id = input.communityId
		end

		return self
	end

	_SocialLinkDataImpl.descriptor = {
		name = "SocialLinkData",
		fullName = "roblox.apppageplatform.shared.v1beta1.SocialLinkData",
	}

	messages.SocialLinkData = _SocialLinkDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SocialLinkData)
end

return {
	SocialLinkData = messages.SocialLinkData,
}
