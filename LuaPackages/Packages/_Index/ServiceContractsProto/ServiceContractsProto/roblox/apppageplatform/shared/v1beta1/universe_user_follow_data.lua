--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	UniverseUserFollowData: _UniverseUserFollowDataMessage,
}
local messages: _Messages = {} :: _Messages

type _UniverseUserFollowDataImpl = {
	__index: _UniverseUserFollowDataImpl,
	new: (fields: _UniverseUserFollowDataPartialFields?) -> UniverseUserFollowData,
	encode: (self: UniverseUserFollowData) -> buffer,
	decode: (input: buffer) -> UniverseUserFollowData,
	jsonEncode: (self: UniverseUserFollowData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UniverseUserFollowData,
	descriptor: proto.Descriptor,
}

type _UniverseUserFollowDataFields = {
	can_follow: boolean,
	is_following: boolean,
	following_count_by_type: number,
	following_limit_by_type: number,
}

type _UniverseUserFollowDataPartialFields = {
	can_follow: boolean?,
	is_following: boolean?,
	following_count_by_type: number?,
	following_limit_by_type: number?,
}

export type UniverseUserFollowData = typeof(setmetatable(
	{} :: _UniverseUserFollowDataFields,
	{} :: _UniverseUserFollowDataImpl
))
type _UniverseUserFollowDataMessage = proto.Message<UniverseUserFollowData, _UniverseUserFollowDataPartialFields>

do
	local _UniverseUserFollowDataImpl = {}
	_UniverseUserFollowDataImpl.__index = _UniverseUserFollowDataImpl

	function _UniverseUserFollowDataImpl.new(data: _UniverseUserFollowDataPartialFields?): UniverseUserFollowData
		return setmetatable({
			can_follow = if data == nil or data.can_follow == nil then false else data.can_follow,
			is_following = if data == nil or data.is_following == nil then false else data.is_following,
			following_count_by_type = if data == nil or data.following_count_by_type == nil
				then 0
				else data.following_count_by_type,
			following_limit_by_type = if data == nil or data.following_limit_by_type == nil
				then 0
				else data.following_limit_by_type,
		}, _UniverseUserFollowDataImpl :: _UniverseUserFollowDataImpl)
	end

	function _UniverseUserFollowDataImpl.encode(self: UniverseUserFollowData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.can_follow then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.can_follow then 1 else 0)
		end

		if self.is_following then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_following then 1 else 0)
		end

		if self.following_count_by_type ~= nil and self.following_count_by_type ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.following_count_by_type)
		end

		if self.following_limit_by_type ~= nil and self.following_limit_by_type ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.following_limit_by_type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UniverseUserFollowDataImpl.decode(input: buffer): UniverseUserFollowData
		local self = _UniverseUserFollowDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.can_follow = value ~= 0
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_following = value ~= 0
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.following_count_by_type = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.following_limit_by_type = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UniverseUserFollowDataImpl.jsonEncode(self: UniverseUserFollowData): any
		local output = {}

		if self.can_follow then
			output.canFollow = self.can_follow
		end

		if self.is_following then
			output.isFollowing = self.is_following
		end

		if self.following_count_by_type ~= nil and self.following_count_by_type ~= 0 then
			output.followingCountByType = self.following_count_by_type
		end

		if self.following_limit_by_type ~= nil and self.following_limit_by_type ~= 0 then
			output.followingLimitByType = self.following_limit_by_type
		end

		return output
	end

	function _UniverseUserFollowDataImpl.jsonDecode(input: { [string]: any }): UniverseUserFollowData
		local self = _UniverseUserFollowDataImpl.new()

		if input.can_follow ~= nil then
			self.can_follow = input.can_follow
		end

		if input.canFollow ~= nil then
			self.can_follow = input.canFollow
		end

		if input.is_following ~= nil then
			self.is_following = input.is_following
		end

		if input.isFollowing ~= nil then
			self.is_following = input.isFollowing
		end

		if input.following_count_by_type ~= nil then
			self.following_count_by_type = input.following_count_by_type
		end

		if input.followingCountByType ~= nil then
			self.following_count_by_type = input.followingCountByType
		end

		if input.following_limit_by_type ~= nil then
			self.following_limit_by_type = input.following_limit_by_type
		end

		if input.followingLimitByType ~= nil then
			self.following_limit_by_type = input.followingLimitByType
		end

		return self
	end

	_UniverseUserFollowDataImpl.descriptor = {
		name = "UniverseUserFollowData",
		fullName = "roblox.apppageplatform.shared.v1beta1.UniverseUserFollowData",
	}

	messages.UniverseUserFollowData = _UniverseUserFollowDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UniverseUserFollowData)
end

return {
	UniverseUserFollowData = messages.UniverseUserFollowData,
}
