--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GameFilterCarouselSchema: _GameFilterCarouselSchemaMessage,
	GameFilterCarouselSchema_Props: _GameFilterCarouselSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _GameFilterCarouselSchemaImpl = {
	__index: _GameFilterCarouselSchemaImpl,
	new: (fields: _GameFilterCarouselSchemaPartialFields?) -> GameFilterCarouselSchema,
	encode: (self: GameFilterCarouselSchema) -> buffer,
	decode: (input: buffer) -> GameFilterCarouselSchema,
	jsonEncode: (self: GameFilterCarouselSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GameFilterCarouselSchema,
	descriptor: proto.Descriptor,
}

type _GameFilterCarouselSchemaFields = {
	props: GameFilterCarouselSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _GameFilterCarouselSchemaPartialFields = {
	props: GameFilterCarouselSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type GameFilterCarouselSchema = typeof(setmetatable(
	{} :: _GameFilterCarouselSchemaFields,
	{} :: _GameFilterCarouselSchemaImpl
))
type _GameFilterCarouselSchemaMessage = proto.Message<GameFilterCarouselSchema, _GameFilterCarouselSchemaPartialFields>

type _GameFilterCarouselSchema_PropsImpl = {
	__index: _GameFilterCarouselSchema_PropsImpl,
	new: (fields: _GameFilterCarouselSchema_PropsPartialFields?) -> GameFilterCarouselSchema_Props,
	encode: (self: GameFilterCarouselSchema_Props) -> buffer,
	decode: (input: buffer) -> GameFilterCarouselSchema_Props,
	jsonEncode: (self: GameFilterCarouselSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GameFilterCarouselSchema_Props,
	descriptor: proto.Descriptor,
}

type _GameFilterCarouselSchema_PropsFields = {
	filter_groups: _roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp?,
}

type _GameFilterCarouselSchema_PropsPartialFields = {
	filter_groups: _roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp?,
}

export type GameFilterCarouselSchema_Props = typeof(setmetatable(
	{} :: _GameFilterCarouselSchema_PropsFields,
	{} :: _GameFilterCarouselSchema_PropsImpl
))
type _GameFilterCarouselSchema_PropsMessage = proto.Message<
	GameFilterCarouselSchema_Props,
	_GameFilterCarouselSchema_PropsPartialFields
>

do
	local _GameFilterCarouselSchemaImpl = {}
	_GameFilterCarouselSchemaImpl.__index = _GameFilterCarouselSchemaImpl

	function _GameFilterCarouselSchemaImpl.new(data: _GameFilterCarouselSchemaPartialFields?): GameFilterCarouselSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _GameFilterCarouselSchemaImpl :: _GameFilterCarouselSchemaImpl)
	end

	function _GameFilterCarouselSchemaImpl.encode(self: GameFilterCarouselSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GameFilterCarouselSchemaImpl.decode(input: buffer): GameFilterCarouselSchema
		local self = _GameFilterCarouselSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.GameFilterCarouselSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GameFilterCarouselSchemaImpl.jsonEncode(self: GameFilterCarouselSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _GameFilterCarouselSchemaImpl.jsonDecode(input: { [string]: any }): GameFilterCarouselSchema
		local self = _GameFilterCarouselSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.GameFilterCarouselSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_GameFilterCarouselSchemaImpl.descriptor = {
		name = "GameFilterCarouselSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.GameFilterCarouselSchema",
	}

	messages.GameFilterCarouselSchema = _GameFilterCarouselSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GameFilterCarouselSchema)
end

do
	local _GameFilterCarouselSchema_PropsImpl = {}
	_GameFilterCarouselSchema_PropsImpl.__index = _GameFilterCarouselSchema_PropsImpl

	function _GameFilterCarouselSchema_PropsImpl.new(
		data: _GameFilterCarouselSchema_PropsPartialFields?
	): GameFilterCarouselSchema_Props
		return setmetatable({
			filter_groups = if data == nil or data.filter_groups == nil then nil else data.filter_groups,
		}, _GameFilterCarouselSchema_PropsImpl :: _GameFilterCarouselSchema_PropsImpl)
	end

	function _GameFilterCarouselSchema_PropsImpl.encode(self: GameFilterCarouselSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.filter_groups ~= nil then
			local encoded = self.filter_groups:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GameFilterCarouselSchema_PropsImpl.decode(input: buffer): GameFilterCarouselSchema_Props
		local self = _GameFilterCarouselSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.filter_groups =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GameFilterCarouselSchema_PropsImpl.jsonEncode(self: GameFilterCarouselSchema_Props): any
		local output = {}

		if self.filter_groups ~= nil then
			output.filterGroups = self.filter_groups:jsonEncode()
		end

		return output
	end

	function _GameFilterCarouselSchema_PropsImpl.jsonDecode(input: { [string]: any }): GameFilterCarouselSchema_Props
		local self = _GameFilterCarouselSchema_PropsImpl.new()

		if input.filter_groups ~= nil then
			self.filter_groups =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp.jsonDecode(input.filter_groups)
		end

		if input.filterGroups ~= nil then
			self.filter_groups =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp.jsonDecode(input.filterGroups)
		end

		return self
	end

	_GameFilterCarouselSchema_PropsImpl.descriptor = {
		name = "GameFilterCarouselSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.GameFilterCarouselSchema_Props = _GameFilterCarouselSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GameFilterCarouselSchema_Props)
end

return {
	GameFilterCarouselSchema = messages.GameFilterCarouselSchema,
	GameFilterCarouselSchema_Props = messages.GameFilterCarouselSchema_Props,
}
