--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	UniverseUserFollowLimitData: _UniverseUserFollowLimitDataMessage,
}
local messages: _Messages = {} :: _Messages

type _UniverseUserFollowLimitDataImpl = {
	__index: _UniverseUserFollowLimitDataImpl,
	new: (fields: _UniverseUserFollowLimitDataPartialFields?) -> UniverseUserFollowLimitData,
	encode: (self: UniverseUserFollowLimitData) -> buffer,
	decode: (input: buffer) -> UniverseUserFollowLimitData,
	jsonEncode: (self: UniverseUserFollowLimitData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UniverseUserFollowLimitData,
	descriptor: proto.Descriptor,
}

type _UniverseUserFollowLimitDataFields = {
	following_count_by_type: number,
	following_limit_by_type: number,
}

type _UniverseUserFollowLimitDataPartialFields = {
	following_count_by_type: number?,
	following_limit_by_type: number?,
}

export type UniverseUserFollowLimitData = typeof(setmetatable(
	{} :: _UniverseUserFollowLimitDataFields,
	{} :: _UniverseUserFollowLimitDataImpl
))
type _UniverseUserFollowLimitDataMessage = proto.Message<
	UniverseUserFollowLimitData,
	_UniverseUserFollowLimitDataPartialFields
>

do
	local _UniverseUserFollowLimitDataImpl = {}
	_UniverseUserFollowLimitDataImpl.__index = _UniverseUserFollowLimitDataImpl

	function _UniverseUserFollowLimitDataImpl.new(
		data: _UniverseUserFollowLimitDataPartialFields?
	): UniverseUserFollowLimitData
		return setmetatable({
			following_count_by_type = if data == nil or data.following_count_by_type == nil
				then 0
				else data.following_count_by_type,
			following_limit_by_type = if data == nil or data.following_limit_by_type == nil
				then 0
				else data.following_limit_by_type,
		}, _UniverseUserFollowLimitDataImpl :: _UniverseUserFollowLimitDataImpl)
	end

	function _UniverseUserFollowLimitDataImpl.encode(self: UniverseUserFollowLimitData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.following_count_by_type ~= nil and self.following_count_by_type ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.following_count_by_type)
		end

		if self.following_limit_by_type ~= nil and self.following_limit_by_type ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.following_limit_by_type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UniverseUserFollowLimitDataImpl.decode(input: buffer): UniverseUserFollowLimitData
		local self = _UniverseUserFollowLimitDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.following_count_by_type = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.following_limit_by_type = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UniverseUserFollowLimitDataImpl.jsonEncode(self: UniverseUserFollowLimitData): any
		local output = {}

		if self.following_count_by_type ~= nil and self.following_count_by_type ~= 0 then
			output.followingCountByType = self.following_count_by_type
		end

		if self.following_limit_by_type ~= nil and self.following_limit_by_type ~= 0 then
			output.followingLimitByType = self.following_limit_by_type
		end

		return output
	end

	function _UniverseUserFollowLimitDataImpl.jsonDecode(input: { [string]: any }): UniverseUserFollowLimitData
		local self = _UniverseUserFollowLimitDataImpl.new()

		if input.following_count_by_type ~= nil then
			self.following_count_by_type = input.following_count_by_type
		end

		if input.followingCountByType ~= nil then
			self.following_count_by_type = input.followingCountByType
		end

		if input.following_limit_by_type ~= nil then
			self.following_limit_by_type = input.following_limit_by_type
		end

		if input.followingLimitByType ~= nil then
			self.following_limit_by_type = input.followingLimitByType
		end

		return self
	end

	_UniverseUserFollowLimitDataImpl.descriptor = {
		name = "UniverseUserFollowLimitData",
		fullName = "roblox.apppageplatform.shared.v1beta1.UniverseUserFollowLimitData",
	}

	messages.UniverseUserFollowLimitData = _UniverseUserFollowLimitDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UniverseUserFollowLimitData)
end

return {
	UniverseUserFollowLimitData = messages.UniverseUserFollowLimitData,
}
