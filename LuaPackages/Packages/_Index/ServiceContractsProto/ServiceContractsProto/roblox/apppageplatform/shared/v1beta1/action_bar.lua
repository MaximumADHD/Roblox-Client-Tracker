--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	ActionBarSchema: _ActionBarSchemaMessage,
	ActionBarSchema_Props: _ActionBarSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _ActionBarSchemaImpl = {
	__index: _ActionBarSchemaImpl,
	new: (fields: _ActionBarSchemaPartialFields?) -> ActionBarSchema,
	encode: (self: ActionBarSchema) -> buffer,
	decode: (input: buffer) -> ActionBarSchema,
	jsonEncode: (self: ActionBarSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ActionBarSchema,
	descriptor: proto.Descriptor,
}

type _ActionBarSchemaFields = {
	props: ActionBarSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _ActionBarSchemaPartialFields = {
	props: ActionBarSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type ActionBarSchema = typeof(setmetatable({} :: _ActionBarSchemaFields, {} :: _ActionBarSchemaImpl))
type _ActionBarSchemaMessage = proto.Message<ActionBarSchema, _ActionBarSchemaPartialFields>

type _ActionBarSchema_PropsImpl = {
	__index: _ActionBarSchema_PropsImpl,
	new: (fields: _ActionBarSchema_PropsPartialFields?) -> ActionBarSchema_Props,
	encode: (self: ActionBarSchema_Props) -> buffer,
	decode: (input: buffer) -> ActionBarSchema_Props,
	jsonEncode: (self: ActionBarSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ActionBarSchema_Props,
	descriptor: proto.Descriptor,
}

type _ActionBarSchema_PropsFields = {
	items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	footer: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	row_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	wrapper_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _ActionBarSchema_PropsPartialFields = {
	items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	footer: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	row_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	wrapper_tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type ActionBarSchema_Props = typeof(setmetatable(
	{} :: _ActionBarSchema_PropsFields,
	{} :: _ActionBarSchema_PropsImpl
))
type _ActionBarSchema_PropsMessage = proto.Message<ActionBarSchema_Props, _ActionBarSchema_PropsPartialFields>

do
	local _ActionBarSchemaImpl = {}
	_ActionBarSchemaImpl.__index = _ActionBarSchemaImpl

	function _ActionBarSchemaImpl.new(data: _ActionBarSchemaPartialFields?): ActionBarSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _ActionBarSchemaImpl :: _ActionBarSchemaImpl)
	end

	function _ActionBarSchemaImpl.encode(self: ActionBarSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ActionBarSchemaImpl.decode(input: buffer): ActionBarSchema
		local self = _ActionBarSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.ActionBarSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ActionBarSchemaImpl.jsonEncode(self: ActionBarSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _ActionBarSchemaImpl.jsonDecode(input: { [string]: any }): ActionBarSchema
		local self = _ActionBarSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.ActionBarSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_ActionBarSchemaImpl.descriptor = {
		name = "ActionBarSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.ActionBarSchema",
	}

	messages.ActionBarSchema = _ActionBarSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ActionBarSchema)
end

do
	local _ActionBarSchema_PropsImpl = {}
	_ActionBarSchema_PropsImpl.__index = _ActionBarSchema_PropsImpl

	function _ActionBarSchema_PropsImpl.new(data: _ActionBarSchema_PropsPartialFields?): ActionBarSchema_Props
		return setmetatable({
			items = if data == nil or data.items == nil then nil else data.items,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			position = if data == nil or data.position == nil then nil else data.position,
			size = if data == nil or data.size == nil then nil else data.size,
			footer = if data == nil or data.footer == nil then nil else data.footer,
			row_tag = if data == nil or data.row_tag == nil then nil else data.row_tag,
			wrapper_tag = if data == nil or data.wrapper_tag == nil then nil else data.wrapper_tag,
		}, _ActionBarSchema_PropsImpl :: _ActionBarSchema_PropsImpl)
	end

	function _ActionBarSchema_PropsImpl.encode(self: ActionBarSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.items ~= nil then
			local encoded = self.items:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.footer ~= nil then
			local encoded = self.footer:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.row_tag ~= nil then
			local encoded = self.row_tag:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.wrapper_tag ~= nil then
			local encoded = self.wrapper_tag:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ActionBarSchema_PropsImpl.decode(input: buffer): ActionBarSchema_Props
		local self = _ActionBarSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.footer = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.row_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.wrapper_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ActionBarSchema_PropsImpl.jsonEncode(self: ActionBarSchema_Props): any
		local output = {}

		if self.items ~= nil then
			output.items = self.items:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.footer ~= nil then
			output.footer = self.footer:jsonEncode()
		end

		if self.row_tag ~= nil then
			output.rowTag = self.row_tag:jsonEncode()
		end

		if self.wrapper_tag ~= nil then
			output.wrapperTag = self.wrapper_tag:jsonEncode()
		end

		return output
	end

	function _ActionBarSchema_PropsImpl.jsonDecode(input: { [string]: any }): ActionBarSchema_Props
		local self = _ActionBarSchema_PropsImpl.new()

		if input.items ~= nil then
			self.items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(input.items)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.footer ~= nil then
			self.footer = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.footer)
		end

		if input.row_tag ~= nil then
			self.row_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.row_tag)
		end

		if input.rowTag ~= nil then
			self.row_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.rowTag)
		end

		if input.wrapper_tag ~= nil then
			self.wrapper_tag =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.wrapper_tag)
		end

		if input.wrapperTag ~= nil then
			self.wrapper_tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.wrapperTag)
		end

		return self
	end

	_ActionBarSchema_PropsImpl.descriptor = {
		name = "ActionBarSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.ActionBarSchema_Props = _ActionBarSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ActionBarSchema_Props)
end

return {
	ActionBarSchema = messages.ActionBarSchema,
	ActionBarSchema_Props = messages.ActionBarSchema_Props,
}
