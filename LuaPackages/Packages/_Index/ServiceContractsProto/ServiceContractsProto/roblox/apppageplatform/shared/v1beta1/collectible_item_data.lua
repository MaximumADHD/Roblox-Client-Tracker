--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CollectibleItemCreator: _CollectibleItemCreatorMessage,
	CollectibleItemExperience: _CollectibleItemExperienceMessage,
	CollectibleItemData: _CollectibleItemDataMessage,
}
local messages: _Messages = {} :: _Messages

type _CollectibleItemCreatorImpl = {
	__index: _CollectibleItemCreatorImpl,
	new: (fields: _CollectibleItemCreatorPartialFields?) -> CollectibleItemCreator,
	encode: (self: CollectibleItemCreator) -> buffer,
	decode: (input: buffer) -> CollectibleItemCreator,
	jsonEncode: (self: CollectibleItemCreator) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CollectibleItemCreator,
	descriptor: proto.Descriptor,
}

type _CollectibleItemCreatorFields = {
	id: string?,
	name: string?,
	type: string?,
	target_id: string?,
	has_verified_badge: boolean?,
}

type _CollectibleItemCreatorPartialFields = {
	id: string?,
	name: string?,
	type: string?,
	target_id: string?,
	has_verified_badge: boolean?,
}

export type CollectibleItemCreator = typeof(setmetatable(
	{} :: _CollectibleItemCreatorFields,
	{} :: _CollectibleItemCreatorImpl
))
type _CollectibleItemCreatorMessage = proto.Message<CollectibleItemCreator, _CollectibleItemCreatorPartialFields>

type _CollectibleItemExperienceImpl = {
	__index: _CollectibleItemExperienceImpl,
	new: (fields: _CollectibleItemExperiencePartialFields?) -> CollectibleItemExperience,
	encode: (self: CollectibleItemExperience) -> buffer,
	decode: (input: buffer) -> CollectibleItemExperience,
	jsonEncode: (self: CollectibleItemExperience) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CollectibleItemExperience,
	descriptor: proto.Descriptor,
}

type _CollectibleItemExperienceFields = {
	id: string,
	name: string?,
	detail_page_link: string?,
}

type _CollectibleItemExperiencePartialFields = {
	id: string?,
	name: string?,
	detail_page_link: string?,
}

export type CollectibleItemExperience = typeof(setmetatable(
	{} :: _CollectibleItemExperienceFields,
	{} :: _CollectibleItemExperienceImpl
))
type _CollectibleItemExperienceMessage = proto.Message<
	CollectibleItemExperience,
	_CollectibleItemExperiencePartialFields
>

type _CollectibleItemDataImpl = {
	__index: _CollectibleItemDataImpl,
	new: (fields: _CollectibleItemDataPartialFields?) -> CollectibleItemData,
	encode: (self: CollectibleItemData) -> buffer,
	decode: (input: buffer) -> CollectibleItemData,
	jsonEncode: (self: CollectibleItemData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CollectibleItemData,
	descriptor: proto.Descriptor,
}

type _CollectibleItemDataFields = {
	id: string,
	collectible_product_id: string?,
	name: string?,
	description: string?,
	creator: CollectibleItemCreator?,
	price: number?,
	lowest_price: number?,
	units_available_for_consumption: number?,
	offsale_deadline: string?,
	asset_stock: number?,
	has_resellers: boolean?,
	universe_ids: { string },
	sale_location_type: string?,
	quantity_limit_per_user: number?,
	resale_restriction: number?,
	lowest_available_resale_product_id: string?,
	lowest_available_resale_item_instance_id: string?,
	experiences: { CollectibleItemExperience },
}

type _CollectibleItemDataPartialFields = {
	id: string?,
	collectible_product_id: string?,
	name: string?,
	description: string?,
	creator: CollectibleItemCreator?,
	price: number?,
	lowest_price: number?,
	units_available_for_consumption: number?,
	offsale_deadline: string?,
	asset_stock: number?,
	has_resellers: boolean?,
	universe_ids: { string }?,
	sale_location_type: string?,
	quantity_limit_per_user: number?,
	resale_restriction: number?,
	lowest_available_resale_product_id: string?,
	lowest_available_resale_item_instance_id: string?,
	experiences: { CollectibleItemExperience }?,
}

export type CollectibleItemData = typeof(setmetatable({} :: _CollectibleItemDataFields, {} :: _CollectibleItemDataImpl))
type _CollectibleItemDataMessage = proto.Message<CollectibleItemData, _CollectibleItemDataPartialFields>

do
	local _CollectibleItemCreatorImpl = {}
	_CollectibleItemCreatorImpl.__index = _CollectibleItemCreatorImpl

	function _CollectibleItemCreatorImpl.new(data: _CollectibleItemCreatorPartialFields?): CollectibleItemCreator
		return setmetatable({
			id = if data == nil or data.id == nil then nil else data.id,
			name = if data == nil or data.name == nil then nil else data.name,
			type = if data == nil or data.type == nil then nil else data.type,
			target_id = if data == nil or data.target_id == nil then nil else data.target_id,
			has_verified_badge = if data == nil or data.has_verified_badge == nil then nil else data.has_verified_badge,
		}, _CollectibleItemCreatorImpl :: _CollectibleItemCreatorImpl)
	end

	function _CollectibleItemCreatorImpl.encode(self: CollectibleItemCreator): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.name ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		if self.target_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.target_id)
		end

		if self.has_verified_badge ~= nil then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.has_verified_badge then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CollectibleItemCreatorImpl.decode(input: buffer): CollectibleItemCreator
		local self = _CollectibleItemCreatorImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.has_verified_badge = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.target_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CollectibleItemCreatorImpl.jsonEncode(self: CollectibleItemCreator): any
		local output = {}

		if self.id ~= nil then
			output.id = self.id
		end

		if self.name ~= nil then
			output.name = self.name
		end

		if self.type ~= nil then
			output.type = self.type
		end

		if self.target_id ~= nil then
			output.targetId = self.target_id
		end

		if self.has_verified_badge ~= nil then
			output.hasVerifiedBadge = self.has_verified_badge
		end

		return output
	end

	function _CollectibleItemCreatorImpl.jsonDecode(input: { [string]: any }): CollectibleItemCreator
		local self = _CollectibleItemCreatorImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.type ~= nil then
			self.type = input.type
		end

		if input.target_id ~= nil then
			self.target_id = input.target_id
		end

		if input.targetId ~= nil then
			self.target_id = input.targetId
		end

		if input.has_verified_badge ~= nil then
			self.has_verified_badge = input.has_verified_badge
		end

		if input.hasVerifiedBadge ~= nil then
			self.has_verified_badge = input.hasVerifiedBadge
		end

		return self
	end

	_CollectibleItemCreatorImpl.descriptor = {
		name = "CollectibleItemCreator",
		fullName = "roblox.apppageplatform.shared.v1beta1.CollectibleItemCreator",
	}

	messages.CollectibleItemCreator = _CollectibleItemCreatorImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CollectibleItemCreator)
end

do
	local _CollectibleItemExperienceImpl = {}
	_CollectibleItemExperienceImpl.__index = _CollectibleItemExperienceImpl

	function _CollectibleItemExperienceImpl.new(
		data: _CollectibleItemExperiencePartialFields?
	): CollectibleItemExperience
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			name = if data == nil or data.name == nil then nil else data.name,
			detail_page_link = if data == nil or data.detail_page_link == nil then nil else data.detail_page_link,
		}, _CollectibleItemExperienceImpl :: _CollectibleItemExperienceImpl)
	end

	function _CollectibleItemExperienceImpl.encode(self: CollectibleItemExperience): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.name ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.detail_page_link ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.detail_page_link)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CollectibleItemExperienceImpl.decode(input: buffer): CollectibleItemExperience
		local self = _CollectibleItemExperienceImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.detail_page_link = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CollectibleItemExperienceImpl.jsonEncode(self: CollectibleItemExperience): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.name ~= nil then
			output.name = self.name
		end

		if self.detail_page_link ~= nil then
			output.detailPageLink = self.detail_page_link
		end

		return output
	end

	function _CollectibleItemExperienceImpl.jsonDecode(input: { [string]: any }): CollectibleItemExperience
		local self = _CollectibleItemExperienceImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.detail_page_link ~= nil then
			self.detail_page_link = input.detail_page_link
		end

		if input.detailPageLink ~= nil then
			self.detail_page_link = input.detailPageLink
		end

		return self
	end

	_CollectibleItemExperienceImpl.descriptor = {
		name = "CollectibleItemExperience",
		fullName = "roblox.apppageplatform.shared.v1beta1.CollectibleItemExperience",
	}

	messages.CollectibleItemExperience = _CollectibleItemExperienceImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CollectibleItemExperience)
end

do
	local _CollectibleItemDataImpl = {}
	_CollectibleItemDataImpl.__index = _CollectibleItemDataImpl

	function _CollectibleItemDataImpl.new(data: _CollectibleItemDataPartialFields?): CollectibleItemData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			collectible_product_id = if data == nil or data.collectible_product_id == nil
				then nil
				else data.collectible_product_id,
			name = if data == nil or data.name == nil then nil else data.name,
			description = if data == nil or data.description == nil then nil else data.description,
			creator = if data == nil or data.creator == nil then nil else data.creator,
			price = if data == nil or data.price == nil then nil else data.price,
			lowest_price = if data == nil or data.lowest_price == nil then nil else data.lowest_price,
			units_available_for_consumption = if data == nil or data.units_available_for_consumption == nil
				then nil
				else data.units_available_for_consumption,
			offsale_deadline = if data == nil or data.offsale_deadline == nil then nil else data.offsale_deadline,
			asset_stock = if data == nil or data.asset_stock == nil then nil else data.asset_stock,
			has_resellers = if data == nil or data.has_resellers == nil then nil else data.has_resellers,
			universe_ids = if data == nil or data.universe_ids == nil then {} else data.universe_ids,
			sale_location_type = if data == nil or data.sale_location_type == nil then nil else data.sale_location_type,
			quantity_limit_per_user = if data == nil or data.quantity_limit_per_user == nil
				then nil
				else data.quantity_limit_per_user,
			resale_restriction = if data == nil or data.resale_restriction == nil then nil else data.resale_restriction,
			lowest_available_resale_product_id = if data == nil
					or data.lowest_available_resale_product_id == nil
				then nil
				else data.lowest_available_resale_product_id,
			lowest_available_resale_item_instance_id = if data == nil
					or data.lowest_available_resale_item_instance_id == nil
				then nil
				else data.lowest_available_resale_item_instance_id,
			experiences = if data == nil or data.experiences == nil then {} else data.experiences,
		}, _CollectibleItemDataImpl :: _CollectibleItemDataImpl)
	end

	function _CollectibleItemDataImpl.encode(self: CollectibleItemData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.collectible_product_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.collectible_product_id)
		end

		if self.name ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.description ~= nil then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		if self.creator ~= nil then
			local encoded = self.creator:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.price ~= nil then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.price)
		end

		if self.lowest_price ~= nil then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.lowest_price)
		end

		if self.units_available_for_consumption ~= nil then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.units_available_for_consumption)
		end

		if self.offsale_deadline ~= nil then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.offsale_deadline)
		end

		if self.asset_stock ~= nil then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.asset_stock)
		end

		if self.has_resellers ~= nil then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.has_resellers then 1 else 0)
		end

		if self.universe_ids ~= nil and #self.universe_ids > 0 then
			for _, value in self.universe_ids do
				output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.sale_location_type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sale_location_type)
		end

		if self.quantity_limit_per_user ~= nil then
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.quantity_limit_per_user)
		end

		if self.resale_restriction ~= nil then
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.resale_restriction)
		end

		if self.lowest_available_resale_product_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.lowest_available_resale_product_id)
		end

		if self.lowest_available_resale_item_instance_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.lowest_available_resale_item_instance_id)
		end

		if self.experiences ~= nil and #self.experiences > 0 then
			for _, value in self.experiences do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CollectibleItemDataImpl.decode(input: buffer): CollectibleItemData
		local self = _CollectibleItemDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 6 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.price = value
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.lowest_price = value
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.units_available_for_consumption = value
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.asset_stock = value
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.has_resellers = value ~= 0
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.quantity_limit_per_user = value
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.resale_restriction = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.collectible_product_id = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.creator = messages.CollectibleItemCreator.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.offsale_deadline = buffer.tostring(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.universe_ids, buffer.tostring(value))
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sale_location_type = buffer.tostring(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.lowest_available_resale_product_id = buffer.tostring(value)
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.lowest_available_resale_item_instance_id = buffer.tostring(value)
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.experiences, messages.CollectibleItemExperience.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CollectibleItemDataImpl.jsonEncode(self: CollectibleItemData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.collectible_product_id ~= nil then
			output.collectibleProductId = self.collectible_product_id
		end

		if self.name ~= nil then
			output.name = self.name
		end

		if self.description ~= nil then
			output.description = self.description
		end

		if self.creator ~= nil then
			output.creator = self.creator:jsonEncode()
		end

		if self.price ~= nil then
			output.price = self.price
		end

		if self.lowest_price ~= nil then
			output.lowestPrice = self.lowest_price
		end

		if self.units_available_for_consumption ~= nil then
			output.unitsAvailableForConsumption = self.units_available_for_consumption
		end

		if self.offsale_deadline ~= nil then
			output.offsaleDeadline = self.offsale_deadline
		end

		if self.asset_stock ~= nil then
			output.assetStock = self.asset_stock
		end

		if self.has_resellers ~= nil then
			output.hasResellers = self.has_resellers
		end

		if self.universe_ids ~= nil and #self.universe_ids > 0 then
			local newOutput = {}
			for _, value in self.universe_ids do
				table.insert(newOutput, value)
			end
			output.universeIds = newOutput
		end

		if self.sale_location_type ~= nil then
			output.saleLocationType = self.sale_location_type
		end

		if self.quantity_limit_per_user ~= nil then
			output.quantityLimitPerUser = self.quantity_limit_per_user
		end

		if self.resale_restriction ~= nil then
			output.resaleRestriction = self.resale_restriction
		end

		if self.lowest_available_resale_product_id ~= nil then
			output.lowestAvailableResaleProductId = self.lowest_available_resale_product_id
		end

		if self.lowest_available_resale_item_instance_id ~= nil then
			output.lowestAvailableResaleItemInstanceId = self.lowest_available_resale_item_instance_id
		end

		if self.experiences ~= nil and #self.experiences > 0 then
			local newOutput = {}
			for _, value in self.experiences do
				table.insert(newOutput, value:jsonEncode())
			end
			output.experiences = newOutput
		end

		return output
	end

	function _CollectibleItemDataImpl.jsonDecode(input: { [string]: any }): CollectibleItemData
		local self = _CollectibleItemDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.collectible_product_id ~= nil then
			self.collectible_product_id = input.collectible_product_id
		end

		if input.collectibleProductId ~= nil then
			self.collectible_product_id = input.collectibleProductId
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.description ~= nil then
			self.description = input.description
		end

		if input.creator ~= nil then
			self.creator = messages.CollectibleItemCreator.jsonDecode(input.creator)
		end

		if input.price ~= nil then
			self.price = input.price
		end

		if input.lowest_price ~= nil then
			self.lowest_price = input.lowest_price
		end

		if input.lowestPrice ~= nil then
			self.lowest_price = input.lowestPrice
		end

		if input.units_available_for_consumption ~= nil then
			self.units_available_for_consumption = input.units_available_for_consumption
		end

		if input.unitsAvailableForConsumption ~= nil then
			self.units_available_for_consumption = input.unitsAvailableForConsumption
		end

		if input.offsale_deadline ~= nil then
			self.offsale_deadline = input.offsale_deadline
		end

		if input.offsaleDeadline ~= nil then
			self.offsale_deadline = input.offsaleDeadline
		end

		if input.asset_stock ~= nil then
			self.asset_stock = input.asset_stock
		end

		if input.assetStock ~= nil then
			self.asset_stock = input.assetStock
		end

		if input.has_resellers ~= nil then
			self.has_resellers = input.has_resellers
		end

		if input.hasResellers ~= nil then
			self.has_resellers = input.hasResellers
		end

		if input.universe_ids ~= nil then
			local newOutput: { string } = {}
			for _, value in input.universe_ids do
				table.insert(newOutput, value)
			end

			self.universe_ids = newOutput
		end

		if input.universeIds ~= nil then
			local newOutput: { string } = {}
			for _, value in input.universeIds do
				table.insert(newOutput, value)
			end

			self.universe_ids = newOutput
		end

		if input.sale_location_type ~= nil then
			self.sale_location_type = input.sale_location_type
		end

		if input.saleLocationType ~= nil then
			self.sale_location_type = input.saleLocationType
		end

		if input.quantity_limit_per_user ~= nil then
			self.quantity_limit_per_user = input.quantity_limit_per_user
		end

		if input.quantityLimitPerUser ~= nil then
			self.quantity_limit_per_user = input.quantityLimitPerUser
		end

		if input.resale_restriction ~= nil then
			self.resale_restriction = input.resale_restriction
		end

		if input.resaleRestriction ~= nil then
			self.resale_restriction = input.resaleRestriction
		end

		if input.lowest_available_resale_product_id ~= nil then
			self.lowest_available_resale_product_id = input.lowest_available_resale_product_id
		end

		if input.lowestAvailableResaleProductId ~= nil then
			self.lowest_available_resale_product_id = input.lowestAvailableResaleProductId
		end

		if input.lowest_available_resale_item_instance_id ~= nil then
			self.lowest_available_resale_item_instance_id = input.lowest_available_resale_item_instance_id
		end

		if input.lowestAvailableResaleItemInstanceId ~= nil then
			self.lowest_available_resale_item_instance_id = input.lowestAvailableResaleItemInstanceId
		end

		if input.experiences ~= nil then
			local newOutput: { CollectibleItemExperience } = {}
			for _, value in input.experiences do
				table.insert(newOutput, messages.CollectibleItemExperience.jsonDecode(value))
			end

			self.experiences = newOutput
		end

		return self
	end

	_CollectibleItemDataImpl.descriptor = {
		name = "CollectibleItemData",
		fullName = "roblox.apppageplatform.shared.v1beta1.CollectibleItemData",
	}

	messages.CollectibleItemData = _CollectibleItemDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CollectibleItemData)
end

return {
	CollectibleItemCreator = messages.CollectibleItemCreator,
	CollectibleItemExperience = messages.CollectibleItemExperience,
	CollectibleItemData = messages.CollectibleItemData,
}
