--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CommunityUserMembershipData: _CommunityUserMembershipDataMessage,
}
local messages: _Messages = {} :: _Messages

type _CommunityUserMembershipDataImpl = {
	__index: _CommunityUserMembershipDataImpl,
	new: (fields: _CommunityUserMembershipDataPartialFields?) -> CommunityUserMembershipData,
	encode: (self: CommunityUserMembershipData) -> buffer,
	decode: (input: buffer) -> CommunityUserMembershipData,
	jsonEncode: (self: CommunityUserMembershipData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CommunityUserMembershipData,
	descriptor: proto.Descriptor,
}

type _CommunityUserMembershipDataFields = {
	can_join: boolean,
	is_pending_approval: boolean,
}

type _CommunityUserMembershipDataPartialFields = {
	can_join: boolean?,
	is_pending_approval: boolean?,
}

export type CommunityUserMembershipData = typeof(setmetatable(
	{} :: _CommunityUserMembershipDataFields,
	{} :: _CommunityUserMembershipDataImpl
))
type _CommunityUserMembershipDataMessage = proto.Message<
	CommunityUserMembershipData,
	_CommunityUserMembershipDataPartialFields
>

do
	local _CommunityUserMembershipDataImpl = {}
	_CommunityUserMembershipDataImpl.__index = _CommunityUserMembershipDataImpl

	function _CommunityUserMembershipDataImpl.new(
		data: _CommunityUserMembershipDataPartialFields?
	): CommunityUserMembershipData
		return setmetatable({
			can_join = if data == nil or data.can_join == nil then false else data.can_join,
			is_pending_approval = if data == nil or data.is_pending_approval == nil
				then false
				else data.is_pending_approval,
		}, _CommunityUserMembershipDataImpl :: _CommunityUserMembershipDataImpl)
	end

	function _CommunityUserMembershipDataImpl.encode(self: CommunityUserMembershipData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.can_join then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.can_join then 1 else 0)
		end

		if self.is_pending_approval then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_pending_approval then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CommunityUserMembershipDataImpl.decode(input: buffer): CommunityUserMembershipData
		local self = _CommunityUserMembershipDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.can_join = value ~= 0
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_pending_approval = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CommunityUserMembershipDataImpl.jsonEncode(self: CommunityUserMembershipData): any
		local output = {}

		if self.can_join then
			output.canJoin = self.can_join
		end

		if self.is_pending_approval then
			output.isPendingApproval = self.is_pending_approval
		end

		return output
	end

	function _CommunityUserMembershipDataImpl.jsonDecode(input: { [string]: any }): CommunityUserMembershipData
		local self = _CommunityUserMembershipDataImpl.new()

		if input.can_join ~= nil then
			self.can_join = input.can_join
		end

		if input.canJoin ~= nil then
			self.can_join = input.canJoin
		end

		if input.is_pending_approval ~= nil then
			self.is_pending_approval = input.is_pending_approval
		end

		if input.isPendingApproval ~= nil then
			self.is_pending_approval = input.isPendingApproval
		end

		return self
	end

	_CommunityUserMembershipDataImpl.descriptor = {
		name = "CommunityUserMembershipData",
		fullName = "roblox.apppageplatform.shared.v1beta1.CommunityUserMembershipData",
	}

	messages.CommunityUserMembershipData = _CommunityUserMembershipDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CommunityUserMembershipData)
end

return {
	CommunityUserMembershipData = messages.CommunityUserMembershipData,
}
