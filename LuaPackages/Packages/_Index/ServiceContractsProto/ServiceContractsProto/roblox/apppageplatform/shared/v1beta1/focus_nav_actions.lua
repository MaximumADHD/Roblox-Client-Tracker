--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	FocusNavActionsSchema: _FocusNavActionsSchemaMessage,
	FocusNavActionsSchema_Props: _FocusNavActionsSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _FocusNavActionsSchemaImpl = {
	__index: _FocusNavActionsSchemaImpl,
	new: (fields: _FocusNavActionsSchemaPartialFields?) -> FocusNavActionsSchema,
	encode: (self: FocusNavActionsSchema) -> buffer,
	decode: (input: buffer) -> FocusNavActionsSchema,
	jsonEncode: (self: FocusNavActionsSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FocusNavActionsSchema,
	descriptor: proto.Descriptor,
}

type _FocusNavActionsSchemaFields = {
	props: FocusNavActionsSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _FocusNavActionsSchemaPartialFields = {
	props: FocusNavActionsSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type FocusNavActionsSchema = typeof(setmetatable(
	{} :: _FocusNavActionsSchemaFields,
	{} :: _FocusNavActionsSchemaImpl
))
type _FocusNavActionsSchemaMessage = proto.Message<FocusNavActionsSchema, _FocusNavActionsSchemaPartialFields>

type _FocusNavActionsSchema_PropsImpl = {
	__index: _FocusNavActionsSchema_PropsImpl,
	new: (fields: _FocusNavActionsSchema_PropsPartialFields?) -> FocusNavActionsSchema_Props,
	encode: (self: FocusNavActionsSchema_Props) -> buffer,
	decode: (input: buffer) -> FocusNavActionsSchema_Props,
	jsonEncode: (self: FocusNavActionsSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FocusNavActionsSchema_Props,
	descriptor: proto.Descriptor,
}

type _FocusNavActionsSchema_PropsFields = {
	focus_nav_actions: _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp?,
}

type _FocusNavActionsSchema_PropsPartialFields = {
	focus_nav_actions: _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp?,
}

export type FocusNavActionsSchema_Props = typeof(setmetatable(
	{} :: _FocusNavActionsSchema_PropsFields,
	{} :: _FocusNavActionsSchema_PropsImpl
))
type _FocusNavActionsSchema_PropsMessage = proto.Message<
	FocusNavActionsSchema_Props,
	_FocusNavActionsSchema_PropsPartialFields
>

do
	local _FocusNavActionsSchemaImpl = {}
	_FocusNavActionsSchemaImpl.__index = _FocusNavActionsSchemaImpl

	function _FocusNavActionsSchemaImpl.new(data: _FocusNavActionsSchemaPartialFields?): FocusNavActionsSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _FocusNavActionsSchemaImpl :: _FocusNavActionsSchemaImpl)
	end

	function _FocusNavActionsSchemaImpl.encode(self: FocusNavActionsSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FocusNavActionsSchemaImpl.decode(input: buffer): FocusNavActionsSchema
		local self = _FocusNavActionsSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.FocusNavActionsSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FocusNavActionsSchemaImpl.jsonEncode(self: FocusNavActionsSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _FocusNavActionsSchemaImpl.jsonDecode(input: { [string]: any }): FocusNavActionsSchema
		local self = _FocusNavActionsSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.FocusNavActionsSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_FocusNavActionsSchemaImpl.descriptor = {
		name = "FocusNavActionsSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.FocusNavActionsSchema",
	}

	messages.FocusNavActionsSchema = _FocusNavActionsSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FocusNavActionsSchema)
end

do
	local _FocusNavActionsSchema_PropsImpl = {}
	_FocusNavActionsSchema_PropsImpl.__index = _FocusNavActionsSchema_PropsImpl

	function _FocusNavActionsSchema_PropsImpl.new(
		data: _FocusNavActionsSchema_PropsPartialFields?
	): FocusNavActionsSchema_Props
		return setmetatable({
			focus_nav_actions = if data == nil or data.focus_nav_actions == nil then nil else data.focus_nav_actions,
		}, _FocusNavActionsSchema_PropsImpl :: _FocusNavActionsSchema_PropsImpl)
	end

	function _FocusNavActionsSchema_PropsImpl.encode(self: FocusNavActionsSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.focus_nav_actions ~= nil then
			local encoded = self.focus_nav_actions:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FocusNavActionsSchema_PropsImpl.decode(input: buffer): FocusNavActionsSchema_Props
		local self = _FocusNavActionsSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.focus_nav_actions = _roblox_apppageplatform_shared_v1beta1_prop_types.StructProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FocusNavActionsSchema_PropsImpl.jsonEncode(self: FocusNavActionsSchema_Props): any
		local output = {}

		if self.focus_nav_actions ~= nil then
			output.focusNavActions = self.focus_nav_actions:jsonEncode()
		end

		return output
	end

	function _FocusNavActionsSchema_PropsImpl.jsonDecode(input: { [string]: any }): FocusNavActionsSchema_Props
		local self = _FocusNavActionsSchema_PropsImpl.new()

		if input.focus_nav_actions ~= nil then
			self.focus_nav_actions =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StructProp.jsonDecode(input.focus_nav_actions)
		end

		if input.focusNavActions ~= nil then
			self.focus_nav_actions =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StructProp.jsonDecode(input.focusNavActions)
		end

		return self
	end

	_FocusNavActionsSchema_PropsImpl.descriptor = {
		name = "FocusNavActionsSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.FocusNavActionsSchema_Props = _FocusNavActionsSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FocusNavActionsSchema_Props)
end

return {
	FocusNavActionsSchema = messages.FocusNavActionsSchema,
	FocusNavActionsSchema_Props = messages.FocusNavActionsSchema_Props,
}
