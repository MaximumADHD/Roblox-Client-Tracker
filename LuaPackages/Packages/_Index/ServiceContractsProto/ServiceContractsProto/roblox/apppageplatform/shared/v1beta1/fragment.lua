--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	FragmentSchema: _FragmentSchemaMessage,
	FragmentSchema_Props: _FragmentSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _FragmentSchemaImpl = {
	__index: _FragmentSchemaImpl,
	new: (fields: _FragmentSchemaPartialFields?) -> FragmentSchema,
	encode: (self: FragmentSchema) -> buffer,
	decode: (input: buffer) -> FragmentSchema,
	jsonEncode: (self: FragmentSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FragmentSchema,
	descriptor: proto.Descriptor,
}

type _FragmentSchemaFields = {
	props: FragmentSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _FragmentSchemaPartialFields = {
	props: FragmentSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type FragmentSchema = typeof(setmetatable({} :: _FragmentSchemaFields, {} :: _FragmentSchemaImpl))
type _FragmentSchemaMessage = proto.Message<FragmentSchema, _FragmentSchemaPartialFields>

type _FragmentSchema_PropsImpl = {
	__index: _FragmentSchema_PropsImpl,
	new: (fields: _FragmentSchema_PropsPartialFields?) -> FragmentSchema_Props,
	encode: (self: FragmentSchema_Props) -> buffer,
	decode: (input: buffer) -> FragmentSchema_Props,
	jsonEncode: (self: FragmentSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FragmentSchema_Props,
	descriptor: proto.Descriptor,
}

type _FragmentSchema_PropsFields = {}

type _FragmentSchema_PropsPartialFields = {}

export type FragmentSchema_Props = typeof(setmetatable(
	{} :: _FragmentSchema_PropsFields,
	{} :: _FragmentSchema_PropsImpl
))
type _FragmentSchema_PropsMessage = proto.Message<FragmentSchema_Props, _FragmentSchema_PropsPartialFields>

do
	local _FragmentSchemaImpl = {}
	_FragmentSchemaImpl.__index = _FragmentSchemaImpl

	function _FragmentSchemaImpl.new(data: _FragmentSchemaPartialFields?): FragmentSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _FragmentSchemaImpl :: _FragmentSchemaImpl)
	end

	function _FragmentSchemaImpl.encode(self: FragmentSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FragmentSchemaImpl.decode(input: buffer): FragmentSchema
		local self = _FragmentSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.FragmentSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FragmentSchemaImpl.jsonEncode(self: FragmentSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _FragmentSchemaImpl.jsonDecode(input: { [string]: any }): FragmentSchema
		local self = _FragmentSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.FragmentSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_FragmentSchemaImpl.descriptor = {
		name = "FragmentSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.FragmentSchema",
	}

	messages.FragmentSchema = _FragmentSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FragmentSchema)
end

do
	local _FragmentSchema_PropsImpl = {}
	_FragmentSchema_PropsImpl.__index = _FragmentSchema_PropsImpl

	function _FragmentSchema_PropsImpl.new(data: _FragmentSchema_PropsPartialFields?): FragmentSchema_Props
		return setmetatable({}, _FragmentSchema_PropsImpl :: _FragmentSchema_PropsImpl)
	end

	function _FragmentSchema_PropsImpl.encode(self: FragmentSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FragmentSchema_PropsImpl.decode(input: buffer): FragmentSchema_Props
		local self = _FragmentSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FragmentSchema_PropsImpl.jsonEncode(self: FragmentSchema_Props): any
		local output = {}

		return output
	end

	function _FragmentSchema_PropsImpl.jsonDecode(input: { [string]: any }): FragmentSchema_Props
		local self = _FragmentSchema_PropsImpl.new()

		return self
	end

	_FragmentSchema_PropsImpl.descriptor = {
		name = "FragmentSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.FragmentSchema_Props = _FragmentSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FragmentSchema_Props)
end

return {
	FragmentSchema = messages.FragmentSchema,
	FragmentSchema_Props = messages.FragmentSchema_Props,
}
