--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GamePassData: _GamePassDataMessage,
}
local messages: _Messages = {} :: _Messages

type _GamePassDataImpl = {
	__index: _GamePassDataImpl,
	new: (fields: _GamePassDataPartialFields?) -> GamePassData,
	encode: (self: GamePassData) -> buffer,
	decode: (input: buffer) -> GamePassData,
	jsonEncode: (self: GamePassData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GamePassData,
	descriptor: proto.Descriptor,
}

type _GamePassDataFields = {
	id: string,
	name: string,
	price: number,
	product_id: number,
	is_owned: boolean,
	image_asset_id: number,
	description: string,
}

type _GamePassDataPartialFields = {
	id: string?,
	name: string?,
	price: number?,
	product_id: number?,
	is_owned: boolean?,
	image_asset_id: number?,
	description: string?,
}

export type GamePassData = typeof(setmetatable({} :: _GamePassDataFields, {} :: _GamePassDataImpl))
type _GamePassDataMessage = proto.Message<GamePassData, _GamePassDataPartialFields>

do
	local _GamePassDataImpl = {}
	_GamePassDataImpl.__index = _GamePassDataImpl

	function _GamePassDataImpl.new(data: _GamePassDataPartialFields?): GamePassData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			name = if data == nil or data.name == nil then "" else data.name,
			price = if data == nil or data.price == nil then 0 else data.price,
			product_id = if data == nil or data.product_id == nil then 0 else data.product_id,
			is_owned = if data == nil or data.is_owned == nil then false else data.is_owned,
			image_asset_id = if data == nil or data.image_asset_id == nil then 0 else data.image_asset_id,
			description = if data == nil or data.description == nil then "" else data.description,
		}, _GamePassDataImpl :: _GamePassDataImpl)
	end

	function _GamePassDataImpl.encode(self: GamePassData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.price ~= nil and self.price ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.price)
		end

		if self.product_id ~= nil and self.product_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.product_id)
		end

		if self.is_owned then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_owned then 1 else 0)
		end

		if self.image_asset_id ~= nil and self.image_asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.image_asset_id)
		end

		if self.description ~= nil and self.description ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GamePassDataImpl.decode(input: buffer): GamePassData
		local self = _GamePassDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.price = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.product_id = value
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_owned = value ~= 0
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.image_asset_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GamePassDataImpl.jsonEncode(self: GamePassData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.price ~= nil and self.price ~= 0 then
			output.price = self.price
		end

		if self.product_id ~= nil and self.product_id ~= 0 then
			output.productId = self.product_id
		end

		if self.is_owned then
			output.isOwned = self.is_owned
		end

		if self.image_asset_id ~= nil and self.image_asset_id ~= 0 then
			output.imageAssetId = self.image_asset_id
		end

		if self.description ~= nil and self.description ~= "" then
			output.description = self.description
		end

		return output
	end

	function _GamePassDataImpl.jsonDecode(input: { [string]: any }): GamePassData
		local self = _GamePassDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.price ~= nil then
			self.price = input.price
		end

		if input.product_id ~= nil then
			self.product_id = input.product_id
		end

		if input.productId ~= nil then
			self.product_id = input.productId
		end

		if input.is_owned ~= nil then
			self.is_owned = input.is_owned
		end

		if input.isOwned ~= nil then
			self.is_owned = input.isOwned
		end

		if input.image_asset_id ~= nil then
			self.image_asset_id = input.image_asset_id
		end

		if input.imageAssetId ~= nil then
			self.image_asset_id = input.imageAssetId
		end

		if input.description ~= nil then
			self.description = input.description
		end

		return self
	end

	_GamePassDataImpl.descriptor = {
		name = "GamePassData",
		fullName = "roblox.apppageplatform.shared.v1beta1.GamePassData",
	}

	messages.GamePassData = _GamePassDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GamePassData)
end

return {
	GamePassData = messages.GamePassData,
}
