--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	MarketplaceLookData: _MarketplaceLookDataMessage,
}
local messages: _Messages = {} :: _Messages

type _MarketplaceLookDataImpl = {
	__index: _MarketplaceLookDataImpl,
	new: (fields: _MarketplaceLookDataPartialFields?) -> MarketplaceLookData,
	encode: (self: MarketplaceLookData) -> buffer,
	decode: (input: buffer) -> MarketplaceLookData,
	jsonEncode: (self: MarketplaceLookData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceLookData,
	descriptor: proto.Descriptor,
}

type _MarketplaceLookDataFields = {
	id: string,
}

type _MarketplaceLookDataPartialFields = {
	id: string?,
}

export type MarketplaceLookData = typeof(setmetatable({} :: _MarketplaceLookDataFields, {} :: _MarketplaceLookDataImpl))
type _MarketplaceLookDataMessage = proto.Message<MarketplaceLookData, _MarketplaceLookDataPartialFields>

do
	local _MarketplaceLookDataImpl = {}
	_MarketplaceLookDataImpl.__index = _MarketplaceLookDataImpl

	function _MarketplaceLookDataImpl.new(data: _MarketplaceLookDataPartialFields?): MarketplaceLookData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
		}, _MarketplaceLookDataImpl :: _MarketplaceLookDataImpl)
	end

	function _MarketplaceLookDataImpl.encode(self: MarketplaceLookData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceLookDataImpl.decode(input: buffer): MarketplaceLookData
		local self = _MarketplaceLookDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceLookDataImpl.jsonEncode(self: MarketplaceLookData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		return output
	end

	function _MarketplaceLookDataImpl.jsonDecode(input: { [string]: any }): MarketplaceLookData
		local self = _MarketplaceLookDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		return self
	end

	_MarketplaceLookDataImpl.descriptor = {
		name = "MarketplaceLookData",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceLookData",
	}

	messages.MarketplaceLookData = _MarketplaceLookDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceLookData)
end

return {
	MarketplaceLookData = messages.MarketplaceLookData,
}
