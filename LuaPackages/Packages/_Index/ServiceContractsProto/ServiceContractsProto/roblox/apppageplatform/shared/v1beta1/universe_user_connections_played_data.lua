--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	UniverseUserConnectionsPlayedData: _UniverseUserConnectionsPlayedDataMessage,
}
local messages: _Messages = {} :: _Messages

type _UniverseUserConnectionsPlayedDataImpl = {
	__index: _UniverseUserConnectionsPlayedDataImpl,
	new: (fields: _UniverseUserConnectionsPlayedDataPartialFields?) -> UniverseUserConnectionsPlayedData,
	encode: (self: UniverseUserConnectionsPlayedData) -> buffer,
	decode: (input: buffer) -> UniverseUserConnectionsPlayedData,
	jsonEncode: (self: UniverseUserConnectionsPlayedData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> UniverseUserConnectionsPlayedData,
	descriptor: proto.Descriptor,
}

type _UniverseUserConnectionsPlayedDataFields = {
	user_ids: { number },
	total_count: number,
}

type _UniverseUserConnectionsPlayedDataPartialFields = {
	user_ids: { number }?,
	total_count: number?,
}

export type UniverseUserConnectionsPlayedData = typeof(setmetatable(
	{} :: _UniverseUserConnectionsPlayedDataFields,
	{} :: _UniverseUserConnectionsPlayedDataImpl
))
type _UniverseUserConnectionsPlayedDataMessage = proto.Message<
	UniverseUserConnectionsPlayedData,
	_UniverseUserConnectionsPlayedDataPartialFields
>

do
	local _UniverseUserConnectionsPlayedDataImpl = {}
	_UniverseUserConnectionsPlayedDataImpl.__index = _UniverseUserConnectionsPlayedDataImpl

	function _UniverseUserConnectionsPlayedDataImpl.new(
		data: _UniverseUserConnectionsPlayedDataPartialFields?
	): UniverseUserConnectionsPlayedData
		return setmetatable({
			user_ids = if data == nil or data.user_ids == nil then {} else data.user_ids,
			total_count = if data == nil or data.total_count == nil then 0 else data.total_count,
		}, _UniverseUserConnectionsPlayedDataImpl :: _UniverseUserConnectionsPlayedDataImpl)
	end

	function _UniverseUserConnectionsPlayedDataImpl.encode(self: UniverseUserConnectionsPlayedData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.user_ids ~= nil and #self.user_ids > 0 then
			local packedBuffer = buffer.create(0)
			local packedCursor = 0

			for _, value in self.user_ids do
				packedBuffer, packedCursor = proto.writeVarInt(packedBuffer, packedCursor, value)
			end

			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, packedBuffer, packedCursor)
		end

		if self.total_count ~= nil and self.total_count ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.total_count)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _UniverseUserConnectionsPlayedDataImpl.decode(input: buffer): UniverseUserConnectionsPlayedData
		local self = _UniverseUserConnectionsPlayedDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					table.insert(self.user_ids, value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.total_count = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local length
					length, cursor = proto.readVarInt(input, cursor)

					local limit = cursor + length

					while cursor < limit do
						local value
						value, cursor = proto.readVarIntI64(input, cursor)
						table.insert(self.user_ids, value)
					end

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _UniverseUserConnectionsPlayedDataImpl.jsonEncode(self: UniverseUserConnectionsPlayedData): any
		local output = {}

		if self.user_ids ~= nil and #self.user_ids > 0 then
			local newOutput = {}
			for _, value in self.user_ids do
				table.insert(newOutput, value)
			end
			output.userIds = newOutput
		end

		if self.total_count ~= nil and self.total_count ~= 0 then
			output.totalCount = self.total_count
		end

		return output
	end

	function _UniverseUserConnectionsPlayedDataImpl.jsonDecode(
		input: { [string]: any }
	): UniverseUserConnectionsPlayedData
		local self = _UniverseUserConnectionsPlayedDataImpl.new()

		if input.user_ids ~= nil then
			local newOutput: { number } = {}
			for _, value in input.user_ids do
				table.insert(newOutput, value)
			end

			self.user_ids = newOutput
		end

		if input.userIds ~= nil then
			local newOutput: { number } = {}
			for _, value in input.userIds do
				table.insert(newOutput, value)
			end

			self.user_ids = newOutput
		end

		if input.total_count ~= nil then
			self.total_count = input.total_count
		end

		if input.totalCount ~= nil then
			self.total_count = input.totalCount
		end

		return self
	end

	_UniverseUserConnectionsPlayedDataImpl.descriptor = {
		name = "UniverseUserConnectionsPlayedData",
		fullName = "roblox.apppageplatform.shared.v1beta1.UniverseUserConnectionsPlayedData",
	}

	messages.UniverseUserConnectionsPlayedData = _UniverseUserConnectionsPlayedDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.UniverseUserConnectionsPlayedData)
end

return {
	UniverseUserConnectionsPlayedData = messages.UniverseUserConnectionsPlayedData,
}
