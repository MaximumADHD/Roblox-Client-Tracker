--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	NotificationBadgeData: _NotificationBadgeDataMessage,
}
local messages: _Messages = {} :: _Messages

type _NotificationBadgeDataImpl = {
	__index: _NotificationBadgeDataImpl,
	new: (fields: _NotificationBadgeDataPartialFields?) -> NotificationBadgeData,
	encode: (self: NotificationBadgeData) -> buffer,
	decode: (input: buffer) -> NotificationBadgeData,
	jsonEncode: (self: NotificationBadgeData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> NotificationBadgeData,
	descriptor: proto.Descriptor,
}

type _NotificationBadgeDataFields = {
	count: number,
	is_visible: boolean,
}

type _NotificationBadgeDataPartialFields = {
	count: number?,
	is_visible: boolean?,
}

export type NotificationBadgeData = typeof(setmetatable(
	{} :: _NotificationBadgeDataFields,
	{} :: _NotificationBadgeDataImpl
))
type _NotificationBadgeDataMessage = proto.Message<NotificationBadgeData, _NotificationBadgeDataPartialFields>

do
	local _NotificationBadgeDataImpl = {}
	_NotificationBadgeDataImpl.__index = _NotificationBadgeDataImpl

	function _NotificationBadgeDataImpl.new(data: _NotificationBadgeDataPartialFields?): NotificationBadgeData
		return setmetatable({
			count = if data == nil or data.count == nil then 0 else data.count,
			is_visible = if data == nil or data.is_visible == nil then false else data.is_visible,
		}, _NotificationBadgeDataImpl :: _NotificationBadgeDataImpl)
	end

	function _NotificationBadgeDataImpl.encode(self: NotificationBadgeData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.count ~= nil and self.count ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.count)
		end

		if self.is_visible then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_visible then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _NotificationBadgeDataImpl.decode(input: buffer): NotificationBadgeData
		local self = _NotificationBadgeDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.count = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_visible = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _NotificationBadgeDataImpl.jsonEncode(self: NotificationBadgeData): any
		local output = {}

		if self.count ~= nil and self.count ~= 0 then
			output.count = self.count
		end

		if self.is_visible then
			output.isVisible = self.is_visible
		end

		return output
	end

	function _NotificationBadgeDataImpl.jsonDecode(input: { [string]: any }): NotificationBadgeData
		local self = _NotificationBadgeDataImpl.new()

		if input.count ~= nil then
			self.count = input.count
		end

		if input.is_visible ~= nil then
			self.is_visible = input.is_visible
		end

		if input.isVisible ~= nil then
			self.is_visible = input.isVisible
		end

		return self
	end

	_NotificationBadgeDataImpl.descriptor = {
		name = "NotificationBadgeData",
		fullName = "roblox.apppageplatform.shared.v1beta1.NotificationBadgeData",
	}

	messages.NotificationBadgeData = _NotificationBadgeDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.NotificationBadgeData)
end

return {
	NotificationBadgeData = messages.NotificationBadgeData,
}
