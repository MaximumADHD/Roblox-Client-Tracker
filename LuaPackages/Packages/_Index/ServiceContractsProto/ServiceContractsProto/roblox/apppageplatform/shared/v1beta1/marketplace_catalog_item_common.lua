--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	MarketplaceCatalogTaxonomyNode: _MarketplaceCatalogTaxonomyNodeMessage,
	MarketplaceCatalogTimedOption: _MarketplaceCatalogTimedOptionMessage,
	MarketplaceCatalogVariantAttribute: _MarketplaceCatalogVariantAttributeMessage,
	MarketplaceCatalogBundledItem: _MarketplaceCatalogBundledItemMessage,
}
local messages: _Messages = {} :: _Messages

type _MarketplaceCatalogTaxonomyNodeImpl = {
	__index: _MarketplaceCatalogTaxonomyNodeImpl,
	new: (fields: _MarketplaceCatalogTaxonomyNodePartialFields?) -> MarketplaceCatalogTaxonomyNode,
	encode: (self: MarketplaceCatalogTaxonomyNode) -> buffer,
	decode: (input: buffer) -> MarketplaceCatalogTaxonomyNode,
	jsonEncode: (self: MarketplaceCatalogTaxonomyNode) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceCatalogTaxonomyNode,
	descriptor: proto.Descriptor,
}

type _MarketplaceCatalogTaxonomyNodeFields = {
	taxonomy_id: string?,
	taxonomy_name: string,
}

type _MarketplaceCatalogTaxonomyNodePartialFields = {
	taxonomy_id: string?,
	taxonomy_name: string?,
}

export type MarketplaceCatalogTaxonomyNode = typeof(setmetatable(
	{} :: _MarketplaceCatalogTaxonomyNodeFields,
	{} :: _MarketplaceCatalogTaxonomyNodeImpl
))
type _MarketplaceCatalogTaxonomyNodeMessage = proto.Message<
	MarketplaceCatalogTaxonomyNode,
	_MarketplaceCatalogTaxonomyNodePartialFields
>

type _MarketplaceCatalogTimedOptionImpl = {
	__index: _MarketplaceCatalogTimedOptionImpl,
	new: (fields: _MarketplaceCatalogTimedOptionPartialFields?) -> MarketplaceCatalogTimedOption,
	encode: (self: MarketplaceCatalogTimedOption) -> buffer,
	decode: (input: buffer) -> MarketplaceCatalogTimedOption,
	jsonEncode: (self: MarketplaceCatalogTimedOption) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceCatalogTimedOption,
	descriptor: proto.Descriptor,
}

type _MarketplaceCatalogTimedOptionFields = {
	days: number,
	price: number,
	selected: boolean?,
}

type _MarketplaceCatalogTimedOptionPartialFields = {
	days: number?,
	price: number?,
	selected: boolean?,
}

export type MarketplaceCatalogTimedOption = typeof(setmetatable(
	{} :: _MarketplaceCatalogTimedOptionFields,
	{} :: _MarketplaceCatalogTimedOptionImpl
))
type _MarketplaceCatalogTimedOptionMessage = proto.Message<
	MarketplaceCatalogTimedOption,
	_MarketplaceCatalogTimedOptionPartialFields
>

type _MarketplaceCatalogVariantAttributeImpl = {
	__index: _MarketplaceCatalogVariantAttributeImpl,
	new: (fields: _MarketplaceCatalogVariantAttributePartialFields?) -> MarketplaceCatalogVariantAttribute,
	encode: (self: MarketplaceCatalogVariantAttribute) -> buffer,
	decode: (input: buffer) -> MarketplaceCatalogVariantAttribute,
	jsonEncode: (self: MarketplaceCatalogVariantAttribute) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceCatalogVariantAttribute,
	descriptor: proto.Descriptor,
}

type _MarketplaceCatalogVariantAttributeFields = {
	name: string,
	value: string,
}

type _MarketplaceCatalogVariantAttributePartialFields = {
	name: string?,
	value: string?,
}

export type MarketplaceCatalogVariantAttribute = typeof(setmetatable(
	{} :: _MarketplaceCatalogVariantAttributeFields,
	{} :: _MarketplaceCatalogVariantAttributeImpl
))
type _MarketplaceCatalogVariantAttributeMessage = proto.Message<
	MarketplaceCatalogVariantAttribute,
	_MarketplaceCatalogVariantAttributePartialFields
>

type _MarketplaceCatalogBundledItemImpl = {
	__index: _MarketplaceCatalogBundledItemImpl,
	new: (fields: _MarketplaceCatalogBundledItemPartialFields?) -> MarketplaceCatalogBundledItem,
	encode: (self: MarketplaceCatalogBundledItem) -> buffer,
	decode: (input: buffer) -> MarketplaceCatalogBundledItem,
	jsonEncode: (self: MarketplaceCatalogBundledItem) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceCatalogBundledItem,
	descriptor: proto.Descriptor,
}

type _MarketplaceCatalogBundledItemFields = {
	id: string,
	asset_type: string?,
	type: string,
}

type _MarketplaceCatalogBundledItemPartialFields = {
	id: string?,
	asset_type: string?,
	type: string?,
}

export type MarketplaceCatalogBundledItem = typeof(setmetatable(
	{} :: _MarketplaceCatalogBundledItemFields,
	{} :: _MarketplaceCatalogBundledItemImpl
))
type _MarketplaceCatalogBundledItemMessage = proto.Message<
	MarketplaceCatalogBundledItem,
	_MarketplaceCatalogBundledItemPartialFields
>

do
	local _MarketplaceCatalogTaxonomyNodeImpl = {}
	_MarketplaceCatalogTaxonomyNodeImpl.__index = _MarketplaceCatalogTaxonomyNodeImpl

	function _MarketplaceCatalogTaxonomyNodeImpl.new(
		data: _MarketplaceCatalogTaxonomyNodePartialFields?
	): MarketplaceCatalogTaxonomyNode
		return setmetatable({
			taxonomy_id = if data == nil or data.taxonomy_id == nil then nil else data.taxonomy_id,
			taxonomy_name = if data == nil or data.taxonomy_name == nil then "" else data.taxonomy_name,
		}, _MarketplaceCatalogTaxonomyNodeImpl :: _MarketplaceCatalogTaxonomyNodeImpl)
	end

	function _MarketplaceCatalogTaxonomyNodeImpl.encode(self: MarketplaceCatalogTaxonomyNode): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.taxonomy_id ~= nil then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.taxonomy_id)
		end

		if self.taxonomy_name ~= nil and self.taxonomy_name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.taxonomy_name)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceCatalogTaxonomyNodeImpl.decode(input: buffer): MarketplaceCatalogTaxonomyNode
		local self = _MarketplaceCatalogTaxonomyNodeImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.taxonomy_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.taxonomy_name = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceCatalogTaxonomyNodeImpl.jsonEncode(self: MarketplaceCatalogTaxonomyNode): any
		local output = {}

		if self.taxonomy_id ~= nil then
			output.taxonomyId = self.taxonomy_id
		end

		if self.taxonomy_name ~= nil and self.taxonomy_name ~= "" then
			output.taxonomyName = self.taxonomy_name
		end

		return output
	end

	function _MarketplaceCatalogTaxonomyNodeImpl.jsonDecode(input: { [string]: any }): MarketplaceCatalogTaxonomyNode
		local self = _MarketplaceCatalogTaxonomyNodeImpl.new()

		if input.taxonomy_id ~= nil then
			self.taxonomy_id = input.taxonomy_id
		end

		if input.taxonomyId ~= nil then
			self.taxonomy_id = input.taxonomyId
		end

		if input.taxonomy_name ~= nil then
			self.taxonomy_name = input.taxonomy_name
		end

		if input.taxonomyName ~= nil then
			self.taxonomy_name = input.taxonomyName
		end

		return self
	end

	_MarketplaceCatalogTaxonomyNodeImpl.descriptor = {
		name = "MarketplaceCatalogTaxonomyNode",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceCatalogTaxonomyNode",
	}

	messages.MarketplaceCatalogTaxonomyNode = _MarketplaceCatalogTaxonomyNodeImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceCatalogTaxonomyNode)
end

do
	local _MarketplaceCatalogTimedOptionImpl = {}
	_MarketplaceCatalogTimedOptionImpl.__index = _MarketplaceCatalogTimedOptionImpl

	function _MarketplaceCatalogTimedOptionImpl.new(
		data: _MarketplaceCatalogTimedOptionPartialFields?
	): MarketplaceCatalogTimedOption
		return setmetatable({
			days = if data == nil or data.days == nil then 0 else data.days,
			price = if data == nil or data.price == nil then 0 else data.price,
			selected = if data == nil or data.selected == nil then nil else data.selected,
		}, _MarketplaceCatalogTimedOptionImpl :: _MarketplaceCatalogTimedOptionImpl)
	end

	function _MarketplaceCatalogTimedOptionImpl.encode(self: MarketplaceCatalogTimedOption): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.days ~= nil and self.days ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.days)
		end

		if self.price ~= nil and self.price ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.price)
		end

		if self.selected ~= nil then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.selected then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceCatalogTimedOptionImpl.decode(input: buffer): MarketplaceCatalogTimedOption
		local self = _MarketplaceCatalogTimedOptionImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.days = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.price = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.selected = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceCatalogTimedOptionImpl.jsonEncode(self: MarketplaceCatalogTimedOption): any
		local output = {}

		if self.days ~= nil and self.days ~= 0 then
			output.days = self.days
		end

		if self.price ~= nil and self.price ~= 0 then
			output.price = self.price
		end

		if self.selected ~= nil then
			output.selected = self.selected
		end

		return output
	end

	function _MarketplaceCatalogTimedOptionImpl.jsonDecode(input: { [string]: any }): MarketplaceCatalogTimedOption
		local self = _MarketplaceCatalogTimedOptionImpl.new()

		if input.days ~= nil then
			self.days = input.days
		end

		if input.price ~= nil then
			self.price = input.price
		end

		if input.selected ~= nil then
			self.selected = input.selected
		end

		return self
	end

	_MarketplaceCatalogTimedOptionImpl.descriptor = {
		name = "MarketplaceCatalogTimedOption",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceCatalogTimedOption",
	}

	messages.MarketplaceCatalogTimedOption = _MarketplaceCatalogTimedOptionImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceCatalogTimedOption)
end

do
	local _MarketplaceCatalogVariantAttributeImpl = {}
	_MarketplaceCatalogVariantAttributeImpl.__index = _MarketplaceCatalogVariantAttributeImpl

	function _MarketplaceCatalogVariantAttributeImpl.new(
		data: _MarketplaceCatalogVariantAttributePartialFields?
	): MarketplaceCatalogVariantAttribute
		return setmetatable({
			name = if data == nil or data.name == nil then "" else data.name,
			value = if data == nil or data.value == nil then "" else data.value,
		}, _MarketplaceCatalogVariantAttributeImpl :: _MarketplaceCatalogVariantAttributeImpl)
	end

	function _MarketplaceCatalogVariantAttributeImpl.encode(self: MarketplaceCatalogVariantAttribute): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceCatalogVariantAttributeImpl.decode(input: buffer): MarketplaceCatalogVariantAttribute
		local self = _MarketplaceCatalogVariantAttributeImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceCatalogVariantAttributeImpl.jsonEncode(self: MarketplaceCatalogVariantAttribute): any
		local output = {}

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _MarketplaceCatalogVariantAttributeImpl.jsonDecode(
		input: { [string]: any }
	): MarketplaceCatalogVariantAttribute
		local self = _MarketplaceCatalogVariantAttributeImpl.new()

		if input.name ~= nil then
			self.name = input.name
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_MarketplaceCatalogVariantAttributeImpl.descriptor = {
		name = "MarketplaceCatalogVariantAttribute",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceCatalogVariantAttribute",
	}

	messages.MarketplaceCatalogVariantAttribute = _MarketplaceCatalogVariantAttributeImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceCatalogVariantAttribute)
end

do
	local _MarketplaceCatalogBundledItemImpl = {}
	_MarketplaceCatalogBundledItemImpl.__index = _MarketplaceCatalogBundledItemImpl

	function _MarketplaceCatalogBundledItemImpl.new(
		data: _MarketplaceCatalogBundledItemPartialFields?
	): MarketplaceCatalogBundledItem
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			asset_type = if data == nil or data.asset_type == nil then nil else data.asset_type,
			type = if data == nil or data.type == nil then "" else data.type,
		}, _MarketplaceCatalogBundledItemImpl :: _MarketplaceCatalogBundledItemImpl)
	end

	function _MarketplaceCatalogBundledItemImpl.encode(self: MarketplaceCatalogBundledItem): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.asset_type ~= nil then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.asset_type)
		end

		if self.type ~= nil and self.type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceCatalogBundledItemImpl.decode(input: buffer): MarketplaceCatalogBundledItem
		local self = _MarketplaceCatalogBundledItemImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.asset_type = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceCatalogBundledItemImpl.jsonEncode(self: MarketplaceCatalogBundledItem): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.asset_type ~= nil then
			output.assetType = self.asset_type
		end

		if self.type ~= nil and self.type ~= "" then
			output.type = self.type
		end

		return output
	end

	function _MarketplaceCatalogBundledItemImpl.jsonDecode(input: { [string]: any }): MarketplaceCatalogBundledItem
		local self = _MarketplaceCatalogBundledItemImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.asset_type ~= nil then
			self.asset_type = input.asset_type
		end

		if input.assetType ~= nil then
			self.asset_type = input.assetType
		end

		if input.type ~= nil then
			self.type = input.type
		end

		return self
	end

	_MarketplaceCatalogBundledItemImpl.descriptor = {
		name = "MarketplaceCatalogBundledItem",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceCatalogBundledItem",
	}

	messages.MarketplaceCatalogBundledItem = _MarketplaceCatalogBundledItemImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceCatalogBundledItem)
end

return {
	MarketplaceCatalogTaxonomyNode = messages.MarketplaceCatalogTaxonomyNode,
	MarketplaceCatalogTimedOption = messages.MarketplaceCatalogTimedOption,
	MarketplaceCatalogVariantAttribute = messages.MarketplaceCatalogVariantAttribute,
	MarketplaceCatalogBundledItem = messages.MarketplaceCatalogBundledItem,
}
