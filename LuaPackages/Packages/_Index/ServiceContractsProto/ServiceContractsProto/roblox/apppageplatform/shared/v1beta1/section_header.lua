--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	SectionHeaderSchema: _SectionHeaderSchemaMessage,
	SectionHeaderSchema_Props: _SectionHeaderSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _SectionHeaderSchemaImpl = {
	__index: _SectionHeaderSchemaImpl,
	new: (fields: _SectionHeaderSchemaPartialFields?) -> SectionHeaderSchema,
	encode: (self: SectionHeaderSchema) -> buffer,
	decode: (input: buffer) -> SectionHeaderSchema,
	jsonEncode: (self: SectionHeaderSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SectionHeaderSchema,
	descriptor: proto.Descriptor,
}

type _SectionHeaderSchemaFields = {
	props: SectionHeaderSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _SectionHeaderSchemaPartialFields = {
	props: SectionHeaderSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type SectionHeaderSchema = typeof(setmetatable({} :: _SectionHeaderSchemaFields, {} :: _SectionHeaderSchemaImpl))
type _SectionHeaderSchemaMessage = proto.Message<SectionHeaderSchema, _SectionHeaderSchemaPartialFields>

type _SectionHeaderSchema_PropsImpl = {
	__index: _SectionHeaderSchema_PropsImpl,
	new: (fields: _SectionHeaderSchema_PropsPartialFields?) -> SectionHeaderSchema_Props,
	encode: (self: SectionHeaderSchema_Props) -> buffer,
	decode: (input: buffer) -> SectionHeaderSchema_Props,
	jsonEncode: (self: SectionHeaderSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SectionHeaderSchema_Props,
	descriptor: proto.Descriptor,
}

type _SectionHeaderSchema_PropsFields = {
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	icon_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	info_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	on_title_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	on_subtitle_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	on_info_icon_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	subtitle_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	subtitle_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	subtitle_icon: _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp?,
	subtitle_icon_width: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	subtitle_font_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	subtitle_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	subtitle_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	title_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	title_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	title_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	title_icon: _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp?,
	title_icon_width: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	title_font_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	title_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	vertical_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	allow_interaction_for_directional_input: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
}

type _SectionHeaderSchema_PropsPartialFields = {
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	z_index: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	icon_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	info_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	on_title_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	on_subtitle_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	on_info_icon_activated: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	subtitle_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	subtitle_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	subtitle_icon: _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp?,
	subtitle_icon_width: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	subtitle_font_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	subtitle_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	subtitle_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	title_color: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp?,
	title_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	title_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	title_icon: _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp?,
	title_icon_width: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	title_font_style: _roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp?,
	title_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	vertical_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	allow_interaction_for_directional_input: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
}

export type SectionHeaderSchema_Props = typeof(setmetatable(
	{} :: _SectionHeaderSchema_PropsFields,
	{} :: _SectionHeaderSchema_PropsImpl
))
type _SectionHeaderSchema_PropsMessage = proto.Message<
	SectionHeaderSchema_Props,
	_SectionHeaderSchema_PropsPartialFields
>

do
	local _SectionHeaderSchemaImpl = {}
	_SectionHeaderSchemaImpl.__index = _SectionHeaderSchemaImpl

	function _SectionHeaderSchemaImpl.new(data: _SectionHeaderSchemaPartialFields?): SectionHeaderSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _SectionHeaderSchemaImpl :: _SectionHeaderSchemaImpl)
	end

	function _SectionHeaderSchemaImpl.encode(self: SectionHeaderSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SectionHeaderSchemaImpl.decode(input: buffer): SectionHeaderSchema
		local self = _SectionHeaderSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.SectionHeaderSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SectionHeaderSchemaImpl.jsonEncode(self: SectionHeaderSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _SectionHeaderSchemaImpl.jsonDecode(input: { [string]: any }): SectionHeaderSchema
		local self = _SectionHeaderSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.SectionHeaderSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_SectionHeaderSchemaImpl.descriptor = {
		name = "SectionHeaderSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.SectionHeaderSchema",
	}

	messages.SectionHeaderSchema = _SectionHeaderSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SectionHeaderSchema)
end

do
	local _SectionHeaderSchema_PropsImpl = {}
	_SectionHeaderSchema_PropsImpl.__index = _SectionHeaderSchema_PropsImpl

	function _SectionHeaderSchema_PropsImpl.new(
		data: _SectionHeaderSchema_PropsPartialFields?
	): SectionHeaderSchema_Props
		return setmetatable({
			automatic_size = if data == nil or data.automatic_size == nil then nil else data.automatic_size,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			size = if data == nil or data.size == nil then nil else data.size,
			z_index = if data == nil or data.z_index == nil then nil else data.z_index,
			position = if data == nil or data.position == nil then nil else data.position,
			icon_component = if data == nil or data.icon_component == nil then nil else data.icon_component,
			info_text = if data == nil or data.info_text == nil then nil else data.info_text,
			on_title_activated = if data == nil or data.on_title_activated == nil then nil else data.on_title_activated,
			on_subtitle_activated = if data == nil or data.on_subtitle_activated == nil
				then nil
				else data.on_subtitle_activated,
			on_info_icon_activated = if data == nil or data.on_info_icon_activated == nil
				then nil
				else data.on_info_icon_activated,
			subtitle_color = if data == nil or data.subtitle_color == nil then nil else data.subtitle_color,
			subtitle_component = if data == nil or data.subtitle_component == nil then nil else data.subtitle_component,
			subtitle_icon = if data == nil or data.subtitle_icon == nil then nil else data.subtitle_icon,
			subtitle_icon_width = if data == nil or data.subtitle_icon_width == nil
				then nil
				else data.subtitle_icon_width,
			subtitle_font_style = if data == nil or data.subtitle_font_style == nil
				then nil
				else data.subtitle_font_style,
			subtitle_gap = if data == nil or data.subtitle_gap == nil then nil else data.subtitle_gap,
			subtitle_text = if data == nil or data.subtitle_text == nil then nil else data.subtitle_text,
			title_color = if data == nil or data.title_color == nil then nil else data.title_color,
			title_component = if data == nil or data.title_component == nil then nil else data.title_component,
			title_gap = if data == nil or data.title_gap == nil then nil else data.title_gap,
			title_icon = if data == nil or data.title_icon == nil then nil else data.title_icon,
			title_icon_width = if data == nil or data.title_icon_width == nil then nil else data.title_icon_width,
			title_font_style = if data == nil or data.title_font_style == nil then nil else data.title_font_style,
			title_text = if data == nil or data.title_text == nil then nil else data.title_text,
			vertical_gap = if data == nil or data.vertical_gap == nil then nil else data.vertical_gap,
			allow_interaction_for_directional_input = if data == nil
					or data.allow_interaction_for_directional_input == nil
				then nil
				else data.allow_interaction_for_directional_input,
		}, _SectionHeaderSchema_PropsImpl :: _SectionHeaderSchema_PropsImpl)
	end

	function _SectionHeaderSchema_PropsImpl.encode(self: SectionHeaderSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.automatic_size ~= nil then
			local encoded = self.automatic_size:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.z_index ~= nil then
			local encoded = self.z_index:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.icon_component ~= nil then
			local encoded = self.icon_component:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.info_text ~= nil then
			local encoded = self.info_text:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_title_activated ~= nil then
			local encoded = self.on_title_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_subtitle_activated ~= nil then
			local encoded = self.on_subtitle_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_info_icon_activated ~= nil then
			local encoded = self.on_info_icon_activated:encode()
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_color ~= nil then
			local encoded = self.subtitle_color:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_component ~= nil then
			local encoded = self.subtitle_component:encode()
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_icon ~= nil then
			local encoded = self.subtitle_icon:encode()
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_icon_width ~= nil then
			local encoded = self.subtitle_icon_width:encode()
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_font_style ~= nil then
			local encoded = self.subtitle_font_style:encode()
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_gap ~= nil then
			local encoded = self.subtitle_gap:encode()
			output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.subtitle_text ~= nil then
			local encoded = self.subtitle_text:encode()
			output, cursor = proto.writeTag(output, cursor, 18, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_color ~= nil then
			local encoded = self.title_color:encode()
			output, cursor = proto.writeTag(output, cursor, 19, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_component ~= nil then
			local encoded = self.title_component:encode()
			output, cursor = proto.writeTag(output, cursor, 20, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_gap ~= nil then
			local encoded = self.title_gap:encode()
			output, cursor = proto.writeTag(output, cursor, 21, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_icon ~= nil then
			local encoded = self.title_icon:encode()
			output, cursor = proto.writeTag(output, cursor, 22, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_icon_width ~= nil then
			local encoded = self.title_icon_width:encode()
			output, cursor = proto.writeTag(output, cursor, 23, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_font_style ~= nil then
			local encoded = self.title_font_style:encode()
			output, cursor = proto.writeTag(output, cursor, 24, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_text ~= nil then
			local encoded = self.title_text:encode()
			output, cursor = proto.writeTag(output, cursor, 25, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.vertical_gap ~= nil then
			local encoded = self.vertical_gap:encode()
			output, cursor = proto.writeTag(output, cursor, 26, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.allow_interaction_for_directional_input ~= nil then
			local encoded = self.allow_interaction_for_directional_input:encode()
			output, cursor = proto.writeTag(output, cursor, 27, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SectionHeaderSchema_PropsImpl.decode(input: buffer): SectionHeaderSchema_Props
		local self = _SectionHeaderSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.automatic_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.icon_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.info_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_title_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_subtitle_activated = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_info_icon_activated =
						_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_icon = _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.decode(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_icon_width = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_font_style =
						_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.decode(value)
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 18 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.subtitle_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 19 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.decode(value)
					continue
				elseif field == 20 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 21 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 22 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_icon = _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.decode(value)
					continue
				elseif field == 23 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_icon_width = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 24 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_font_style =
						_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.decode(value)
					continue
				elseif field == 25 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 26 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.vertical_gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 27 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.allow_interaction_for_directional_input =
						_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SectionHeaderSchema_PropsImpl.jsonEncode(self: SectionHeaderSchema_Props): any
		local output = {}

		if self.automatic_size ~= nil then
			output.automaticSize = self.automatic_size:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.z_index ~= nil then
			output.zIndex = self.z_index:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.icon_component ~= nil then
			output.iconComponent = self.icon_component:jsonEncode()
		end

		if self.info_text ~= nil then
			output.infoText = self.info_text:jsonEncode()
		end

		if self.on_title_activated ~= nil then
			output.onTitleActivated = self.on_title_activated:jsonEncode()
		end

		if self.on_subtitle_activated ~= nil then
			output.onSubtitleActivated = self.on_subtitle_activated:jsonEncode()
		end

		if self.on_info_icon_activated ~= nil then
			output.onInfoIconActivated = self.on_info_icon_activated:jsonEncode()
		end

		if self.subtitle_color ~= nil then
			output.subtitleColor = self.subtitle_color:jsonEncode()
		end

		if self.subtitle_component ~= nil then
			output.subtitleComponent = self.subtitle_component:jsonEncode()
		end

		if self.subtitle_icon ~= nil then
			output.subtitleIcon = self.subtitle_icon:jsonEncode()
		end

		if self.subtitle_icon_width ~= nil then
			output.subtitleIconWidth = self.subtitle_icon_width:jsonEncode()
		end

		if self.subtitle_font_style ~= nil then
			output.subtitleFontStyle = self.subtitle_font_style:jsonEncode()
		end

		if self.subtitle_gap ~= nil then
			output.subtitleGap = self.subtitle_gap:jsonEncode()
		end

		if self.subtitle_text ~= nil then
			output.subtitleText = self.subtitle_text:jsonEncode()
		end

		if self.title_color ~= nil then
			output.titleColor = self.title_color:jsonEncode()
		end

		if self.title_component ~= nil then
			output.titleComponent = self.title_component:jsonEncode()
		end

		if self.title_gap ~= nil then
			output.titleGap = self.title_gap:jsonEncode()
		end

		if self.title_icon ~= nil then
			output.titleIcon = self.title_icon:jsonEncode()
		end

		if self.title_icon_width ~= nil then
			output.titleIconWidth = self.title_icon_width:jsonEncode()
		end

		if self.title_font_style ~= nil then
			output.titleFontStyle = self.title_font_style:jsonEncode()
		end

		if self.title_text ~= nil then
			output.titleText = self.title_text:jsonEncode()
		end

		if self.vertical_gap ~= nil then
			output.verticalGap = self.vertical_gap:jsonEncode()
		end

		if self.allow_interaction_for_directional_input ~= nil then
			output.allowInteractionForDirectionalInput = self.allow_interaction_for_directional_input:jsonEncode()
		end

		return output
	end

	function _SectionHeaderSchema_PropsImpl.jsonDecode(input: { [string]: any }): SectionHeaderSchema_Props
		local self = _SectionHeaderSchema_PropsImpl.new()

		if input.automatic_size ~= nil then
			self.automatic_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.automatic_size)
		end

		if input.automaticSize ~= nil then
			self.automatic_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.automaticSize)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.z_index ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.z_index)
		end

		if input.zIndex ~= nil then
			self.z_index = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.zIndex)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.icon_component ~= nil then
			self.icon_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.icon_component)
		end

		if input.iconComponent ~= nil then
			self.icon_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.iconComponent)
		end

		if input.info_text ~= nil then
			self.info_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.info_text)
		end

		if input.infoText ~= nil then
			self.info_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.infoText)
		end

		if input.on_title_activated ~= nil then
			self.on_title_activated =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_title_activated)
		end

		if input.onTitleActivated ~= nil then
			self.on_title_activated =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onTitleActivated)
		end

		if input.on_subtitle_activated ~= nil then
			self.on_subtitle_activated =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_subtitle_activated)
		end

		if input.onSubtitleActivated ~= nil then
			self.on_subtitle_activated =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onSubtitleActivated)
		end

		if input.on_info_icon_activated ~= nil then
			self.on_info_icon_activated =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_info_icon_activated)
		end

		if input.onInfoIconActivated ~= nil then
			self.on_info_icon_activated =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onInfoIconActivated)
		end

		if input.subtitle_color ~= nil then
			self.subtitle_color =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.subtitle_color)
		end

		if input.subtitleColor ~= nil then
			self.subtitle_color =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.subtitleColor)
		end

		if input.subtitle_component ~= nil then
			self.subtitle_component = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
				input.subtitle_component
			)
		end

		if input.subtitleComponent ~= nil then
			self.subtitle_component = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
				input.subtitleComponent
			)
		end

		if input.subtitle_icon ~= nil then
			self.subtitle_icon =
				_roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.jsonDecode(input.subtitle_icon)
		end

		if input.subtitleIcon ~= nil then
			self.subtitle_icon =
				_roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.jsonDecode(input.subtitleIcon)
		end

		if input.subtitle_icon_width ~= nil then
			self.subtitle_icon_width =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.subtitle_icon_width)
		end

		if input.subtitleIconWidth ~= nil then
			self.subtitle_icon_width =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.subtitleIconWidth)
		end

		if input.subtitle_font_style ~= nil then
			self.subtitle_font_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.subtitle_font_style)
		end

		if input.subtitleFontStyle ~= nil then
			self.subtitle_font_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.subtitleFontStyle)
		end

		if input.subtitle_gap ~= nil then
			self.subtitle_gap =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.subtitle_gap)
		end

		if input.subtitleGap ~= nil then
			self.subtitle_gap =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.subtitleGap)
		end

		if input.subtitle_text ~= nil then
			self.subtitle_text =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.subtitle_text)
		end

		if input.subtitleText ~= nil then
			self.subtitle_text =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.subtitleText)
		end

		if input.title_color ~= nil then
			self.title_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.title_color)
		end

		if input.titleColor ~= nil then
			self.title_color = _roblox_apppageplatform_shared_v1beta1_prop_types.ColorProp.jsonDecode(input.titleColor)
		end

		if input.title_component ~= nil then
			self.title_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.title_component)
		end

		if input.titleComponent ~= nil then
			self.title_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.titleComponent)
		end

		if input.title_gap ~= nil then
			self.title_gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.title_gap)
		end

		if input.titleGap ~= nil then
			self.title_gap = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.titleGap)
		end

		if input.title_icon ~= nil then
			self.title_icon = _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.jsonDecode(input.title_icon)
		end

		if input.titleIcon ~= nil then
			self.title_icon = _roblox_apppageplatform_shared_v1beta1_prop_types.IconProp.jsonDecode(input.titleIcon)
		end

		if input.title_icon_width ~= nil then
			self.title_icon_width =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.title_icon_width)
		end

		if input.titleIconWidth ~= nil then
			self.title_icon_width =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.titleIconWidth)
		end

		if input.title_font_style ~= nil then
			self.title_font_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.title_font_style)
		end

		if input.titleFontStyle ~= nil then
			self.title_font_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.TypographyProp.jsonDecode(input.titleFontStyle)
		end

		if input.title_text ~= nil then
			self.title_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.title_text)
		end

		if input.titleText ~= nil then
			self.title_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.titleText)
		end

		if input.vertical_gap ~= nil then
			self.vertical_gap =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.vertical_gap)
		end

		if input.verticalGap ~= nil then
			self.vertical_gap =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.verticalGap)
		end

		if input.allow_interaction_for_directional_input ~= nil then
			self.allow_interaction_for_directional_input =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(
					input.allow_interaction_for_directional_input
				)
		end

		if input.allowInteractionForDirectionalInput ~= nil then
			self.allow_interaction_for_directional_input =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(
					input.allowInteractionForDirectionalInput
				)
		end

		return self
	end

	_SectionHeaderSchema_PropsImpl.descriptor = {
		name = "SectionHeaderSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.SectionHeaderSchema_Props = _SectionHeaderSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SectionHeaderSchema_Props)
end

return {
	SectionHeaderSchema = messages.SectionHeaderSchema,
	SectionHeaderSchema_Props = messages.SectionHeaderSchema_Props,
}
