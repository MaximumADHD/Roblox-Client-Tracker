--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	MarketplaceCatalogCategoryData: _MarketplaceCatalogCategoryDataMessage,
}
local messages: _Messages = {} :: _Messages

type _MarketplaceCatalogCategoryDataImpl = {
	__index: _MarketplaceCatalogCategoryDataImpl,
	new: (fields: _MarketplaceCatalogCategoryDataPartialFields?) -> MarketplaceCatalogCategoryData,
	encode: (self: MarketplaceCatalogCategoryData) -> buffer,
	decode: (input: buffer) -> MarketplaceCatalogCategoryData,
	jsonEncode: (self: MarketplaceCatalogCategoryData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceCatalogCategoryData,
	descriptor: proto.Descriptor,
}

type _MarketplaceCatalogCategoryDataFields = {
	id: string,
	title: string,
	name: string,
	taxonomy_id: string,
	buttons_enabled: { string },
	subcategories: { string },
	default_subcategory_id: string,
	asset_types: { string },
	bundle_types: { string },
	can_animate_camera_based_mood: boolean,
	camera_focus: string,
	camera_zoom_radius: number,
	tile_type: string,
	looks_widget_context_for_organic_content: string,
	time_to_refresh: number,
	category_id: number,
	subcategory_id: number,
}

type _MarketplaceCatalogCategoryDataPartialFields = {
	id: string?,
	title: string?,
	name: string?,
	taxonomy_id: string?,
	buttons_enabled: { string }?,
	subcategories: { string }?,
	default_subcategory_id: string?,
	asset_types: { string }?,
	bundle_types: { string }?,
	can_animate_camera_based_mood: boolean?,
	camera_focus: string?,
	camera_zoom_radius: number?,
	tile_type: string?,
	looks_widget_context_for_organic_content: string?,
	time_to_refresh: number?,
	category_id: number?,
	subcategory_id: number?,
}

export type MarketplaceCatalogCategoryData = typeof(setmetatable(
	{} :: _MarketplaceCatalogCategoryDataFields,
	{} :: _MarketplaceCatalogCategoryDataImpl
))
type _MarketplaceCatalogCategoryDataMessage = proto.Message<
	MarketplaceCatalogCategoryData,
	_MarketplaceCatalogCategoryDataPartialFields
>

do
	local _MarketplaceCatalogCategoryDataImpl = {}
	_MarketplaceCatalogCategoryDataImpl.__index = _MarketplaceCatalogCategoryDataImpl

	function _MarketplaceCatalogCategoryDataImpl.new(
		data: _MarketplaceCatalogCategoryDataPartialFields?
	): MarketplaceCatalogCategoryData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			title = if data == nil or data.title == nil then "" else data.title,
			name = if data == nil or data.name == nil then "" else data.name,
			taxonomy_id = if data == nil or data.taxonomy_id == nil then "" else data.taxonomy_id,
			buttons_enabled = if data == nil or data.buttons_enabled == nil then {} else data.buttons_enabled,
			subcategories = if data == nil or data.subcategories == nil then {} else data.subcategories,
			default_subcategory_id = if data == nil or data.default_subcategory_id == nil
				then ""
				else data.default_subcategory_id,
			asset_types = if data == nil or data.asset_types == nil then {} else data.asset_types,
			bundle_types = if data == nil or data.bundle_types == nil then {} else data.bundle_types,
			can_animate_camera_based_mood = if data == nil or data.can_animate_camera_based_mood == nil
				then false
				else data.can_animate_camera_based_mood,
			camera_focus = if data == nil or data.camera_focus == nil then "" else data.camera_focus,
			camera_zoom_radius = if data == nil or data.camera_zoom_radius == nil then 0 else data.camera_zoom_radius,
			tile_type = if data == nil or data.tile_type == nil then "" else data.tile_type,
			looks_widget_context_for_organic_content = if data == nil
					or data.looks_widget_context_for_organic_content == nil
				then ""
				else data.looks_widget_context_for_organic_content,
			time_to_refresh = if data == nil or data.time_to_refresh == nil then 0 else data.time_to_refresh,
			category_id = if data == nil or data.category_id == nil then 0 else data.category_id,
			subcategory_id = if data == nil or data.subcategory_id == nil then 0 else data.subcategory_id,
		}, _MarketplaceCatalogCategoryDataImpl :: _MarketplaceCatalogCategoryDataImpl)
	end

	function _MarketplaceCatalogCategoryDataImpl.encode(self: MarketplaceCatalogCategoryData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.title ~= nil and self.title ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.title)
		end

		if self.name ~= nil and self.name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.name)
		end

		if self.taxonomy_id ~= nil and self.taxonomy_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.taxonomy_id)
		end

		if self.buttons_enabled ~= nil and #self.buttons_enabled > 0 then
			for _, value in self.buttons_enabled do
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.subcategories ~= nil and #self.subcategories > 0 then
			for _, value in self.subcategories do
				output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.default_subcategory_id ~= nil and self.default_subcategory_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.default_subcategory_id)
		end

		if self.asset_types ~= nil and #self.asset_types > 0 then
			for _, value in self.asset_types do
				output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.bundle_types ~= nil and #self.bundle_types > 0 then
			for _, value in self.bundle_types do
				output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.can_animate_camera_based_mood then
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.can_animate_camera_based_mood then 1 else 0)
		end

		if self.camera_focus ~= nil and self.camera_focus ~= "" then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.camera_focus)
		end

		if self.camera_zoom_radius ~= nil and self.camera_zoom_radius ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.camera_zoom_radius)
		end

		if self.tile_type ~= nil and self.tile_type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.tile_type)
		end

		if
			self.looks_widget_context_for_organic_content ~= nil
			and self.looks_widget_context_for_organic_content ~= ""
		then
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.looks_widget_context_for_organic_content)
		end

		if self.time_to_refresh ~= nil and self.time_to_refresh ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.time_to_refresh)
		end

		if self.category_id ~= nil and self.category_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 16, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.category_id)
		end

		if self.subcategory_id ~= nil and self.subcategory_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 17, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.subcategory_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceCatalogCategoryDataImpl.decode(input: buffer): MarketplaceCatalogCategoryData
		local self = _MarketplaceCatalogCategoryDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 10 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.can_animate_camera_based_mood = value ~= 0
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.time_to_refresh = value
					continue
				elseif field == 16 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.category_id = value
					continue
				elseif field == 17 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.subcategory_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.name = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.taxonomy_id = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.buttons_enabled, buffer.tostring(value))
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.subcategories, buffer.tostring(value))
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.default_subcategory_id = buffer.tostring(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.asset_types, buffer.tostring(value))
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.bundle_types, buffer.tostring(value))
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.camera_focus = buffer.tostring(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tile_type = buffer.tostring(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.looks_widget_context_for_organic_content = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 12 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.camera_zoom_radius = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceCatalogCategoryDataImpl.jsonEncode(self: MarketplaceCatalogCategoryData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.title ~= nil and self.title ~= "" then
			output.title = self.title
		end

		if self.name ~= nil and self.name ~= "" then
			output.name = self.name
		end

		if self.taxonomy_id ~= nil and self.taxonomy_id ~= "" then
			output.taxonomyId = self.taxonomy_id
		end

		if self.buttons_enabled ~= nil and #self.buttons_enabled > 0 then
			local newOutput = {}
			for _, value in self.buttons_enabled do
				table.insert(newOutput, value)
			end
			output.buttonsEnabled = newOutput
		end

		if self.subcategories ~= nil and #self.subcategories > 0 then
			local newOutput = {}
			for _, value in self.subcategories do
				table.insert(newOutput, value)
			end
			output.subcategories = newOutput
		end

		if self.default_subcategory_id ~= nil and self.default_subcategory_id ~= "" then
			output.defaultSubcategoryId = self.default_subcategory_id
		end

		if self.asset_types ~= nil and #self.asset_types > 0 then
			local newOutput = {}
			for _, value in self.asset_types do
				table.insert(newOutput, value)
			end
			output.assetTypes = newOutput
		end

		if self.bundle_types ~= nil and #self.bundle_types > 0 then
			local newOutput = {}
			for _, value in self.bundle_types do
				table.insert(newOutput, value)
			end
			output.bundleTypes = newOutput
		end

		if self.can_animate_camera_based_mood then
			output.canAnimateCameraBasedMood = self.can_animate_camera_based_mood
		end

		if self.camera_focus ~= nil and self.camera_focus ~= "" then
			output.cameraFocus = self.camera_focus
		end

		if self.camera_zoom_radius ~= nil and self.camera_zoom_radius ~= 0 then
			output.cameraZoomRadius = proto.json.serializeNumber(self.camera_zoom_radius)
		end

		if self.tile_type ~= nil and self.tile_type ~= "" then
			output.tileType = self.tile_type
		end

		if
			self.looks_widget_context_for_organic_content ~= nil
			and self.looks_widget_context_for_organic_content ~= ""
		then
			output.looksWidgetContextForOrganicContent = self.looks_widget_context_for_organic_content
		end

		if self.time_to_refresh ~= nil and self.time_to_refresh ~= 0 then
			output.timeToRefresh = self.time_to_refresh
		end

		if self.category_id ~= nil and self.category_id ~= 0 then
			output.categoryId = self.category_id
		end

		if self.subcategory_id ~= nil and self.subcategory_id ~= 0 then
			output.subcategoryId = self.subcategory_id
		end

		return output
	end

	function _MarketplaceCatalogCategoryDataImpl.jsonDecode(input: { [string]: any }): MarketplaceCatalogCategoryData
		local self = _MarketplaceCatalogCategoryDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.title ~= nil then
			self.title = input.title
		end

		if input.name ~= nil then
			self.name = input.name
		end

		if input.taxonomy_id ~= nil then
			self.taxonomy_id = input.taxonomy_id
		end

		if input.taxonomyId ~= nil then
			self.taxonomy_id = input.taxonomyId
		end

		if input.buttons_enabled ~= nil then
			local newOutput: { string } = {}
			for _, value in input.buttons_enabled do
				table.insert(newOutput, value)
			end

			self.buttons_enabled = newOutput
		end

		if input.buttonsEnabled ~= nil then
			local newOutput: { string } = {}
			for _, value in input.buttonsEnabled do
				table.insert(newOutput, value)
			end

			self.buttons_enabled = newOutput
		end

		if input.subcategories ~= nil then
			local newOutput: { string } = {}
			for _, value in input.subcategories do
				table.insert(newOutput, value)
			end

			self.subcategories = newOutput
		end

		if input.default_subcategory_id ~= nil then
			self.default_subcategory_id = input.default_subcategory_id
		end

		if input.defaultSubcategoryId ~= nil then
			self.default_subcategory_id = input.defaultSubcategoryId
		end

		if input.asset_types ~= nil then
			local newOutput: { string } = {}
			for _, value in input.asset_types do
				table.insert(newOutput, value)
			end

			self.asset_types = newOutput
		end

		if input.assetTypes ~= nil then
			local newOutput: { string } = {}
			for _, value in input.assetTypes do
				table.insert(newOutput, value)
			end

			self.asset_types = newOutput
		end

		if input.bundle_types ~= nil then
			local newOutput: { string } = {}
			for _, value in input.bundle_types do
				table.insert(newOutput, value)
			end

			self.bundle_types = newOutput
		end

		if input.bundleTypes ~= nil then
			local newOutput: { string } = {}
			for _, value in input.bundleTypes do
				table.insert(newOutput, value)
			end

			self.bundle_types = newOutput
		end

		if input.can_animate_camera_based_mood ~= nil then
			self.can_animate_camera_based_mood = input.can_animate_camera_based_mood
		end

		if input.canAnimateCameraBasedMood ~= nil then
			self.can_animate_camera_based_mood = input.canAnimateCameraBasedMood
		end

		if input.camera_focus ~= nil then
			self.camera_focus = input.camera_focus
		end

		if input.cameraFocus ~= nil then
			self.camera_focus = input.cameraFocus
		end

		if input.camera_zoom_radius ~= nil then
			self.camera_zoom_radius = proto.json.deserializeNumber(input.camera_zoom_radius)
		end

		if input.cameraZoomRadius ~= nil then
			self.camera_zoom_radius = proto.json.deserializeNumber(input.cameraZoomRadius)
		end

		if input.tile_type ~= nil then
			self.tile_type = input.tile_type
		end

		if input.tileType ~= nil then
			self.tile_type = input.tileType
		end

		if input.looks_widget_context_for_organic_content ~= nil then
			self.looks_widget_context_for_organic_content = input.looks_widget_context_for_organic_content
		end

		if input.looksWidgetContextForOrganicContent ~= nil then
			self.looks_widget_context_for_organic_content = input.looksWidgetContextForOrganicContent
		end

		if input.time_to_refresh ~= nil then
			self.time_to_refresh = input.time_to_refresh
		end

		if input.timeToRefresh ~= nil then
			self.time_to_refresh = input.timeToRefresh
		end

		if input.category_id ~= nil then
			self.category_id = input.category_id
		end

		if input.categoryId ~= nil then
			self.category_id = input.categoryId
		end

		if input.subcategory_id ~= nil then
			self.subcategory_id = input.subcategory_id
		end

		if input.subcategoryId ~= nil then
			self.subcategory_id = input.subcategoryId
		end

		return self
	end

	_MarketplaceCatalogCategoryDataImpl.descriptor = {
		name = "MarketplaceCatalogCategoryData",
		fullName = "roblox.apppageplatform.shared.v1beta1.MarketplaceCatalogCategoryData",
	}

	messages.MarketplaceCatalogCategoryData = _MarketplaceCatalogCategoryDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceCatalogCategoryData)
end

return {
	MarketplaceCatalogCategoryData = messages.MarketplaceCatalogCategoryData,
}
