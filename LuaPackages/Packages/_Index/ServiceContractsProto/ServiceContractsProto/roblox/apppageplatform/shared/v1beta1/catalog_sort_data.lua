--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CatalogSortContentData: _CatalogSortContentDataMessage,
	CatalogSortCardItemFooter: _CatalogSortCardItemFooterMessage,
	CatalogSortCardStyle: _CatalogSortCardStyleMessage,
	CatalogSortData: _CatalogSortDataMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_background_layer = require(script.Parent.background_layer)

type _CatalogSortContentDataImpl = {
	__index: _CatalogSortContentDataImpl,
	new: (fields: _CatalogSortContentDataPartialFields?) -> CatalogSortContentData,
	encode: (self: CatalogSortContentData) -> buffer,
	decode: (input: buffer) -> CatalogSortContentData,
	jsonEncode: (self: CatalogSortContentData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogSortContentData,
	descriptor: proto.Descriptor,
}

type _CatalogSortContentDataFields = {
	id: string,
	type: string,
}

type _CatalogSortContentDataPartialFields = {
	id: string?,
	type: string?,
}

export type CatalogSortContentData = typeof(setmetatable(
	{} :: _CatalogSortContentDataFields,
	{} :: _CatalogSortContentDataImpl
))
type _CatalogSortContentDataMessage = proto.Message<CatalogSortContentData, _CatalogSortContentDataPartialFields>

type _CatalogSortCardItemFooterImpl = {
	__index: _CatalogSortCardItemFooterImpl,
	new: (fields: _CatalogSortCardItemFooterPartialFields?) -> CatalogSortCardItemFooter,
	encode: (self: CatalogSortCardItemFooter) -> buffer,
	decode: (input: buffer) -> CatalogSortCardItemFooter,
	jsonEncode: (self: CatalogSortCardItemFooter) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogSortCardItemFooter,
	descriptor: proto.Descriptor,
}

type _CatalogSortCardItemFooterFields = {
	content_type: string,
}

type _CatalogSortCardItemFooterPartialFields = {
	content_type: string?,
}

export type CatalogSortCardItemFooter = typeof(setmetatable(
	{} :: _CatalogSortCardItemFooterFields,
	{} :: _CatalogSortCardItemFooterImpl
))
type _CatalogSortCardItemFooterMessage = proto.Message<
	CatalogSortCardItemFooter,
	_CatalogSortCardItemFooterPartialFields
>

type _CatalogSortCardStyleImpl = {
	__index: _CatalogSortCardStyleImpl,
	new: (fields: _CatalogSortCardStylePartialFields?) -> CatalogSortCardStyle,
	encode: (self: CatalogSortCardStyle) -> buffer,
	decode: (input: buffer) -> CatalogSortCardStyle,
	jsonEncode: (self: CatalogSortCardStyle) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogSortCardStyle,
	descriptor: proto.Descriptor,
}

type _CatalogSortCardStyleFields = {
	thumbnail_aspect_ratio: string,
	background_layers: { _roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer },
}

type _CatalogSortCardStylePartialFields = {
	thumbnail_aspect_ratio: string?,
	background_layers: { _roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer }?,
}

export type CatalogSortCardStyle = typeof(setmetatable(
	{} :: _CatalogSortCardStyleFields,
	{} :: _CatalogSortCardStyleImpl
))
type _CatalogSortCardStyleMessage = proto.Message<CatalogSortCardStyle, _CatalogSortCardStylePartialFields>

type _CatalogSortDataImpl = {
	__index: _CatalogSortDataImpl,
	new: (fields: _CatalogSortDataPartialFields?) -> CatalogSortData,
	encode: (self: CatalogSortData) -> buffer,
	decode: (input: buffer) -> CatalogSortData,
	jsonEncode: (self: CatalogSortData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CatalogSortData,
	descriptor: proto.Descriptor,
}

type _CatalogSortDataFields = {
	id: string,
	type: string,
	hydration_priority: number,
	hydration_count: number,
	content: { CatalogSortContentData },
	preview_columns: number,
	title: string,
	description: string,
	card_style: CatalogSortCardStyle?,
	card_footers: { CatalogSortCardItemFooter },
	tag_id: string,
}

type _CatalogSortDataPartialFields = {
	id: string?,
	type: string?,
	hydration_priority: number?,
	hydration_count: number?,
	content: { CatalogSortContentData }?,
	preview_columns: number?,
	title: string?,
	description: string?,
	card_style: CatalogSortCardStyle?,
	card_footers: { CatalogSortCardItemFooter }?,
	tag_id: string?,
}

export type CatalogSortData = typeof(setmetatable({} :: _CatalogSortDataFields, {} :: _CatalogSortDataImpl))
type _CatalogSortDataMessage = proto.Message<CatalogSortData, _CatalogSortDataPartialFields>

do
	local _CatalogSortContentDataImpl = {}
	_CatalogSortContentDataImpl.__index = _CatalogSortContentDataImpl

	function _CatalogSortContentDataImpl.new(data: _CatalogSortContentDataPartialFields?): CatalogSortContentData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			type = if data == nil or data.type == nil then "" else data.type,
		}, _CatalogSortContentDataImpl :: _CatalogSortContentDataImpl)
	end

	function _CatalogSortContentDataImpl.encode(self: CatalogSortContentData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.type ~= nil and self.type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogSortContentDataImpl.decode(input: buffer): CatalogSortContentData
		local self = _CatalogSortContentDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogSortContentDataImpl.jsonEncode(self: CatalogSortContentData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.type ~= nil and self.type ~= "" then
			output.type = self.type
		end

		return output
	end

	function _CatalogSortContentDataImpl.jsonDecode(input: { [string]: any }): CatalogSortContentData
		local self = _CatalogSortContentDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.type ~= nil then
			self.type = input.type
		end

		return self
	end

	_CatalogSortContentDataImpl.descriptor = {
		name = "CatalogSortContentData",
		fullName = "roblox.apppageplatform.shared.v1beta1.CatalogSortContentData",
	}

	messages.CatalogSortContentData = _CatalogSortContentDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogSortContentData)
end

do
	local _CatalogSortCardItemFooterImpl = {}
	_CatalogSortCardItemFooterImpl.__index = _CatalogSortCardItemFooterImpl

	function _CatalogSortCardItemFooterImpl.new(
		data: _CatalogSortCardItemFooterPartialFields?
	): CatalogSortCardItemFooter
		return setmetatable({
			content_type = if data == nil or data.content_type == nil then "" else data.content_type,
		}, _CatalogSortCardItemFooterImpl :: _CatalogSortCardItemFooterImpl)
	end

	function _CatalogSortCardItemFooterImpl.encode(self: CatalogSortCardItemFooter): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.content_type ~= nil and self.content_type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.content_type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogSortCardItemFooterImpl.decode(input: buffer): CatalogSortCardItemFooter
		local self = _CatalogSortCardItemFooterImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content_type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogSortCardItemFooterImpl.jsonEncode(self: CatalogSortCardItemFooter): any
		local output = {}

		if self.content_type ~= nil and self.content_type ~= "" then
			output.contentType = self.content_type
		end

		return output
	end

	function _CatalogSortCardItemFooterImpl.jsonDecode(input: { [string]: any }): CatalogSortCardItemFooter
		local self = _CatalogSortCardItemFooterImpl.new()

		if input.content_type ~= nil then
			self.content_type = input.content_type
		end

		if input.contentType ~= nil then
			self.content_type = input.contentType
		end

		return self
	end

	_CatalogSortCardItemFooterImpl.descriptor = {
		name = "CatalogSortCardItemFooter",
		fullName = "roblox.apppageplatform.shared.v1beta1.CatalogSortCardItemFooter",
	}

	messages.CatalogSortCardItemFooter = _CatalogSortCardItemFooterImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogSortCardItemFooter)
end

do
	local _CatalogSortCardStyleImpl = {}
	_CatalogSortCardStyleImpl.__index = _CatalogSortCardStyleImpl

	function _CatalogSortCardStyleImpl.new(data: _CatalogSortCardStylePartialFields?): CatalogSortCardStyle
		return setmetatable({
			thumbnail_aspect_ratio = if data == nil or data.thumbnail_aspect_ratio == nil
				then ""
				else data.thumbnail_aspect_ratio,
			background_layers = if data == nil or data.background_layers == nil then {} else data.background_layers,
		}, _CatalogSortCardStyleImpl :: _CatalogSortCardStyleImpl)
	end

	function _CatalogSortCardStyleImpl.encode(self: CatalogSortCardStyle): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.thumbnail_aspect_ratio ~= nil and self.thumbnail_aspect_ratio ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.thumbnail_aspect_ratio)
		end

		if self.background_layers ~= nil and #self.background_layers > 0 then
			for _, value in self.background_layers do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogSortCardStyleImpl.decode(input: buffer): CatalogSortCardStyle
		local self = _CatalogSortCardStyleImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.thumbnail_aspect_ratio = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.background_layers,
						_roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer.decode(value)
					)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogSortCardStyleImpl.jsonEncode(self: CatalogSortCardStyle): any
		local output = {}

		if self.thumbnail_aspect_ratio ~= nil and self.thumbnail_aspect_ratio ~= "" then
			output.thumbnailAspectRatio = self.thumbnail_aspect_ratio
		end

		if self.background_layers ~= nil and #self.background_layers > 0 then
			local newOutput = {}
			for _, value in self.background_layers do
				table.insert(newOutput, value:jsonEncode())
			end
			output.backgroundLayers = newOutput
		end

		return output
	end

	function _CatalogSortCardStyleImpl.jsonDecode(input: { [string]: any }): CatalogSortCardStyle
		local self = _CatalogSortCardStyleImpl.new()

		if input.thumbnail_aspect_ratio ~= nil then
			self.thumbnail_aspect_ratio = input.thumbnail_aspect_ratio
		end

		if input.thumbnailAspectRatio ~= nil then
			self.thumbnail_aspect_ratio = input.thumbnailAspectRatio
		end

		if input.background_layers ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer } = {}
			for _, value in input.background_layers do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer.jsonDecode(value)
				)
			end

			self.background_layers = newOutput
		end

		if input.backgroundLayers ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer } = {}
			for _, value in input.backgroundLayers do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_background_layer.BackgroundLayer.jsonDecode(value)
				)
			end

			self.background_layers = newOutput
		end

		return self
	end

	_CatalogSortCardStyleImpl.descriptor = {
		name = "CatalogSortCardStyle",
		fullName = "roblox.apppageplatform.shared.v1beta1.CatalogSortCardStyle",
	}

	messages.CatalogSortCardStyle = _CatalogSortCardStyleImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogSortCardStyle)
end

do
	local _CatalogSortDataImpl = {}
	_CatalogSortDataImpl.__index = _CatalogSortDataImpl

	function _CatalogSortDataImpl.new(data: _CatalogSortDataPartialFields?): CatalogSortData
		return setmetatable({
			id = if data == nil or data.id == nil then "" else data.id,
			type = if data == nil or data.type == nil then "" else data.type,
			hydration_priority = if data == nil or data.hydration_priority == nil then 0 else data.hydration_priority,
			hydration_count = if data == nil or data.hydration_count == nil then 0 else data.hydration_count,
			content = if data == nil or data.content == nil then {} else data.content,
			preview_columns = if data == nil or data.preview_columns == nil then 0 else data.preview_columns,
			title = if data == nil or data.title == nil then "" else data.title,
			description = if data == nil or data.description == nil then "" else data.description,
			card_style = if data == nil or data.card_style == nil then nil else data.card_style,
			card_footers = if data == nil or data.card_footers == nil then {} else data.card_footers,
			tag_id = if data == nil or data.tag_id == nil then "" else data.tag_id,
		}, _CatalogSortDataImpl :: _CatalogSortDataImpl)
	end

	function _CatalogSortDataImpl.encode(self: CatalogSortData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.id ~= nil and self.id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.id)
		end

		if self.type ~= nil and self.type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.type)
		end

		if self.hydration_priority ~= nil and self.hydration_priority ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.hydration_priority)
		end

		if self.hydration_count ~= nil and self.hydration_count ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.hydration_count)
		end

		if self.content ~= nil and #self.content > 0 then
			for _, value in self.content do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.preview_columns ~= nil and self.preview_columns ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.preview_columns)
		end

		if self.title ~= nil and self.title ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.title)
		end

		if self.description ~= nil and self.description ~= "" then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.description)
		end

		if self.card_style ~= nil then
			local encoded = self.card_style:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.card_footers ~= nil and #self.card_footers > 0 then
			for _, value in self.card_footers do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.tag_id ~= nil and self.tag_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.tag_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CatalogSortDataImpl.decode(input: buffer): CatalogSortData
		local self = _CatalogSortDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.hydration_priority = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.hydration_count = value
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.preview_columns = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.type = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.content, messages.CatalogSortContentData.decode(value))
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title = buffer.tostring(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.description = buffer.tostring(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.card_style = messages.CatalogSortCardStyle.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.card_footers, messages.CatalogSortCardItemFooter.decode(value))
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tag_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CatalogSortDataImpl.jsonEncode(self: CatalogSortData): any
		local output = {}

		if self.id ~= nil and self.id ~= "" then
			output.id = self.id
		end

		if self.type ~= nil and self.type ~= "" then
			output.type = self.type
		end

		if self.hydration_priority ~= nil and self.hydration_priority ~= 0 then
			output.hydrationPriority = self.hydration_priority
		end

		if self.hydration_count ~= nil and self.hydration_count ~= 0 then
			output.hydrationCount = self.hydration_count
		end

		if self.content ~= nil and #self.content > 0 then
			local newOutput = {}
			for _, value in self.content do
				table.insert(newOutput, value:jsonEncode())
			end
			output.content = newOutput
		end

		if self.preview_columns ~= nil and self.preview_columns ~= 0 then
			output.previewColumns = self.preview_columns
		end

		if self.title ~= nil and self.title ~= "" then
			output.title = self.title
		end

		if self.description ~= nil and self.description ~= "" then
			output.description = self.description
		end

		if self.card_style ~= nil then
			output.cardStyle = self.card_style:jsonEncode()
		end

		if self.card_footers ~= nil and #self.card_footers > 0 then
			local newOutput = {}
			for _, value in self.card_footers do
				table.insert(newOutput, value:jsonEncode())
			end
			output.cardFooters = newOutput
		end

		if self.tag_id ~= nil and self.tag_id ~= "" then
			output.tagId = self.tag_id
		end

		return output
	end

	function _CatalogSortDataImpl.jsonDecode(input: { [string]: any }): CatalogSortData
		local self = _CatalogSortDataImpl.new()

		if input.id ~= nil then
			self.id = input.id
		end

		if input.type ~= nil then
			self.type = input.type
		end

		if input.hydration_priority ~= nil then
			self.hydration_priority = input.hydration_priority
		end

		if input.hydrationPriority ~= nil then
			self.hydration_priority = input.hydrationPriority
		end

		if input.hydration_count ~= nil then
			self.hydration_count = input.hydration_count
		end

		if input.hydrationCount ~= nil then
			self.hydration_count = input.hydrationCount
		end

		if input.content ~= nil then
			local newOutput: { CatalogSortContentData } = {}
			for _, value in input.content do
				table.insert(newOutput, messages.CatalogSortContentData.jsonDecode(value))
			end

			self.content = newOutput
		end

		if input.preview_columns ~= nil then
			self.preview_columns = input.preview_columns
		end

		if input.previewColumns ~= nil then
			self.preview_columns = input.previewColumns
		end

		if input.title ~= nil then
			self.title = input.title
		end

		if input.description ~= nil then
			self.description = input.description
		end

		if input.card_style ~= nil then
			self.card_style = messages.CatalogSortCardStyle.jsonDecode(input.card_style)
		end

		if input.cardStyle ~= nil then
			self.card_style = messages.CatalogSortCardStyle.jsonDecode(input.cardStyle)
		end

		if input.card_footers ~= nil then
			local newOutput: { CatalogSortCardItemFooter } = {}
			for _, value in input.card_footers do
				table.insert(newOutput, messages.CatalogSortCardItemFooter.jsonDecode(value))
			end

			self.card_footers = newOutput
		end

		if input.cardFooters ~= nil then
			local newOutput: { CatalogSortCardItemFooter } = {}
			for _, value in input.cardFooters do
				table.insert(newOutput, messages.CatalogSortCardItemFooter.jsonDecode(value))
			end

			self.card_footers = newOutput
		end

		if input.tag_id ~= nil then
			self.tag_id = input.tag_id
		end

		if input.tagId ~= nil then
			self.tag_id = input.tagId
		end

		return self
	end

	_CatalogSortDataImpl.descriptor = {
		name = "CatalogSortData",
		fullName = "roblox.apppageplatform.shared.v1beta1.CatalogSortData",
	}

	messages.CatalogSortData = _CatalogSortDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CatalogSortData)
end

return {
	CatalogSortContentData = messages.CatalogSortContentData,
	CatalogSortCardItemFooter = messages.CatalogSortCardItemFooter,
	CatalogSortCardStyle = messages.CatalogSortCardStyle,
	CatalogSortData = messages.CatalogSortData,
}
