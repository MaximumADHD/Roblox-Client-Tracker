--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CreatorData: _CreatorDataMessage,
}
local messages: _Messages = {} :: _Messages

type _CreatorDataImpl = {
	__index: _CreatorDataImpl,
	new: (fields: _CreatorDataPartialFields?) -> CreatorData,
	encode: (self: CreatorData) -> buffer,
	decode: (input: buffer) -> CreatorData,
	jsonEncode: (self: CreatorData) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CreatorData,
	descriptor: proto.Descriptor,
}

type _CreatorDataFields = {
	creator_type: string,
	creator_id: number,
	creator_name: string,
	is_verified: boolean,
}

type _CreatorDataPartialFields = {
	creator_type: string?,
	creator_id: number?,
	creator_name: string?,
	is_verified: boolean?,
}

export type CreatorData = typeof(setmetatable({} :: _CreatorDataFields, {} :: _CreatorDataImpl))
type _CreatorDataMessage = proto.Message<CreatorData, _CreatorDataPartialFields>

do
	local _CreatorDataImpl = {}
	_CreatorDataImpl.__index = _CreatorDataImpl

	function _CreatorDataImpl.new(data: _CreatorDataPartialFields?): CreatorData
		return setmetatable({
			creator_type = if data == nil or data.creator_type == nil then "" else data.creator_type,
			creator_id = if data == nil or data.creator_id == nil then 0 else data.creator_id,
			creator_name = if data == nil or data.creator_name == nil then "" else data.creator_name,
			is_verified = if data == nil or data.is_verified == nil then false else data.is_verified,
		}, _CreatorDataImpl :: _CreatorDataImpl)
	end

	function _CreatorDataImpl.encode(self: CreatorData): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.creator_type ~= nil and self.creator_type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.creator_type)
		end

		if self.creator_id ~= nil and self.creator_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.creator_id)
		end

		if self.creator_name ~= nil and self.creator_name ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.creator_name)
		end

		if self.is_verified then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, if self.is_verified then 1 else 0)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CreatorDataImpl.decode(input: buffer): CreatorData
		local self = _CreatorDataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.creator_id = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readVarInt(input, cursor)
					self.is_verified = value ~= 0
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.creator_type = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.creator_name = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CreatorDataImpl.jsonEncode(self: CreatorData): any
		local output = {}

		if self.creator_type ~= nil and self.creator_type ~= "" then
			output.creatorType = self.creator_type
		end

		if self.creator_id ~= nil and self.creator_id ~= 0 then
			output.creatorId = self.creator_id
		end

		if self.creator_name ~= nil and self.creator_name ~= "" then
			output.creatorName = self.creator_name
		end

		if self.is_verified then
			output.isVerified = self.is_verified
		end

		return output
	end

	function _CreatorDataImpl.jsonDecode(input: { [string]: any }): CreatorData
		local self = _CreatorDataImpl.new()

		if input.creator_type ~= nil then
			self.creator_type = input.creator_type
		end

		if input.creatorType ~= nil then
			self.creator_type = input.creatorType
		end

		if input.creator_id ~= nil then
			self.creator_id = input.creator_id
		end

		if input.creatorId ~= nil then
			self.creator_id = input.creatorId
		end

		if input.creator_name ~= nil then
			self.creator_name = input.creator_name
		end

		if input.creatorName ~= nil then
			self.creator_name = input.creatorName
		end

		if input.is_verified ~= nil then
			self.is_verified = input.is_verified
		end

		if input.isVerified ~= nil then
			self.is_verified = input.isVerified
		end

		return self
	end

	_CreatorDataImpl.descriptor = {
		name = "CreatorData",
		fullName = "roblox.apppageplatform.shared.v1beta1.CreatorData",
	}

	messages.CreatorData = _CreatorDataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CreatorData)
end

return {
	CreatorData = messages.CreatorData,
}
