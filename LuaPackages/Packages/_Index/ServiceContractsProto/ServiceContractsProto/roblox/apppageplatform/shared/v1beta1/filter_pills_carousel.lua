--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	FilterPillsCarouselSchema: _FilterPillsCarouselSchemaMessage,
	FilterPillsCarouselSchema_Props: _FilterPillsCarouselSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_actions = require(script.Parent.actions)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _FilterPillsCarouselSchemaImpl = {
	__index: _FilterPillsCarouselSchemaImpl,
	new: (fields: _FilterPillsCarouselSchemaPartialFields?) -> FilterPillsCarouselSchema,
	encode: (self: FilterPillsCarouselSchema) -> buffer,
	decode: (input: buffer) -> FilterPillsCarouselSchema,
	jsonEncode: (self: FilterPillsCarouselSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FilterPillsCarouselSchema,
	descriptor: proto.Descriptor,
}

type _FilterPillsCarouselSchemaFields = {
	props: FilterPillsCarouselSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _FilterPillsCarouselSchemaPartialFields = {
	props: FilterPillsCarouselSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type FilterPillsCarouselSchema = typeof(setmetatable(
	{} :: _FilterPillsCarouselSchemaFields,
	{} :: _FilterPillsCarouselSchemaImpl
))
type _FilterPillsCarouselSchemaMessage = proto.Message<
	FilterPillsCarouselSchema,
	_FilterPillsCarouselSchemaPartialFields
>

type _FilterPillsCarouselSchema_PropsImpl = {
	__index: _FilterPillsCarouselSchema_PropsImpl,
	new: (fields: _FilterPillsCarouselSchema_PropsPartialFields?) -> FilterPillsCarouselSchema_Props,
	encode: (self: FilterPillsCarouselSchema_Props) -> buffer,
	decode: (input: buffer) -> FilterPillsCarouselSchema_Props,
	jsonEncode: (self: FilterPillsCarouselSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FilterPillsCarouselSchema_Props,
	descriptor: proto.Descriptor,
}

type _FilterPillsCarouselSchema_PropsFields = {
	filter_groups: _roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp?,
	on_filter_applied: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	impression_event_name: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	skip_item_impressions_log: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	on_filter_click: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	title: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp?,
	tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _FilterPillsCarouselSchema_PropsPartialFields = {
	filter_groups: _roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp?,
	on_filter_applied: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	impression_event_name: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	skip_item_impressions_log: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	on_filter_click: _roblox_apppageplatform_shared_v1beta1_actions.ActionProp?,
	title: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	background_style: _roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp?,
	tag: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type FilterPillsCarouselSchema_Props = typeof(setmetatable(
	{} :: _FilterPillsCarouselSchema_PropsFields,
	{} :: _FilterPillsCarouselSchema_PropsImpl
))
type _FilterPillsCarouselSchema_PropsMessage = proto.Message<
	FilterPillsCarouselSchema_Props,
	_FilterPillsCarouselSchema_PropsPartialFields
>

do
	local _FilterPillsCarouselSchemaImpl = {}
	_FilterPillsCarouselSchemaImpl.__index = _FilterPillsCarouselSchemaImpl

	function _FilterPillsCarouselSchemaImpl.new(
		data: _FilterPillsCarouselSchemaPartialFields?
	): FilterPillsCarouselSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _FilterPillsCarouselSchemaImpl :: _FilterPillsCarouselSchemaImpl)
	end

	function _FilterPillsCarouselSchemaImpl.encode(self: FilterPillsCarouselSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FilterPillsCarouselSchemaImpl.decode(input: buffer): FilterPillsCarouselSchema
		local self = _FilterPillsCarouselSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.FilterPillsCarouselSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FilterPillsCarouselSchemaImpl.jsonEncode(self: FilterPillsCarouselSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _FilterPillsCarouselSchemaImpl.jsonDecode(input: { [string]: any }): FilterPillsCarouselSchema
		local self = _FilterPillsCarouselSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.FilterPillsCarouselSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_FilterPillsCarouselSchemaImpl.descriptor = {
		name = "FilterPillsCarouselSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.FilterPillsCarouselSchema",
	}

	messages.FilterPillsCarouselSchema = _FilterPillsCarouselSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FilterPillsCarouselSchema)
end

do
	local _FilterPillsCarouselSchema_PropsImpl = {}
	_FilterPillsCarouselSchema_PropsImpl.__index = _FilterPillsCarouselSchema_PropsImpl

	function _FilterPillsCarouselSchema_PropsImpl.new(
		data: _FilterPillsCarouselSchema_PropsPartialFields?
	): FilterPillsCarouselSchema_Props
		return setmetatable({
			filter_groups = if data == nil or data.filter_groups == nil then nil else data.filter_groups,
			on_filter_applied = if data == nil or data.on_filter_applied == nil then nil else data.on_filter_applied,
			impression_event_name = if data == nil or data.impression_event_name == nil
				then nil
				else data.impression_event_name,
			skip_item_impressions_log = if data == nil or data.skip_item_impressions_log == nil
				then nil
				else data.skip_item_impressions_log,
			on_filter_click = if data == nil or data.on_filter_click == nil then nil else data.on_filter_click,
			title = if data == nil or data.title == nil then nil else data.title,
			background_style = if data == nil or data.background_style == nil then nil else data.background_style,
			tag = if data == nil or data.tag == nil then nil else data.tag,
		}, _FilterPillsCarouselSchema_PropsImpl :: _FilterPillsCarouselSchema_PropsImpl)
	end

	function _FilterPillsCarouselSchema_PropsImpl.encode(self: FilterPillsCarouselSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.filter_groups ~= nil then
			local encoded = self.filter_groups:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_filter_applied ~= nil then
			local encoded = self.on_filter_applied:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.impression_event_name ~= nil then
			local encoded = self.impression_event_name:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.skip_item_impressions_log ~= nil then
			local encoded = self.skip_item_impressions_log:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.on_filter_click ~= nil then
			local encoded = self.on_filter_click:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title ~= nil then
			local encoded = self.title:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.background_style ~= nil then
			local encoded = self.background_style:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.tag ~= nil then
			local encoded = self.tag:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FilterPillsCarouselSchema_PropsImpl.decode(input: buffer): FilterPillsCarouselSchema_Props
		local self = _FilterPillsCarouselSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.filter_groups =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_filter_applied = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.impression_event_name =
						_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.skip_item_impressions_log =
						_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.on_filter_click = _roblox_apppageplatform_shared_v1beta1_actions.ActionProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.background_style =
						_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FilterPillsCarouselSchema_PropsImpl.jsonEncode(self: FilterPillsCarouselSchema_Props): any
		local output = {}

		if self.filter_groups ~= nil then
			output.filterGroups = self.filter_groups:jsonEncode()
		end

		if self.on_filter_applied ~= nil then
			output.onFilterApplied = self.on_filter_applied:jsonEncode()
		end

		if self.impression_event_name ~= nil then
			output.impressionEventName = self.impression_event_name:jsonEncode()
		end

		if self.skip_item_impressions_log ~= nil then
			output.skipItemImpressionsLog = self.skip_item_impressions_log:jsonEncode()
		end

		if self.on_filter_click ~= nil then
			output.onFilterClick = self.on_filter_click:jsonEncode()
		end

		if self.title ~= nil then
			output.title = self.title:jsonEncode()
		end

		if self.background_style ~= nil then
			output.backgroundStyle = self.background_style:jsonEncode()
		end

		if self.tag ~= nil then
			output.tag = self.tag:jsonEncode()
		end

		return output
	end

	function _FilterPillsCarouselSchema_PropsImpl.jsonDecode(input: { [string]: any }): FilterPillsCarouselSchema_Props
		local self = _FilterPillsCarouselSchema_PropsImpl.new()

		if input.filter_groups ~= nil then
			self.filter_groups =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp.jsonDecode(input.filter_groups)
		end

		if input.filterGroups ~= nil then
			self.filter_groups =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ArrayOfStructProp.jsonDecode(input.filterGroups)
		end

		if input.on_filter_applied ~= nil then
			self.on_filter_applied =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_filter_applied)
		end

		if input.onFilterApplied ~= nil then
			self.on_filter_applied =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onFilterApplied)
		end

		if input.impression_event_name ~= nil then
			self.impression_event_name =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.impression_event_name)
		end

		if input.impressionEventName ~= nil then
			self.impression_event_name =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.impressionEventName)
		end

		if input.skip_item_impressions_log ~= nil then
			self.skip_item_impressions_log =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.skip_item_impressions_log)
		end

		if input.skipItemImpressionsLog ~= nil then
			self.skip_item_impressions_log =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.skipItemImpressionsLog)
		end

		if input.on_filter_click ~= nil then
			self.on_filter_click =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.on_filter_click)
		end

		if input.onFilterClick ~= nil then
			self.on_filter_click =
				_roblox_apppageplatform_shared_v1beta1_actions.ActionProp.jsonDecode(input.onFilterClick)
		end

		if input.title ~= nil then
			self.title = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.title)
		end

		if input.background_style ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.jsonDecode(input.background_style)
		end

		if input.backgroundStyle ~= nil then
			self.background_style =
				_roblox_apppageplatform_shared_v1beta1_prop_types.ColorStyleProp.jsonDecode(input.backgroundStyle)
		end

		if input.tag ~= nil then
			self.tag = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.tag)
		end

		return self
	end

	_FilterPillsCarouselSchema_PropsImpl.descriptor = {
		name = "FilterPillsCarouselSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.FilterPillsCarouselSchema_Props = _FilterPillsCarouselSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FilterPillsCarouselSchema_Props)
end

return {
	FilterPillsCarouselSchema = messages.FilterPillsCarouselSchema,
	FilterPillsCarouselSchema_Props = messages.FilterPillsCarouselSchema_Props,
}
