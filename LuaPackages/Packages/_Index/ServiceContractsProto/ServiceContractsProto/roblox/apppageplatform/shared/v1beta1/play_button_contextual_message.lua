--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	PlayButtonContextualMessageSchema: _PlayButtonContextualMessageSchemaMessage,
	PlayButtonContextualMessageSchema_Props: _PlayButtonContextualMessageSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _PlayButtonContextualMessageSchemaImpl = {
	__index: _PlayButtonContextualMessageSchemaImpl,
	new: (fields: _PlayButtonContextualMessageSchemaPartialFields?) -> PlayButtonContextualMessageSchema,
	encode: (self: PlayButtonContextualMessageSchema) -> buffer,
	decode: (input: buffer) -> PlayButtonContextualMessageSchema,
	jsonEncode: (self: PlayButtonContextualMessageSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PlayButtonContextualMessageSchema,
	descriptor: proto.Descriptor,
}

type _PlayButtonContextualMessageSchemaFields = {
	props: PlayButtonContextualMessageSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _PlayButtonContextualMessageSchemaPartialFields = {
	props: PlayButtonContextualMessageSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type PlayButtonContextualMessageSchema = typeof(setmetatable(
	{} :: _PlayButtonContextualMessageSchemaFields,
	{} :: _PlayButtonContextualMessageSchemaImpl
))
type _PlayButtonContextualMessageSchemaMessage = proto.Message<
	PlayButtonContextualMessageSchema,
	_PlayButtonContextualMessageSchemaPartialFields
>

type _PlayButtonContextualMessageSchema_PropsImpl = {
	__index: _PlayButtonContextualMessageSchema_PropsImpl,
	new: (fields: _PlayButtonContextualMessageSchema_PropsPartialFields?) -> PlayButtonContextualMessageSchema_Props,
	encode: (self: PlayButtonContextualMessageSchema_Props) -> buffer,
	decode: (input: buffer) -> PlayButtonContextualMessageSchema_Props,
	jsonEncode: (self: PlayButtonContextualMessageSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PlayButtonContextualMessageSchema_Props,
	descriptor: proto.Descriptor,
}

type _PlayButtonContextualMessageSchema_PropsFields = {
	universe_id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	fetch_delay_in_seconds: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

type _PlayButtonContextualMessageSchema_PropsPartialFields = {
	universe_id: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	fetch_delay_in_seconds: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	layout_order: _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop?,
}

export type PlayButtonContextualMessageSchema_Props = typeof(setmetatable(
	{} :: _PlayButtonContextualMessageSchema_PropsFields,
	{} :: _PlayButtonContextualMessageSchema_PropsImpl
))
type _PlayButtonContextualMessageSchema_PropsMessage = proto.Message<
	PlayButtonContextualMessageSchema_Props,
	_PlayButtonContextualMessageSchema_PropsPartialFields
>

do
	local _PlayButtonContextualMessageSchemaImpl = {}
	_PlayButtonContextualMessageSchemaImpl.__index = _PlayButtonContextualMessageSchemaImpl

	function _PlayButtonContextualMessageSchemaImpl.new(
		data: _PlayButtonContextualMessageSchemaPartialFields?
	): PlayButtonContextualMessageSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _PlayButtonContextualMessageSchemaImpl :: _PlayButtonContextualMessageSchemaImpl)
	end

	function _PlayButtonContextualMessageSchemaImpl.encode(self: PlayButtonContextualMessageSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PlayButtonContextualMessageSchemaImpl.decode(input: buffer): PlayButtonContextualMessageSchema
		local self = _PlayButtonContextualMessageSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.PlayButtonContextualMessageSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PlayButtonContextualMessageSchemaImpl.jsonEncode(self: PlayButtonContextualMessageSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _PlayButtonContextualMessageSchemaImpl.jsonDecode(
		input: { [string]: any }
	): PlayButtonContextualMessageSchema
		local self = _PlayButtonContextualMessageSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.PlayButtonContextualMessageSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_PlayButtonContextualMessageSchemaImpl.descriptor = {
		name = "PlayButtonContextualMessageSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.PlayButtonContextualMessageSchema",
	}

	messages.PlayButtonContextualMessageSchema = _PlayButtonContextualMessageSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PlayButtonContextualMessageSchema)
end

do
	local _PlayButtonContextualMessageSchema_PropsImpl = {}
	_PlayButtonContextualMessageSchema_PropsImpl.__index = _PlayButtonContextualMessageSchema_PropsImpl

	function _PlayButtonContextualMessageSchema_PropsImpl.new(
		data: _PlayButtonContextualMessageSchema_PropsPartialFields?
	): PlayButtonContextualMessageSchema_Props
		return setmetatable({
			universe_id = if data == nil or data.universe_id == nil then nil else data.universe_id,
			fetch_delay_in_seconds = if data == nil or data.fetch_delay_in_seconds == nil
				then nil
				else data.fetch_delay_in_seconds,
			layout_order = if data == nil or data.layout_order == nil then nil else data.layout_order,
		}, _PlayButtonContextualMessageSchema_PropsImpl :: _PlayButtonContextualMessageSchema_PropsImpl)
	end

	function _PlayButtonContextualMessageSchema_PropsImpl.encode(self: PlayButtonContextualMessageSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.universe_id ~= nil then
			local encoded = self.universe_id:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.fetch_delay_in_seconds ~= nil then
			local encoded = self.fetch_delay_in_seconds:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.layout_order ~= nil then
			local encoded = self.layout_order:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PlayButtonContextualMessageSchema_PropsImpl.decode(input: buffer): PlayButtonContextualMessageSchema_Props
		local self = _PlayButtonContextualMessageSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.universe_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.fetch_delay_in_seconds =
						_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.layout_order = _roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PlayButtonContextualMessageSchema_PropsImpl.jsonEncode(self: PlayButtonContextualMessageSchema_Props): any
		local output = {}

		if self.universe_id ~= nil then
			output.universeId = self.universe_id:jsonEncode()
		end

		if self.fetch_delay_in_seconds ~= nil then
			output.fetchDelayInSeconds = self.fetch_delay_in_seconds:jsonEncode()
		end

		if self.layout_order ~= nil then
			output.layoutOrder = self.layout_order:jsonEncode()
		end

		return output
	end

	function _PlayButtonContextualMessageSchema_PropsImpl.jsonDecode(
		input: { [string]: any }
	): PlayButtonContextualMessageSchema_Props
		local self = _PlayButtonContextualMessageSchema_PropsImpl.new()

		if input.universe_id ~= nil then
			self.universe_id =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.universe_id)
		end

		if input.universeId ~= nil then
			self.universe_id = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.universeId)
		end

		if input.fetch_delay_in_seconds ~= nil then
			self.fetch_delay_in_seconds =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.fetch_delay_in_seconds)
		end

		if input.fetchDelayInSeconds ~= nil then
			self.fetch_delay_in_seconds =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.fetchDelayInSeconds)
		end

		if input.layout_order ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layout_order)
		end

		if input.layoutOrder ~= nil then
			self.layout_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Int32Prop.jsonDecode(input.layoutOrder)
		end

		return self
	end

	_PlayButtonContextualMessageSchema_PropsImpl.descriptor = {
		name = "PlayButtonContextualMessageSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.PlayButtonContextualMessageSchema_Props = _PlayButtonContextualMessageSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PlayButtonContextualMessageSchema_Props)
end

return {
	PlayButtonContextualMessageSchema = messages.PlayButtonContextualMessageSchema,
	PlayButtonContextualMessageSchema_Props = messages.PlayButtonContextualMessageSchema_Props,
}
