--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CollectionGridSchema: _CollectionGridSchemaMessage,
	CollectionGridSchema_Props: _CollectionGridSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _CollectionGridSchemaImpl = {
	__index: _CollectionGridSchemaImpl,
	new: (fields: _CollectionGridSchemaPartialFields?) -> CollectionGridSchema,
	encode: (self: CollectionGridSchema) -> buffer,
	decode: (input: buffer) -> CollectionGridSchema,
	jsonEncode: (self: CollectionGridSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CollectionGridSchema,
	descriptor: proto.Descriptor,
}

type _CollectionGridSchemaFields = {
	props: CollectionGridSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _CollectionGridSchemaPartialFields = {
	props: CollectionGridSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type CollectionGridSchema = typeof(setmetatable(
	{} :: _CollectionGridSchemaFields,
	{} :: _CollectionGridSchemaImpl
))
type _CollectionGridSchemaMessage = proto.Message<CollectionGridSchema, _CollectionGridSchemaPartialFields>

type _CollectionGridSchema_PropsImpl = {
	__index: _CollectionGridSchema_PropsImpl,
	new: (fields: _CollectionGridSchema_PropsPartialFields?) -> CollectionGridSchema_Props,
	encode: (self: CollectionGridSchema_Props) -> buffer,
	decode: (input: buffer) -> CollectionGridSchema_Props,
	jsonEncode: (self: CollectionGridSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CollectionGridSchema_Props,
	descriptor: proto.Descriptor,
}

type _CollectionGridSchema_PropsFields = {
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	buffer_rows_below_screen: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	buffer_rows_above_screen: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	collection_item_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	disable_relative_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	focus_nav_actions_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	header_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	item_height_from_width: _roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp?,
	items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	max_num_rows: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	should_use_frame_bounds_for_impressions: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	disable_selection_order: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	title_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

type _CollectionGridSchema_PropsPartialFields = {
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	buffer_rows_below_screen: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	buffer_rows_above_screen: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	collection_item_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	disable_relative_gap: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	focus_nav_actions_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	header_component: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
	item_height_from_width: _roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp?,
	items: _roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp?,
	max_num_rows: _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp?,
	should_use_frame_bounds_for_impressions: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	disable_selection_order: _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp?,
	title_text: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
}

export type CollectionGridSchema_Props = typeof(setmetatable(
	{} :: _CollectionGridSchema_PropsFields,
	{} :: _CollectionGridSchema_PropsImpl
))
type _CollectionGridSchema_PropsMessage = proto.Message<
	CollectionGridSchema_Props,
	_CollectionGridSchema_PropsPartialFields
>

do
	local _CollectionGridSchemaImpl = {}
	_CollectionGridSchemaImpl.__index = _CollectionGridSchemaImpl

	function _CollectionGridSchemaImpl.new(data: _CollectionGridSchemaPartialFields?): CollectionGridSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _CollectionGridSchemaImpl :: _CollectionGridSchemaImpl)
	end

	function _CollectionGridSchemaImpl.encode(self: CollectionGridSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CollectionGridSchemaImpl.decode(input: buffer): CollectionGridSchema
		local self = _CollectionGridSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.CollectionGridSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CollectionGridSchemaImpl.jsonEncode(self: CollectionGridSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _CollectionGridSchemaImpl.jsonDecode(input: { [string]: any }): CollectionGridSchema
		local self = _CollectionGridSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.CollectionGridSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_CollectionGridSchemaImpl.descriptor = {
		name = "CollectionGridSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.CollectionGridSchema",
	}

	messages.CollectionGridSchema = _CollectionGridSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CollectionGridSchema)
end

do
	local _CollectionGridSchema_PropsImpl = {}
	_CollectionGridSchema_PropsImpl.__index = _CollectionGridSchema_PropsImpl

	function _CollectionGridSchema_PropsImpl.new(
		data: _CollectionGridSchema_PropsPartialFields?
	): CollectionGridSchema_Props
		return setmetatable({
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			position = if data == nil or data.position == nil then nil else data.position,
			size = if data == nil or data.size == nil then nil else data.size,
			buffer_rows_below_screen = if data == nil or data.buffer_rows_below_screen == nil
				then nil
				else data.buffer_rows_below_screen,
			buffer_rows_above_screen = if data == nil or data.buffer_rows_above_screen == nil
				then nil
				else data.buffer_rows_above_screen,
			collection_item_size = if data == nil or data.collection_item_size == nil
				then nil
				else data.collection_item_size,
			disable_relative_gap = if data == nil or data.disable_relative_gap == nil
				then nil
				else data.disable_relative_gap,
			focus_nav_actions_component = if data == nil or data.focus_nav_actions_component == nil
				then nil
				else data.focus_nav_actions_component,
			header_component = if data == nil or data.header_component == nil then nil else data.header_component,
			item_height_from_width = if data == nil or data.item_height_from_width == nil
				then nil
				else data.item_height_from_width,
			items = if data == nil or data.items == nil then nil else data.items,
			max_num_rows = if data == nil or data.max_num_rows == nil then nil else data.max_num_rows,
			should_use_frame_bounds_for_impressions = if data == nil
					or data.should_use_frame_bounds_for_impressions == nil
				then nil
				else data.should_use_frame_bounds_for_impressions,
			disable_selection_order = if data == nil or data.disable_selection_order == nil
				then nil
				else data.disable_selection_order,
			title_text = if data == nil or data.title_text == nil then nil else data.title_text,
		}, _CollectionGridSchema_PropsImpl :: _CollectionGridSchema_PropsImpl)
	end

	function _CollectionGridSchema_PropsImpl.encode(self: CollectionGridSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.buffer_rows_below_screen ~= nil then
			local encoded = self.buffer_rows_below_screen:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.buffer_rows_above_screen ~= nil then
			local encoded = self.buffer_rows_above_screen:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.collection_item_size ~= nil then
			local encoded = self.collection_item_size:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.disable_relative_gap ~= nil then
			local encoded = self.disable_relative_gap:encode()
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.focus_nav_actions_component ~= nil then
			local encoded = self.focus_nav_actions_component:encode()
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.header_component ~= nil then
			local encoded = self.header_component:encode()
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.item_height_from_width ~= nil then
			local encoded = self.item_height_from_width:encode()
			output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.items ~= nil then
			local encoded = self.items:encode()
			output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.max_num_rows ~= nil then
			local encoded = self.max_num_rows:encode()
			output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.should_use_frame_bounds_for_impressions ~= nil then
			local encoded = self.should_use_frame_bounds_for_impressions:encode()
			output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.disable_selection_order ~= nil then
			local encoded = self.disable_selection_order:encode()
			output, cursor = proto.writeTag(output, cursor, 14, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.title_text ~= nil then
			local encoded = self.title_text:encode()
			output, cursor = proto.writeTag(output, cursor, 15, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CollectionGridSchema_PropsImpl.decode(input: buffer): CollectionGridSchema_Props
		local self = _CollectionGridSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.buffer_rows_below_screen =
						_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.buffer_rows_above_screen =
						_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.collection_item_size =
						_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.disable_relative_gap = _roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.focus_nav_actions_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.header_component =
						_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.item_height_from_width =
						_roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp.decode(value)
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.items =
						_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.decode(value)
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.max_num_rows = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.decode(value)
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.should_use_frame_bounds_for_impressions =
						_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 14 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.disable_selection_order =
						_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.decode(value)
					continue
				elseif field == 15 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.title_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CollectionGridSchema_PropsImpl.jsonEncode(self: CollectionGridSchema_Props): any
		local output = {}

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.buffer_rows_below_screen ~= nil then
			output.bufferRowsBelowScreen = self.buffer_rows_below_screen:jsonEncode()
		end

		if self.buffer_rows_above_screen ~= nil then
			output.bufferRowsAboveScreen = self.buffer_rows_above_screen:jsonEncode()
		end

		if self.collection_item_size ~= nil then
			output.collectionItemSize = self.collection_item_size:jsonEncode()
		end

		if self.disable_relative_gap ~= nil then
			output.disableRelativeGap = self.disable_relative_gap:jsonEncode()
		end

		if self.focus_nav_actions_component ~= nil then
			output.focusNavActionsComponent = self.focus_nav_actions_component:jsonEncode()
		end

		if self.header_component ~= nil then
			output.headerComponent = self.header_component:jsonEncode()
		end

		if self.item_height_from_width ~= nil then
			output.itemHeightFromWidth = self.item_height_from_width:jsonEncode()
		end

		if self.items ~= nil then
			output.items = self.items:jsonEncode()
		end

		if self.max_num_rows ~= nil then
			output.maxNumRows = self.max_num_rows:jsonEncode()
		end

		if self.should_use_frame_bounds_for_impressions ~= nil then
			output.shouldUseFrameBoundsForImpressions = self.should_use_frame_bounds_for_impressions:jsonEncode()
		end

		if self.disable_selection_order ~= nil then
			output.disableSelectionOrder = self.disable_selection_order:jsonEncode()
		end

		if self.title_text ~= nil then
			output.titleText = self.title_text:jsonEncode()
		end

		return output
	end

	function _CollectionGridSchema_PropsImpl.jsonDecode(input: { [string]: any }): CollectionGridSchema_Props
		local self = _CollectionGridSchema_PropsImpl.new()

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.buffer_rows_below_screen ~= nil then
			self.buffer_rows_below_screen =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.buffer_rows_below_screen)
		end

		if input.bufferRowsBelowScreen ~= nil then
			self.buffer_rows_below_screen =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.bufferRowsBelowScreen)
		end

		if input.buffer_rows_above_screen ~= nil then
			self.buffer_rows_above_screen =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.buffer_rows_above_screen)
		end

		if input.bufferRowsAboveScreen ~= nil then
			self.buffer_rows_above_screen =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.bufferRowsAboveScreen)
		end

		if input.collection_item_size ~= nil then
			self.collection_item_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.collection_item_size)
		end

		if input.collectionItemSize ~= nil then
			self.collection_item_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.collectionItemSize)
		end

		if input.disable_relative_gap ~= nil then
			self.disable_relative_gap =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.disable_relative_gap)
		end

		if input.disableRelativeGap ~= nil then
			self.disable_relative_gap =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.disableRelativeGap)
		end

		if input.focus_nav_actions_component ~= nil then
			self.focus_nav_actions_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.focus_nav_actions_component
				)
		end

		if input.focusNavActionsComponent ~= nil then
			self.focus_nav_actions_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(
					input.focusNavActionsComponent
				)
		end

		if input.header_component ~= nil then
			self.header_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.header_component)
		end

		if input.headerComponent ~= nil then
			self.header_component =
				_roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.headerComponent)
		end

		if input.item_height_from_width ~= nil then
			self.item_height_from_width =
				_roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp.jsonDecode(input.item_height_from_width)
		end

		if input.itemHeightFromWidth ~= nil then
			self.item_height_from_width =
				_roblox_apppageplatform_shared_v1beta1_prop_types.UDimProp.jsonDecode(input.itemHeightFromWidth)
		end

		if input.items ~= nil then
			self.items =
				_roblox_apppageplatform_shared_v1beta1_prop_types.LazyNestedComponentListProp.jsonDecode(input.items)
		end

		if input.max_num_rows ~= nil then
			self.max_num_rows =
				_roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.max_num_rows)
		end

		if input.maxNumRows ~= nil then
			self.max_num_rows = _roblox_apppageplatform_shared_v1beta1_prop_types.FloatProp.jsonDecode(input.maxNumRows)
		end

		if input.should_use_frame_bounds_for_impressions ~= nil then
			self.should_use_frame_bounds_for_impressions =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(
					input.should_use_frame_bounds_for_impressions
				)
		end

		if input.shouldUseFrameBoundsForImpressions ~= nil then
			self.should_use_frame_bounds_for_impressions =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(
					input.shouldUseFrameBoundsForImpressions
				)
		end

		if input.disable_selection_order ~= nil then
			self.disable_selection_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.disable_selection_order)
		end

		if input.disableSelectionOrder ~= nil then
			self.disable_selection_order =
				_roblox_apppageplatform_shared_v1beta1_prop_types.BoolProp.jsonDecode(input.disableSelectionOrder)
		end

		if input.title_text ~= nil then
			self.title_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.title_text)
		end

		if input.titleText ~= nil then
			self.title_text = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.titleText)
		end

		return self
	end

	_CollectionGridSchema_PropsImpl.descriptor = {
		name = "CollectionGridSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.CollectionGridSchema_Props = _CollectionGridSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CollectionGridSchema_Props)
end

return {
	CollectionGridSchema = messages.CollectionGridSchema,
	CollectionGridSchema_Props = messages.CollectionGridSchema_Props,
}
