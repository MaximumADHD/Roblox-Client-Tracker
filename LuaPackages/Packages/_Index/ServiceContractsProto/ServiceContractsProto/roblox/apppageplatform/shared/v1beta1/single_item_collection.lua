--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	SingleItemCollectionSchema: _SingleItemCollectionSchemaMessage,
	SingleItemCollectionSchema_Props: _SingleItemCollectionSchema_PropsMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_prop_types = require(script.Parent.prop_types)
local _roblox_apppageplatform_shared_v1beta1_component_shared = require(script.Parent.component_shared)

type _SingleItemCollectionSchemaImpl = {
	__index: _SingleItemCollectionSchemaImpl,
	new: (fields: _SingleItemCollectionSchemaPartialFields?) -> SingleItemCollectionSchema,
	encode: (self: SingleItemCollectionSchema) -> buffer,
	decode: (input: buffer) -> SingleItemCollectionSchema,
	jsonEncode: (self: SingleItemCollectionSchema) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SingleItemCollectionSchema,
	descriptor: proto.Descriptor,
}

type _SingleItemCollectionSchemaFields = {
	props: SingleItemCollectionSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

type _SingleItemCollectionSchemaPartialFields = {
	props: SingleItemCollectionSchema_Props?,
	shared: _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared?,
}

export type SingleItemCollectionSchema = typeof(setmetatable(
	{} :: _SingleItemCollectionSchemaFields,
	{} :: _SingleItemCollectionSchemaImpl
))
type _SingleItemCollectionSchemaMessage = proto.Message<
	SingleItemCollectionSchema,
	_SingleItemCollectionSchemaPartialFields
>

type _SingleItemCollectionSchema_PropsImpl = {
	__index: _SingleItemCollectionSchema_PropsImpl,
	new: (fields: _SingleItemCollectionSchema_PropsPartialFields?) -> SingleItemCollectionSchema_Props,
	encode: (self: SingleItemCollectionSchema_Props) -> buffer,
	decode: (input: buffer) -> SingleItemCollectionSchema_Props,
	jsonEncode: (self: SingleItemCollectionSchema_Props) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SingleItemCollectionSchema_Props,
	descriptor: proto.Descriptor,
}

type _SingleItemCollectionSchema_PropsFields = {
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	item: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
}

type _SingleItemCollectionSchema_PropsPartialFields = {
	automatic_size: _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp?,
	anchor_point: _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop?,
	position: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	size: _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop?,
	item: _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp?,
}

export type SingleItemCollectionSchema_Props = typeof(setmetatable(
	{} :: _SingleItemCollectionSchema_PropsFields,
	{} :: _SingleItemCollectionSchema_PropsImpl
))
type _SingleItemCollectionSchema_PropsMessage = proto.Message<
	SingleItemCollectionSchema_Props,
	_SingleItemCollectionSchema_PropsPartialFields
>

do
	local _SingleItemCollectionSchemaImpl = {}
	_SingleItemCollectionSchemaImpl.__index = _SingleItemCollectionSchemaImpl

	function _SingleItemCollectionSchemaImpl.new(
		data: _SingleItemCollectionSchemaPartialFields?
	): SingleItemCollectionSchema
		return setmetatable({
			props = if data == nil or data.props == nil then nil else data.props,
			shared = if data == nil or data.shared == nil then nil else data.shared,
		}, _SingleItemCollectionSchemaImpl :: _SingleItemCollectionSchemaImpl)
	end

	function _SingleItemCollectionSchemaImpl.encode(self: SingleItemCollectionSchema): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.props ~= nil then
			local encoded = self.props:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.shared ~= nil then
			local encoded = self.shared:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SingleItemCollectionSchemaImpl.decode(input: buffer): SingleItemCollectionSchema
		local self = _SingleItemCollectionSchemaImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.props = messages.SingleItemCollectionSchema_Props.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.shared = _roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SingleItemCollectionSchemaImpl.jsonEncode(self: SingleItemCollectionSchema): any
		local output = {}

		if self.props ~= nil then
			output.props = self.props:jsonEncode()
		end

		if self.shared ~= nil then
			output.shared = self.shared:jsonEncode()
		end

		return output
	end

	function _SingleItemCollectionSchemaImpl.jsonDecode(input: { [string]: any }): SingleItemCollectionSchema
		local self = _SingleItemCollectionSchemaImpl.new()

		if input.props ~= nil then
			self.props = messages.SingleItemCollectionSchema_Props.jsonDecode(input.props)
		end

		if input.shared ~= nil then
			self.shared =
				_roblox_apppageplatform_shared_v1beta1_component_shared.ComponentShared.jsonDecode(input.shared)
		end

		return self
	end

	_SingleItemCollectionSchemaImpl.descriptor = {
		name = "SingleItemCollectionSchema",
		fullName = "roblox.apppageplatform.shared.v1beta1.SingleItemCollectionSchema",
	}

	messages.SingleItemCollectionSchema = _SingleItemCollectionSchemaImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SingleItemCollectionSchema)
end

do
	local _SingleItemCollectionSchema_PropsImpl = {}
	_SingleItemCollectionSchema_PropsImpl.__index = _SingleItemCollectionSchema_PropsImpl

	function _SingleItemCollectionSchema_PropsImpl.new(
		data: _SingleItemCollectionSchema_PropsPartialFields?
	): SingleItemCollectionSchema_Props
		return setmetatable({
			automatic_size = if data == nil or data.automatic_size == nil then nil else data.automatic_size,
			anchor_point = if data == nil or data.anchor_point == nil then nil else data.anchor_point,
			position = if data == nil or data.position == nil then nil else data.position,
			size = if data == nil or data.size == nil then nil else data.size,
			item = if data == nil or data.item == nil then nil else data.item,
		}, _SingleItemCollectionSchema_PropsImpl :: _SingleItemCollectionSchema_PropsImpl)
	end

	function _SingleItemCollectionSchema_PropsImpl.encode(self: SingleItemCollectionSchema_Props): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.automatic_size ~= nil then
			local encoded = self.automatic_size:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.anchor_point ~= nil then
			local encoded = self.anchor_point:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.size ~= nil then
			local encoded = self.size:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.item ~= nil then
			local encoded = self.item:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SingleItemCollectionSchema_PropsImpl.decode(input: buffer): SingleItemCollectionSchema_Props
		local self = _SingleItemCollectionSchema_PropsImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.automatic_size = _roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.anchor_point = _roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.decode(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.item = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SingleItemCollectionSchema_PropsImpl.jsonEncode(self: SingleItemCollectionSchema_Props): any
		local output = {}

		if self.automatic_size ~= nil then
			output.automaticSize = self.automatic_size:jsonEncode()
		end

		if self.anchor_point ~= nil then
			output.anchorPoint = self.anchor_point:jsonEncode()
		end

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.size ~= nil then
			output.size = self.size:jsonEncode()
		end

		if self.item ~= nil then
			output.item = self.item:jsonEncode()
		end

		return output
	end

	function _SingleItemCollectionSchema_PropsImpl.jsonDecode(
		input: { [string]: any }
	): SingleItemCollectionSchema_Props
		local self = _SingleItemCollectionSchema_PropsImpl.new()

		if input.automatic_size ~= nil then
			self.automatic_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.automatic_size)
		end

		if input.automaticSize ~= nil then
			self.automatic_size =
				_roblox_apppageplatform_shared_v1beta1_prop_types.StringProp.jsonDecode(input.automaticSize)
		end

		if input.anchor_point ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchor_point)
		end

		if input.anchorPoint ~= nil then
			self.anchor_point =
				_roblox_apppageplatform_shared_v1beta1_prop_types.Vector2Prop.jsonDecode(input.anchorPoint)
		end

		if input.position ~= nil then
			self.position = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.position)
		end

		if input.size ~= nil then
			self.size = _roblox_apppageplatform_shared_v1beta1_prop_types.UDim2Prop.jsonDecode(input.size)
		end

		if input.item ~= nil then
			self.item = _roblox_apppageplatform_shared_v1beta1_prop_types.NestedComponentProp.jsonDecode(input.item)
		end

		return self
	end

	_SingleItemCollectionSchema_PropsImpl.descriptor = {
		name = "SingleItemCollectionSchema_Props",
		fullName = "roblox.apppageplatform.shared.v1beta1.Props",
	}

	messages.SingleItemCollectionSchema_Props = _SingleItemCollectionSchema_PropsImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SingleItemCollectionSchema_Props)
end

return {
	SingleItemCollectionSchema = messages.SingleItemCollectionSchema,
	SingleItemCollectionSchema_Props = messages.SingleItemCollectionSchema_Props,
}
