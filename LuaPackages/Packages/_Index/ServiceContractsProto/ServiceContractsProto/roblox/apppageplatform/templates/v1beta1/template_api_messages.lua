--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages =
	{
		DisplayLocationTemplateMapping: _DisplayLocationTemplateMappingMessage,
		DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryMessage,
		GetTemplatesRequest: _GetTemplatesRequestMessage,
		GetTemplatesResponse: _GetTemplatesResponseMessage,
		GetTemplatesResponse_TemplatesEntry: _GetTemplatesResponse_TemplatesEntryMessage,
		GetTemplatesResponse_LocalizedLiteralsEntry: _GetTemplatesResponse_LocalizedLiteralsEntryMessage,
	}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_client_capabilities =
	require(script.Parent.Parent.Parent.shared.v1beta1.client_capabilities)
local _roblox_apppageplatform_shared_v1beta1_display_location =
	require(script.Parent.Parent.Parent.shared.v1beta1.display_location)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)

type _DisplayLocationTemplateMappingImpl = {
	__index: _DisplayLocationTemplateMappingImpl,
	new: (fields: _DisplayLocationTemplateMappingPartialFields?) -> DisplayLocationTemplateMapping,
	encode: (self: DisplayLocationTemplateMapping) -> buffer,
	decode: (input: buffer) -> DisplayLocationTemplateMapping,
	jsonEncode: (self: DisplayLocationTemplateMapping) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DisplayLocationTemplateMapping,
	descriptor: proto.Descriptor,
}

type _DisplayLocationTemplateMappingFields = {
	display_location: _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation,
	roblox_component_to_template_id: { [string]: string },
}

type _DisplayLocationTemplateMappingPartialFields = {
	display_location: _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation?,
	roblox_component_to_template_id: { [string]: string }?,
}

export type DisplayLocationTemplateMapping = typeof(setmetatable(
	{} :: _DisplayLocationTemplateMappingFields,
	{} :: _DisplayLocationTemplateMappingImpl
))
type _DisplayLocationTemplateMappingMessage = proto.Message<
	DisplayLocationTemplateMapping,
	_DisplayLocationTemplateMappingPartialFields
>

type _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl = {
	__index: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl,
	new: (
		fields: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryPartialFields?
	) -> DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry,
	encode: (self: DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry) -> buffer,
	decode: (input: buffer) -> DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry,
	jsonEncode: (self: DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry,
	descriptor: proto.Descriptor,
}

type _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryFields = {
	key: string,
	value: string,
}

type _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryPartialFields = {
	key: string?,
	value: string?,
}

export type DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry = typeof(setmetatable(
	{} :: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryFields,
	{} :: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl
))
type _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryMessage = proto.Message<
	DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry,
	_DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryPartialFields
>

type _GetTemplatesRequestImpl = {
	__index: _GetTemplatesRequestImpl,
	new: (fields: _GetTemplatesRequestPartialFields?) -> GetTemplatesRequest,
	encode: (self: GetTemplatesRequest) -> buffer,
	decode: (input: buffer) -> GetTemplatesRequest,
	jsonEncode: (self: GetTemplatesRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetTemplatesRequest,
	descriptor: proto.Descriptor,
}

type _GetTemplatesRequestFields = {
	display_locations: { _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation },
	client_capabilities: _roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities?,
	cached_component_mappings: { DisplayLocationTemplateMapping },
	locale: string,
}

type _GetTemplatesRequestPartialFields = {
	display_locations: { _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation }?,
	client_capabilities: _roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities?,
	cached_component_mappings: { DisplayLocationTemplateMapping }?,
	locale: string?,
}

export type GetTemplatesRequest = typeof(setmetatable({} :: _GetTemplatesRequestFields, {} :: _GetTemplatesRequestImpl))
type _GetTemplatesRequestMessage = proto.Message<GetTemplatesRequest, _GetTemplatesRequestPartialFields>

type _GetTemplatesResponseImpl = {
	__index: _GetTemplatesResponseImpl,
	new: (fields: _GetTemplatesResponsePartialFields?) -> GetTemplatesResponse,
	encode: (self: GetTemplatesResponse) -> buffer,
	decode: (input: buffer) -> GetTemplatesResponse,
	jsonEncode: (self: GetTemplatesResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetTemplatesResponse,
	descriptor: proto.Descriptor,
}

type _GetTemplatesResponseFields = {
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	component_mappings: { DisplayLocationTemplateMapping },
	valid_cached_component_mappings: { DisplayLocationTemplateMapping },
	localized_literals: { [string]: string },
}

type _GetTemplatesResponsePartialFields = {
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	component_mappings: { DisplayLocationTemplateMapping }?,
	valid_cached_component_mappings: { DisplayLocationTemplateMapping }?,
	localized_literals: { [string]: string }?,
}

export type GetTemplatesResponse = typeof(setmetatable(
	{} :: _GetTemplatesResponseFields,
	{} :: _GetTemplatesResponseImpl
))
type _GetTemplatesResponseMessage = proto.Message<GetTemplatesResponse, _GetTemplatesResponsePartialFields>

type _GetTemplatesResponse_TemplatesEntryImpl = {
	__index: _GetTemplatesResponse_TemplatesEntryImpl,
	new: (fields: _GetTemplatesResponse_TemplatesEntryPartialFields?) -> GetTemplatesResponse_TemplatesEntry,
	encode: (self: GetTemplatesResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> GetTemplatesResponse_TemplatesEntry,
	jsonEncode: (self: GetTemplatesResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetTemplatesResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _GetTemplatesResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _GetTemplatesResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type GetTemplatesResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _GetTemplatesResponse_TemplatesEntryFields,
	{} :: _GetTemplatesResponse_TemplatesEntryImpl
))
type _GetTemplatesResponse_TemplatesEntryMessage = proto.Message<
	GetTemplatesResponse_TemplatesEntry,
	_GetTemplatesResponse_TemplatesEntryPartialFields
>

type _GetTemplatesResponse_LocalizedLiteralsEntryImpl = {
	__index: _GetTemplatesResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _GetTemplatesResponse_LocalizedLiteralsEntryPartialFields?
	) -> GetTemplatesResponse_LocalizedLiteralsEntry,
	encode: (self: GetTemplatesResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> GetTemplatesResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: GetTemplatesResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetTemplatesResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _GetTemplatesResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _GetTemplatesResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type GetTemplatesResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _GetTemplatesResponse_LocalizedLiteralsEntryFields,
	{} :: _GetTemplatesResponse_LocalizedLiteralsEntryImpl
))
type _GetTemplatesResponse_LocalizedLiteralsEntryMessage = proto.Message<
	GetTemplatesResponse_LocalizedLiteralsEntry,
	_GetTemplatesResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _DisplayLocationTemplateMappingImpl = {}
	_DisplayLocationTemplateMappingImpl.__index = _DisplayLocationTemplateMappingImpl

	function _DisplayLocationTemplateMappingImpl.new(
		data: _DisplayLocationTemplateMappingPartialFields?
	): DisplayLocationTemplateMapping
		return setmetatable({
			display_location = if data == nil or data.display_location == nil
				then assert(
					_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(0),
					"Enum has no 0 default"
				)
				else data.display_location,
			roblox_component_to_template_id = if data == nil or data.roblox_component_to_template_id == nil
				then {}
				else data.roblox_component_to_template_id,
		}, _DisplayLocationTemplateMappingImpl :: _DisplayLocationTemplateMappingImpl)
	end

	function _DisplayLocationTemplateMappingImpl.encode(self: DisplayLocationTemplateMapping): buffer
		local output = buffer.create(0)
		local cursor = 0

		if
			self.display_location ~= nil
			and (
				self.display_location ~= nil and self.display_location ~= 0
				or self.display_location
					~= _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(0)
			)
		then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(
				output,
				cursor,
				_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.toNumber(
					self.display_location :: any
				)
			)
		end

		if self.roblox_component_to_template_id ~= nil and next(self.roblox_component_to_template_id) ~= nil then
			for key, value in self.roblox_component_to_template_id do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DisplayLocationTemplateMappingImpl.decode(input: buffer): DisplayLocationTemplateMapping
		local self = _DisplayLocationTemplateMappingImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.display_location = (
						_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(value)
						or value
					) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry =
						messages.DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.roblox_component_to_template_id[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DisplayLocationTemplateMappingImpl.jsonEncode(self: DisplayLocationTemplateMapping): any
		local output = {}

		if
			self.display_location ~= nil
			and (
				self.display_location ~= nil and self.display_location ~= 0
				or self.display_location
					~= _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(0)
			)
		then
			output.displayLocation = if typeof(self.display_location) == "number"
				then self.display_location
				else _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.toNumber(
					self.display_location :: any
				)
		end

		if self.roblox_component_to_template_id ~= nil and next(self.roblox_component_to_template_id) ~= nil then
			local newOutput = {}
			for key, value in self.roblox_component_to_template_id do
				newOutput[key] = value
			end
			output.robloxComponentToTemplateId = newOutput
		end

		return output
	end

	function _DisplayLocationTemplateMappingImpl.jsonDecode(input: { [string]: any }): DisplayLocationTemplateMapping
		local self = _DisplayLocationTemplateMappingImpl.new()

		if input.display_location ~= nil then
			self.display_location = if typeof(input.display_location) == "number"
				then (_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(
					input.display_location
				) or input.display_location)
				else (_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromName(
					input.display_location
				) or input.display_location)
		end

		if input.displayLocation ~= nil then
			self.display_location = if typeof(input.displayLocation) == "number"
				then (_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(
					input.displayLocation
				) or input.displayLocation)
				else (_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromName(
					input.displayLocation
				) or input.displayLocation)
		end

		if input.roblox_component_to_template_id ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.roblox_component_to_template_id do
				newOutput[key] = value
			end

			self.roblox_component_to_template_id = newOutput
		end

		if input.robloxComponentToTemplateId ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.robloxComponentToTemplateId do
				newOutput[key] = value
			end

			self.roblox_component_to_template_id = newOutput
		end

		return self
	end

	_DisplayLocationTemplateMappingImpl.descriptor = {
		name = "DisplayLocationTemplateMapping",
		fullName = "roblox.apppageplatform.templates.v1beta1.DisplayLocationTemplateMapping",
	}

	messages.DisplayLocationTemplateMapping = _DisplayLocationTemplateMappingImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DisplayLocationTemplateMapping)
end

do
	local _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl = {}
	_DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.__index =
		_DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl

	function _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.new(
		data: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryPartialFields?
	): DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl :: _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl
		)
	end

	function _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.encode(
		self: DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.decode(
		input: buffer
	): DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry
		local self = _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.jsonEncode(
		self: DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.jsonDecode(
		input: { [string]: any }
	): DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry
		local self = _DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl.descriptor = {
		name = "DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry",
		fullName = "roblox.apppageplatform.templates.v1beta1.RobloxComponentToTemplateIdEntry",
	}

	messages.DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry =
		_DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.DisplayLocationTemplateMapping_RobloxComponentToTemplateIdEntry)
end

do
	local _GetTemplatesRequestImpl = {}
	_GetTemplatesRequestImpl.__index = _GetTemplatesRequestImpl

	function _GetTemplatesRequestImpl.new(data: _GetTemplatesRequestPartialFields?): GetTemplatesRequest
		return setmetatable({
			display_locations = if data == nil or data.display_locations == nil then {} else data.display_locations,
			client_capabilities = if data == nil or data.client_capabilities == nil
				then nil
				else data.client_capabilities,
			cached_component_mappings = if data == nil or data.cached_component_mappings == nil
				then {}
				else data.cached_component_mappings,
			locale = if data == nil or data.locale == nil then "" else data.locale,
		}, _GetTemplatesRequestImpl :: _GetTemplatesRequestImpl)
	end

	function _GetTemplatesRequestImpl.encode(self: GetTemplatesRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.display_locations ~= nil and #self.display_locations > 0 then
			for _, value in self.display_locations do
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(
					output,
					cursor,
					_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.toNumber(value :: any)
				)
			end
		end

		if self.client_capabilities ~= nil then
			local encoded = self.client_capabilities:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.cached_component_mappings ~= nil and #self.cached_component_mappings > 0 then
			for _, value in self.cached_component_mappings do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.locale ~= nil and self.locale ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.locale)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetTemplatesRequestImpl.decode(input: buffer): GetTemplatesRequest
		local self = _GetTemplatesRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					table.insert(
						self.display_locations,
						(
								_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(
									value
								) or value
							) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					)
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.client_capabilities =
						_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.cached_component_mappings, messages.DisplayLocationTemplateMapping.decode(value))
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.locale = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetTemplatesRequestImpl.jsonEncode(self: GetTemplatesRequest): any
		local output = {}

		if self.display_locations ~= nil and #self.display_locations > 0 then
			local newOutput = {}
			for _, value in self.display_locations do
				table.insert(
					newOutput,
					if typeof(value) == "number"
						then value
						else _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.toNumber(
							value :: any
						)
				)
			end
			output.displayLocations = newOutput
		end

		if self.client_capabilities ~= nil then
			output.clientCapabilities = self.client_capabilities:jsonEncode()
		end

		if self.cached_component_mappings ~= nil and #self.cached_component_mappings > 0 then
			local newOutput = {}
			for _, value in self.cached_component_mappings do
				table.insert(newOutput, value:jsonEncode())
			end
			output.cachedComponentMappings = newOutput
		end

		if self.locale ~= nil and self.locale ~= "" then
			output.locale = self.locale
		end

		return output
	end

	function _GetTemplatesRequestImpl.jsonDecode(input: { [string]: any }): GetTemplatesRequest
		local self = _GetTemplatesRequestImpl.new()

		if input.display_locations ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation } = {}
			for _, value in input.display_locations do
				table.insert(
					newOutput,
					if typeof(value) == "number"
						then (
							_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(value)
							or value
						)
						else (_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromName(
							value
						) or value)
				)
			end

			self.display_locations = newOutput
		end

		if input.displayLocations ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation } = {}
			for _, value in input.displayLocations do
				table.insert(
					newOutput,
					if typeof(value) == "number"
						then (
							_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromNumber(value)
							or value
						)
						else (_roblox_apppageplatform_shared_v1beta1_display_location.DisplayLocation.fromName(
							value
						) or value)
				)
			end

			self.display_locations = newOutput
		end

		if input.client_capabilities ~= nil then
			self.client_capabilities =
				_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.jsonDecode(
					input.client_capabilities
				)
		end

		if input.clientCapabilities ~= nil then
			self.client_capabilities =
				_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.jsonDecode(
					input.clientCapabilities
				)
		end

		if input.cached_component_mappings ~= nil then
			local newOutput: { DisplayLocationTemplateMapping } = {}
			for _, value in input.cached_component_mappings do
				table.insert(newOutput, messages.DisplayLocationTemplateMapping.jsonDecode(value))
			end

			self.cached_component_mappings = newOutput
		end

		if input.cachedComponentMappings ~= nil then
			local newOutput: { DisplayLocationTemplateMapping } = {}
			for _, value in input.cachedComponentMappings do
				table.insert(newOutput, messages.DisplayLocationTemplateMapping.jsonDecode(value))
			end

			self.cached_component_mappings = newOutput
		end

		if input.locale ~= nil then
			self.locale = input.locale
		end

		return self
	end

	_GetTemplatesRequestImpl.descriptor = {
		name = "GetTemplatesRequest",
		fullName = "roblox.apppageplatform.templates.v1beta1.GetTemplatesRequest",
	}

	messages.GetTemplatesRequest = _GetTemplatesRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetTemplatesRequest)
end

do
	local _GetTemplatesResponseImpl = {}
	_GetTemplatesResponseImpl.__index = _GetTemplatesResponseImpl

	function _GetTemplatesResponseImpl.new(data: _GetTemplatesResponsePartialFields?): GetTemplatesResponse
		return setmetatable({
			templates = if data == nil or data.templates == nil then {} else data.templates,
			component_mappings = if data == nil or data.component_mappings == nil then {} else data.component_mappings,
			valid_cached_component_mappings = if data == nil or data.valid_cached_component_mappings == nil
				then {}
				else data.valid_cached_component_mappings,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _GetTemplatesResponseImpl :: _GetTemplatesResponseImpl)
	end

	function _GetTemplatesResponseImpl.encode(self: GetTemplatesResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.component_mappings ~= nil and #self.component_mappings > 0 then
			for _, value in self.component_mappings do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.valid_cached_component_mappings ~= nil and #self.valid_cached_component_mappings > 0 then
			for _, value in self.valid_cached_component_mappings do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetTemplatesResponseImpl.decode(input: buffer): GetTemplatesResponse
		local self = _GetTemplatesResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetTemplatesResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.component_mappings, messages.DisplayLocationTemplateMapping.decode(value))
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.valid_cached_component_mappings,
						messages.DisplayLocationTemplateMapping.decode(value)
					)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetTemplatesResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetTemplatesResponseImpl.jsonEncode(self: GetTemplatesResponse): any
		local output = {}

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.component_mappings ~= nil and #self.component_mappings > 0 then
			local newOutput = {}
			for _, value in self.component_mappings do
				table.insert(newOutput, value:jsonEncode())
			end
			output.componentMappings = newOutput
		end

		if self.valid_cached_component_mappings ~= nil and #self.valid_cached_component_mappings > 0 then
			local newOutput = {}
			for _, value in self.valid_cached_component_mappings do
				table.insert(newOutput, value:jsonEncode())
			end
			output.validCachedComponentMappings = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _GetTemplatesResponseImpl.jsonDecode(input: { [string]: any }): GetTemplatesResponse
		local self = _GetTemplatesResponseImpl.new()

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.component_mappings ~= nil then
			local newOutput: { DisplayLocationTemplateMapping } = {}
			for _, value in input.component_mappings do
				table.insert(newOutput, messages.DisplayLocationTemplateMapping.jsonDecode(value))
			end

			self.component_mappings = newOutput
		end

		if input.componentMappings ~= nil then
			local newOutput: { DisplayLocationTemplateMapping } = {}
			for _, value in input.componentMappings do
				table.insert(newOutput, messages.DisplayLocationTemplateMapping.jsonDecode(value))
			end

			self.component_mappings = newOutput
		end

		if input.valid_cached_component_mappings ~= nil then
			local newOutput: { DisplayLocationTemplateMapping } = {}
			for _, value in input.valid_cached_component_mappings do
				table.insert(newOutput, messages.DisplayLocationTemplateMapping.jsonDecode(value))
			end

			self.valid_cached_component_mappings = newOutput
		end

		if input.validCachedComponentMappings ~= nil then
			local newOutput: { DisplayLocationTemplateMapping } = {}
			for _, value in input.validCachedComponentMappings do
				table.insert(newOutput, messages.DisplayLocationTemplateMapping.jsonDecode(value))
			end

			self.valid_cached_component_mappings = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_GetTemplatesResponseImpl.descriptor = {
		name = "GetTemplatesResponse",
		fullName = "roblox.apppageplatform.templates.v1beta1.GetTemplatesResponse",
	}

	messages.GetTemplatesResponse = _GetTemplatesResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetTemplatesResponse)
end

do
	local _GetTemplatesResponse_TemplatesEntryImpl = {}
	_GetTemplatesResponse_TemplatesEntryImpl.__index = _GetTemplatesResponse_TemplatesEntryImpl

	function _GetTemplatesResponse_TemplatesEntryImpl.new(
		data: _GetTemplatesResponse_TemplatesEntryPartialFields?
	): GetTemplatesResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _GetTemplatesResponse_TemplatesEntryImpl :: _GetTemplatesResponse_TemplatesEntryImpl)
	end

	function _GetTemplatesResponse_TemplatesEntryImpl.encode(self: GetTemplatesResponse_TemplatesEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetTemplatesResponse_TemplatesEntryImpl.decode(input: buffer): GetTemplatesResponse_TemplatesEntry
		local self = _GetTemplatesResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetTemplatesResponse_TemplatesEntryImpl.jsonEncode(self: GetTemplatesResponse_TemplatesEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _GetTemplatesResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetTemplatesResponse_TemplatesEntry
		local self = _GetTemplatesResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_GetTemplatesResponse_TemplatesEntryImpl.descriptor = {
		name = "GetTemplatesResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.templates.v1beta1.TemplatesEntry",
	}

	messages.GetTemplatesResponse_TemplatesEntry = _GetTemplatesResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetTemplatesResponse_TemplatesEntry)
end

do
	local _GetTemplatesResponse_LocalizedLiteralsEntryImpl = {}
	_GetTemplatesResponse_LocalizedLiteralsEntryImpl.__index = _GetTemplatesResponse_LocalizedLiteralsEntryImpl

	function _GetTemplatesResponse_LocalizedLiteralsEntryImpl.new(
		data: _GetTemplatesResponse_LocalizedLiteralsEntryPartialFields?
	): GetTemplatesResponse_LocalizedLiteralsEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then "" else data.value,
		}, _GetTemplatesResponse_LocalizedLiteralsEntryImpl :: _GetTemplatesResponse_LocalizedLiteralsEntryImpl)
	end

	function _GetTemplatesResponse_LocalizedLiteralsEntryImpl.encode(
		self: GetTemplatesResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetTemplatesResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): GetTemplatesResponse_LocalizedLiteralsEntry
		local self = _GetTemplatesResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetTemplatesResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: GetTemplatesResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _GetTemplatesResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetTemplatesResponse_LocalizedLiteralsEntry
		local self = _GetTemplatesResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_GetTemplatesResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "GetTemplatesResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.templates.v1beta1.LocalizedLiteralsEntry",
	}

	messages.GetTemplatesResponse_LocalizedLiteralsEntry = _GetTemplatesResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetTemplatesResponse_LocalizedLiteralsEntry)
end

return {
	DisplayLocationTemplateMapping = messages.DisplayLocationTemplateMapping,
	GetTemplatesRequest = messages.GetTemplatesRequest,
	GetTemplatesResponse = messages.GetTemplatesResponse,
}
