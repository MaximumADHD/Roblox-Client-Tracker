--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages =
	{
		ExperienceEventDetailsPageRequest: _ExperienceEventDetailsPageRequestMessage,
		ExperienceEventDetailsPageResponse: _ExperienceEventDetailsPageResponseMessage,
		ExperienceEventDetailsPageResponse_TemplatesEntry: _ExperienceEventDetailsPageResponse_TemplatesEntryMessage,
		ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryMessage,
	}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)

type _ExperienceEventDetailsPageRequestImpl = {
	__index: _ExperienceEventDetailsPageRequestImpl,
	new: (fields: _ExperienceEventDetailsPageRequestPartialFields?) -> ExperienceEventDetailsPageRequest,
	encode: (self: ExperienceEventDetailsPageRequest) -> buffer,
	decode: (input: buffer) -> ExperienceEventDetailsPageRequest,
	jsonEncode: (self: ExperienceEventDetailsPageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExperienceEventDetailsPageRequest,
	descriptor: proto.Descriptor,
}

type _ExperienceEventDetailsPageRequestFields = {
	event_id: number,
	user_id: number,
}

type _ExperienceEventDetailsPageRequestPartialFields = {
	event_id: number?,
	user_id: number?,
}

export type ExperienceEventDetailsPageRequest = typeof(setmetatable(
	{} :: _ExperienceEventDetailsPageRequestFields,
	{} :: _ExperienceEventDetailsPageRequestImpl
))
type _ExperienceEventDetailsPageRequestMessage = proto.Message<
	ExperienceEventDetailsPageRequest,
	_ExperienceEventDetailsPageRequestPartialFields
>

type _ExperienceEventDetailsPageResponseImpl = {
	__index: _ExperienceEventDetailsPageResponseImpl,
	new: (fields: _ExperienceEventDetailsPageResponsePartialFields?) -> ExperienceEventDetailsPageResponse,
	encode: (self: ExperienceEventDetailsPageResponse) -> buffer,
	decode: (input: buffer) -> ExperienceEventDetailsPageResponse,
	jsonEncode: (self: ExperienceEventDetailsPageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExperienceEventDetailsPageResponse,
	descriptor: proto.Descriptor,
}

type _ExperienceEventDetailsPageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
}

type _ExperienceEventDetailsPageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
}

export type ExperienceEventDetailsPageResponse = typeof(setmetatable(
	{} :: _ExperienceEventDetailsPageResponseFields,
	{} :: _ExperienceEventDetailsPageResponseImpl
))
type _ExperienceEventDetailsPageResponseMessage = proto.Message<
	ExperienceEventDetailsPageResponse,
	_ExperienceEventDetailsPageResponsePartialFields
>

type _ExperienceEventDetailsPageResponse_TemplatesEntryImpl = {
	__index: _ExperienceEventDetailsPageResponse_TemplatesEntryImpl,
	new: (
		fields: _ExperienceEventDetailsPageResponse_TemplatesEntryPartialFields?
	) -> ExperienceEventDetailsPageResponse_TemplatesEntry,
	encode: (self: ExperienceEventDetailsPageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> ExperienceEventDetailsPageResponse_TemplatesEntry,
	jsonEncode: (self: ExperienceEventDetailsPageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExperienceEventDetailsPageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _ExperienceEventDetailsPageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _ExperienceEventDetailsPageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type ExperienceEventDetailsPageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _ExperienceEventDetailsPageResponse_TemplatesEntryFields,
	{} :: _ExperienceEventDetailsPageResponse_TemplatesEntryImpl
))
type _ExperienceEventDetailsPageResponse_TemplatesEntryMessage = proto.Message<
	ExperienceEventDetailsPageResponse_TemplatesEntry,
	_ExperienceEventDetailsPageResponse_TemplatesEntryPartialFields
>

type _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl = {
	__index: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryPartialFields?
	) -> ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry,
	encode: (self: ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryFields,
	{} :: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl
))
type _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry,
	_ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _ExperienceEventDetailsPageRequestImpl = {}
	_ExperienceEventDetailsPageRequestImpl.__index = _ExperienceEventDetailsPageRequestImpl

	function _ExperienceEventDetailsPageRequestImpl.new(
		data: _ExperienceEventDetailsPageRequestPartialFields?
	): ExperienceEventDetailsPageRequest
		return setmetatable({
			event_id = if data == nil or data.event_id == nil then 0 else data.event_id,
			user_id = if data == nil or data.user_id == nil then 0 else data.user_id,
		}, _ExperienceEventDetailsPageRequestImpl :: _ExperienceEventDetailsPageRequestImpl)
	end

	function _ExperienceEventDetailsPageRequestImpl.encode(self: ExperienceEventDetailsPageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.event_id ~= nil and self.event_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.event_id)
		end

		if self.user_id ~= nil and self.user_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.user_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExperienceEventDetailsPageRequestImpl.decode(input: buffer): ExperienceEventDetailsPageRequest
		local self = _ExperienceEventDetailsPageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.event_id = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.user_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExperienceEventDetailsPageRequestImpl.jsonEncode(self: ExperienceEventDetailsPageRequest): any
		local output = {}

		if self.event_id ~= nil and self.event_id ~= 0 then
			output.eventId = self.event_id
		end

		if self.user_id ~= nil and self.user_id ~= 0 then
			output.userId = self.user_id
		end

		return output
	end

	function _ExperienceEventDetailsPageRequestImpl.jsonDecode(
		input: { [string]: any }
	): ExperienceEventDetailsPageRequest
		local self = _ExperienceEventDetailsPageRequestImpl.new()

		if input.event_id ~= nil then
			self.event_id = input.event_id
		end

		if input.eventId ~= nil then
			self.event_id = input.eventId
		end

		if input.user_id ~= nil then
			self.user_id = input.user_id
		end

		if input.userId ~= nil then
			self.user_id = input.userId
		end

		return self
	end

	_ExperienceEventDetailsPageRequestImpl.descriptor = {
		name = "ExperienceEventDetailsPageRequest",
		fullName = "roblox.apppageplatform.experienceevents.v1beta1.ExperienceEventDetailsPageRequest",
	}

	messages.ExperienceEventDetailsPageRequest = _ExperienceEventDetailsPageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExperienceEventDetailsPageRequest)
end

do
	local _ExperienceEventDetailsPageResponseImpl = {}
	_ExperienceEventDetailsPageResponseImpl.__index = _ExperienceEventDetailsPageResponseImpl

	function _ExperienceEventDetailsPageResponseImpl.new(
		data: _ExperienceEventDetailsPageResponsePartialFields?
	): ExperienceEventDetailsPageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _ExperienceEventDetailsPageResponseImpl :: _ExperienceEventDetailsPageResponseImpl)
	end

	function _ExperienceEventDetailsPageResponseImpl.encode(self: ExperienceEventDetailsPageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExperienceEventDetailsPageResponseImpl.decode(input: buffer): ExperienceEventDetailsPageResponse
		local self = _ExperienceEventDetailsPageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.ExperienceEventDetailsPageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExperienceEventDetailsPageResponseImpl.jsonEncode(self: ExperienceEventDetailsPageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _ExperienceEventDetailsPageResponseImpl.jsonDecode(
		input: { [string]: any }
	): ExperienceEventDetailsPageResponse
		local self = _ExperienceEventDetailsPageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_ExperienceEventDetailsPageResponseImpl.descriptor = {
		name = "ExperienceEventDetailsPageResponse",
		fullName = "roblox.apppageplatform.experienceevents.v1beta1.ExperienceEventDetailsPageResponse",
	}

	messages.ExperienceEventDetailsPageResponse = _ExperienceEventDetailsPageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExperienceEventDetailsPageResponse)
end

do
	local _ExperienceEventDetailsPageResponse_TemplatesEntryImpl = {}
	_ExperienceEventDetailsPageResponse_TemplatesEntryImpl.__index =
		_ExperienceEventDetailsPageResponse_TemplatesEntryImpl

	function _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.new(
		data: _ExperienceEventDetailsPageResponse_TemplatesEntryPartialFields?
	): ExperienceEventDetailsPageResponse_TemplatesEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then nil else data.value,
			},
			_ExperienceEventDetailsPageResponse_TemplatesEntryImpl :: _ExperienceEventDetailsPageResponse_TemplatesEntryImpl
		)
	end

	function _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.encode(
		self: ExperienceEventDetailsPageResponse_TemplatesEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.decode(
		input: buffer
	): ExperienceEventDetailsPageResponse_TemplatesEntry
		local self = _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.jsonEncode(
		self: ExperienceEventDetailsPageResponse_TemplatesEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): ExperienceEventDetailsPageResponse_TemplatesEntry
		local self = _ExperienceEventDetailsPageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_ExperienceEventDetailsPageResponse_TemplatesEntryImpl.descriptor = {
		name = "ExperienceEventDetailsPageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.experienceevents.v1beta1.TemplatesEntry",
	}

	messages.ExperienceEventDetailsPageResponse_TemplatesEntry =
		_ExperienceEventDetailsPageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExperienceEventDetailsPageResponse_TemplatesEntry)
end

do
	local _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl = {}
	_ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.__index =
		_ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl

	function _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.new(
		data: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryPartialFields?
	): ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl :: _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl
		)
	end

	function _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.encode(
		self: ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry
		local self = _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry
		local self = _ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.experienceevents.v1beta1.LocalizedLiteralsEntry",
	}

	messages.ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry =
		_ExperienceEventDetailsPageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExperienceEventDetailsPageResponse_LocalizedLiteralsEntry)
end

return {
	ExperienceEventDetailsPageRequest = messages.ExperienceEventDetailsPageRequest,
	ExperienceEventDetailsPageResponse = messages.ExperienceEventDetailsPageResponse,
}
