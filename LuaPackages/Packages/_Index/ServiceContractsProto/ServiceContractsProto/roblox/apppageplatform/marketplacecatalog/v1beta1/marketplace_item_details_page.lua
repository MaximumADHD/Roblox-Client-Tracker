--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages =
	{
		MarketplaceItemDetailsPageRequest: _MarketplaceItemDetailsPageRequestMessage,
		MarketplaceItemDetailsPageResponse: _MarketplaceItemDetailsPageResponseMessage,
		MarketplaceItemDetailsPageResponse_TemplatesEntry: _MarketplaceItemDetailsPageResponse_TemplatesEntryMessage,
		MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryMessage,
	}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)

type _MarketplaceItemDetailsPageRequestImpl = {
	__index: _MarketplaceItemDetailsPageRequestImpl,
	new: (fields: _MarketplaceItemDetailsPageRequestPartialFields?) -> MarketplaceItemDetailsPageRequest,
	encode: (self: MarketplaceItemDetailsPageRequest) -> buffer,
	decode: (input: buffer) -> MarketplaceItemDetailsPageRequest,
	jsonEncode: (self: MarketplaceItemDetailsPageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceItemDetailsPageRequest,
	descriptor: proto.Descriptor,
}

type _MarketplaceItemDetailsPageRequestFields = {
	item_id: string,
	item_type: string,
	screen_size: string,
}

type _MarketplaceItemDetailsPageRequestPartialFields = {
	item_id: string?,
	item_type: string?,
	screen_size: string?,
}

export type MarketplaceItemDetailsPageRequest = typeof(setmetatable(
	{} :: _MarketplaceItemDetailsPageRequestFields,
	{} :: _MarketplaceItemDetailsPageRequestImpl
))
type _MarketplaceItemDetailsPageRequestMessage = proto.Message<
	MarketplaceItemDetailsPageRequest,
	_MarketplaceItemDetailsPageRequestPartialFields
>

type _MarketplaceItemDetailsPageResponseImpl = {
	__index: _MarketplaceItemDetailsPageResponseImpl,
	new: (fields: _MarketplaceItemDetailsPageResponsePartialFields?) -> MarketplaceItemDetailsPageResponse,
	encode: (self: MarketplaceItemDetailsPageResponse) -> buffer,
	decode: (input: buffer) -> MarketplaceItemDetailsPageResponse,
	jsonEncode: (self: MarketplaceItemDetailsPageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceItemDetailsPageResponse,
	descriptor: proto.Descriptor,
}

type _MarketplaceItemDetailsPageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
}

type _MarketplaceItemDetailsPageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
}

export type MarketplaceItemDetailsPageResponse = typeof(setmetatable(
	{} :: _MarketplaceItemDetailsPageResponseFields,
	{} :: _MarketplaceItemDetailsPageResponseImpl
))
type _MarketplaceItemDetailsPageResponseMessage = proto.Message<
	MarketplaceItemDetailsPageResponse,
	_MarketplaceItemDetailsPageResponsePartialFields
>

type _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl = {
	__index: _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl,
	new: (
		fields: _MarketplaceItemDetailsPageResponse_TemplatesEntryPartialFields?
	) -> MarketplaceItemDetailsPageResponse_TemplatesEntry,
	encode: (self: MarketplaceItemDetailsPageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> MarketplaceItemDetailsPageResponse_TemplatesEntry,
	jsonEncode: (self: MarketplaceItemDetailsPageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceItemDetailsPageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _MarketplaceItemDetailsPageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _MarketplaceItemDetailsPageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type MarketplaceItemDetailsPageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _MarketplaceItemDetailsPageResponse_TemplatesEntryFields,
	{} :: _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl
))
type _MarketplaceItemDetailsPageResponse_TemplatesEntryMessage = proto.Message<
	MarketplaceItemDetailsPageResponse_TemplatesEntry,
	_MarketplaceItemDetailsPageResponse_TemplatesEntryPartialFields
>

type _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl = {
	__index: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryPartialFields?
	) -> MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry,
	encode: (self: MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryFields,
	{} :: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl
))
type _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry,
	_MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _MarketplaceItemDetailsPageRequestImpl = {}
	_MarketplaceItemDetailsPageRequestImpl.__index = _MarketplaceItemDetailsPageRequestImpl

	function _MarketplaceItemDetailsPageRequestImpl.new(
		data: _MarketplaceItemDetailsPageRequestPartialFields?
	): MarketplaceItemDetailsPageRequest
		return setmetatable({
			item_id = if data == nil or data.item_id == nil then "" else data.item_id,
			item_type = if data == nil or data.item_type == nil then "" else data.item_type,
			screen_size = if data == nil or data.screen_size == nil then "" else data.screen_size,
		}, _MarketplaceItemDetailsPageRequestImpl :: _MarketplaceItemDetailsPageRequestImpl)
	end

	function _MarketplaceItemDetailsPageRequestImpl.encode(self: MarketplaceItemDetailsPageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.item_id ~= nil and self.item_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.item_id)
		end

		if self.item_type ~= nil and self.item_type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.item_type)
		end

		if self.screen_size ~= nil and self.screen_size ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.screen_size)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceItemDetailsPageRequestImpl.decode(input: buffer): MarketplaceItemDetailsPageRequest
		local self = _MarketplaceItemDetailsPageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.item_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.item_type = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.screen_size = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceItemDetailsPageRequestImpl.jsonEncode(self: MarketplaceItemDetailsPageRequest): any
		local output = {}

		if self.item_id ~= nil and self.item_id ~= "" then
			output.itemId = self.item_id
		end

		if self.item_type ~= nil and self.item_type ~= "" then
			output.itemType = self.item_type
		end

		if self.screen_size ~= nil and self.screen_size ~= "" then
			output.screenSize = self.screen_size
		end

		return output
	end

	function _MarketplaceItemDetailsPageRequestImpl.jsonDecode(
		input: { [string]: any }
	): MarketplaceItemDetailsPageRequest
		local self = _MarketplaceItemDetailsPageRequestImpl.new()

		if input.item_id ~= nil then
			self.item_id = input.item_id
		end

		if input.itemId ~= nil then
			self.item_id = input.itemId
		end

		if input.item_type ~= nil then
			self.item_type = input.item_type
		end

		if input.itemType ~= nil then
			self.item_type = input.itemType
		end

		if input.screen_size ~= nil then
			self.screen_size = input.screen_size
		end

		if input.screenSize ~= nil then
			self.screen_size = input.screenSize
		end

		return self
	end

	_MarketplaceItemDetailsPageRequestImpl.descriptor = {
		name = "MarketplaceItemDetailsPageRequest",
		fullName = "roblox.apppageplatform.marketplacecatalog.v1beta1.MarketplaceItemDetailsPageRequest",
	}

	messages.MarketplaceItemDetailsPageRequest = _MarketplaceItemDetailsPageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceItemDetailsPageRequest)
end

do
	local _MarketplaceItemDetailsPageResponseImpl = {}
	_MarketplaceItemDetailsPageResponseImpl.__index = _MarketplaceItemDetailsPageResponseImpl

	function _MarketplaceItemDetailsPageResponseImpl.new(
		data: _MarketplaceItemDetailsPageResponsePartialFields?
	): MarketplaceItemDetailsPageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _MarketplaceItemDetailsPageResponseImpl :: _MarketplaceItemDetailsPageResponseImpl)
	end

	function _MarketplaceItemDetailsPageResponseImpl.encode(self: MarketplaceItemDetailsPageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceItemDetailsPageResponseImpl.decode(input: buffer): MarketplaceItemDetailsPageResponse
		local self = _MarketplaceItemDetailsPageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.MarketplaceItemDetailsPageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceItemDetailsPageResponseImpl.jsonEncode(self: MarketplaceItemDetailsPageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _MarketplaceItemDetailsPageResponseImpl.jsonDecode(
		input: { [string]: any }
	): MarketplaceItemDetailsPageResponse
		local self = _MarketplaceItemDetailsPageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_MarketplaceItemDetailsPageResponseImpl.descriptor = {
		name = "MarketplaceItemDetailsPageResponse",
		fullName = "roblox.apppageplatform.marketplacecatalog.v1beta1.MarketplaceItemDetailsPageResponse",
	}

	messages.MarketplaceItemDetailsPageResponse = _MarketplaceItemDetailsPageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceItemDetailsPageResponse)
end

do
	local _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl = {}
	_MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.__index =
		_MarketplaceItemDetailsPageResponse_TemplatesEntryImpl

	function _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.new(
		data: _MarketplaceItemDetailsPageResponse_TemplatesEntryPartialFields?
	): MarketplaceItemDetailsPageResponse_TemplatesEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then nil else data.value,
			},
			_MarketplaceItemDetailsPageResponse_TemplatesEntryImpl :: _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl
		)
	end

	function _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.encode(
		self: MarketplaceItemDetailsPageResponse_TemplatesEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.decode(
		input: buffer
	): MarketplaceItemDetailsPageResponse_TemplatesEntry
		local self = _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.jsonEncode(
		self: MarketplaceItemDetailsPageResponse_TemplatesEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): MarketplaceItemDetailsPageResponse_TemplatesEntry
		local self = _MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_MarketplaceItemDetailsPageResponse_TemplatesEntryImpl.descriptor = {
		name = "MarketplaceItemDetailsPageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.marketplacecatalog.v1beta1.TemplatesEntry",
	}

	messages.MarketplaceItemDetailsPageResponse_TemplatesEntry =
		_MarketplaceItemDetailsPageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceItemDetailsPageResponse_TemplatesEntry)
end

do
	local _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl = {}
	_MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.__index =
		_MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl

	function _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.new(
		data: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryPartialFields?
	): MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl :: _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl
		)
	end

	function _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.encode(
		self: MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry
		local self = _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry
		local self = _MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.marketplacecatalog.v1beta1.LocalizedLiteralsEntry",
	}

	messages.MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry =
		_MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.MarketplaceItemDetailsPageResponse_LocalizedLiteralsEntry)
end

return {
	MarketplaceItemDetailsPageRequest = messages.MarketplaceItemDetailsPageRequest,
	MarketplaceItemDetailsPageResponse = messages.MarketplaceItemDetailsPageResponse,
}
