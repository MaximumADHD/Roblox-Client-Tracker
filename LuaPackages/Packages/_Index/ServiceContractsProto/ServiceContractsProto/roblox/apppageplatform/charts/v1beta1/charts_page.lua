--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GetChartsPageRequest: _GetChartsPageRequestMessage,
	GetChartsPageResponse: _GetChartsPageResponseMessage,
	GetChartsPageResponse_TemplatesEntry: _GetChartsPageResponse_TemplatesEntryMessage,
	GetChartsPageResponse_LocalizedLiteralsEntry: _GetChartsPageResponse_LocalizedLiteralsEntryMessage,
	GetChartsSortDetailRequest: _GetChartsSortDetailRequestMessage,
	GetChartsSortDetailResponse: _GetChartsSortDetailResponseMessage,
	GetChartsSortDetailResponse_TemplatesEntry: _GetChartsSortDetailResponse_TemplatesEntryMessage,
	GetChartsSortDetailResponse_LocalizedLiteralsEntry: _GetChartsSortDetailResponse_LocalizedLiteralsEntryMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)

type _GetChartsPageRequestImpl = {
	__index: _GetChartsPageRequestImpl,
	new: (fields: _GetChartsPageRequestPartialFields?) -> GetChartsPageRequest,
	encode: (self: GetChartsPageRequest) -> buffer,
	decode: (input: buffer) -> GetChartsPageRequest,
	jsonEncode: (self: GetChartsPageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsPageRequest,
	descriptor: proto.Descriptor,
}

type _GetChartsPageRequestFields = {
	feed_page_token: string,
	device: string,
	country: string,
	age_group: string,
	session_id: string,
	entry_pagination_key: string,
	entry_page_token: string,
	max_memory: string,
	page_type: string,
}

type _GetChartsPageRequestPartialFields = {
	feed_page_token: string?,
	device: string?,
	country: string?,
	age_group: string?,
	session_id: string?,
	entry_pagination_key: string?,
	entry_page_token: string?,
	max_memory: string?,
	page_type: string?,
}

export type GetChartsPageRequest = typeof(setmetatable(
	{} :: _GetChartsPageRequestFields,
	{} :: _GetChartsPageRequestImpl
))
type _GetChartsPageRequestMessage = proto.Message<GetChartsPageRequest, _GetChartsPageRequestPartialFields>

type _GetChartsPageResponseImpl = {
	__index: _GetChartsPageResponseImpl,
	new: (fields: _GetChartsPageResponsePartialFields?) -> GetChartsPageResponse,
	encode: (self: GetChartsPageResponse) -> buffer,
	decode: (input: buffer) -> GetChartsPageResponse,
	jsonEncode: (self: GetChartsPageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsPageResponse,
	descriptor: proto.Descriptor,
}

type _GetChartsPageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
}

type _GetChartsPageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
}

export type GetChartsPageResponse = typeof(setmetatable(
	{} :: _GetChartsPageResponseFields,
	{} :: _GetChartsPageResponseImpl
))
type _GetChartsPageResponseMessage = proto.Message<GetChartsPageResponse, _GetChartsPageResponsePartialFields>

type _GetChartsPageResponse_TemplatesEntryImpl = {
	__index: _GetChartsPageResponse_TemplatesEntryImpl,
	new: (fields: _GetChartsPageResponse_TemplatesEntryPartialFields?) -> GetChartsPageResponse_TemplatesEntry,
	encode: (self: GetChartsPageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> GetChartsPageResponse_TemplatesEntry,
	jsonEncode: (self: GetChartsPageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsPageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _GetChartsPageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _GetChartsPageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type GetChartsPageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _GetChartsPageResponse_TemplatesEntryFields,
	{} :: _GetChartsPageResponse_TemplatesEntryImpl
))
type _GetChartsPageResponse_TemplatesEntryMessage = proto.Message<
	GetChartsPageResponse_TemplatesEntry,
	_GetChartsPageResponse_TemplatesEntryPartialFields
>

type _GetChartsPageResponse_LocalizedLiteralsEntryImpl = {
	__index: _GetChartsPageResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _GetChartsPageResponse_LocalizedLiteralsEntryPartialFields?
	) -> GetChartsPageResponse_LocalizedLiteralsEntry,
	encode: (self: GetChartsPageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> GetChartsPageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: GetChartsPageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsPageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _GetChartsPageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _GetChartsPageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type GetChartsPageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _GetChartsPageResponse_LocalizedLiteralsEntryFields,
	{} :: _GetChartsPageResponse_LocalizedLiteralsEntryImpl
))
type _GetChartsPageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	GetChartsPageResponse_LocalizedLiteralsEntry,
	_GetChartsPageResponse_LocalizedLiteralsEntryPartialFields
>

type _GetChartsSortDetailRequestImpl = {
	__index: _GetChartsSortDetailRequestImpl,
	new: (fields: _GetChartsSortDetailRequestPartialFields?) -> GetChartsSortDetailRequest,
	encode: (self: GetChartsSortDetailRequest) -> buffer,
	decode: (input: buffer) -> GetChartsSortDetailRequest,
	jsonEncode: (self: GetChartsSortDetailRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsSortDetailRequest,
	descriptor: proto.Descriptor,
}

type _GetChartsSortDetailRequestFields = {
	sort_id: string,
	content_page_token: string,
	device: string,
	country: string,
	session_id: string,
	age_group: string,
	max_memory: string,
}

type _GetChartsSortDetailRequestPartialFields = {
	sort_id: string?,
	content_page_token: string?,
	device: string?,
	country: string?,
	session_id: string?,
	age_group: string?,
	max_memory: string?,
}

export type GetChartsSortDetailRequest = typeof(setmetatable(
	{} :: _GetChartsSortDetailRequestFields,
	{} :: _GetChartsSortDetailRequestImpl
))
type _GetChartsSortDetailRequestMessage = proto.Message<
	GetChartsSortDetailRequest,
	_GetChartsSortDetailRequestPartialFields
>

type _GetChartsSortDetailResponseImpl = {
	__index: _GetChartsSortDetailResponseImpl,
	new: (fields: _GetChartsSortDetailResponsePartialFields?) -> GetChartsSortDetailResponse,
	encode: (self: GetChartsSortDetailResponse) -> buffer,
	decode: (input: buffer) -> GetChartsSortDetailResponse,
	jsonEncode: (self: GetChartsSortDetailResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsSortDetailResponse,
	descriptor: proto.Descriptor,
}

type _GetChartsSortDetailResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	next_content_page_token: string,
	localized_literals: { [string]: string },
}

type _GetChartsSortDetailResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	next_content_page_token: string?,
	localized_literals: { [string]: string }?,
}

export type GetChartsSortDetailResponse = typeof(setmetatable(
	{} :: _GetChartsSortDetailResponseFields,
	{} :: _GetChartsSortDetailResponseImpl
))
type _GetChartsSortDetailResponseMessage = proto.Message<
	GetChartsSortDetailResponse,
	_GetChartsSortDetailResponsePartialFields
>

type _GetChartsSortDetailResponse_TemplatesEntryImpl = {
	__index: _GetChartsSortDetailResponse_TemplatesEntryImpl,
	new: (
		fields: _GetChartsSortDetailResponse_TemplatesEntryPartialFields?
	) -> GetChartsSortDetailResponse_TemplatesEntry,
	encode: (self: GetChartsSortDetailResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> GetChartsSortDetailResponse_TemplatesEntry,
	jsonEncode: (self: GetChartsSortDetailResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsSortDetailResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _GetChartsSortDetailResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _GetChartsSortDetailResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type GetChartsSortDetailResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _GetChartsSortDetailResponse_TemplatesEntryFields,
	{} :: _GetChartsSortDetailResponse_TemplatesEntryImpl
))
type _GetChartsSortDetailResponse_TemplatesEntryMessage = proto.Message<
	GetChartsSortDetailResponse_TemplatesEntry,
	_GetChartsSortDetailResponse_TemplatesEntryPartialFields
>

type _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl = {
	__index: _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _GetChartsSortDetailResponse_LocalizedLiteralsEntryPartialFields?
	) -> GetChartsSortDetailResponse_LocalizedLiteralsEntry,
	encode: (self: GetChartsSortDetailResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> GetChartsSortDetailResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: GetChartsSortDetailResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetChartsSortDetailResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _GetChartsSortDetailResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _GetChartsSortDetailResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type GetChartsSortDetailResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _GetChartsSortDetailResponse_LocalizedLiteralsEntryFields,
	{} :: _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl
))
type _GetChartsSortDetailResponse_LocalizedLiteralsEntryMessage = proto.Message<
	GetChartsSortDetailResponse_LocalizedLiteralsEntry,
	_GetChartsSortDetailResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _GetChartsPageRequestImpl = {}
	_GetChartsPageRequestImpl.__index = _GetChartsPageRequestImpl

	function _GetChartsPageRequestImpl.new(data: _GetChartsPageRequestPartialFields?): GetChartsPageRequest
		return setmetatable({
			feed_page_token = if data == nil or data.feed_page_token == nil then "" else data.feed_page_token,
			device = if data == nil or data.device == nil then "" else data.device,
			country = if data == nil or data.country == nil then "" else data.country,
			age_group = if data == nil or data.age_group == nil then "" else data.age_group,
			session_id = if data == nil or data.session_id == nil then "" else data.session_id,
			entry_pagination_key = if data == nil or data.entry_pagination_key == nil
				then ""
				else data.entry_pagination_key,
			entry_page_token = if data == nil or data.entry_page_token == nil then "" else data.entry_page_token,
			max_memory = if data == nil or data.max_memory == nil then "" else data.max_memory,
			page_type = if data == nil or data.page_type == nil then "" else data.page_type,
		}, _GetChartsPageRequestImpl :: _GetChartsPageRequestImpl)
	end

	function _GetChartsPageRequestImpl.encode(self: GetChartsPageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.feed_page_token ~= nil and self.feed_page_token ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.feed_page_token)
		end

		if self.device ~= nil and self.device ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.device)
		end

		if self.country ~= nil and self.country ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.country)
		end

		if self.age_group ~= nil and self.age_group ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.age_group)
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.session_id)
		end

		if self.entry_pagination_key ~= nil and self.entry_pagination_key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.entry_pagination_key)
		end

		if self.entry_page_token ~= nil and self.entry_page_token ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.entry_page_token)
		end

		if self.max_memory ~= nil and self.max_memory ~= "" then
			output, cursor = proto.writeTag(output, cursor, 8, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.max_memory)
		end

		if self.page_type ~= nil and self.page_type ~= "" then
			output, cursor = proto.writeTag(output, cursor, 9, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.page_type)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsPageRequestImpl.decode(input: buffer): GetChartsPageRequest
		local self = _GetChartsPageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.feed_page_token = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.device = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.country = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.age_group = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.session_id = buffer.tostring(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.entry_pagination_key = buffer.tostring(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.entry_page_token = buffer.tostring(value)
					continue
				elseif field == 8 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.max_memory = buffer.tostring(value)
					continue
				elseif field == 9 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.page_type = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsPageRequestImpl.jsonEncode(self: GetChartsPageRequest): any
		local output = {}

		if self.feed_page_token ~= nil and self.feed_page_token ~= "" then
			output.feedPageToken = self.feed_page_token
		end

		if self.device ~= nil and self.device ~= "" then
			output.device = self.device
		end

		if self.country ~= nil and self.country ~= "" then
			output.country = self.country
		end

		if self.age_group ~= nil and self.age_group ~= "" then
			output.ageGroup = self.age_group
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output.sessionId = self.session_id
		end

		if self.entry_pagination_key ~= nil and self.entry_pagination_key ~= "" then
			output.entryPaginationKey = self.entry_pagination_key
		end

		if self.entry_page_token ~= nil and self.entry_page_token ~= "" then
			output.entryPageToken = self.entry_page_token
		end

		if self.max_memory ~= nil and self.max_memory ~= "" then
			output.maxMemory = self.max_memory
		end

		if self.page_type ~= nil and self.page_type ~= "" then
			output.pageType = self.page_type
		end

		return output
	end

	function _GetChartsPageRequestImpl.jsonDecode(input: { [string]: any }): GetChartsPageRequest
		local self = _GetChartsPageRequestImpl.new()

		if input.feed_page_token ~= nil then
			self.feed_page_token = input.feed_page_token
		end

		if input.feedPageToken ~= nil then
			self.feed_page_token = input.feedPageToken
		end

		if input.device ~= nil then
			self.device = input.device
		end

		if input.country ~= nil then
			self.country = input.country
		end

		if input.age_group ~= nil then
			self.age_group = input.age_group
		end

		if input.ageGroup ~= nil then
			self.age_group = input.ageGroup
		end

		if input.session_id ~= nil then
			self.session_id = input.session_id
		end

		if input.sessionId ~= nil then
			self.session_id = input.sessionId
		end

		if input.entry_pagination_key ~= nil then
			self.entry_pagination_key = input.entry_pagination_key
		end

		if input.entryPaginationKey ~= nil then
			self.entry_pagination_key = input.entryPaginationKey
		end

		if input.entry_page_token ~= nil then
			self.entry_page_token = input.entry_page_token
		end

		if input.entryPageToken ~= nil then
			self.entry_page_token = input.entryPageToken
		end

		if input.max_memory ~= nil then
			self.max_memory = input.max_memory
		end

		if input.maxMemory ~= nil then
			self.max_memory = input.maxMemory
		end

		if input.page_type ~= nil then
			self.page_type = input.page_type
		end

		if input.pageType ~= nil then
			self.page_type = input.pageType
		end

		return self
	end

	_GetChartsPageRequestImpl.descriptor = {
		name = "GetChartsPageRequest",
		fullName = "roblox.apppageplatform.charts.v1beta1.GetChartsPageRequest",
	}

	messages.GetChartsPageRequest = _GetChartsPageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsPageRequest)
end

do
	local _GetChartsPageResponseImpl = {}
	_GetChartsPageResponseImpl.__index = _GetChartsPageResponseImpl

	function _GetChartsPageResponseImpl.new(data: _GetChartsPageResponsePartialFields?): GetChartsPageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _GetChartsPageResponseImpl :: _GetChartsPageResponseImpl)
	end

	function _GetChartsPageResponseImpl.encode(self: GetChartsPageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsPageResponseImpl.decode(input: buffer): GetChartsPageResponse
		local self = _GetChartsPageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetChartsPageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetChartsPageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsPageResponseImpl.jsonEncode(self: GetChartsPageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _GetChartsPageResponseImpl.jsonDecode(input: { [string]: any }): GetChartsPageResponse
		local self = _GetChartsPageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_GetChartsPageResponseImpl.descriptor = {
		name = "GetChartsPageResponse",
		fullName = "roblox.apppageplatform.charts.v1beta1.GetChartsPageResponse",
	}

	messages.GetChartsPageResponse = _GetChartsPageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsPageResponse)
end

do
	local _GetChartsPageResponse_TemplatesEntryImpl = {}
	_GetChartsPageResponse_TemplatesEntryImpl.__index = _GetChartsPageResponse_TemplatesEntryImpl

	function _GetChartsPageResponse_TemplatesEntryImpl.new(
		data: _GetChartsPageResponse_TemplatesEntryPartialFields?
	): GetChartsPageResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _GetChartsPageResponse_TemplatesEntryImpl :: _GetChartsPageResponse_TemplatesEntryImpl)
	end

	function _GetChartsPageResponse_TemplatesEntryImpl.encode(self: GetChartsPageResponse_TemplatesEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsPageResponse_TemplatesEntryImpl.decode(input: buffer): GetChartsPageResponse_TemplatesEntry
		local self = _GetChartsPageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsPageResponse_TemplatesEntryImpl.jsonEncode(self: GetChartsPageResponse_TemplatesEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _GetChartsPageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetChartsPageResponse_TemplatesEntry
		local self = _GetChartsPageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_GetChartsPageResponse_TemplatesEntryImpl.descriptor = {
		name = "GetChartsPageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.charts.v1beta1.TemplatesEntry",
	}

	messages.GetChartsPageResponse_TemplatesEntry = _GetChartsPageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsPageResponse_TemplatesEntry)
end

do
	local _GetChartsPageResponse_LocalizedLiteralsEntryImpl = {}
	_GetChartsPageResponse_LocalizedLiteralsEntryImpl.__index = _GetChartsPageResponse_LocalizedLiteralsEntryImpl

	function _GetChartsPageResponse_LocalizedLiteralsEntryImpl.new(
		data: _GetChartsPageResponse_LocalizedLiteralsEntryPartialFields?
	): GetChartsPageResponse_LocalizedLiteralsEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then "" else data.value,
		}, _GetChartsPageResponse_LocalizedLiteralsEntryImpl :: _GetChartsPageResponse_LocalizedLiteralsEntryImpl)
	end

	function _GetChartsPageResponse_LocalizedLiteralsEntryImpl.encode(
		self: GetChartsPageResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsPageResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): GetChartsPageResponse_LocalizedLiteralsEntry
		local self = _GetChartsPageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsPageResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: GetChartsPageResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _GetChartsPageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetChartsPageResponse_LocalizedLiteralsEntry
		local self = _GetChartsPageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_GetChartsPageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "GetChartsPageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.charts.v1beta1.LocalizedLiteralsEntry",
	}

	messages.GetChartsPageResponse_LocalizedLiteralsEntry = _GetChartsPageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsPageResponse_LocalizedLiteralsEntry)
end

do
	local _GetChartsSortDetailRequestImpl = {}
	_GetChartsSortDetailRequestImpl.__index = _GetChartsSortDetailRequestImpl

	function _GetChartsSortDetailRequestImpl.new(
		data: _GetChartsSortDetailRequestPartialFields?
	): GetChartsSortDetailRequest
		return setmetatable({
			sort_id = if data == nil or data.sort_id == nil then "" else data.sort_id,
			content_page_token = if data == nil or data.content_page_token == nil then "" else data.content_page_token,
			device = if data == nil or data.device == nil then "" else data.device,
			country = if data == nil or data.country == nil then "" else data.country,
			session_id = if data == nil or data.session_id == nil then "" else data.session_id,
			age_group = if data == nil or data.age_group == nil then "" else data.age_group,
			max_memory = if data == nil or data.max_memory == nil then "" else data.max_memory,
		}, _GetChartsSortDetailRequestImpl :: _GetChartsSortDetailRequestImpl)
	end

	function _GetChartsSortDetailRequestImpl.encode(self: GetChartsSortDetailRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.sort_id ~= nil and self.sort_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.sort_id)
		end

		if self.content_page_token ~= nil and self.content_page_token ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.content_page_token)
		end

		if self.device ~= nil and self.device ~= "" then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.device)
		end

		if self.country ~= nil and self.country ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.country)
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.session_id)
		end

		if self.age_group ~= nil and self.age_group ~= "" then
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.age_group)
		end

		if self.max_memory ~= nil and self.max_memory ~= "" then
			output, cursor = proto.writeTag(output, cursor, 7, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.max_memory)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsSortDetailRequestImpl.decode(input: buffer): GetChartsSortDetailRequest
		local self = _GetChartsSortDetailRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.sort_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content_page_token = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.device = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.country = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.session_id = buffer.tostring(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.age_group = buffer.tostring(value)
					continue
				elseif field == 7 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.max_memory = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsSortDetailRequestImpl.jsonEncode(self: GetChartsSortDetailRequest): any
		local output = {}

		if self.sort_id ~= nil and self.sort_id ~= "" then
			output.sortId = self.sort_id
		end

		if self.content_page_token ~= nil and self.content_page_token ~= "" then
			output.contentPageToken = self.content_page_token
		end

		if self.device ~= nil and self.device ~= "" then
			output.device = self.device
		end

		if self.country ~= nil and self.country ~= "" then
			output.country = self.country
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output.sessionId = self.session_id
		end

		if self.age_group ~= nil and self.age_group ~= "" then
			output.ageGroup = self.age_group
		end

		if self.max_memory ~= nil and self.max_memory ~= "" then
			output.maxMemory = self.max_memory
		end

		return output
	end

	function _GetChartsSortDetailRequestImpl.jsonDecode(input: { [string]: any }): GetChartsSortDetailRequest
		local self = _GetChartsSortDetailRequestImpl.new()

		if input.sort_id ~= nil then
			self.sort_id = input.sort_id
		end

		if input.sortId ~= nil then
			self.sort_id = input.sortId
		end

		if input.content_page_token ~= nil then
			self.content_page_token = input.content_page_token
		end

		if input.contentPageToken ~= nil then
			self.content_page_token = input.contentPageToken
		end

		if input.device ~= nil then
			self.device = input.device
		end

		if input.country ~= nil then
			self.country = input.country
		end

		if input.session_id ~= nil then
			self.session_id = input.session_id
		end

		if input.sessionId ~= nil then
			self.session_id = input.sessionId
		end

		if input.age_group ~= nil then
			self.age_group = input.age_group
		end

		if input.ageGroup ~= nil then
			self.age_group = input.ageGroup
		end

		if input.max_memory ~= nil then
			self.max_memory = input.max_memory
		end

		if input.maxMemory ~= nil then
			self.max_memory = input.maxMemory
		end

		return self
	end

	_GetChartsSortDetailRequestImpl.descriptor = {
		name = "GetChartsSortDetailRequest",
		fullName = "roblox.apppageplatform.charts.v1beta1.GetChartsSortDetailRequest",
	}

	messages.GetChartsSortDetailRequest = _GetChartsSortDetailRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsSortDetailRequest)
end

do
	local _GetChartsSortDetailResponseImpl = {}
	_GetChartsSortDetailResponseImpl.__index = _GetChartsSortDetailResponseImpl

	function _GetChartsSortDetailResponseImpl.new(
		data: _GetChartsSortDetailResponsePartialFields?
	): GetChartsSortDetailResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			next_content_page_token = if data == nil or data.next_content_page_token == nil
				then ""
				else data.next_content_page_token,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _GetChartsSortDetailResponseImpl :: _GetChartsSortDetailResponseImpl)
	end

	function _GetChartsSortDetailResponseImpl.encode(self: GetChartsSortDetailResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.next_content_page_token ~= nil and self.next_content_page_token ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.next_content_page_token)
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsSortDetailResponseImpl.decode(input: buffer): GetChartsSortDetailResponse
		local self = _GetChartsSortDetailResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetChartsSortDetailResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.next_content_page_token = buffer.tostring(value)
					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetChartsSortDetailResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsSortDetailResponseImpl.jsonEncode(self: GetChartsSortDetailResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.next_content_page_token ~= nil and self.next_content_page_token ~= "" then
			output.nextContentPageToken = self.next_content_page_token
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _GetChartsSortDetailResponseImpl.jsonDecode(input: { [string]: any }): GetChartsSortDetailResponse
		local self = _GetChartsSortDetailResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.next_content_page_token ~= nil then
			self.next_content_page_token = input.next_content_page_token
		end

		if input.nextContentPageToken ~= nil then
			self.next_content_page_token = input.nextContentPageToken
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_GetChartsSortDetailResponseImpl.descriptor = {
		name = "GetChartsSortDetailResponse",
		fullName = "roblox.apppageplatform.charts.v1beta1.GetChartsSortDetailResponse",
	}

	messages.GetChartsSortDetailResponse = _GetChartsSortDetailResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsSortDetailResponse)
end

do
	local _GetChartsSortDetailResponse_TemplatesEntryImpl = {}
	_GetChartsSortDetailResponse_TemplatesEntryImpl.__index = _GetChartsSortDetailResponse_TemplatesEntryImpl

	function _GetChartsSortDetailResponse_TemplatesEntryImpl.new(
		data: _GetChartsSortDetailResponse_TemplatesEntryPartialFields?
	): GetChartsSortDetailResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _GetChartsSortDetailResponse_TemplatesEntryImpl :: _GetChartsSortDetailResponse_TemplatesEntryImpl)
	end

	function _GetChartsSortDetailResponse_TemplatesEntryImpl.encode(
		self: GetChartsSortDetailResponse_TemplatesEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsSortDetailResponse_TemplatesEntryImpl.decode(
		input: buffer
	): GetChartsSortDetailResponse_TemplatesEntry
		local self = _GetChartsSortDetailResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsSortDetailResponse_TemplatesEntryImpl.jsonEncode(
		self: GetChartsSortDetailResponse_TemplatesEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _GetChartsSortDetailResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetChartsSortDetailResponse_TemplatesEntry
		local self = _GetChartsSortDetailResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_GetChartsSortDetailResponse_TemplatesEntryImpl.descriptor = {
		name = "GetChartsSortDetailResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.charts.v1beta1.TemplatesEntry",
	}

	messages.GetChartsSortDetailResponse_TemplatesEntry = _GetChartsSortDetailResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsSortDetailResponse_TemplatesEntry)
end

do
	local _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl = {}
	_GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.__index =
		_GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl

	function _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.new(
		data: _GetChartsSortDetailResponse_LocalizedLiteralsEntryPartialFields?
	): GetChartsSortDetailResponse_LocalizedLiteralsEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl :: _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl
		)
	end

	function _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.encode(
		self: GetChartsSortDetailResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): GetChartsSortDetailResponse_LocalizedLiteralsEntry
		local self = _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: GetChartsSortDetailResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetChartsSortDetailResponse_LocalizedLiteralsEntry
		local self = _GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "GetChartsSortDetailResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.charts.v1beta1.LocalizedLiteralsEntry",
	}

	messages.GetChartsSortDetailResponse_LocalizedLiteralsEntry =
		_GetChartsSortDetailResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetChartsSortDetailResponse_LocalizedLiteralsEntry)
end

return {
	GetChartsPageRequest = messages.GetChartsPageRequest,
	GetChartsPageResponse = messages.GetChartsPageResponse,
	GetChartsSortDetailRequest = messages.GetChartsSortDetailRequest,
	GetChartsSortDetailResponse = messages.GetChartsSortDetailResponse,
}
