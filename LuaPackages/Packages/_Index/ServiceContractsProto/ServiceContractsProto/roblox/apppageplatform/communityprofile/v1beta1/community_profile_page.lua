--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	CommunityProfilePageRequest: _CommunityProfilePageRequestMessage,
	CommunityProfilePageResponse: _CommunityProfilePageResponseMessage,
	CommunityProfilePageResponse_TemplatesEntry: _CommunityProfilePageResponse_TemplatesEntryMessage,
	CommunityProfilePageResponse_LocalizedLiteralsEntry: _CommunityProfilePageResponse_LocalizedLiteralsEntryMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_client_capabilities =
	require(script.Parent.Parent.Parent.shared.v1beta1.client_capabilities)
local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)

type _CommunityProfilePageRequestImpl = {
	__index: _CommunityProfilePageRequestImpl,
	new: (fields: _CommunityProfilePageRequestPartialFields?) -> CommunityProfilePageRequest,
	encode: (self: CommunityProfilePageRequest) -> buffer,
	decode: (input: buffer) -> CommunityProfilePageRequest,
	jsonEncode: (self: CommunityProfilePageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CommunityProfilePageRequest,
	descriptor: proto.Descriptor,
}

type _CommunityProfilePageRequestFields = {
	community_id: string,
	client_capabilities: _roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities?,
}

type _CommunityProfilePageRequestPartialFields = {
	community_id: string?,
	client_capabilities: _roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities?,
}

export type CommunityProfilePageRequest = typeof(setmetatable(
	{} :: _CommunityProfilePageRequestFields,
	{} :: _CommunityProfilePageRequestImpl
))
type _CommunityProfilePageRequestMessage = proto.Message<
	CommunityProfilePageRequest,
	_CommunityProfilePageRequestPartialFields
>

type _CommunityProfilePageResponseImpl = {
	__index: _CommunityProfilePageResponseImpl,
	new: (fields: _CommunityProfilePageResponsePartialFields?) -> CommunityProfilePageResponse,
	encode: (self: CommunityProfilePageResponse) -> buffer,
	decode: (input: buffer) -> CommunityProfilePageResponse,
	jsonEncode: (self: CommunityProfilePageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CommunityProfilePageResponse,
	descriptor: proto.Descriptor,
}

type _CommunityProfilePageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
}

type _CommunityProfilePageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
}

export type CommunityProfilePageResponse = typeof(setmetatable(
	{} :: _CommunityProfilePageResponseFields,
	{} :: _CommunityProfilePageResponseImpl
))
type _CommunityProfilePageResponseMessage = proto.Message<
	CommunityProfilePageResponse,
	_CommunityProfilePageResponsePartialFields
>

type _CommunityProfilePageResponse_TemplatesEntryImpl = {
	__index: _CommunityProfilePageResponse_TemplatesEntryImpl,
	new: (
		fields: _CommunityProfilePageResponse_TemplatesEntryPartialFields?
	) -> CommunityProfilePageResponse_TemplatesEntry,
	encode: (self: CommunityProfilePageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> CommunityProfilePageResponse_TemplatesEntry,
	jsonEncode: (self: CommunityProfilePageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CommunityProfilePageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _CommunityProfilePageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _CommunityProfilePageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type CommunityProfilePageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _CommunityProfilePageResponse_TemplatesEntryFields,
	{} :: _CommunityProfilePageResponse_TemplatesEntryImpl
))
type _CommunityProfilePageResponse_TemplatesEntryMessage = proto.Message<
	CommunityProfilePageResponse_TemplatesEntry,
	_CommunityProfilePageResponse_TemplatesEntryPartialFields
>

type _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl = {
	__index: _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _CommunityProfilePageResponse_LocalizedLiteralsEntryPartialFields?
	) -> CommunityProfilePageResponse_LocalizedLiteralsEntry,
	encode: (self: CommunityProfilePageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> CommunityProfilePageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: CommunityProfilePageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> CommunityProfilePageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _CommunityProfilePageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _CommunityProfilePageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type CommunityProfilePageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _CommunityProfilePageResponse_LocalizedLiteralsEntryFields,
	{} :: _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl
))
type _CommunityProfilePageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	CommunityProfilePageResponse_LocalizedLiteralsEntry,
	_CommunityProfilePageResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _CommunityProfilePageRequestImpl = {}
	_CommunityProfilePageRequestImpl.__index = _CommunityProfilePageRequestImpl

	function _CommunityProfilePageRequestImpl.new(
		data: _CommunityProfilePageRequestPartialFields?
	): CommunityProfilePageRequest
		return setmetatable({
			community_id = if data == nil or data.community_id == nil then "" else data.community_id,
			client_capabilities = if data == nil or data.client_capabilities == nil
				then nil
				else data.client_capabilities,
		}, _CommunityProfilePageRequestImpl :: _CommunityProfilePageRequestImpl)
	end

	function _CommunityProfilePageRequestImpl.encode(self: CommunityProfilePageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.community_id ~= nil and self.community_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.community_id)
		end

		if self.client_capabilities ~= nil then
			local encoded = self.client_capabilities:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CommunityProfilePageRequestImpl.decode(input: buffer): CommunityProfilePageRequest
		local self = _CommunityProfilePageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.community_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.client_capabilities =
						_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CommunityProfilePageRequestImpl.jsonEncode(self: CommunityProfilePageRequest): any
		local output = {}

		if self.community_id ~= nil and self.community_id ~= "" then
			output.communityId = self.community_id
		end

		if self.client_capabilities ~= nil then
			output.clientCapabilities = self.client_capabilities:jsonEncode()
		end

		return output
	end

	function _CommunityProfilePageRequestImpl.jsonDecode(input: { [string]: any }): CommunityProfilePageRequest
		local self = _CommunityProfilePageRequestImpl.new()

		if input.community_id ~= nil then
			self.community_id = input.community_id
		end

		if input.communityId ~= nil then
			self.community_id = input.communityId
		end

		if input.client_capabilities ~= nil then
			self.client_capabilities =
				_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.jsonDecode(
					input.client_capabilities
				)
		end

		if input.clientCapabilities ~= nil then
			self.client_capabilities =
				_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.jsonDecode(
					input.clientCapabilities
				)
		end

		return self
	end

	_CommunityProfilePageRequestImpl.descriptor = {
		name = "CommunityProfilePageRequest",
		fullName = "roblox.apppageplatform.communityprofile.v1beta1.CommunityProfilePageRequest",
	}

	messages.CommunityProfilePageRequest = _CommunityProfilePageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CommunityProfilePageRequest)
end

do
	local _CommunityProfilePageResponseImpl = {}
	_CommunityProfilePageResponseImpl.__index = _CommunityProfilePageResponseImpl

	function _CommunityProfilePageResponseImpl.new(
		data: _CommunityProfilePageResponsePartialFields?
	): CommunityProfilePageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _CommunityProfilePageResponseImpl :: _CommunityProfilePageResponseImpl)
	end

	function _CommunityProfilePageResponseImpl.encode(self: CommunityProfilePageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CommunityProfilePageResponseImpl.decode(input: buffer): CommunityProfilePageResponse
		local self = _CommunityProfilePageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.CommunityProfilePageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.CommunityProfilePageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CommunityProfilePageResponseImpl.jsonEncode(self: CommunityProfilePageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _CommunityProfilePageResponseImpl.jsonDecode(input: { [string]: any }): CommunityProfilePageResponse
		local self = _CommunityProfilePageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_CommunityProfilePageResponseImpl.descriptor = {
		name = "CommunityProfilePageResponse",
		fullName = "roblox.apppageplatform.communityprofile.v1beta1.CommunityProfilePageResponse",
	}

	messages.CommunityProfilePageResponse = _CommunityProfilePageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CommunityProfilePageResponse)
end

do
	local _CommunityProfilePageResponse_TemplatesEntryImpl = {}
	_CommunityProfilePageResponse_TemplatesEntryImpl.__index = _CommunityProfilePageResponse_TemplatesEntryImpl

	function _CommunityProfilePageResponse_TemplatesEntryImpl.new(
		data: _CommunityProfilePageResponse_TemplatesEntryPartialFields?
	): CommunityProfilePageResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _CommunityProfilePageResponse_TemplatesEntryImpl :: _CommunityProfilePageResponse_TemplatesEntryImpl)
	end

	function _CommunityProfilePageResponse_TemplatesEntryImpl.encode(
		self: CommunityProfilePageResponse_TemplatesEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CommunityProfilePageResponse_TemplatesEntryImpl.decode(
		input: buffer
	): CommunityProfilePageResponse_TemplatesEntry
		local self = _CommunityProfilePageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CommunityProfilePageResponse_TemplatesEntryImpl.jsonEncode(
		self: CommunityProfilePageResponse_TemplatesEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _CommunityProfilePageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): CommunityProfilePageResponse_TemplatesEntry
		local self = _CommunityProfilePageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_CommunityProfilePageResponse_TemplatesEntryImpl.descriptor = {
		name = "CommunityProfilePageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.communityprofile.v1beta1.TemplatesEntry",
	}

	messages.CommunityProfilePageResponse_TemplatesEntry = _CommunityProfilePageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CommunityProfilePageResponse_TemplatesEntry)
end

do
	local _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl = {}
	_CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.__index =
		_CommunityProfilePageResponse_LocalizedLiteralsEntryImpl

	function _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.new(
		data: _CommunityProfilePageResponse_LocalizedLiteralsEntryPartialFields?
	): CommunityProfilePageResponse_LocalizedLiteralsEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_CommunityProfilePageResponse_LocalizedLiteralsEntryImpl :: _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl
		)
	end

	function _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.encode(
		self: CommunityProfilePageResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): CommunityProfilePageResponse_LocalizedLiteralsEntry
		local self = _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: CommunityProfilePageResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): CommunityProfilePageResponse_LocalizedLiteralsEntry
		local self = _CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_CommunityProfilePageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "CommunityProfilePageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.communityprofile.v1beta1.LocalizedLiteralsEntry",
	}

	messages.CommunityProfilePageResponse_LocalizedLiteralsEntry =
		_CommunityProfilePageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.CommunityProfilePageResponse_LocalizedLiteralsEntry)
end

return {
	CommunityProfilePageRequest = messages.CommunityProfilePageRequest,
	CommunityProfilePageResponse = messages.CommunityProfilePageResponse,
}
