--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	HomePageRequest: _HomePageRequestMessage,
	HomePageRequest_ContextualFeaturesEntry: _HomePageRequest_ContextualFeaturesEntryMessage,
	HomePageRequest_CachedRobloxComponentToTemplateIdEntry: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryMessage,
	HomeCollectionLayoutInfo: _HomeCollectionLayoutInfoMessage,
	HomeCollectionLayoutInfo_ItemsPerRowEntry: _HomeCollectionLayoutInfo_ItemsPerRowEntryMessage,
	HomePageResponse: _HomePageResponseMessage,
	HomePageResponse_TemplatesEntry: _HomePageResponse_TemplatesEntryMessage,
	HomePageResponse_LocalizedLiteralsEntry: _HomePageResponse_LocalizedLiteralsEntryMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)
local _roblox_apppageplatform_shared_v1beta1_client_capabilities =
	require(script.Parent.Parent.Parent.shared.v1beta1.client_capabilities)

type _HomePageRequestImpl = {
	__index: _HomePageRequestImpl,
	new: (fields: _HomePageRequestPartialFields?) -> HomePageRequest,
	encode: (self: HomePageRequest) -> buffer,
	decode: (input: buffer) -> HomePageRequest,
	jsonEncode: (self: HomePageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomePageRequest,
	descriptor: proto.Descriptor,
}

type _HomePageRequestFields = {
	session_id: string,
	topic_ids: { string },
	contextual_features: { [string]: string },
	cached_roblox_component_to_template_id: { [string]: string },
	client_capabilities: _roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities?,
	collection_layout: HomeCollectionLayoutInfo?,
}

type _HomePageRequestPartialFields = {
	session_id: string?,
	topic_ids: { string }?,
	contextual_features: { [string]: string }?,
	cached_roblox_component_to_template_id: { [string]: string }?,
	client_capabilities: _roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities?,
	collection_layout: HomeCollectionLayoutInfo?,
}

export type HomePageRequest = typeof(setmetatable({} :: _HomePageRequestFields, {} :: _HomePageRequestImpl))
type _HomePageRequestMessage = proto.Message<HomePageRequest, _HomePageRequestPartialFields>

type _HomePageRequest_ContextualFeaturesEntryImpl = {
	__index: _HomePageRequest_ContextualFeaturesEntryImpl,
	new: (fields: _HomePageRequest_ContextualFeaturesEntryPartialFields?) -> HomePageRequest_ContextualFeaturesEntry,
	encode: (self: HomePageRequest_ContextualFeaturesEntry) -> buffer,
	decode: (input: buffer) -> HomePageRequest_ContextualFeaturesEntry,
	jsonEncode: (self: HomePageRequest_ContextualFeaturesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomePageRequest_ContextualFeaturesEntry,
	descriptor: proto.Descriptor,
}

type _HomePageRequest_ContextualFeaturesEntryFields = {
	key: string,
	value: string,
}

type _HomePageRequest_ContextualFeaturesEntryPartialFields = {
	key: string?,
	value: string?,
}

export type HomePageRequest_ContextualFeaturesEntry = typeof(setmetatable(
	{} :: _HomePageRequest_ContextualFeaturesEntryFields,
	{} :: _HomePageRequest_ContextualFeaturesEntryImpl
))
type _HomePageRequest_ContextualFeaturesEntryMessage = proto.Message<
	HomePageRequest_ContextualFeaturesEntry,
	_HomePageRequest_ContextualFeaturesEntryPartialFields
>

type _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl = {
	__index: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl,
	new: (
		fields: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryPartialFields?
	) -> HomePageRequest_CachedRobloxComponentToTemplateIdEntry,
	encode: (self: HomePageRequest_CachedRobloxComponentToTemplateIdEntry) -> buffer,
	decode: (input: buffer) -> HomePageRequest_CachedRobloxComponentToTemplateIdEntry,
	jsonEncode: (self: HomePageRequest_CachedRobloxComponentToTemplateIdEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomePageRequest_CachedRobloxComponentToTemplateIdEntry,
	descriptor: proto.Descriptor,
}

type _HomePageRequest_CachedRobloxComponentToTemplateIdEntryFields = {
	key: string,
	value: string,
}

type _HomePageRequest_CachedRobloxComponentToTemplateIdEntryPartialFields = {
	key: string?,
	value: string?,
}

export type HomePageRequest_CachedRobloxComponentToTemplateIdEntry = typeof(setmetatable(
	{} :: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryFields,
	{} :: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl
))
type _HomePageRequest_CachedRobloxComponentToTemplateIdEntryMessage = proto.Message<
	HomePageRequest_CachedRobloxComponentToTemplateIdEntry,
	_HomePageRequest_CachedRobloxComponentToTemplateIdEntryPartialFields
>

type _HomeCollectionLayoutInfoImpl = {
	__index: _HomeCollectionLayoutInfoImpl,
	new: (fields: _HomeCollectionLayoutInfoPartialFields?) -> HomeCollectionLayoutInfo,
	encode: (self: HomeCollectionLayoutInfo) -> buffer,
	decode: (input: buffer) -> HomeCollectionLayoutInfo,
	jsonEncode: (self: HomeCollectionLayoutInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomeCollectionLayoutInfo,
	descriptor: proto.Descriptor,
}

type _HomeCollectionLayoutInfoFields = {
	items_per_row: { [string]: number },
	rows_above_fold: number,
}

type _HomeCollectionLayoutInfoPartialFields = {
	items_per_row: { [string]: number }?,
	rows_above_fold: number?,
}

export type HomeCollectionLayoutInfo = typeof(setmetatable(
	{} :: _HomeCollectionLayoutInfoFields,
	{} :: _HomeCollectionLayoutInfoImpl
))
type _HomeCollectionLayoutInfoMessage = proto.Message<HomeCollectionLayoutInfo, _HomeCollectionLayoutInfoPartialFields>

type _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl = {
	__index: _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl,
	new: (
		fields: _HomeCollectionLayoutInfo_ItemsPerRowEntryPartialFields?
	) -> HomeCollectionLayoutInfo_ItemsPerRowEntry,
	encode: (self: HomeCollectionLayoutInfo_ItemsPerRowEntry) -> buffer,
	decode: (input: buffer) -> HomeCollectionLayoutInfo_ItemsPerRowEntry,
	jsonEncode: (self: HomeCollectionLayoutInfo_ItemsPerRowEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomeCollectionLayoutInfo_ItemsPerRowEntry,
	descriptor: proto.Descriptor,
}

type _HomeCollectionLayoutInfo_ItemsPerRowEntryFields = {
	key: string,
	value: number,
}

type _HomeCollectionLayoutInfo_ItemsPerRowEntryPartialFields = {
	key: string?,
	value: number?,
}

export type HomeCollectionLayoutInfo_ItemsPerRowEntry = typeof(setmetatable(
	{} :: _HomeCollectionLayoutInfo_ItemsPerRowEntryFields,
	{} :: _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl
))
type _HomeCollectionLayoutInfo_ItemsPerRowEntryMessage = proto.Message<
	HomeCollectionLayoutInfo_ItemsPerRowEntry,
	_HomeCollectionLayoutInfo_ItemsPerRowEntryPartialFields
>

type _HomePageResponseImpl = {
	__index: _HomePageResponseImpl,
	new: (fields: _HomePageResponsePartialFields?) -> HomePageResponse,
	encode: (self: HomePageResponse) -> buffer,
	decode: (input: buffer) -> HomePageResponse,
	jsonEncode: (self: HomePageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomePageResponse,
	descriptor: proto.Descriptor,
}

type _HomePageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
	request_id: string,
}

type _HomePageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
	request_id: string?,
}

export type HomePageResponse = typeof(setmetatable({} :: _HomePageResponseFields, {} :: _HomePageResponseImpl))
type _HomePageResponseMessage = proto.Message<HomePageResponse, _HomePageResponsePartialFields>

type _HomePageResponse_TemplatesEntryImpl = {
	__index: _HomePageResponse_TemplatesEntryImpl,
	new: (fields: _HomePageResponse_TemplatesEntryPartialFields?) -> HomePageResponse_TemplatesEntry,
	encode: (self: HomePageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> HomePageResponse_TemplatesEntry,
	jsonEncode: (self: HomePageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomePageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _HomePageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _HomePageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type HomePageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _HomePageResponse_TemplatesEntryFields,
	{} :: _HomePageResponse_TemplatesEntryImpl
))
type _HomePageResponse_TemplatesEntryMessage = proto.Message<
	HomePageResponse_TemplatesEntry,
	_HomePageResponse_TemplatesEntryPartialFields
>

type _HomePageResponse_LocalizedLiteralsEntryImpl = {
	__index: _HomePageResponse_LocalizedLiteralsEntryImpl,
	new: (fields: _HomePageResponse_LocalizedLiteralsEntryPartialFields?) -> HomePageResponse_LocalizedLiteralsEntry,
	encode: (self: HomePageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> HomePageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: HomePageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> HomePageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _HomePageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _HomePageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type HomePageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _HomePageResponse_LocalizedLiteralsEntryFields,
	{} :: _HomePageResponse_LocalizedLiteralsEntryImpl
))
type _HomePageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	HomePageResponse_LocalizedLiteralsEntry,
	_HomePageResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _HomePageRequestImpl = {}
	_HomePageRequestImpl.__index = _HomePageRequestImpl

	function _HomePageRequestImpl.new(data: _HomePageRequestPartialFields?): HomePageRequest
		return setmetatable({
			session_id = if data == nil or data.session_id == nil then "" else data.session_id,
			topic_ids = if data == nil or data.topic_ids == nil then {} else data.topic_ids,
			contextual_features = if data == nil or data.contextual_features == nil
				then {}
				else data.contextual_features,
			cached_roblox_component_to_template_id = if data == nil
					or data.cached_roblox_component_to_template_id == nil
				then {}
				else data.cached_roblox_component_to_template_id,
			client_capabilities = if data == nil or data.client_capabilities == nil
				then nil
				else data.client_capabilities,
			collection_layout = if data == nil or data.collection_layout == nil then nil else data.collection_layout,
		}, _HomePageRequestImpl :: _HomePageRequestImpl)
	end

	function _HomePageRequestImpl.encode(self: HomePageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.session_id ~= nil and self.session_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.session_id)
		end

		if self.topic_ids ~= nil and #self.topic_ids > 0 then
			for _, value in self.topic_ids do
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeString(output, cursor, value)
			end
		end

		if self.contextual_features ~= nil and next(self.contextual_features) ~= nil then
			for key, value in self.contextual_features do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if
			self.cached_roblox_component_to_template_id ~= nil
			and next(self.cached_roblox_component_to_template_id) ~= nil
		then
			for key, value in self.cached_roblox_component_to_template_id do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.client_capabilities ~= nil then
			local encoded = self.client_capabilities:encode()
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.collection_layout ~= nil then
			local encoded = self.collection_layout:encode()
			output, cursor = proto.writeTag(output, cursor, 6, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomePageRequestImpl.decode(input: buffer): HomePageRequest
		local self = _HomePageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.session_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.topic_ids, buffer.tostring(value))
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.HomePageRequest_ContextualFeaturesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.contextual_features[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.HomePageRequest_CachedRobloxComponentToTemplateIdEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.cached_roblox_component_to_template_id[mapEntry.key or keyDefault] = mapEntry.value
						or valueDefault

					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.client_capabilities =
						_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.decode(value)
					continue
				elseif field == 6 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.collection_layout = messages.HomeCollectionLayoutInfo.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomePageRequestImpl.jsonEncode(self: HomePageRequest): any
		local output = {}

		if self.session_id ~= nil and self.session_id ~= "" then
			output.sessionId = self.session_id
		end

		if self.topic_ids ~= nil and #self.topic_ids > 0 then
			local newOutput = {}
			for _, value in self.topic_ids do
				table.insert(newOutput, value)
			end
			output.topicIds = newOutput
		end

		if self.contextual_features ~= nil and next(self.contextual_features) ~= nil then
			local newOutput = {}
			for key, value in self.contextual_features do
				newOutput[key] = value
			end
			output.contextualFeatures = newOutput
		end

		if
			self.cached_roblox_component_to_template_id ~= nil
			and next(self.cached_roblox_component_to_template_id) ~= nil
		then
			local newOutput = {}
			for key, value in self.cached_roblox_component_to_template_id do
				newOutput[key] = value
			end
			output.cachedRobloxComponentToTemplateId = newOutput
		end

		if self.client_capabilities ~= nil then
			output.clientCapabilities = self.client_capabilities:jsonEncode()
		end

		if self.collection_layout ~= nil then
			output.collectionLayout = self.collection_layout:jsonEncode()
		end

		return output
	end

	function _HomePageRequestImpl.jsonDecode(input: { [string]: any }): HomePageRequest
		local self = _HomePageRequestImpl.new()

		if input.session_id ~= nil then
			self.session_id = input.session_id
		end

		if input.sessionId ~= nil then
			self.session_id = input.sessionId
		end

		if input.topic_ids ~= nil then
			local newOutput: { string } = {}
			for _, value in input.topic_ids do
				table.insert(newOutput, value)
			end

			self.topic_ids = newOutput
		end

		if input.topicIds ~= nil then
			local newOutput: { string } = {}
			for _, value in input.topicIds do
				table.insert(newOutput, value)
			end

			self.topic_ids = newOutput
		end

		if input.contextual_features ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.contextual_features do
				newOutput[key] = value
			end

			self.contextual_features = newOutput
		end

		if input.contextualFeatures ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.contextualFeatures do
				newOutput[key] = value
			end

			self.contextual_features = newOutput
		end

		if input.cached_roblox_component_to_template_id ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.cached_roblox_component_to_template_id do
				newOutput[key] = value
			end

			self.cached_roblox_component_to_template_id = newOutput
		end

		if input.cachedRobloxComponentToTemplateId ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.cachedRobloxComponentToTemplateId do
				newOutput[key] = value
			end

			self.cached_roblox_component_to_template_id = newOutput
		end

		if input.client_capabilities ~= nil then
			self.client_capabilities =
				_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.jsonDecode(
					input.client_capabilities
				)
		end

		if input.clientCapabilities ~= nil then
			self.client_capabilities =
				_roblox_apppageplatform_shared_v1beta1_client_capabilities.ClientCapabilities.jsonDecode(
					input.clientCapabilities
				)
		end

		if input.collection_layout ~= nil then
			self.collection_layout = messages.HomeCollectionLayoutInfo.jsonDecode(input.collection_layout)
		end

		if input.collectionLayout ~= nil then
			self.collection_layout = messages.HomeCollectionLayoutInfo.jsonDecode(input.collectionLayout)
		end

		return self
	end

	_HomePageRequestImpl.descriptor = {
		name = "HomePageRequest",
		fullName = "roblox.apppageplatform.home.v1beta1.HomePageRequest",
	}

	messages.HomePageRequest = _HomePageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomePageRequest)
end

do
	local _HomePageRequest_ContextualFeaturesEntryImpl = {}
	_HomePageRequest_ContextualFeaturesEntryImpl.__index = _HomePageRequest_ContextualFeaturesEntryImpl

	function _HomePageRequest_ContextualFeaturesEntryImpl.new(
		data: _HomePageRequest_ContextualFeaturesEntryPartialFields?
	): HomePageRequest_ContextualFeaturesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then "" else data.value,
		}, _HomePageRequest_ContextualFeaturesEntryImpl :: _HomePageRequest_ContextualFeaturesEntryImpl)
	end

	function _HomePageRequest_ContextualFeaturesEntryImpl.encode(self: HomePageRequest_ContextualFeaturesEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomePageRequest_ContextualFeaturesEntryImpl.decode(input: buffer): HomePageRequest_ContextualFeaturesEntry
		local self = _HomePageRequest_ContextualFeaturesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomePageRequest_ContextualFeaturesEntryImpl.jsonEncode(self: HomePageRequest_ContextualFeaturesEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _HomePageRequest_ContextualFeaturesEntryImpl.jsonDecode(
		input: { [string]: any }
	): HomePageRequest_ContextualFeaturesEntry
		local self = _HomePageRequest_ContextualFeaturesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_HomePageRequest_ContextualFeaturesEntryImpl.descriptor = {
		name = "HomePageRequest_ContextualFeaturesEntry",
		fullName = "roblox.apppageplatform.home.v1beta1.ContextualFeaturesEntry",
	}

	messages.HomePageRequest_ContextualFeaturesEntry = _HomePageRequest_ContextualFeaturesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomePageRequest_ContextualFeaturesEntry)
end

do
	local _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl = {}
	_HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.__index =
		_HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl

	function _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.new(
		data: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryPartialFields?
	): HomePageRequest_CachedRobloxComponentToTemplateIdEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl :: _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl
		)
	end

	function _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.encode(
		self: HomePageRequest_CachedRobloxComponentToTemplateIdEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.decode(
		input: buffer
	): HomePageRequest_CachedRobloxComponentToTemplateIdEntry
		local self = _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.jsonEncode(
		self: HomePageRequest_CachedRobloxComponentToTemplateIdEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.jsonDecode(
		input: { [string]: any }
	): HomePageRequest_CachedRobloxComponentToTemplateIdEntry
		local self = _HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl.descriptor = {
		name = "HomePageRequest_CachedRobloxComponentToTemplateIdEntry",
		fullName = "roblox.apppageplatform.home.v1beta1.CachedRobloxComponentToTemplateIdEntry",
	}

	messages.HomePageRequest_CachedRobloxComponentToTemplateIdEntry =
		_HomePageRequest_CachedRobloxComponentToTemplateIdEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomePageRequest_CachedRobloxComponentToTemplateIdEntry)
end

do
	local _HomeCollectionLayoutInfoImpl = {}
	_HomeCollectionLayoutInfoImpl.__index = _HomeCollectionLayoutInfoImpl

	function _HomeCollectionLayoutInfoImpl.new(data: _HomeCollectionLayoutInfoPartialFields?): HomeCollectionLayoutInfo
		return setmetatable({
			items_per_row = if data == nil or data.items_per_row == nil then {} else data.items_per_row,
			rows_above_fold = if data == nil or data.rows_above_fold == nil then 0 else data.rows_above_fold,
		}, _HomeCollectionLayoutInfoImpl :: _HomeCollectionLayoutInfoImpl)
	end

	function _HomeCollectionLayoutInfoImpl.encode(self: HomeCollectionLayoutInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.items_per_row ~= nil and next(self.items_per_row) ~= nil then
			for key, value in self.items_per_row do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.varint)
				mapBuffer, mapCursor = proto.writeVarInt(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.rows_above_fold ~= nil and self.rows_above_fold ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.rows_above_fold)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomeCollectionLayoutInfoImpl.decode(input: buffer): HomeCollectionLayoutInfo
		local self = _HomeCollectionLayoutInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.rows_above_fold = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.HomeCollectionLayoutInfo_ItemsPerRowEntry.decode(value)

					local keyDefault = ""
					local valueDefault = 0

					self.items_per_row[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomeCollectionLayoutInfoImpl.jsonEncode(self: HomeCollectionLayoutInfo): any
		local output = {}

		if self.items_per_row ~= nil and next(self.items_per_row) ~= nil then
			local newOutput = {}
			for key, value in self.items_per_row do
				newOutput[key] = value
			end
			output.itemsPerRow = newOutput
		end

		if self.rows_above_fold ~= nil and self.rows_above_fold ~= 0 then
			output.rowsAboveFold = self.rows_above_fold
		end

		return output
	end

	function _HomeCollectionLayoutInfoImpl.jsonDecode(input: { [string]: any }): HomeCollectionLayoutInfo
		local self = _HomeCollectionLayoutInfoImpl.new()

		if input.items_per_row ~= nil then
			local newOutput: { [string]: number } = {}
			for key, value in input.items_per_row do
				newOutput[key] = value
			end

			self.items_per_row = newOutput
		end

		if input.itemsPerRow ~= nil then
			local newOutput: { [string]: number } = {}
			for key, value in input.itemsPerRow do
				newOutput[key] = value
			end

			self.items_per_row = newOutput
		end

		if input.rows_above_fold ~= nil then
			self.rows_above_fold = input.rows_above_fold
		end

		if input.rowsAboveFold ~= nil then
			self.rows_above_fold = input.rowsAboveFold
		end

		return self
	end

	_HomeCollectionLayoutInfoImpl.descriptor = {
		name = "HomeCollectionLayoutInfo",
		fullName = "roblox.apppageplatform.home.v1beta1.HomeCollectionLayoutInfo",
	}

	messages.HomeCollectionLayoutInfo = _HomeCollectionLayoutInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomeCollectionLayoutInfo)
end

do
	local _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl = {}
	_HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.__index = _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl

	function _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.new(
		data: _HomeCollectionLayoutInfo_ItemsPerRowEntryPartialFields?
	): HomeCollectionLayoutInfo_ItemsPerRowEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then 0 else data.value,
		}, _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl :: _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl)
	end

	function _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.encode(
		self: HomeCollectionLayoutInfo_ItemsPerRowEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.decode(
		input: buffer
	): HomeCollectionLayoutInfo_ItemsPerRowEntry
		local self = _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.value = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.jsonEncode(
		self: HomeCollectionLayoutInfo_ItemsPerRowEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= 0 then
			output.value = self.value
		end

		return output
	end

	function _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.jsonDecode(
		input: { [string]: any }
	): HomeCollectionLayoutInfo_ItemsPerRowEntry
		local self = _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_HomeCollectionLayoutInfo_ItemsPerRowEntryImpl.descriptor = {
		name = "HomeCollectionLayoutInfo_ItemsPerRowEntry",
		fullName = "roblox.apppageplatform.home.v1beta1.ItemsPerRowEntry",
	}

	messages.HomeCollectionLayoutInfo_ItemsPerRowEntry = _HomeCollectionLayoutInfo_ItemsPerRowEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomeCollectionLayoutInfo_ItemsPerRowEntry)
end

do
	local _HomePageResponseImpl = {}
	_HomePageResponseImpl.__index = _HomePageResponseImpl

	function _HomePageResponseImpl.new(data: _HomePageResponsePartialFields?): HomePageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
			request_id = if data == nil or data.request_id == nil then "" else data.request_id,
		}, _HomePageResponseImpl :: _HomePageResponseImpl)
	end

	function _HomePageResponseImpl.encode(self: HomePageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.request_id ~= nil and self.request_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.request_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomePageResponseImpl.decode(input: buffer): HomePageResponse
		local self = _HomePageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.HomePageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.HomePageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 5 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.request_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomePageResponseImpl.jsonEncode(self: HomePageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		if self.request_id ~= nil and self.request_id ~= "" then
			output.requestId = self.request_id
		end

		return output
	end

	function _HomePageResponseImpl.jsonDecode(input: { [string]: any }): HomePageResponse
		local self = _HomePageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.request_id ~= nil then
			self.request_id = input.request_id
		end

		if input.requestId ~= nil then
			self.request_id = input.requestId
		end

		return self
	end

	_HomePageResponseImpl.descriptor = {
		name = "HomePageResponse",
		fullName = "roblox.apppageplatform.home.v1beta1.HomePageResponse",
	}

	messages.HomePageResponse = _HomePageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomePageResponse)
end

do
	local _HomePageResponse_TemplatesEntryImpl = {}
	_HomePageResponse_TemplatesEntryImpl.__index = _HomePageResponse_TemplatesEntryImpl

	function _HomePageResponse_TemplatesEntryImpl.new(
		data: _HomePageResponse_TemplatesEntryPartialFields?
	): HomePageResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _HomePageResponse_TemplatesEntryImpl :: _HomePageResponse_TemplatesEntryImpl)
	end

	function _HomePageResponse_TemplatesEntryImpl.encode(self: HomePageResponse_TemplatesEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomePageResponse_TemplatesEntryImpl.decode(input: buffer): HomePageResponse_TemplatesEntry
		local self = _HomePageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomePageResponse_TemplatesEntryImpl.jsonEncode(self: HomePageResponse_TemplatesEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _HomePageResponse_TemplatesEntryImpl.jsonDecode(input: { [string]: any }): HomePageResponse_TemplatesEntry
		local self = _HomePageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_HomePageResponse_TemplatesEntryImpl.descriptor = {
		name = "HomePageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.home.v1beta1.TemplatesEntry",
	}

	messages.HomePageResponse_TemplatesEntry = _HomePageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomePageResponse_TemplatesEntry)
end

do
	local _HomePageResponse_LocalizedLiteralsEntryImpl = {}
	_HomePageResponse_LocalizedLiteralsEntryImpl.__index = _HomePageResponse_LocalizedLiteralsEntryImpl

	function _HomePageResponse_LocalizedLiteralsEntryImpl.new(
		data: _HomePageResponse_LocalizedLiteralsEntryPartialFields?
	): HomePageResponse_LocalizedLiteralsEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then "" else data.value,
		}, _HomePageResponse_LocalizedLiteralsEntryImpl :: _HomePageResponse_LocalizedLiteralsEntryImpl)
	end

	function _HomePageResponse_LocalizedLiteralsEntryImpl.encode(self: HomePageResponse_LocalizedLiteralsEntry): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _HomePageResponse_LocalizedLiteralsEntryImpl.decode(input: buffer): HomePageResponse_LocalizedLiteralsEntry
		local self = _HomePageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _HomePageResponse_LocalizedLiteralsEntryImpl.jsonEncode(self: HomePageResponse_LocalizedLiteralsEntry): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _HomePageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): HomePageResponse_LocalizedLiteralsEntry
		local self = _HomePageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_HomePageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "HomePageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.home.v1beta1.LocalizedLiteralsEntry",
	}

	messages.HomePageResponse_LocalizedLiteralsEntry = _HomePageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.HomePageResponse_LocalizedLiteralsEntry)
end

return {
	HomePageRequest = messages.HomePageRequest,
	HomeCollectionLayoutInfo = messages.HomeCollectionLayoutInfo,
	HomePageResponse = messages.HomePageResponse,
}
