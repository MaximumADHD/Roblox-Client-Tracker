--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	GetSearchResultsPageRequest: _GetSearchResultsPageRequestMessage,
	GetSearchResultsPageResponse: _GetSearchResultsPageResponseMessage,
	GetSearchResultsPageResponse_TemplatesEntry: _GetSearchResultsPageResponse_TemplatesEntryMessage,
	GetSearchResultsPageResponse_LocalizedLiteralsEntry: _GetSearchResultsPageResponse_LocalizedLiteralsEntryMessage,
	SearchResultsPageVerticalType: _SearchResultsPageVerticalTypeMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)

type _GetSearchResultsPageRequestImpl = {
	__index: _GetSearchResultsPageRequestImpl,
	new: (fields: _GetSearchResultsPageRequestPartialFields?) -> GetSearchResultsPageRequest,
	encode: (self: GetSearchResultsPageRequest) -> buffer,
	decode: (input: buffer) -> GetSearchResultsPageRequest,
	jsonEncode: (self: GetSearchResultsPageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetSearchResultsPageRequest,
	descriptor: proto.Descriptor,
}

type _GetSearchResultsPageRequestFields = {
	search_query: string,
	session_id: string,
	vertical_type: SearchResultsPageVerticalType,
	global_session_id: string,
}

type _GetSearchResultsPageRequestPartialFields = {
	search_query: string?,
	session_id: string?,
	vertical_type: SearchResultsPageVerticalType?,
	global_session_id: string?,
}

export type GetSearchResultsPageRequest = typeof(setmetatable(
	{} :: _GetSearchResultsPageRequestFields,
	{} :: _GetSearchResultsPageRequestImpl
))
type _GetSearchResultsPageRequestMessage = proto.Message<
	GetSearchResultsPageRequest,
	_GetSearchResultsPageRequestPartialFields
>

type _GetSearchResultsPageResponseImpl = {
	__index: _GetSearchResultsPageResponseImpl,
	new: (fields: _GetSearchResultsPageResponsePartialFields?) -> GetSearchResultsPageResponse,
	encode: (self: GetSearchResultsPageResponse) -> buffer,
	decode: (input: buffer) -> GetSearchResultsPageResponse,
	jsonEncode: (self: GetSearchResultsPageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetSearchResultsPageResponse,
	descriptor: proto.Descriptor,
}

type _GetSearchResultsPageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
}

type _GetSearchResultsPageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
}

export type GetSearchResultsPageResponse = typeof(setmetatable(
	{} :: _GetSearchResultsPageResponseFields,
	{} :: _GetSearchResultsPageResponseImpl
))
type _GetSearchResultsPageResponseMessage = proto.Message<
	GetSearchResultsPageResponse,
	_GetSearchResultsPageResponsePartialFields
>

type _GetSearchResultsPageResponse_TemplatesEntryImpl = {
	__index: _GetSearchResultsPageResponse_TemplatesEntryImpl,
	new: (
		fields: _GetSearchResultsPageResponse_TemplatesEntryPartialFields?
	) -> GetSearchResultsPageResponse_TemplatesEntry,
	encode: (self: GetSearchResultsPageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> GetSearchResultsPageResponse_TemplatesEntry,
	jsonEncode: (self: GetSearchResultsPageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetSearchResultsPageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _GetSearchResultsPageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _GetSearchResultsPageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type GetSearchResultsPageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _GetSearchResultsPageResponse_TemplatesEntryFields,
	{} :: _GetSearchResultsPageResponse_TemplatesEntryImpl
))
type _GetSearchResultsPageResponse_TemplatesEntryMessage = proto.Message<
	GetSearchResultsPageResponse_TemplatesEntry,
	_GetSearchResultsPageResponse_TemplatesEntryPartialFields
>

type _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl = {
	__index: _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _GetSearchResultsPageResponse_LocalizedLiteralsEntryPartialFields?
	) -> GetSearchResultsPageResponse_LocalizedLiteralsEntry,
	encode: (self: GetSearchResultsPageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> GetSearchResultsPageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: GetSearchResultsPageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> GetSearchResultsPageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _GetSearchResultsPageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _GetSearchResultsPageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type GetSearchResultsPageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _GetSearchResultsPageResponse_LocalizedLiteralsEntryFields,
	{} :: _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl
))
type _GetSearchResultsPageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	GetSearchResultsPageResponse_LocalizedLiteralsEntry,
	_GetSearchResultsPageResponse_LocalizedLiteralsEntryPartialFields
>

type _SearchResultsPageVerticalTypeMessage = proto.Enum<SearchResultsPageVerticalType>
export type SearchResultsPageVerticalType =
	"SEARCH_RESULTS_PAGE_VERTICAL_TYPE_INVALID"
	| "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_BLENDED"
	| "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_GAME"
	| "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_USER"
	| number -- Unknown

do
	local _GetSearchResultsPageRequestImpl = {}
	_GetSearchResultsPageRequestImpl.__index = _GetSearchResultsPageRequestImpl

	function _GetSearchResultsPageRequestImpl.new(
		data: _GetSearchResultsPageRequestPartialFields?
	): GetSearchResultsPageRequest
		return setmetatable({
			search_query = if data == nil or data.search_query == nil then "" else data.search_query,
			session_id = if data == nil or data.session_id == nil then "" else data.session_id,
			vertical_type = if data == nil or data.vertical_type == nil
				then assert(messages.SearchResultsPageVerticalType.fromNumber(0), "Enum has no 0 default")
				else data.vertical_type,
			global_session_id = if data == nil or data.global_session_id == nil then "" else data.global_session_id,
		}, _GetSearchResultsPageRequestImpl :: _GetSearchResultsPageRequestImpl)
	end

	function _GetSearchResultsPageRequestImpl.encode(self: GetSearchResultsPageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.search_query ~= nil and self.search_query ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.search_query)
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.session_id)
		end

		if
			self.vertical_type ~= nil
			and (
				self.vertical_type ~= nil and self.vertical_type ~= 0
				or self.vertical_type ~= messages.SearchResultsPageVerticalType.fromNumber(0)
			)
		then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(
				output,
				cursor,
				messages.SearchResultsPageVerticalType.toNumber(self.vertical_type :: any)
			)
		end

		if self.global_session_id ~= nil and self.global_session_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.global_session_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetSearchResultsPageRequestImpl.decode(input: buffer): GetSearchResultsPageRequest
		local self = _GetSearchResultsPageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.vertical_type = (messages.SearchResultsPageVerticalType.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.search_query = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.session_id = buffer.tostring(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.global_session_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetSearchResultsPageRequestImpl.jsonEncode(self: GetSearchResultsPageRequest): any
		local output = {}

		if self.search_query ~= nil and self.search_query ~= "" then
			output.searchQuery = self.search_query
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output.sessionId = self.session_id
		end

		if
			self.vertical_type ~= nil
			and (
				self.vertical_type ~= nil and self.vertical_type ~= 0
				or self.vertical_type ~= messages.SearchResultsPageVerticalType.fromNumber(0)
			)
		then
			output.verticalType = if typeof(self.vertical_type) == "number"
				then self.vertical_type
				else messages.SearchResultsPageVerticalType.toNumber(self.vertical_type :: any)
		end

		if self.global_session_id ~= nil and self.global_session_id ~= "" then
			output.globalSessionId = self.global_session_id
		end

		return output
	end

	function _GetSearchResultsPageRequestImpl.jsonDecode(input: { [string]: any }): GetSearchResultsPageRequest
		local self = _GetSearchResultsPageRequestImpl.new()

		if input.search_query ~= nil then
			self.search_query = input.search_query
		end

		if input.searchQuery ~= nil then
			self.search_query = input.searchQuery
		end

		if input.session_id ~= nil then
			self.session_id = input.session_id
		end

		if input.sessionId ~= nil then
			self.session_id = input.sessionId
		end

		if input.vertical_type ~= nil then
			self.vertical_type = if typeof(input.vertical_type) == "number"
				then (messages.SearchResultsPageVerticalType.fromNumber(input.vertical_type) or input.vertical_type)
				else (messages.SearchResultsPageVerticalType.fromName(input.vertical_type) or input.vertical_type)
		end

		if input.verticalType ~= nil then
			self.vertical_type = if typeof(input.verticalType) == "number"
				then (messages.SearchResultsPageVerticalType.fromNumber(input.verticalType) or input.verticalType)
				else (messages.SearchResultsPageVerticalType.fromName(input.verticalType) or input.verticalType)
		end

		if input.global_session_id ~= nil then
			self.global_session_id = input.global_session_id
		end

		if input.globalSessionId ~= nil then
			self.global_session_id = input.globalSessionId
		end

		return self
	end

	_GetSearchResultsPageRequestImpl.descriptor = {
		name = "GetSearchResultsPageRequest",
		fullName = "roblox.apppageplatform.search.v1beta1.GetSearchResultsPageRequest",
	}

	messages.GetSearchResultsPageRequest = _GetSearchResultsPageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetSearchResultsPageRequest)
end

do
	local _GetSearchResultsPageResponseImpl = {}
	_GetSearchResultsPageResponseImpl.__index = _GetSearchResultsPageResponseImpl

	function _GetSearchResultsPageResponseImpl.new(
		data: _GetSearchResultsPageResponsePartialFields?
	): GetSearchResultsPageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _GetSearchResultsPageResponseImpl :: _GetSearchResultsPageResponseImpl)
	end

	function _GetSearchResultsPageResponseImpl.encode(self: GetSearchResultsPageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetSearchResultsPageResponseImpl.decode(input: buffer): GetSearchResultsPageResponse
		local self = _GetSearchResultsPageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetSearchResultsPageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.GetSearchResultsPageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetSearchResultsPageResponseImpl.jsonEncode(self: GetSearchResultsPageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _GetSearchResultsPageResponseImpl.jsonDecode(input: { [string]: any }): GetSearchResultsPageResponse
		local self = _GetSearchResultsPageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_GetSearchResultsPageResponseImpl.descriptor = {
		name = "GetSearchResultsPageResponse",
		fullName = "roblox.apppageplatform.search.v1beta1.GetSearchResultsPageResponse",
	}

	messages.GetSearchResultsPageResponse = _GetSearchResultsPageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetSearchResultsPageResponse)
end

do
	local _GetSearchResultsPageResponse_TemplatesEntryImpl = {}
	_GetSearchResultsPageResponse_TemplatesEntryImpl.__index = _GetSearchResultsPageResponse_TemplatesEntryImpl

	function _GetSearchResultsPageResponse_TemplatesEntryImpl.new(
		data: _GetSearchResultsPageResponse_TemplatesEntryPartialFields?
	): GetSearchResultsPageResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _GetSearchResultsPageResponse_TemplatesEntryImpl :: _GetSearchResultsPageResponse_TemplatesEntryImpl)
	end

	function _GetSearchResultsPageResponse_TemplatesEntryImpl.encode(
		self: GetSearchResultsPageResponse_TemplatesEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetSearchResultsPageResponse_TemplatesEntryImpl.decode(
		input: buffer
	): GetSearchResultsPageResponse_TemplatesEntry
		local self = _GetSearchResultsPageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetSearchResultsPageResponse_TemplatesEntryImpl.jsonEncode(
		self: GetSearchResultsPageResponse_TemplatesEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _GetSearchResultsPageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetSearchResultsPageResponse_TemplatesEntry
		local self = _GetSearchResultsPageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_GetSearchResultsPageResponse_TemplatesEntryImpl.descriptor = {
		name = "GetSearchResultsPageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.search.v1beta1.TemplatesEntry",
	}

	messages.GetSearchResultsPageResponse_TemplatesEntry = _GetSearchResultsPageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetSearchResultsPageResponse_TemplatesEntry)
end

do
	local _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl = {}
	_GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.__index =
		_GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl

	function _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.new(
		data: _GetSearchResultsPageResponse_LocalizedLiteralsEntryPartialFields?
	): GetSearchResultsPageResponse_LocalizedLiteralsEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl :: _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl
		)
	end

	function _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.encode(
		self: GetSearchResultsPageResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): GetSearchResultsPageResponse_LocalizedLiteralsEntry
		local self = _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: GetSearchResultsPageResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): GetSearchResultsPageResponse_LocalizedLiteralsEntry
		local self = _GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "GetSearchResultsPageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.search.v1beta1.LocalizedLiteralsEntry",
	}

	messages.GetSearchResultsPageResponse_LocalizedLiteralsEntry =
		_GetSearchResultsPageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.GetSearchResultsPageResponse_LocalizedLiteralsEntry)
end

messages.SearchResultsPageVerticalType = {
	fromNumber = function(value: number): SearchResultsPageVerticalType?
		if value == 0 then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_INVALID"
		elseif value == 1 then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_BLENDED"
		elseif value == 2 then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_GAME"
		elseif value == 3 then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_USER"
		else
			return nil
		end
	end,

	toNumber = function(self: SearchResultsPageVerticalType): number
		if self == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_INVALID" then
			return 0
		elseif self == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_BLENDED" then
			return 1
		elseif self == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_GAME" then
			return 2
		elseif self == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_USER" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): SearchResultsPageVerticalType?
		if name == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_INVALID" then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_INVALID"
		elseif name == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_BLENDED" then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_BLENDED"
		elseif name == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_GAME" then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_GAME"
		elseif name == "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_USER" then
			return "SEARCH_RESULTS_PAGE_VERTICAL_TYPE_USER"
		else
			return nil
		end
	end,
}

return {
	GetSearchResultsPageRequest = messages.GetSearchResultsPageRequest,
	GetSearchResultsPageResponse = messages.GetSearchResultsPageResponse,
	SearchResultsPageVerticalType = messages.SearchResultsPageVerticalType,
}
