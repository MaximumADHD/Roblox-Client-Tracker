--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	SpotlightCollectionPageRequest: _SpotlightCollectionPageRequestMessage,
	SpotlightCollectionPageResponse: _SpotlightCollectionPageResponseMessage,
	SpotlightCollectionPageResponse_TemplatesEntry: _SpotlightCollectionPageResponse_TemplatesEntryMessage,
	SpotlightCollectionPageResponse_LocalizedLiteralsEntry: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryMessage,
}
local messages: _Messages = {} :: _Messages

local _roblox_apppageplatform_shared_v1beta1_hydration_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.hydration_content)
local _roblox_apppageplatform_shared_v1beta1_page_entry_content =
	require(script.Parent.Parent.Parent.shared.v1beta1.page_entry_content)
local _roblox_apppageplatform_shared_v1beta1_template_entry =
	require(script.Parent.Parent.Parent.shared.v1beta1.template_entry)

type _SpotlightCollectionPageRequestImpl = {
	__index: _SpotlightCollectionPageRequestImpl,
	new: (fields: _SpotlightCollectionPageRequestPartialFields?) -> SpotlightCollectionPageRequest,
	encode: (self: SpotlightCollectionPageRequest) -> buffer,
	decode: (input: buffer) -> SpotlightCollectionPageRequest,
	jsonEncode: (self: SpotlightCollectionPageRequest) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SpotlightCollectionPageRequest,
	descriptor: proto.Descriptor,
}

type _SpotlightCollectionPageRequestFields = {
	spotlight_collection_id: string,
	session_id: string,
}

type _SpotlightCollectionPageRequestPartialFields = {
	spotlight_collection_id: string?,
	session_id: string?,
}

export type SpotlightCollectionPageRequest = typeof(setmetatable(
	{} :: _SpotlightCollectionPageRequestFields,
	{} :: _SpotlightCollectionPageRequestImpl
))
type _SpotlightCollectionPageRequestMessage = proto.Message<
	SpotlightCollectionPageRequest,
	_SpotlightCollectionPageRequestPartialFields
>

type _SpotlightCollectionPageResponseImpl = {
	__index: _SpotlightCollectionPageResponseImpl,
	new: (fields: _SpotlightCollectionPageResponsePartialFields?) -> SpotlightCollectionPageResponse,
	encode: (self: SpotlightCollectionPageResponse) -> buffer,
	decode: (input: buffer) -> SpotlightCollectionPageResponse,
	jsonEncode: (self: SpotlightCollectionPageResponse) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SpotlightCollectionPageResponse,
	descriptor: proto.Descriptor,
}

type _SpotlightCollectionPageResponseFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry },
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry },
	localized_literals: { [string]: string },
}

type _SpotlightCollectionPageResponsePartialFields = {
	page_entries: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry }?,
	hydration_data: _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent?,
	templates: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry }?,
	localized_literals: { [string]: string }?,
}

export type SpotlightCollectionPageResponse = typeof(setmetatable(
	{} :: _SpotlightCollectionPageResponseFields,
	{} :: _SpotlightCollectionPageResponseImpl
))
type _SpotlightCollectionPageResponseMessage = proto.Message<
	SpotlightCollectionPageResponse,
	_SpotlightCollectionPageResponsePartialFields
>

type _SpotlightCollectionPageResponse_TemplatesEntryImpl = {
	__index: _SpotlightCollectionPageResponse_TemplatesEntryImpl,
	new: (
		fields: _SpotlightCollectionPageResponse_TemplatesEntryPartialFields?
	) -> SpotlightCollectionPageResponse_TemplatesEntry,
	encode: (self: SpotlightCollectionPageResponse_TemplatesEntry) -> buffer,
	decode: (input: buffer) -> SpotlightCollectionPageResponse_TemplatesEntry,
	jsonEncode: (self: SpotlightCollectionPageResponse_TemplatesEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SpotlightCollectionPageResponse_TemplatesEntry,
	descriptor: proto.Descriptor,
}

type _SpotlightCollectionPageResponse_TemplatesEntryFields = {
	key: string,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

type _SpotlightCollectionPageResponse_TemplatesEntryPartialFields = {
	key: string?,
	value: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry?,
}

export type SpotlightCollectionPageResponse_TemplatesEntry = typeof(setmetatable(
	{} :: _SpotlightCollectionPageResponse_TemplatesEntryFields,
	{} :: _SpotlightCollectionPageResponse_TemplatesEntryImpl
))
type _SpotlightCollectionPageResponse_TemplatesEntryMessage = proto.Message<
	SpotlightCollectionPageResponse_TemplatesEntry,
	_SpotlightCollectionPageResponse_TemplatesEntryPartialFields
>

type _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl = {
	__index: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl,
	new: (
		fields: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryPartialFields?
	) -> SpotlightCollectionPageResponse_LocalizedLiteralsEntry,
	encode: (self: SpotlightCollectionPageResponse_LocalizedLiteralsEntry) -> buffer,
	decode: (input: buffer) -> SpotlightCollectionPageResponse_LocalizedLiteralsEntry,
	jsonEncode: (self: SpotlightCollectionPageResponse_LocalizedLiteralsEntry) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SpotlightCollectionPageResponse_LocalizedLiteralsEntry,
	descriptor: proto.Descriptor,
}

type _SpotlightCollectionPageResponse_LocalizedLiteralsEntryFields = {
	key: string,
	value: string,
}

type _SpotlightCollectionPageResponse_LocalizedLiteralsEntryPartialFields = {
	key: string?,
	value: string?,
}

export type SpotlightCollectionPageResponse_LocalizedLiteralsEntry = typeof(setmetatable(
	{} :: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryFields,
	{} :: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl
))
type _SpotlightCollectionPageResponse_LocalizedLiteralsEntryMessage = proto.Message<
	SpotlightCollectionPageResponse_LocalizedLiteralsEntry,
	_SpotlightCollectionPageResponse_LocalizedLiteralsEntryPartialFields
>

do
	local _SpotlightCollectionPageRequestImpl = {}
	_SpotlightCollectionPageRequestImpl.__index = _SpotlightCollectionPageRequestImpl

	function _SpotlightCollectionPageRequestImpl.new(
		data: _SpotlightCollectionPageRequestPartialFields?
	): SpotlightCollectionPageRequest
		return setmetatable({
			spotlight_collection_id = if data == nil or data.spotlight_collection_id == nil
				then ""
				else data.spotlight_collection_id,
			session_id = if data == nil or data.session_id == nil then "" else data.session_id,
		}, _SpotlightCollectionPageRequestImpl :: _SpotlightCollectionPageRequestImpl)
	end

	function _SpotlightCollectionPageRequestImpl.encode(self: SpotlightCollectionPageRequest): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.spotlight_collection_id ~= nil and self.spotlight_collection_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.spotlight_collection_id)
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.session_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SpotlightCollectionPageRequestImpl.decode(input: buffer): SpotlightCollectionPageRequest
		local self = _SpotlightCollectionPageRequestImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.spotlight_collection_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.session_id = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SpotlightCollectionPageRequestImpl.jsonEncode(self: SpotlightCollectionPageRequest): any
		local output = {}

		if self.spotlight_collection_id ~= nil and self.spotlight_collection_id ~= "" then
			output.spotlightCollectionId = self.spotlight_collection_id
		end

		if self.session_id ~= nil and self.session_id ~= "" then
			output.sessionId = self.session_id
		end

		return output
	end

	function _SpotlightCollectionPageRequestImpl.jsonDecode(input: { [string]: any }): SpotlightCollectionPageRequest
		local self = _SpotlightCollectionPageRequestImpl.new()

		if input.spotlight_collection_id ~= nil then
			self.spotlight_collection_id = input.spotlight_collection_id
		end

		if input.spotlightCollectionId ~= nil then
			self.spotlight_collection_id = input.spotlightCollectionId
		end

		if input.session_id ~= nil then
			self.session_id = input.session_id
		end

		if input.sessionId ~= nil then
			self.session_id = input.sessionId
		end

		return self
	end

	_SpotlightCollectionPageRequestImpl.descriptor = {
		name = "SpotlightCollectionPageRequest",
		fullName = "roblox.apppageplatform.spotlightcollection.v1beta1.SpotlightCollectionPageRequest",
	}

	messages.SpotlightCollectionPageRequest = _SpotlightCollectionPageRequestImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SpotlightCollectionPageRequest)
end

do
	local _SpotlightCollectionPageResponseImpl = {}
	_SpotlightCollectionPageResponseImpl.__index = _SpotlightCollectionPageResponseImpl

	function _SpotlightCollectionPageResponseImpl.new(
		data: _SpotlightCollectionPageResponsePartialFields?
	): SpotlightCollectionPageResponse
		return setmetatable({
			page_entries = if data == nil or data.page_entries == nil then {} else data.page_entries,
			hydration_data = if data == nil or data.hydration_data == nil then nil else data.hydration_data,
			templates = if data == nil or data.templates == nil then {} else data.templates,
			localized_literals = if data == nil or data.localized_literals == nil then {} else data.localized_literals,
		}, _SpotlightCollectionPageResponseImpl :: _SpotlightCollectionPageResponseImpl)
	end

	function _SpotlightCollectionPageResponseImpl.encode(self: SpotlightCollectionPageResponse): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.page_entries ~= nil and #self.page_entries > 0 then
			for _, value in self.page_entries do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		if self.hydration_data ~= nil then
			local encoded = self.hydration_data:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			for key, value in self.templates do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				local encoded = value:encode()
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeBuffer(mapBuffer, mapCursor, encoded, buffer.len(encoded))
				output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			for key, value in self.localized_literals do
				local mapBuffer = buffer.create(0)
				local mapCursor = 0
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 1, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, key)
				mapBuffer, mapCursor = proto.writeTag(mapBuffer, mapCursor, 2, proto.wireTypes.lengthDelimited)
				mapBuffer, mapCursor = proto.writeString(mapBuffer, mapCursor, value)
				output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, mapBuffer, mapCursor)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SpotlightCollectionPageResponseImpl.decode(input: buffer): SpotlightCollectionPageResponse
		local self = _SpotlightCollectionPageResponseImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(
						self.page_entries,
						_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.decode(value)
					)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.hydration_data =
						_roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.decode(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.SpotlightCollectionPageResponse_TemplatesEntry.decode(value)

					local keyDefault = ""
					local valueDefault = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.new()

					self.templates[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)

					local mapEntry = messages.SpotlightCollectionPageResponse_LocalizedLiteralsEntry.decode(value)

					local keyDefault = ""
					local valueDefault = ""

					self.localized_literals[mapEntry.key or keyDefault] = mapEntry.value or valueDefault

					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SpotlightCollectionPageResponseImpl.jsonEncode(self: SpotlightCollectionPageResponse): any
		local output = {}

		if self.page_entries ~= nil and #self.page_entries > 0 then
			local newOutput = {}
			for _, value in self.page_entries do
				table.insert(newOutput, value:jsonEncode())
			end
			output.pageEntries = newOutput
		end

		if self.hydration_data ~= nil then
			output.hydrationData = self.hydration_data:jsonEncode()
		end

		if self.templates ~= nil and next(self.templates) ~= nil then
			local newOutput = {}
			for key, value in self.templates do
				newOutput[key] = value:jsonEncode()
			end
			output.templates = newOutput
		end

		if self.localized_literals ~= nil and next(self.localized_literals) ~= nil then
			local newOutput = {}
			for key, value in self.localized_literals do
				newOutput[key] = value
			end
			output.localizedLiterals = newOutput
		end

		return output
	end

	function _SpotlightCollectionPageResponseImpl.jsonDecode(input: { [string]: any }): SpotlightCollectionPageResponse
		local self = _SpotlightCollectionPageResponseImpl.new()

		if input.page_entries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.page_entries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.pageEntries ~= nil then
			local newOutput: { _roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry } = {}
			for _, value in input.pageEntries do
				table.insert(
					newOutput,
					_roblox_apppageplatform_shared_v1beta1_page_entry_content.UniversalPageEntry.jsonDecode(value)
				)
			end

			self.page_entries = newOutput
		end

		if input.hydration_data ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydration_data
			)
		end

		if input.hydrationData ~= nil then
			self.hydration_data = _roblox_apppageplatform_shared_v1beta1_hydration_content.HydrationContent.jsonDecode(
				input.hydrationData
			)
		end

		if input.templates ~= nil then
			local newOutput: { [string]: _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry } = {}
			for key, value in input.templates do
				newOutput[key] = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(value)
			end

			self.templates = newOutput
		end

		if input.localized_literals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localized_literals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		if input.localizedLiterals ~= nil then
			local newOutput: { [string]: string } = {}
			for key, value in input.localizedLiterals do
				newOutput[key] = value
			end

			self.localized_literals = newOutput
		end

		return self
	end

	_SpotlightCollectionPageResponseImpl.descriptor = {
		name = "SpotlightCollectionPageResponse",
		fullName = "roblox.apppageplatform.spotlightcollection.v1beta1.SpotlightCollectionPageResponse",
	}

	messages.SpotlightCollectionPageResponse = _SpotlightCollectionPageResponseImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SpotlightCollectionPageResponse)
end

do
	local _SpotlightCollectionPageResponse_TemplatesEntryImpl = {}
	_SpotlightCollectionPageResponse_TemplatesEntryImpl.__index = _SpotlightCollectionPageResponse_TemplatesEntryImpl

	function _SpotlightCollectionPageResponse_TemplatesEntryImpl.new(
		data: _SpotlightCollectionPageResponse_TemplatesEntryPartialFields?
	): SpotlightCollectionPageResponse_TemplatesEntry
		return setmetatable({
			key = if data == nil or data.key == nil then "" else data.key,
			value = if data == nil or data.value == nil then nil else data.value,
		}, _SpotlightCollectionPageResponse_TemplatesEntryImpl :: _SpotlightCollectionPageResponse_TemplatesEntryImpl)
	end

	function _SpotlightCollectionPageResponse_TemplatesEntryImpl.encode(
		self: SpotlightCollectionPageResponse_TemplatesEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil then
			local encoded = self.value:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SpotlightCollectionPageResponse_TemplatesEntryImpl.decode(
		input: buffer
	): SpotlightCollectionPageResponse_TemplatesEntry
		local self = _SpotlightCollectionPageResponse_TemplatesEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SpotlightCollectionPageResponse_TemplatesEntryImpl.jsonEncode(
		self: SpotlightCollectionPageResponse_TemplatesEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil then
			output.value = self.value:jsonEncode()
		end

		return output
	end

	function _SpotlightCollectionPageResponse_TemplatesEntryImpl.jsonDecode(
		input: { [string]: any }
	): SpotlightCollectionPageResponse_TemplatesEntry
		local self = _SpotlightCollectionPageResponse_TemplatesEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = _roblox_apppageplatform_shared_v1beta1_template_entry.TemplateEntry.jsonDecode(input.value)
		end

		return self
	end

	_SpotlightCollectionPageResponse_TemplatesEntryImpl.descriptor = {
		name = "SpotlightCollectionPageResponse_TemplatesEntry",
		fullName = "roblox.apppageplatform.spotlightcollection.v1beta1.TemplatesEntry",
	}

	messages.SpotlightCollectionPageResponse_TemplatesEntry = _SpotlightCollectionPageResponse_TemplatesEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SpotlightCollectionPageResponse_TemplatesEntry)
end

do
	local _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl = {}
	_SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.__index =
		_SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl

	function _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.new(
		data: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryPartialFields?
	): SpotlightCollectionPageResponse_LocalizedLiteralsEntry
		return setmetatable(
			{
				key = if data == nil or data.key == nil then "" else data.key,
				value = if data == nil or data.value == nil then "" else data.value,
			},
			_SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl :: _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl
		)
	end

	function _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.encode(
		self: SpotlightCollectionPageResponse_LocalizedLiteralsEntry
	): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.key ~= nil and self.key ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.key)
		end

		if self.value ~= nil and self.value ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.value)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.decode(
		input: buffer
	): SpotlightCollectionPageResponse_LocalizedLiteralsEntry
		local self = _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.key = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.value = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.jsonEncode(
		self: SpotlightCollectionPageResponse_LocalizedLiteralsEntry
	): any
		local output = {}

		if self.key ~= nil and self.key ~= "" then
			output.key = self.key
		end

		if self.value ~= nil and self.value ~= "" then
			output.value = self.value
		end

		return output
	end

	function _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.jsonDecode(
		input: { [string]: any }
	): SpotlightCollectionPageResponse_LocalizedLiteralsEntry
		local self = _SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.new()

		if input.key ~= nil then
			self.key = input.key
		end

		if input.value ~= nil then
			self.value = input.value
		end

		return self
	end

	_SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl.descriptor = {
		name = "SpotlightCollectionPageResponse_LocalizedLiteralsEntry",
		fullName = "roblox.apppageplatform.spotlightcollection.v1beta1.LocalizedLiteralsEntry",
	}

	messages.SpotlightCollectionPageResponse_LocalizedLiteralsEntry =
		_SpotlightCollectionPageResponse_LocalizedLiteralsEntryImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SpotlightCollectionPageResponse_LocalizedLiteralsEntry)
end

return {
	SpotlightCollectionPageRequest = messages.SpotlightCollectionPageRequest,
	SpotlightCollectionPageResponse = messages.SpotlightCollectionPageResponse,
}
