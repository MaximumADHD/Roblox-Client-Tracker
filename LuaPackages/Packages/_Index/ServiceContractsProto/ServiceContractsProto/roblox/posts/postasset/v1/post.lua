--!strict
--!nolint LocalUnused
--!nolint ImportUnused
--# selene: allow(empty_if, if_same_then_else, manual_table_clone, unused_variable)
-- This file was @autogenerated by protoc-gen-luau
local proto = require(script.Parent.Parent.Parent.Parent.Parent.proto)
local typeRegistry = require(script.Parent.Parent.Parent.Parent.Parent.proto.typeRegistry)

type _Messages = {
	Post: _PostMessage,
	PostMetadata: _PostMetadataMessage,
	Track: _TrackMessage,
	Slot: _SlotMessage,
	VisualSlot: _VisualSlotMessage,
	VideoSlot: _VideoSlotMessage,
	ImageSlot: _ImageSlotMessage,
	TextLabelSlot: _TextLabelSlotMessage,
	SceneSlot: _SceneSlotMessage,
	AudioSlot: _AudioSlotMessage,
	TtsAudioContext: _TtsAudioContextMessage,
	ExperienceAnnotationSlot: _ExperienceAnnotationSlotMessage,
	AvatarAnnotationSlot: _AvatarAnnotationSlotMessage,
	TimeRange: _TimeRangeMessage,
	Transform: _TransformMessage,
	Vec2: _Vec2Message,
	Color3: _Color3Message,
	FontInfo: _FontInfoMessage,
	PostOrigin: _PostOriginMessage,
	TrackType: _TrackTypeMessage,
	AudioType: _AudioTypeMessage,
	EnumeratedFont: _EnumeratedFontMessage,
	TextXAlignment: _TextXAlignmentMessage,
}
local messages: _Messages = {} :: _Messages

local _google_protobuf_duration = require(script.Parent.Parent.Parent.Parent.Parent.google.protobuf.duration)

type _PostImpl = {
	__index: _PostImpl,
	new: (fields: _PostPartialFields?) -> Post,
	encode: (self: Post) -> buffer,
	decode: (input: buffer) -> Post,
	jsonEncode: (self: Post) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Post,
	descriptor: proto.Descriptor,
}

type _PostFields = {
	metadata: PostMetadata?,
	tracks: { Track },
}

type _PostPartialFields = {
	metadata: PostMetadata?,
	tracks: { Track }?,
}

export type Post = typeof(setmetatable({} :: _PostFields, {} :: _PostImpl))
type _PostMessage = proto.Message<Post, _PostPartialFields>

type _PostMetadataImpl = {
	__index: _PostMetadataImpl,
	new: (fields: _PostMetadataPartialFields?) -> PostMetadata,
	encode: (self: PostMetadata) -> buffer,
	decode: (input: buffer) -> PostMetadata,
	jsonEncode: (self: PostMetadata) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> PostMetadata,
	descriptor: proto.Descriptor,
}

type _PostMetadataFields = {
	origin: PostOrigin,
	caption: string,
	total_duration: _google_protobuf_duration.Duration?,
	content_language: string,
}

type _PostMetadataPartialFields = {
	origin: PostOrigin?,
	caption: string?,
	total_duration: _google_protobuf_duration.Duration?,
	content_language: string?,
}

export type PostMetadata = typeof(setmetatable({} :: _PostMetadataFields, {} :: _PostMetadataImpl))
type _PostMetadataMessage = proto.Message<PostMetadata, _PostMetadataPartialFields>

type _TrackImpl = {
	__index: _TrackImpl,
	new: (fields: _TrackPartialFields?) -> Track,
	encode: (self: Track) -> buffer,
	decode: (input: buffer) -> Track,
	jsonEncode: (self: Track) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Track,
	descriptor: proto.Descriptor,
}

type _TrackFields = {
	type: TrackType,
	slots: { Slot },
}

type _TrackPartialFields = {
	type: TrackType?,
	slots: { Slot }?,
}

export type Track = typeof(setmetatable({} :: _TrackFields, {} :: _TrackImpl))
type _TrackMessage = proto.Message<Track, _TrackPartialFields>

type _SlotImpl = {
	__index: _SlotImpl,
	new: (fields: _SlotPartialFields?) -> Slot,
	encode: (self: Slot) -> buffer,
	decode: (input: buffer) -> Slot,
	jsonEncode: (self: Slot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Slot,
	descriptor: proto.Descriptor,
}

type _SlotFields = {
	slot_id: string,
	time_range: TimeRange?,
	content: (
		{ type: "visual", value: VisualSlot }
		| { type: "audio", value: AudioSlot }
		| { type: "experience_annotation", value: ExperienceAnnotationSlot }
		| { type: "avatar_annotation", value: AvatarAnnotationSlot }
	)?,
}

type _SlotPartialFields = {
	slot_id: string?,
	time_range: TimeRange?,
	content: (
		{ type: "visual", value: VisualSlot }
		| { type: "audio", value: AudioSlot }
		| { type: "experience_annotation", value: ExperienceAnnotationSlot }
		| { type: "avatar_annotation", value: AvatarAnnotationSlot }
	)?,
}

export type Slot = typeof(setmetatable({} :: _SlotFields, {} :: _SlotImpl))
type _SlotMessage = proto.Message<Slot, _SlotPartialFields>

type _VisualSlotImpl = {
	__index: _VisualSlotImpl,
	new: (fields: _VisualSlotPartialFields?) -> VisualSlot,
	encode: (self: VisualSlot) -> buffer,
	decode: (input: buffer) -> VisualSlot,
	jsonEncode: (self: VisualSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VisualSlot,
	descriptor: proto.Descriptor,
}

type _VisualSlotFields = {
	z_index: number,
	transform: Transform?,
	content: (
		{ type: "video", value: VideoSlot }
		| { type: "image", value: ImageSlot }
		| { type: "text_label", value: TextLabelSlot }
		| { type: "scene", value: SceneSlot }
	)?,
}

type _VisualSlotPartialFields = {
	z_index: number?,
	transform: Transform?,
	content: (
		{ type: "video", value: VideoSlot }
		| { type: "image", value: ImageSlot }
		| { type: "text_label", value: TextLabelSlot }
		| { type: "scene", value: SceneSlot }
	)?,
}

export type VisualSlot = typeof(setmetatable({} :: _VisualSlotFields, {} :: _VisualSlotImpl))
type _VisualSlotMessage = proto.Message<VisualSlot, _VisualSlotPartialFields>

type _VideoSlotImpl = {
	__index: _VideoSlotImpl,
	new: (fields: _VideoSlotPartialFields?) -> VideoSlot,
	encode: (self: VideoSlot) -> buffer,
	decode: (input: buffer) -> VideoSlot,
	jsonEncode: (self: VideoSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> VideoSlot,
	descriptor: proto.Descriptor,
}

type _VideoSlotFields = {
	asset_id: number,
	trim: TimeRange?,
}

type _VideoSlotPartialFields = {
	asset_id: number?,
	trim: TimeRange?,
}

export type VideoSlot = typeof(setmetatable({} :: _VideoSlotFields, {} :: _VideoSlotImpl))
type _VideoSlotMessage = proto.Message<VideoSlot, _VideoSlotPartialFields>

type _ImageSlotImpl = {
	__index: _ImageSlotImpl,
	new: (fields: _ImageSlotPartialFields?) -> ImageSlot,
	encode: (self: ImageSlot) -> buffer,
	decode: (input: buffer) -> ImageSlot,
	jsonEncode: (self: ImageSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ImageSlot,
	descriptor: proto.Descriptor,
}

type _ImageSlotFields = {
	asset_id: number,
}

type _ImageSlotPartialFields = {
	asset_id: number?,
}

export type ImageSlot = typeof(setmetatable({} :: _ImageSlotFields, {} :: _ImageSlotImpl))
type _ImageSlotMessage = proto.Message<ImageSlot, _ImageSlotPartialFields>

type _TextLabelSlotImpl = {
	__index: _TextLabelSlotImpl,
	new: (fields: _TextLabelSlotPartialFields?) -> TextLabelSlot,
	encode: (self: TextLabelSlot) -> buffer,
	decode: (input: buffer) -> TextLabelSlot,
	jsonEncode: (self: TextLabelSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TextLabelSlot,
	descriptor: proto.Descriptor,
}

type _TextLabelSlotFields = {
	text: string,
	font: FontInfo?,
	text_x_alignment: TextXAlignment,
	font_color: Color3?,
	font_size: number,
}

type _TextLabelSlotPartialFields = {
	text: string?,
	font: FontInfo?,
	text_x_alignment: TextXAlignment?,
	font_color: Color3?,
	font_size: number?,
}

export type TextLabelSlot = typeof(setmetatable({} :: _TextLabelSlotFields, {} :: _TextLabelSlotImpl))
type _TextLabelSlotMessage = proto.Message<TextLabelSlot, _TextLabelSlotPartialFields>

type _SceneSlotImpl = {
	__index: _SceneSlotImpl,
	new: (fields: _SceneSlotPartialFields?) -> SceneSlot,
	encode: (self: SceneSlot) -> buffer,
	decode: (input: buffer) -> SceneSlot,
	jsonEncode: (self: SceneSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> SceneSlot,
	descriptor: proto.Descriptor,
}

type _SceneSlotFields = {
	asset_id: number,
}

type _SceneSlotPartialFields = {
	asset_id: number?,
}

export type SceneSlot = typeof(setmetatable({} :: _SceneSlotFields, {} :: _SceneSlotImpl))
type _SceneSlotMessage = proto.Message<SceneSlot, _SceneSlotPartialFields>

type _AudioSlotImpl = {
	__index: _AudioSlotImpl,
	new: (fields: _AudioSlotPartialFields?) -> AudioSlot,
	encode: (self: AudioSlot) -> buffer,
	decode: (input: buffer) -> AudioSlot,
	jsonEncode: (self: AudioSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AudioSlot,
	descriptor: proto.Descriptor,
}

type _AudioSlotFields = {
	asset_id: number,
	trim: TimeRange?,
	audio_type: AudioType,
	context: { type: "tts", value: TtsAudioContext }?,
}

type _AudioSlotPartialFields = {
	asset_id: number?,
	trim: TimeRange?,
	audio_type: AudioType?,
	context: { type: "tts", value: TtsAudioContext }?,
}

export type AudioSlot = typeof(setmetatable({} :: _AudioSlotFields, {} :: _AudioSlotImpl))
type _AudioSlotMessage = proto.Message<AudioSlot, _AudioSlotPartialFields>

type _TtsAudioContextImpl = {
	__index: _TtsAudioContextImpl,
	new: (fields: _TtsAudioContextPartialFields?) -> TtsAudioContext,
	encode: (self: TtsAudioContext) -> buffer,
	decode: (input: buffer) -> TtsAudioContext,
	jsonEncode: (self: TtsAudioContext) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TtsAudioContext,
	descriptor: proto.Descriptor,
}

type _TtsAudioContextFields = {
	text: string,
	voice_id: number,
	pitch: number,
	speed: number,
}

type _TtsAudioContextPartialFields = {
	text: string?,
	voice_id: number?,
	pitch: number?,
	speed: number?,
}

export type TtsAudioContext = typeof(setmetatable({} :: _TtsAudioContextFields, {} :: _TtsAudioContextImpl))
type _TtsAudioContextMessage = proto.Message<TtsAudioContext, _TtsAudioContextPartialFields>

type _ExperienceAnnotationSlotImpl = {
	__index: _ExperienceAnnotationSlotImpl,
	new: (fields: _ExperienceAnnotationSlotPartialFields?) -> ExperienceAnnotationSlot,
	encode: (self: ExperienceAnnotationSlot) -> buffer,
	decode: (input: buffer) -> ExperienceAnnotationSlot,
	jsonEncode: (self: ExperienceAnnotationSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> ExperienceAnnotationSlot,
	descriptor: proto.Descriptor,
}

type _ExperienceAnnotationSlotFields = {
	universe_id: number,
	place_id: number,
}

type _ExperienceAnnotationSlotPartialFields = {
	universe_id: number?,
	place_id: number?,
}

export type ExperienceAnnotationSlot = typeof(setmetatable(
	{} :: _ExperienceAnnotationSlotFields,
	{} :: _ExperienceAnnotationSlotImpl
))
type _ExperienceAnnotationSlotMessage = proto.Message<ExperienceAnnotationSlot, _ExperienceAnnotationSlotPartialFields>

type _AvatarAnnotationSlotImpl = {
	__index: _AvatarAnnotationSlotImpl,
	new: (fields: _AvatarAnnotationSlotPartialFields?) -> AvatarAnnotationSlot,
	encode: (self: AvatarAnnotationSlot) -> buffer,
	decode: (input: buffer) -> AvatarAnnotationSlot,
	jsonEncode: (self: AvatarAnnotationSlot) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> AvatarAnnotationSlot,
	descriptor: proto.Descriptor,
}

type _AvatarAnnotationSlotFields = {
	outfit_id: number,
}

type _AvatarAnnotationSlotPartialFields = {
	outfit_id: number?,
}

export type AvatarAnnotationSlot = typeof(setmetatable(
	{} :: _AvatarAnnotationSlotFields,
	{} :: _AvatarAnnotationSlotImpl
))
type _AvatarAnnotationSlotMessage = proto.Message<AvatarAnnotationSlot, _AvatarAnnotationSlotPartialFields>

type _TimeRangeImpl = {
	__index: _TimeRangeImpl,
	new: (fields: _TimeRangePartialFields?) -> TimeRange,
	encode: (self: TimeRange) -> buffer,
	decode: (input: buffer) -> TimeRange,
	jsonEncode: (self: TimeRange) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> TimeRange,
	descriptor: proto.Descriptor,
}

type _TimeRangeFields = {
	offset_duration: _google_protobuf_duration.Duration?,
	duration: _google_protobuf_duration.Duration?,
}

type _TimeRangePartialFields = {
	offset_duration: _google_protobuf_duration.Duration?,
	duration: _google_protobuf_duration.Duration?,
}

export type TimeRange = typeof(setmetatable({} :: _TimeRangeFields, {} :: _TimeRangeImpl))
type _TimeRangeMessage = proto.Message<TimeRange, _TimeRangePartialFields>

type _TransformImpl = {
	__index: _TransformImpl,
	new: (fields: _TransformPartialFields?) -> Transform,
	encode: (self: Transform) -> buffer,
	decode: (input: buffer) -> Transform,
	jsonEncode: (self: Transform) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Transform,
	descriptor: proto.Descriptor,
}

type _TransformFields = {
	position: Vec2?,
	rotation: number,
	scale: number,
}

type _TransformPartialFields = {
	position: Vec2?,
	rotation: number?,
	scale: number?,
}

export type Transform = typeof(setmetatable({} :: _TransformFields, {} :: _TransformImpl))
type _TransformMessage = proto.Message<Transform, _TransformPartialFields>

type _Vec2Impl = {
	__index: _Vec2Impl,
	new: (fields: _Vec2PartialFields?) -> Vec2,
	encode: (self: Vec2) -> buffer,
	decode: (input: buffer) -> Vec2,
	jsonEncode: (self: Vec2) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Vec2,
	descriptor: proto.Descriptor,
}

type _Vec2Fields = {
	x: number,
	y: number,
}

type _Vec2PartialFields = {
	x: number?,
	y: number?,
}

export type Vec2 = typeof(setmetatable({} :: _Vec2Fields, {} :: _Vec2Impl))
type _Vec2Message = proto.Message<Vec2, _Vec2PartialFields>

type _Color3Impl = {
	__index: _Color3Impl,
	new: (fields: _Color3PartialFields?) -> Color3,
	encode: (self: Color3) -> buffer,
	decode: (input: buffer) -> Color3,
	jsonEncode: (self: Color3) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> Color3,
	descriptor: proto.Descriptor,
}

type _Color3Fields = {
	r: number,
	g: number,
	b: number,
}

type _Color3PartialFields = {
	r: number?,
	g: number?,
	b: number?,
}

export type Color3 = typeof(setmetatable({} :: _Color3Fields, {} :: _Color3Impl))
type _Color3Message = proto.Message<Color3, _Color3PartialFields>

type _FontInfoImpl = {
	__index: _FontInfoImpl,
	new: (fields: _FontInfoPartialFields?) -> FontInfo,
	encode: (self: FontInfo) -> buffer,
	decode: (input: buffer) -> FontInfo,
	jsonEncode: (self: FontInfo) -> { [string]: any },
	jsonDecode: (input: { [string]: any }) -> FontInfo,
	descriptor: proto.Descriptor,
}

type _FontInfoFields = {
	font_source: ({ type: "enumerated_font", value: EnumeratedFont } | { type: "family_asset_id", value: number })?,
}

type _FontInfoPartialFields = {
	font_source: ({ type: "enumerated_font", value: EnumeratedFont } | { type: "family_asset_id", value: number })?,
}

export type FontInfo = typeof(setmetatable({} :: _FontInfoFields, {} :: _FontInfoImpl))
type _FontInfoMessage = proto.Message<FontInfo, _FontInfoPartialFields>

type _PostOriginMessage = proto.Enum<PostOrigin>
export type PostOrigin =
	"POST_ORIGIN_INVALID"
	| "POST_ORIGIN_CAPTURE_VIDEO"
	| "POST_ORIGIN_CAPTURE_SCREENSHOT"
	| "POST_ORIGIN_INFLUENCER_UPLOAD"
	| "POST_ORIGIN_AVATAR"
	| number -- Unknown

type _TrackTypeMessage = proto.Enum<TrackType>
export type TrackType = "TRACK_TYPE_INVALID" | "TRACK_TYPE_VISUAL" | "TRACK_TYPE_AUDIO" | "TRACK_TYPE_ANNOTATION" | number -- Unknown

type _AudioTypeMessage = proto.Enum<AudioType>
export type AudioType = "AUDIO_TYPE_INVALID" | "AUDIO_TYPE_MUSIC" | "AUDIO_TYPE_TTS" | "AUDIO_TYPE_VOICE_OVER" | number -- Unknown

type _EnumeratedFontMessage = proto.Enum<EnumeratedFont>
export type EnumeratedFont =
	"ENUMERATED_FONT_INVALID"
	| "ENUMERATED_FONT_GOTHAM_BOLD"
	| "ENUMERATED_FONT_GOTHAM_BLACK"
	| "ENUMERATED_FONT_SOURCE_SANS"
	| "ENUMERATED_FONT_SOURCE_SANS_BOLD"
	| "ENUMERATED_FONT_FREDOKA_ONE"
	| "ENUMERATED_FONT_BANGERS"
	| number -- Unknown

type _TextXAlignmentMessage = proto.Enum<TextXAlignment>
export type TextXAlignment =
	"TEXT_X_ALIGNMENT_INVALID"
	| "TEXT_X_ALIGNMENT_LEFT"
	| "TEXT_X_ALIGNMENT_CENTER"
	| "TEXT_X_ALIGNMENT_RIGHT"
	| number -- Unknown

do
	local _PostImpl = {}
	_PostImpl.__index = _PostImpl

	function _PostImpl.new(data: _PostPartialFields?): Post
		return setmetatable({
			metadata = if data == nil or data.metadata == nil then nil else data.metadata,
			tracks = if data == nil or data.tracks == nil then {} else data.tracks,
		}, _PostImpl :: _PostImpl)
	end

	function _PostImpl.encode(self: Post): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.metadata ~= nil then
			local encoded = self.metadata:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.tracks ~= nil and #self.tracks > 0 then
			for _, value in self.tracks do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PostImpl.decode(input: buffer): Post
		local self = _PostImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.metadata = messages.PostMetadata.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.tracks, messages.Track.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PostImpl.jsonEncode(self: Post): any
		local output = {}

		if self.metadata ~= nil then
			output.metadata = self.metadata:jsonEncode()
		end

		if self.tracks ~= nil and #self.tracks > 0 then
			local newOutput = {}
			for _, value in self.tracks do
				table.insert(newOutput, value:jsonEncode())
			end
			output.tracks = newOutput
		end

		return output
	end

	function _PostImpl.jsonDecode(input: { [string]: any }): Post
		local self = _PostImpl.new()

		if input.metadata ~= nil then
			self.metadata = messages.PostMetadata.jsonDecode(input.metadata)
		end

		if input.tracks ~= nil then
			local newOutput: { Track } = {}
			for _, value in input.tracks do
				table.insert(newOutput, messages.Track.jsonDecode(value))
			end

			self.tracks = newOutput
		end

		return self
	end

	_PostImpl.descriptor = {
		name = "Post",
		fullName = "roblox.posts.postasset.v1.Post",
	}

	messages.Post = _PostImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Post)
end

do
	local _PostMetadataImpl = {}
	_PostMetadataImpl.__index = _PostMetadataImpl

	function _PostMetadataImpl.new(data: _PostMetadataPartialFields?): PostMetadata
		return setmetatable({
			origin = if data == nil or data.origin == nil
				then assert(messages.PostOrigin.fromNumber(0), "Enum has no 0 default")
				else data.origin,
			caption = if data == nil or data.caption == nil then "" else data.caption,
			total_duration = if data == nil or data.total_duration == nil then nil else data.total_duration,
			content_language = if data == nil or data.content_language == nil then "" else data.content_language,
		}, _PostMetadataImpl :: _PostMetadataImpl)
	end

	function _PostMetadataImpl.encode(self: PostMetadata): buffer
		local output = buffer.create(0)
		local cursor = 0

		if
			self.origin ~= nil
			and (self.origin ~= nil and self.origin ~= 0 or self.origin ~= messages.PostOrigin.fromNumber(0))
		then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, messages.PostOrigin.toNumber(self.origin :: any))
		end

		if self.caption ~= nil and self.caption ~= "" then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.caption)
		end

		if self.total_duration ~= nil then
			local encoded = self.total_duration:encode()
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.content_language ~= nil and self.content_language ~= "" then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.content_language)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _PostMetadataImpl.decode(input: buffer): PostMetadata
		local self = _PostMetadataImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.origin = (messages.PostOrigin.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.caption = buffer.tostring(value)
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.total_duration = _google_protobuf_duration.Duration.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content_language = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _PostMetadataImpl.jsonEncode(self: PostMetadata): any
		local output = {}

		if
			self.origin ~= nil
			and (self.origin ~= nil and self.origin ~= 0 or self.origin ~= messages.PostOrigin.fromNumber(0))
		then
			output.origin = if typeof(self.origin) == "number"
				then self.origin
				else messages.PostOrigin.toNumber(self.origin :: any)
		end

		if self.caption ~= nil and self.caption ~= "" then
			output.caption = self.caption
		end

		if self.total_duration ~= nil then
			output.totalDuration = self.total_duration:jsonEncode()
		end

		if self.content_language ~= nil and self.content_language ~= "" then
			output.contentLanguage = self.content_language
		end

		return output
	end

	function _PostMetadataImpl.jsonDecode(input: { [string]: any }): PostMetadata
		local self = _PostMetadataImpl.new()

		if input.origin ~= nil then
			self.origin = if typeof(input.origin) == "number"
				then (messages.PostOrigin.fromNumber(input.origin) or input.origin)
				else (messages.PostOrigin.fromName(input.origin) or input.origin)
		end

		if input.caption ~= nil then
			self.caption = input.caption
		end

		if input.total_duration ~= nil then
			self.total_duration = _google_protobuf_duration.Duration.jsonDecode(input.total_duration)
		end

		if input.totalDuration ~= nil then
			self.total_duration = _google_protobuf_duration.Duration.jsonDecode(input.totalDuration)
		end

		if input.content_language ~= nil then
			self.content_language = input.content_language
		end

		if input.contentLanguage ~= nil then
			self.content_language = input.contentLanguage
		end

		return self
	end

	_PostMetadataImpl.descriptor = {
		name = "PostMetadata",
		fullName = "roblox.posts.postasset.v1.PostMetadata",
	}

	messages.PostMetadata = _PostMetadataImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.PostMetadata)
end

do
	local _TrackImpl = {}
	_TrackImpl.__index = _TrackImpl

	function _TrackImpl.new(data: _TrackPartialFields?): Track
		return setmetatable({
			type = if data == nil or data.type == nil
				then assert(messages.TrackType.fromNumber(0), "Enum has no 0 default")
				else data.type,
			slots = if data == nil or data.slots == nil then {} else data.slots,
		}, _TrackImpl :: _TrackImpl)
	end

	function _TrackImpl.encode(self: Track): buffer
		local output = buffer.create(0)
		local cursor = 0

		if
			self.type ~= nil and (self.type ~= nil and self.type ~= 0 or self.type ~= messages.TrackType.fromNumber(0))
		then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, messages.TrackType.toNumber(self.type :: any))
		end

		if self.slots ~= nil and #self.slots > 0 then
			for _, value in self.slots do
				local encoded = value:encode()
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TrackImpl.decode(input: buffer): Track
		local self = _TrackImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.type = (messages.TrackType.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					table.insert(self.slots, messages.Slot.decode(value))
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TrackImpl.jsonEncode(self: Track): any
		local output = {}

		if
			self.type ~= nil and (self.type ~= nil and self.type ~= 0 or self.type ~= messages.TrackType.fromNumber(0))
		then
			output.type = if typeof(self.type) == "number"
				then self.type
				else messages.TrackType.toNumber(self.type :: any)
		end

		if self.slots ~= nil and #self.slots > 0 then
			local newOutput = {}
			for _, value in self.slots do
				table.insert(newOutput, value:jsonEncode())
			end
			output.slots = newOutput
		end

		return output
	end

	function _TrackImpl.jsonDecode(input: { [string]: any }): Track
		local self = _TrackImpl.new()

		if input.type ~= nil then
			self.type = if typeof(input.type) == "number"
				then (messages.TrackType.fromNumber(input.type) or input.type)
				else (messages.TrackType.fromName(input.type) or input.type)
		end

		if input.slots ~= nil then
			local newOutput: { Slot } = {}
			for _, value in input.slots do
				table.insert(newOutput, messages.Slot.jsonDecode(value))
			end

			self.slots = newOutput
		end

		return self
	end

	_TrackImpl.descriptor = {
		name = "Track",
		fullName = "roblox.posts.postasset.v1.Track",
	}

	messages.Track = _TrackImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Track)
end

do
	local _SlotImpl = {}
	_SlotImpl.__index = _SlotImpl

	function _SlotImpl.new(data: _SlotPartialFields?): Slot
		return setmetatable({
			slot_id = if data == nil or data.slot_id == nil then "" else data.slot_id,
			time_range = if data == nil or data.time_range == nil then nil else data.time_range,
			content = if data == nil or data.content == nil then nil else data.content,
		}, _SlotImpl :: _SlotImpl)
	end

	function _SlotImpl.encode(self: Slot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.slot_id ~= nil and self.slot_id ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.slot_id)
		end

		if self.time_range ~= nil then
			local encoded = self.time_range:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.content ~= nil then
			if self.content.type == "visual" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.content.type == "audio" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.content.type == "experience_annotation" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.content.type == "avatar_annotation" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SlotImpl.decode(input: buffer): Slot
		local self = _SlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.slot_id = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.time_range = messages.TimeRange.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "visual", value = messages.VisualSlot.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "audio", value = messages.AudioSlot.decode(value) }
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content =
						{ type = "experience_annotation", value = messages.ExperienceAnnotationSlot.decode(value) }
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "avatar_annotation", value = messages.AvatarAnnotationSlot.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SlotImpl.jsonEncode(self: Slot): any
		local output = {}

		if self.slot_id ~= nil and self.slot_id ~= "" then
			output.slotId = self.slot_id
		end

		if self.time_range ~= nil then
			output.timeRange = self.time_range:jsonEncode()
		end

		if self.content ~= nil then
			if self.content.type == "visual" then
				output.visual = self.content.value:jsonEncode()
			elseif self.content.type == "audio" then
				output.audio = self.content.value:jsonEncode()
			elseif self.content.type == "experience_annotation" then
				output.experienceAnnotation = self.content.value:jsonEncode()
			elseif self.content.type == "avatar_annotation" then
				output.avatarAnnotation = self.content.value:jsonEncode()
			end
		end

		return output
	end

	function _SlotImpl.jsonDecode(input: { [string]: any }): Slot
		local self = _SlotImpl.new()

		if input.slot_id ~= nil then
			self.slot_id = input.slot_id
		end

		if input.slotId ~= nil then
			self.slot_id = input.slotId
		end

		if input.time_range ~= nil then
			self.time_range = messages.TimeRange.jsonDecode(input.time_range)
		end

		if input.timeRange ~= nil then
			self.time_range = messages.TimeRange.jsonDecode(input.timeRange)
		end

		if input.visual ~= nil then
			self.content = { type = "visual", value = messages.VisualSlot.jsonDecode(input.visual) }
		end

		if input.audio ~= nil then
			self.content = { type = "audio", value = messages.AudioSlot.jsonDecode(input.audio) }
		end

		if input.experience_annotation ~= nil then
			self.content = {
				type = "experience_annotation",
				value = messages.ExperienceAnnotationSlot.jsonDecode(input.experience_annotation),
			}
		end

		if input.experienceAnnotation ~= nil then
			self.content = {
				type = "experience_annotation",
				value = messages.ExperienceAnnotationSlot.jsonDecode(input.experienceAnnotation),
			}
		end

		if input.avatar_annotation ~= nil then
			self.content = {
				type = "avatar_annotation",
				value = messages.AvatarAnnotationSlot.jsonDecode(input.avatar_annotation),
			}
		end

		if input.avatarAnnotation ~= nil then
			self.content =
				{ type = "avatar_annotation", value = messages.AvatarAnnotationSlot.jsonDecode(input.avatarAnnotation) }
		end

		return self
	end

	_SlotImpl.descriptor = {
		name = "Slot",
		fullName = "roblox.posts.postasset.v1.Slot",
	}

	messages.Slot = _SlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Slot)
end

do
	local _VisualSlotImpl = {}
	_VisualSlotImpl.__index = _VisualSlotImpl

	function _VisualSlotImpl.new(data: _VisualSlotPartialFields?): VisualSlot
		return setmetatable({
			z_index = if data == nil or data.z_index == nil then 0 else data.z_index,
			transform = if data == nil or data.transform == nil then nil else data.transform,
			content = if data == nil or data.content == nil then nil else data.content,
		}, _VisualSlotImpl :: _VisualSlotImpl)
	end

	function _VisualSlotImpl.encode(self: VisualSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.z_index ~= nil and self.z_index ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.z_index)
		end

		if self.transform ~= nil then
			local encoded = self.transform:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.content ~= nil then
			if self.content.type == "video" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.content.type == "image" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 11, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.content.type == "text_label" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 12, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			elseif self.content.type == "scene" then
				local encoded = self.content.value:encode()
				output, cursor = proto.writeTag(output, cursor, 13, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VisualSlotImpl.decode(input: buffer): VisualSlot
		local self = _VisualSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.z_index = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.transform = messages.Transform.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "video", value = messages.VideoSlot.decode(value) }
					continue
				elseif field == 11 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "image", value = messages.ImageSlot.decode(value) }
					continue
				elseif field == 12 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "text_label", value = messages.TextLabelSlot.decode(value) }
					continue
				elseif field == 13 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.content = { type = "scene", value = messages.SceneSlot.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VisualSlotImpl.jsonEncode(self: VisualSlot): any
		local output = {}

		if self.z_index ~= nil and self.z_index ~= 0 then
			output.zIndex = self.z_index
		end

		if self.transform ~= nil then
			output.transform = self.transform:jsonEncode()
		end

		if self.content ~= nil then
			if self.content.type == "video" then
				output.video = self.content.value:jsonEncode()
			elseif self.content.type == "image" then
				output.image = self.content.value:jsonEncode()
			elseif self.content.type == "text_label" then
				output.textLabel = self.content.value:jsonEncode()
			elseif self.content.type == "scene" then
				output.scene = self.content.value:jsonEncode()
			end
		end

		return output
	end

	function _VisualSlotImpl.jsonDecode(input: { [string]: any }): VisualSlot
		local self = _VisualSlotImpl.new()

		if input.z_index ~= nil then
			self.z_index = input.z_index
		end

		if input.zIndex ~= nil then
			self.z_index = input.zIndex
		end

		if input.transform ~= nil then
			self.transform = messages.Transform.jsonDecode(input.transform)
		end

		if input.video ~= nil then
			self.content = { type = "video", value = messages.VideoSlot.jsonDecode(input.video) }
		end

		if input.image ~= nil then
			self.content = { type = "image", value = messages.ImageSlot.jsonDecode(input.image) }
		end

		if input.text_label ~= nil then
			self.content = { type = "text_label", value = messages.TextLabelSlot.jsonDecode(input.text_label) }
		end

		if input.textLabel ~= nil then
			self.content = { type = "text_label", value = messages.TextLabelSlot.jsonDecode(input.textLabel) }
		end

		if input.scene ~= nil then
			self.content = { type = "scene", value = messages.SceneSlot.jsonDecode(input.scene) }
		end

		return self
	end

	_VisualSlotImpl.descriptor = {
		name = "VisualSlot",
		fullName = "roblox.posts.postasset.v1.VisualSlot",
	}

	messages.VisualSlot = _VisualSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VisualSlot)
end

do
	local _VideoSlotImpl = {}
	_VideoSlotImpl.__index = _VideoSlotImpl

	function _VideoSlotImpl.new(data: _VideoSlotPartialFields?): VideoSlot
		return setmetatable({
			asset_id = if data == nil or data.asset_id == nil then 0 else data.asset_id,
			trim = if data == nil or data.trim == nil then nil else data.trim,
		}, _VideoSlotImpl :: _VideoSlotImpl)
	end

	function _VideoSlotImpl.encode(self: VideoSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.asset_id)
		end

		if self.trim ~= nil then
			local encoded = self.trim:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _VideoSlotImpl.decode(input: buffer): VideoSlot
		local self = _VideoSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.asset_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.trim = messages.TimeRange.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _VideoSlotImpl.jsonEncode(self: VideoSlot): any
		local output = {}

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output.assetId = self.asset_id
		end

		if self.trim ~= nil then
			output.trim = self.trim:jsonEncode()
		end

		return output
	end

	function _VideoSlotImpl.jsonDecode(input: { [string]: any }): VideoSlot
		local self = _VideoSlotImpl.new()

		if input.asset_id ~= nil then
			self.asset_id = input.asset_id
		end

		if input.assetId ~= nil then
			self.asset_id = input.assetId
		end

		if input.trim ~= nil then
			self.trim = messages.TimeRange.jsonDecode(input.trim)
		end

		return self
	end

	_VideoSlotImpl.descriptor = {
		name = "VideoSlot",
		fullName = "roblox.posts.postasset.v1.VideoSlot",
	}

	messages.VideoSlot = _VideoSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.VideoSlot)
end

do
	local _ImageSlotImpl = {}
	_ImageSlotImpl.__index = _ImageSlotImpl

	function _ImageSlotImpl.new(data: _ImageSlotPartialFields?): ImageSlot
		return setmetatable({
			asset_id = if data == nil or data.asset_id == nil then 0 else data.asset_id,
		}, _ImageSlotImpl :: _ImageSlotImpl)
	end

	function _ImageSlotImpl.encode(self: ImageSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.asset_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ImageSlotImpl.decode(input: buffer): ImageSlot
		local self = _ImageSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.asset_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ImageSlotImpl.jsonEncode(self: ImageSlot): any
		local output = {}

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output.assetId = self.asset_id
		end

		return output
	end

	function _ImageSlotImpl.jsonDecode(input: { [string]: any }): ImageSlot
		local self = _ImageSlotImpl.new()

		if input.asset_id ~= nil then
			self.asset_id = input.asset_id
		end

		if input.assetId ~= nil then
			self.asset_id = input.assetId
		end

		return self
	end

	_ImageSlotImpl.descriptor = {
		name = "ImageSlot",
		fullName = "roblox.posts.postasset.v1.ImageSlot",
	}

	messages.ImageSlot = _ImageSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ImageSlot)
end

do
	local _TextLabelSlotImpl = {}
	_TextLabelSlotImpl.__index = _TextLabelSlotImpl

	function _TextLabelSlotImpl.new(data: _TextLabelSlotPartialFields?): TextLabelSlot
		return setmetatable({
			text = if data == nil or data.text == nil then "" else data.text,
			font = if data == nil or data.font == nil then nil else data.font,
			text_x_alignment = if data == nil or data.text_x_alignment == nil
				then assert(messages.TextXAlignment.fromNumber(0), "Enum has no 0 default")
				else data.text_x_alignment,
			font_color = if data == nil or data.font_color == nil then nil else data.font_color,
			font_size = if data == nil or data.font_size == nil then 0 else data.font_size,
		}, _TextLabelSlotImpl :: _TextLabelSlotImpl)
	end

	function _TextLabelSlotImpl.encode(self: TextLabelSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.text ~= nil and self.text ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.text)
		end

		if self.font ~= nil then
			local encoded = self.font:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if
			self.text_x_alignment ~= nil
			and (
				self.text_x_alignment ~= nil and self.text_x_alignment ~= 0
				or self.text_x_alignment ~= messages.TextXAlignment.fromNumber(0)
			)
		then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor =
				proto.writeVarInt(output, cursor, messages.TextXAlignment.toNumber(self.text_x_alignment :: any))
		end

		if self.font_color ~= nil then
			local encoded = self.font_color:encode()
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.font_size ~= nil and self.font_size ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 5, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.font_size)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TextLabelSlotImpl.decode(input: buffer): TextLabelSlot
		local self = _TextLabelSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.text_x_alignment = (messages.TextXAlignment.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text = buffer.tostring(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.font = messages.FontInfo.decode(value)
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.font_color = messages.Color3.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 5 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.font_size = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TextLabelSlotImpl.jsonEncode(self: TextLabelSlot): any
		local output = {}

		if self.text ~= nil and self.text ~= "" then
			output.text = self.text
		end

		if self.font ~= nil then
			output.font = self.font:jsonEncode()
		end

		if
			self.text_x_alignment ~= nil
			and (
				self.text_x_alignment ~= nil and self.text_x_alignment ~= 0
				or self.text_x_alignment ~= messages.TextXAlignment.fromNumber(0)
			)
		then
			output.textXAlignment = if typeof(self.text_x_alignment) == "number"
				then self.text_x_alignment
				else messages.TextXAlignment.toNumber(self.text_x_alignment :: any)
		end

		if self.font_color ~= nil then
			output.fontColor = self.font_color:jsonEncode()
		end

		if self.font_size ~= nil and self.font_size ~= 0 then
			output.fontSize = proto.json.serializeNumber(self.font_size)
		end

		return output
	end

	function _TextLabelSlotImpl.jsonDecode(input: { [string]: any }): TextLabelSlot
		local self = _TextLabelSlotImpl.new()

		if input.text ~= nil then
			self.text = input.text
		end

		if input.font ~= nil then
			self.font = messages.FontInfo.jsonDecode(input.font)
		end

		if input.text_x_alignment ~= nil then
			self.text_x_alignment = if typeof(input.text_x_alignment) == "number"
				then (messages.TextXAlignment.fromNumber(input.text_x_alignment) or input.text_x_alignment)
				else (messages.TextXAlignment.fromName(input.text_x_alignment) or input.text_x_alignment)
		end

		if input.textXAlignment ~= nil then
			self.text_x_alignment = if typeof(input.textXAlignment) == "number"
				then (messages.TextXAlignment.fromNumber(input.textXAlignment) or input.textXAlignment)
				else (messages.TextXAlignment.fromName(input.textXAlignment) or input.textXAlignment)
		end

		if input.font_color ~= nil then
			self.font_color = messages.Color3.jsonDecode(input.font_color)
		end

		if input.fontColor ~= nil then
			self.font_color = messages.Color3.jsonDecode(input.fontColor)
		end

		if input.font_size ~= nil then
			self.font_size = proto.json.deserializeNumber(input.font_size)
		end

		if input.fontSize ~= nil then
			self.font_size = proto.json.deserializeNumber(input.fontSize)
		end

		return self
	end

	_TextLabelSlotImpl.descriptor = {
		name = "TextLabelSlot",
		fullName = "roblox.posts.postasset.v1.TextLabelSlot",
	}

	messages.TextLabelSlot = _TextLabelSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TextLabelSlot)
end

do
	local _SceneSlotImpl = {}
	_SceneSlotImpl.__index = _SceneSlotImpl

	function _SceneSlotImpl.new(data: _SceneSlotPartialFields?): SceneSlot
		return setmetatable({
			asset_id = if data == nil or data.asset_id == nil then 0 else data.asset_id,
		}, _SceneSlotImpl :: _SceneSlotImpl)
	end

	function _SceneSlotImpl.encode(self: SceneSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.asset_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _SceneSlotImpl.decode(input: buffer): SceneSlot
		local self = _SceneSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.asset_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _SceneSlotImpl.jsonEncode(self: SceneSlot): any
		local output = {}

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output.assetId = self.asset_id
		end

		return output
	end

	function _SceneSlotImpl.jsonDecode(input: { [string]: any }): SceneSlot
		local self = _SceneSlotImpl.new()

		if input.asset_id ~= nil then
			self.asset_id = input.asset_id
		end

		if input.assetId ~= nil then
			self.asset_id = input.assetId
		end

		return self
	end

	_SceneSlotImpl.descriptor = {
		name = "SceneSlot",
		fullName = "roblox.posts.postasset.v1.SceneSlot",
	}

	messages.SceneSlot = _SceneSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.SceneSlot)
end

do
	local _AudioSlotImpl = {}
	_AudioSlotImpl.__index = _AudioSlotImpl

	function _AudioSlotImpl.new(data: _AudioSlotPartialFields?): AudioSlot
		return setmetatable({
			asset_id = if data == nil or data.asset_id == nil then 0 else data.asset_id,
			trim = if data == nil or data.trim == nil then nil else data.trim,
			audio_type = if data == nil or data.audio_type == nil
				then assert(messages.AudioType.fromNumber(0), "Enum has no 0 default")
				else data.audio_type,
			context = if data == nil or data.context == nil then nil else data.context,
		}, _AudioSlotImpl :: _AudioSlotImpl)
	end

	function _AudioSlotImpl.encode(self: AudioSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.asset_id)
		end

		if self.trim ~= nil then
			local encoded = self.trim:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if
			self.audio_type ~= nil
			and (self.audio_type ~= nil and self.audio_type ~= 0 or self.audio_type ~= messages.AudioType.fromNumber(0))
		then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, messages.AudioType.toNumber(self.audio_type :: any))
		end

		if self.context ~= nil then
			if self.context.type == "tts" then
				local encoded = self.context.value:encode()
				output, cursor = proto.writeTag(output, cursor, 10, proto.wireTypes.lengthDelimited)
				output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AudioSlotImpl.decode(input: buffer): AudioSlot
		local self = _AudioSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.asset_id = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.audio_type = (messages.AudioType.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]]
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.trim = messages.TimeRange.decode(value)
					continue
				elseif field == 10 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.context = { type = "tts", value = messages.TtsAudioContext.decode(value) }
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AudioSlotImpl.jsonEncode(self: AudioSlot): any
		local output = {}

		if self.asset_id ~= nil and self.asset_id ~= 0 then
			output.assetId = self.asset_id
		end

		if self.trim ~= nil then
			output.trim = self.trim:jsonEncode()
		end

		if
			self.audio_type ~= nil
			and (self.audio_type ~= nil and self.audio_type ~= 0 or self.audio_type ~= messages.AudioType.fromNumber(0))
		then
			output.audioType = if typeof(self.audio_type) == "number"
				then self.audio_type
				else messages.AudioType.toNumber(self.audio_type :: any)
		end

		if self.context ~= nil then
			if self.context.type == "tts" then
				output.tts = self.context.value:jsonEncode()
			end
		end

		return output
	end

	function _AudioSlotImpl.jsonDecode(input: { [string]: any }): AudioSlot
		local self = _AudioSlotImpl.new()

		if input.asset_id ~= nil then
			self.asset_id = input.asset_id
		end

		if input.assetId ~= nil then
			self.asset_id = input.assetId
		end

		if input.trim ~= nil then
			self.trim = messages.TimeRange.jsonDecode(input.trim)
		end

		if input.audio_type ~= nil then
			self.audio_type = if typeof(input.audio_type) == "number"
				then (messages.AudioType.fromNumber(input.audio_type) or input.audio_type)
				else (messages.AudioType.fromName(input.audio_type) or input.audio_type)
		end

		if input.audioType ~= nil then
			self.audio_type = if typeof(input.audioType) == "number"
				then (messages.AudioType.fromNumber(input.audioType) or input.audioType)
				else (messages.AudioType.fromName(input.audioType) or input.audioType)
		end

		if input.tts ~= nil then
			self.context = { type = "tts", value = messages.TtsAudioContext.jsonDecode(input.tts) }
		end

		return self
	end

	_AudioSlotImpl.descriptor = {
		name = "AudioSlot",
		fullName = "roblox.posts.postasset.v1.AudioSlot",
	}

	messages.AudioSlot = _AudioSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AudioSlot)
end

do
	local _TtsAudioContextImpl = {}
	_TtsAudioContextImpl.__index = _TtsAudioContextImpl

	function _TtsAudioContextImpl.new(data: _TtsAudioContextPartialFields?): TtsAudioContext
		return setmetatable({
			text = if data == nil or data.text == nil then "" else data.text,
			voice_id = if data == nil or data.voice_id == nil then 0 else data.voice_id,
			pitch = if data == nil or data.pitch == nil then 0 else data.pitch,
			speed = if data == nil or data.speed == nil then 0 else data.speed,
		}, _TtsAudioContextImpl :: _TtsAudioContextImpl)
	end

	function _TtsAudioContextImpl.encode(self: TtsAudioContext): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.text ~= nil and self.text ~= "" then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeString(output, cursor, self.text)
		end

		if self.voice_id ~= nil and self.voice_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.voice_id)
		end

		if self.pitch ~= nil and self.pitch ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.i64)
			output, cursor = proto.writeDouble(output, cursor, self.pitch)
		end

		if self.speed ~= nil and self.speed ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 4, proto.wireTypes.i64)
			output, cursor = proto.writeDouble(output, cursor, self.speed)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TtsAudioContextImpl.decode(input: buffer): TtsAudioContext
		local self = _TtsAudioContextImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.voice_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.text = buffer.tostring(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				if field == 3 then
					local value
					value, cursor = proto.readDouble(input, cursor)
					self.pitch = value
					continue
				elseif field == 4 then
					local value
					value, cursor = proto.readDouble(input, cursor)
					self.speed = value
					continue
				end

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TtsAudioContextImpl.jsonEncode(self: TtsAudioContext): any
		local output = {}

		if self.text ~= nil and self.text ~= "" then
			output.text = self.text
		end

		if self.voice_id ~= nil and self.voice_id ~= 0 then
			output.voiceId = self.voice_id
		end

		if self.pitch ~= nil and self.pitch ~= 0 then
			output.pitch = proto.json.serializeNumber(self.pitch)
		end

		if self.speed ~= nil and self.speed ~= 0 then
			output.speed = proto.json.serializeNumber(self.speed)
		end

		return output
	end

	function _TtsAudioContextImpl.jsonDecode(input: { [string]: any }): TtsAudioContext
		local self = _TtsAudioContextImpl.new()

		if input.text ~= nil then
			self.text = input.text
		end

		if input.voice_id ~= nil then
			self.voice_id = input.voice_id
		end

		if input.voiceId ~= nil then
			self.voice_id = input.voiceId
		end

		if input.pitch ~= nil then
			self.pitch = proto.json.deserializeNumber(input.pitch)
		end

		if input.speed ~= nil then
			self.speed = proto.json.deserializeNumber(input.speed)
		end

		return self
	end

	_TtsAudioContextImpl.descriptor = {
		name = "TtsAudioContext",
		fullName = "roblox.posts.postasset.v1.TtsAudioContext",
	}

	messages.TtsAudioContext = _TtsAudioContextImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TtsAudioContext)
end

do
	local _ExperienceAnnotationSlotImpl = {}
	_ExperienceAnnotationSlotImpl.__index = _ExperienceAnnotationSlotImpl

	function _ExperienceAnnotationSlotImpl.new(data: _ExperienceAnnotationSlotPartialFields?): ExperienceAnnotationSlot
		return setmetatable({
			universe_id = if data == nil or data.universe_id == nil then 0 else data.universe_id,
			place_id = if data == nil or data.place_id == nil then 0 else data.place_id,
		}, _ExperienceAnnotationSlotImpl :: _ExperienceAnnotationSlotImpl)
	end

	function _ExperienceAnnotationSlotImpl.encode(self: ExperienceAnnotationSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.universe_id ~= nil and self.universe_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.universe_id)
		end

		if self.place_id ~= nil and self.place_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.place_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _ExperienceAnnotationSlotImpl.decode(input: buffer): ExperienceAnnotationSlot
		local self = _ExperienceAnnotationSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.universe_id = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.place_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _ExperienceAnnotationSlotImpl.jsonEncode(self: ExperienceAnnotationSlot): any
		local output = {}

		if self.universe_id ~= nil and self.universe_id ~= 0 then
			output.universeId = self.universe_id
		end

		if self.place_id ~= nil and self.place_id ~= 0 then
			output.placeId = self.place_id
		end

		return output
	end

	function _ExperienceAnnotationSlotImpl.jsonDecode(input: { [string]: any }): ExperienceAnnotationSlot
		local self = _ExperienceAnnotationSlotImpl.new()

		if input.universe_id ~= nil then
			self.universe_id = input.universe_id
		end

		if input.universeId ~= nil then
			self.universe_id = input.universeId
		end

		if input.place_id ~= nil then
			self.place_id = input.place_id
		end

		if input.placeId ~= nil then
			self.place_id = input.placeId
		end

		return self
	end

	_ExperienceAnnotationSlotImpl.descriptor = {
		name = "ExperienceAnnotationSlot",
		fullName = "roblox.posts.postasset.v1.ExperienceAnnotationSlot",
	}

	messages.ExperienceAnnotationSlot = _ExperienceAnnotationSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.ExperienceAnnotationSlot)
end

do
	local _AvatarAnnotationSlotImpl = {}
	_AvatarAnnotationSlotImpl.__index = _AvatarAnnotationSlotImpl

	function _AvatarAnnotationSlotImpl.new(data: _AvatarAnnotationSlotPartialFields?): AvatarAnnotationSlot
		return setmetatable({
			outfit_id = if data == nil or data.outfit_id == nil then 0 else data.outfit_id,
		}, _AvatarAnnotationSlotImpl :: _AvatarAnnotationSlotImpl)
	end

	function _AvatarAnnotationSlotImpl.encode(self: AvatarAnnotationSlot): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.outfit_id ~= nil and self.outfit_id ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
			output, cursor = proto.writeVarInt(output, cursor, self.outfit_id)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _AvatarAnnotationSlotImpl.decode(input: buffer): AvatarAnnotationSlot
		local self = _AvatarAnnotationSlotImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.outfit_id = value
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _AvatarAnnotationSlotImpl.jsonEncode(self: AvatarAnnotationSlot): any
		local output = {}

		if self.outfit_id ~= nil and self.outfit_id ~= 0 then
			output.outfitId = self.outfit_id
		end

		return output
	end

	function _AvatarAnnotationSlotImpl.jsonDecode(input: { [string]: any }): AvatarAnnotationSlot
		local self = _AvatarAnnotationSlotImpl.new()

		if input.outfit_id ~= nil then
			self.outfit_id = input.outfit_id
		end

		if input.outfitId ~= nil then
			self.outfit_id = input.outfitId
		end

		return self
	end

	_AvatarAnnotationSlotImpl.descriptor = {
		name = "AvatarAnnotationSlot",
		fullName = "roblox.posts.postasset.v1.AvatarAnnotationSlot",
	}

	messages.AvatarAnnotationSlot = _AvatarAnnotationSlotImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.AvatarAnnotationSlot)
end

do
	local _TimeRangeImpl = {}
	_TimeRangeImpl.__index = _TimeRangeImpl

	function _TimeRangeImpl.new(data: _TimeRangePartialFields?): TimeRange
		return setmetatable({
			offset_duration = if data == nil or data.offset_duration == nil then nil else data.offset_duration,
			duration = if data == nil or data.duration == nil then nil else data.duration,
		}, _TimeRangeImpl :: _TimeRangeImpl)
	end

	function _TimeRangeImpl.encode(self: TimeRange): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.offset_duration ~= nil then
			local encoded = self.offset_duration:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.duration ~= nil then
			local encoded = self.duration:encode()
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TimeRangeImpl.decode(input: buffer): TimeRange
		local self = _TimeRangeImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.offset_duration = _google_protobuf_duration.Duration.decode(value)
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.duration = _google_protobuf_duration.Duration.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TimeRangeImpl.jsonEncode(self: TimeRange): any
		local output = {}

		if self.offset_duration ~= nil then
			output.offsetDuration = self.offset_duration:jsonEncode()
		end

		if self.duration ~= nil then
			output.duration = self.duration:jsonEncode()
		end

		return output
	end

	function _TimeRangeImpl.jsonDecode(input: { [string]: any }): TimeRange
		local self = _TimeRangeImpl.new()

		if input.offset_duration ~= nil then
			self.offset_duration = _google_protobuf_duration.Duration.jsonDecode(input.offset_duration)
		end

		if input.offsetDuration ~= nil then
			self.offset_duration = _google_protobuf_duration.Duration.jsonDecode(input.offsetDuration)
		end

		if input.duration ~= nil then
			self.duration = _google_protobuf_duration.Duration.jsonDecode(input.duration)
		end

		return self
	end

	_TimeRangeImpl.descriptor = {
		name = "TimeRange",
		fullName = "roblox.posts.postasset.v1.TimeRange",
	}

	messages.TimeRange = _TimeRangeImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.TimeRange)
end

do
	local _TransformImpl = {}
	_TransformImpl.__index = _TransformImpl

	function _TransformImpl.new(data: _TransformPartialFields?): Transform
		return setmetatable({
			position = if data == nil or data.position == nil then nil else data.position,
			rotation = if data == nil or data.rotation == nil then 0 else data.rotation,
			scale = if data == nil or data.scale == nil then 0 else data.scale,
		}, _TransformImpl :: _TransformImpl)
	end

	function _TransformImpl.encode(self: Transform): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.position ~= nil then
			local encoded = self.position:encode()
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.lengthDelimited)
			output, cursor = proto.writeBuffer(output, cursor, encoded, buffer.len(encoded))
		end

		if self.rotation ~= nil and self.rotation ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.rotation)
		end

		if self.scale ~= nil and self.scale ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.scale)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _TransformImpl.decode(input: buffer): Transform
		local self = _TransformImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				if field == 1 then
					local value
					value, cursor = proto.readBuffer(input, cursor)
					self.position = messages.Vec2.decode(value)
					continue
				end

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.rotation = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.scale = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _TransformImpl.jsonEncode(self: Transform): any
		local output = {}

		if self.position ~= nil then
			output.position = self.position:jsonEncode()
		end

		if self.rotation ~= nil and self.rotation ~= 0 then
			output.rotation = proto.json.serializeNumber(self.rotation)
		end

		if self.scale ~= nil and self.scale ~= 0 then
			output.scale = proto.json.serializeNumber(self.scale)
		end

		return output
	end

	function _TransformImpl.jsonDecode(input: { [string]: any }): Transform
		local self = _TransformImpl.new()

		if input.position ~= nil then
			self.position = messages.Vec2.jsonDecode(input.position)
		end

		if input.rotation ~= nil then
			self.rotation = proto.json.deserializeNumber(input.rotation)
		end

		if input.scale ~= nil then
			self.scale = proto.json.deserializeNumber(input.scale)
		end

		return self
	end

	_TransformImpl.descriptor = {
		name = "Transform",
		fullName = "roblox.posts.postasset.v1.Transform",
	}

	messages.Transform = _TransformImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Transform)
end

do
	local _Vec2Impl = {}
	_Vec2Impl.__index = _Vec2Impl

	function _Vec2Impl.new(data: _Vec2PartialFields?): Vec2
		return setmetatable({
			x = if data == nil or data.x == nil then 0 else data.x,
			y = if data == nil or data.y == nil then 0 else data.y,
		}, _Vec2Impl :: _Vec2Impl)
	end

	function _Vec2Impl.encode(self: Vec2): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.x ~= nil and self.x ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.x)
		end

		if self.y ~= nil and self.y ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.y)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _Vec2Impl.decode(input: buffer): Vec2
		local self = _Vec2Impl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 1 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.x = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.y = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _Vec2Impl.jsonEncode(self: Vec2): any
		local output = {}

		if self.x ~= nil and self.x ~= 0 then
			output.x = proto.json.serializeNumber(self.x)
		end

		if self.y ~= nil and self.y ~= 0 then
			output.y = proto.json.serializeNumber(self.y)
		end

		return output
	end

	function _Vec2Impl.jsonDecode(input: { [string]: any }): Vec2
		local self = _Vec2Impl.new()

		if input.x ~= nil then
			self.x = proto.json.deserializeNumber(input.x)
		end

		if input.y ~= nil then
			self.y = proto.json.deserializeNumber(input.y)
		end

		return self
	end

	_Vec2Impl.descriptor = {
		name = "Vec2",
		fullName = "roblox.posts.postasset.v1.Vec2",
	}

	messages.Vec2 = _Vec2Impl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Vec2)
end

do
	local _Color3Impl = {}
	_Color3Impl.__index = _Color3Impl

	function _Color3Impl.new(data: _Color3PartialFields?): Color3
		return setmetatable({
			r = if data == nil or data.r == nil then 0 else data.r,
			g = if data == nil or data.g == nil then 0 else data.g,
			b = if data == nil or data.b == nil then 0 else data.b,
		}, _Color3Impl :: _Color3Impl)
	end

	function _Color3Impl.encode(self: Color3): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.r ~= nil and self.r ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.r)
		end

		if self.g ~= nil and self.g ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.g)
		end

		if self.b ~= nil and self.b ~= 0 then
			output, cursor = proto.writeTag(output, cursor, 3, proto.wireTypes.i32)
			output, cursor = proto.writeFloat(output, cursor, self.b)
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _Color3Impl.decode(input: buffer): Color3
		local self = _Color3Impl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				-- No fields

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				if field == 1 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.r = value
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.g = value
					continue
				elseif field == 3 then
					local value
					value, cursor = proto.readFloat(input, cursor)
					self.b = value
					continue
				end

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _Color3Impl.jsonEncode(self: Color3): any
		local output = {}

		if self.r ~= nil and self.r ~= 0 then
			output.r = proto.json.serializeNumber(self.r)
		end

		if self.g ~= nil and self.g ~= 0 then
			output.g = proto.json.serializeNumber(self.g)
		end

		if self.b ~= nil and self.b ~= 0 then
			output.b = proto.json.serializeNumber(self.b)
		end

		return output
	end

	function _Color3Impl.jsonDecode(input: { [string]: any }): Color3
		local self = _Color3Impl.new()

		if input.r ~= nil then
			self.r = proto.json.deserializeNumber(input.r)
		end

		if input.g ~= nil then
			self.g = proto.json.deserializeNumber(input.g)
		end

		if input.b ~= nil then
			self.b = proto.json.deserializeNumber(input.b)
		end

		return self
	end

	_Color3Impl.descriptor = {
		name = "Color3",
		fullName = "roblox.posts.postasset.v1.Color3",
	}

	messages.Color3 = _Color3Impl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.Color3)
end

do
	local _FontInfoImpl = {}
	_FontInfoImpl.__index = _FontInfoImpl

	function _FontInfoImpl.new(data: _FontInfoPartialFields?): FontInfo
		return setmetatable({
			font_source = if data == nil or data.font_source == nil then nil else data.font_source,
		}, _FontInfoImpl :: _FontInfoImpl)
	end

	function _FontInfoImpl.encode(self: FontInfo): buffer
		local output = buffer.create(0)
		local cursor = 0

		if self.font_source ~= nil then
			if self.font_source.type == "enumerated_font" then
				output, cursor = proto.writeTag(output, cursor, 1, proto.wireTypes.varint)
				output, cursor =
					proto.writeVarInt(output, cursor, messages.EnumeratedFont.toNumber(self.font_source.value :: any))
			elseif self.font_source.type == "family_asset_id" then
				output, cursor = proto.writeTag(output, cursor, 2, proto.wireTypes.varint)
				output, cursor = proto.writeVarInt(output, cursor, self.font_source.value)
			end
		end

		local shrunkBuffer = buffer.create(cursor)
		buffer.copy(shrunkBuffer, 0, output, 0, cursor)
		return shrunkBuffer
	end

	function _FontInfoImpl.decode(input: buffer): FontInfo
		local self = _FontInfoImpl.new()
		local cursor = 0

		while cursor < buffer.len(input) do
			local field, wireType
			field, wireType, cursor = proto.readTag(input, cursor)

			if wireType == proto.wireTypes.varint then
				if field == 1 then
					local value
					value, cursor = proto.readVarIntI32(input, cursor)
					self.font_source = {
						type = "enumerated_font",
						value = (messages.EnumeratedFont.fromNumber(value) or value) :: any --[[ Luau: Enums are a string intersection which Luau is quick to dismantle ]],
					}
					continue
				elseif field == 2 then
					local value
					value, cursor = proto.readVarIntI64(input, cursor)
					self.font_source = { type = "family_asset_id", value = value }
					continue
				end

				local _
				_, cursor = proto.readVarInt(input, cursor)
			elseif wireType == proto.wireTypes.lengthDelimited then
				-- No fields

				local length
				length, cursor = proto.readVarInt(input, cursor)

				cursor += length
			elseif wireType == proto.wireTypes.i32 then
				-- No fields

				local _
				_, cursor = proto.readFixed32(input, cursor)
			elseif wireType == proto.wireTypes.i64 then
				-- No fields

				local _
				_, cursor = proto.readFixed64(input, cursor)
			else
				error("Unsupported wire type: " .. wireType)
			end
		end

		return self
	end

	function _FontInfoImpl.jsonEncode(self: FontInfo): any
		local output = {}

		if self.font_source ~= nil then
			if self.font_source.type == "enumerated_font" then
				output.enumeratedFont = if typeof(self.font_source.value) == "number"
					then self.font_source.value
					else messages.EnumeratedFont.toNumber(self.font_source.value :: any)
			elseif self.font_source.type == "family_asset_id" then
				output.familyAssetId = self.font_source.value
			end
		end

		return output
	end

	function _FontInfoImpl.jsonDecode(input: { [string]: any }): FontInfo
		local self = _FontInfoImpl.new()

		if input.enumerated_font ~= nil then
			self.font_source = {
				type = "enumerated_font",
				value = if typeof(input.enumerated_font) == "number"
					then (messages.EnumeratedFont.fromNumber(input.enumerated_font) or input.enumerated_font)
					else (messages.EnumeratedFont.fromName(input.enumerated_font) or input.enumerated_font),
			}
		end

		if input.enumeratedFont ~= nil then
			self.font_source = {
				type = "enumerated_font",
				value = if typeof(input.enumeratedFont) == "number"
					then (messages.EnumeratedFont.fromNumber(input.enumeratedFont) or input.enumeratedFont)
					else (messages.EnumeratedFont.fromName(input.enumeratedFont) or input.enumeratedFont),
			}
		end

		if input.family_asset_id ~= nil then
			self.font_source = { type = "family_asset_id", value = input.family_asset_id }
		end

		if input.familyAssetId ~= nil then
			self.font_source = { type = "family_asset_id", value = input.familyAssetId }
		end

		return self
	end

	_FontInfoImpl.descriptor = {
		name = "FontInfo",
		fullName = "roblox.posts.postasset.v1.FontInfo",
	}

	messages.FontInfo = _FontInfoImpl :: any -- Luau: Not sure why this intersection fails.

	typeRegistry.default:register(messages.FontInfo)
end

messages.PostOrigin = {
	fromNumber = function(value: number): PostOrigin?
		if value == 0 then
			return "POST_ORIGIN_INVALID"
		elseif value == 1 then
			return "POST_ORIGIN_CAPTURE_VIDEO"
		elseif value == 2 then
			return "POST_ORIGIN_CAPTURE_SCREENSHOT"
		elseif value == 3 then
			return "POST_ORIGIN_INFLUENCER_UPLOAD"
		elseif value == 4 then
			return "POST_ORIGIN_AVATAR"
		else
			return nil
		end
	end,

	toNumber = function(self: PostOrigin): number
		if self == "POST_ORIGIN_INVALID" then
			return 0
		elseif self == "POST_ORIGIN_CAPTURE_VIDEO" then
			return 1
		elseif self == "POST_ORIGIN_CAPTURE_SCREENSHOT" then
			return 2
		elseif self == "POST_ORIGIN_INFLUENCER_UPLOAD" then
			return 3
		elseif self == "POST_ORIGIN_AVATAR" then
			return 4
		else
			return self
		end
	end,

	fromName = function(name: string): PostOrigin?
		if name == "POST_ORIGIN_INVALID" then
			return "POST_ORIGIN_INVALID"
		elseif name == "POST_ORIGIN_CAPTURE_VIDEO" then
			return "POST_ORIGIN_CAPTURE_VIDEO"
		elseif name == "POST_ORIGIN_CAPTURE_SCREENSHOT" then
			return "POST_ORIGIN_CAPTURE_SCREENSHOT"
		elseif name == "POST_ORIGIN_INFLUENCER_UPLOAD" then
			return "POST_ORIGIN_INFLUENCER_UPLOAD"
		elseif name == "POST_ORIGIN_AVATAR" then
			return "POST_ORIGIN_AVATAR"
		else
			return nil
		end
	end,
}

messages.TrackType = {
	fromNumber = function(value: number): TrackType?
		if value == 0 then
			return "TRACK_TYPE_INVALID"
		elseif value == 1 then
			return "TRACK_TYPE_VISUAL"
		elseif value == 2 then
			return "TRACK_TYPE_AUDIO"
		elseif value == 3 then
			return "TRACK_TYPE_ANNOTATION"
		else
			return nil
		end
	end,

	toNumber = function(self: TrackType): number
		if self == "TRACK_TYPE_INVALID" then
			return 0
		elseif self == "TRACK_TYPE_VISUAL" then
			return 1
		elseif self == "TRACK_TYPE_AUDIO" then
			return 2
		elseif self == "TRACK_TYPE_ANNOTATION" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): TrackType?
		if name == "TRACK_TYPE_INVALID" then
			return "TRACK_TYPE_INVALID"
		elseif name == "TRACK_TYPE_VISUAL" then
			return "TRACK_TYPE_VISUAL"
		elseif name == "TRACK_TYPE_AUDIO" then
			return "TRACK_TYPE_AUDIO"
		elseif name == "TRACK_TYPE_ANNOTATION" then
			return "TRACK_TYPE_ANNOTATION"
		else
			return nil
		end
	end,
}

messages.AudioType = {
	fromNumber = function(value: number): AudioType?
		if value == 0 then
			return "AUDIO_TYPE_INVALID"
		elseif value == 1 then
			return "AUDIO_TYPE_MUSIC"
		elseif value == 2 then
			return "AUDIO_TYPE_TTS"
		elseif value == 3 then
			return "AUDIO_TYPE_VOICE_OVER"
		else
			return nil
		end
	end,

	toNumber = function(self: AudioType): number
		if self == "AUDIO_TYPE_INVALID" then
			return 0
		elseif self == "AUDIO_TYPE_MUSIC" then
			return 1
		elseif self == "AUDIO_TYPE_TTS" then
			return 2
		elseif self == "AUDIO_TYPE_VOICE_OVER" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): AudioType?
		if name == "AUDIO_TYPE_INVALID" then
			return "AUDIO_TYPE_INVALID"
		elseif name == "AUDIO_TYPE_MUSIC" then
			return "AUDIO_TYPE_MUSIC"
		elseif name == "AUDIO_TYPE_TTS" then
			return "AUDIO_TYPE_TTS"
		elseif name == "AUDIO_TYPE_VOICE_OVER" then
			return "AUDIO_TYPE_VOICE_OVER"
		else
			return nil
		end
	end,
}

messages.EnumeratedFont = {
	fromNumber = function(value: number): EnumeratedFont?
		if value == 0 then
			return "ENUMERATED_FONT_INVALID"
		elseif value == 1 then
			return "ENUMERATED_FONT_GOTHAM_BOLD"
		elseif value == 2 then
			return "ENUMERATED_FONT_GOTHAM_BLACK"
		elseif value == 3 then
			return "ENUMERATED_FONT_SOURCE_SANS"
		elseif value == 4 then
			return "ENUMERATED_FONT_SOURCE_SANS_BOLD"
		elseif value == 5 then
			return "ENUMERATED_FONT_FREDOKA_ONE"
		elseif value == 6 then
			return "ENUMERATED_FONT_BANGERS"
		else
			return nil
		end
	end,

	toNumber = function(self: EnumeratedFont): number
		if self == "ENUMERATED_FONT_INVALID" then
			return 0
		elseif self == "ENUMERATED_FONT_GOTHAM_BOLD" then
			return 1
		elseif self == "ENUMERATED_FONT_GOTHAM_BLACK" then
			return 2
		elseif self == "ENUMERATED_FONT_SOURCE_SANS" then
			return 3
		elseif self == "ENUMERATED_FONT_SOURCE_SANS_BOLD" then
			return 4
		elseif self == "ENUMERATED_FONT_FREDOKA_ONE" then
			return 5
		elseif self == "ENUMERATED_FONT_BANGERS" then
			return 6
		else
			return self
		end
	end,

	fromName = function(name: string): EnumeratedFont?
		if name == "ENUMERATED_FONT_INVALID" then
			return "ENUMERATED_FONT_INVALID"
		elseif name == "ENUMERATED_FONT_GOTHAM_BOLD" then
			return "ENUMERATED_FONT_GOTHAM_BOLD"
		elseif name == "ENUMERATED_FONT_GOTHAM_BLACK" then
			return "ENUMERATED_FONT_GOTHAM_BLACK"
		elseif name == "ENUMERATED_FONT_SOURCE_SANS" then
			return "ENUMERATED_FONT_SOURCE_SANS"
		elseif name == "ENUMERATED_FONT_SOURCE_SANS_BOLD" then
			return "ENUMERATED_FONT_SOURCE_SANS_BOLD"
		elseif name == "ENUMERATED_FONT_FREDOKA_ONE" then
			return "ENUMERATED_FONT_FREDOKA_ONE"
		elseif name == "ENUMERATED_FONT_BANGERS" then
			return "ENUMERATED_FONT_BANGERS"
		else
			return nil
		end
	end,
}

messages.TextXAlignment = {
	fromNumber = function(value: number): TextXAlignment?
		if value == 0 then
			return "TEXT_X_ALIGNMENT_INVALID"
		elseif value == 1 then
			return "TEXT_X_ALIGNMENT_LEFT"
		elseif value == 2 then
			return "TEXT_X_ALIGNMENT_CENTER"
		elseif value == 3 then
			return "TEXT_X_ALIGNMENT_RIGHT"
		else
			return nil
		end
	end,

	toNumber = function(self: TextXAlignment): number
		if self == "TEXT_X_ALIGNMENT_INVALID" then
			return 0
		elseif self == "TEXT_X_ALIGNMENT_LEFT" then
			return 1
		elseif self == "TEXT_X_ALIGNMENT_CENTER" then
			return 2
		elseif self == "TEXT_X_ALIGNMENT_RIGHT" then
			return 3
		else
			return self
		end
	end,

	fromName = function(name: string): TextXAlignment?
		if name == "TEXT_X_ALIGNMENT_INVALID" then
			return "TEXT_X_ALIGNMENT_INVALID"
		elseif name == "TEXT_X_ALIGNMENT_LEFT" then
			return "TEXT_X_ALIGNMENT_LEFT"
		elseif name == "TEXT_X_ALIGNMENT_CENTER" then
			return "TEXT_X_ALIGNMENT_CENTER"
		elseif name == "TEXT_X_ALIGNMENT_RIGHT" then
			return "TEXT_X_ALIGNMENT_RIGHT"
		else
			return nil
		end
	end,
}

return {
	Post = messages.Post,
	PostMetadata = messages.PostMetadata,
	Track = messages.Track,
	Slot = messages.Slot,
	VisualSlot = messages.VisualSlot,
	VideoSlot = messages.VideoSlot,
	ImageSlot = messages.ImageSlot,
	TextLabelSlot = messages.TextLabelSlot,
	SceneSlot = messages.SceneSlot,
	AudioSlot = messages.AudioSlot,
	TtsAudioContext = messages.TtsAudioContext,
	ExperienceAnnotationSlot = messages.ExperienceAnnotationSlot,
	AvatarAnnotationSlot = messages.AvatarAnnotationSlot,
	TimeRange = messages.TimeRange,
	Transform = messages.Transform,
	Vec2 = messages.Vec2,
	Color3 = messages.Color3,
	FontInfo = messages.FontInfo,
	PostOrigin = messages.PostOrigin,
	TrackType = messages.TrackType,
	AudioType = messages.AudioType,
	EnumeratedFont = messages.EnumeratedFont,
	TextXAlignment = messages.TextXAlignment,
}
