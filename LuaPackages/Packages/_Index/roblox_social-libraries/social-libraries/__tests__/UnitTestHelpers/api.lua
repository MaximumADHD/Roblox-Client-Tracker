return {
	expectedFields = require(script.Parent.expectedFields),
	mountFrame = require(script.Parent.mountFrame),
	mockProviders = require(script.Parent.mockProviders),
	mountStyledFrame = require(script.Parent.mountStyledFrame),
	mockStyle = require(script.Parent.mockStyle),
	findImageSet = require(script.Parent.findImageSet),
}
