export type NavigationType = "Current" | "Next" | "Previous"

local NavigationType = {
	Current = "Current" :: "Current",
	Next = "Next" :: "Next",
	Previous = "Previous" :: "Previous",
}

return NavigationType
