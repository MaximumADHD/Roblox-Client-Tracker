local GameModel = require(script.GameModel)
local CreatorModel = require(script.CreatorModel)
local ProductModel = require(script.ProductModel)

return {
	CreatorModel = CreatorModel,
	GameModel = GameModel,
	ProductModel = ProductModel,
}
