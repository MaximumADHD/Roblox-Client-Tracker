--[[
	Configuration used in the feedback module, values can be set here to tune various settings
]]
local GlobalConfig = {
	propValidation = true,
	elementTracing = false,
	logStore = false,
}

return GlobalConfig
